{
    "sourceFile": "js/app.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 57,
            "patches": [
                {
                    "date": 1774366390638,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1774366399790,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,4 +1,138 @@\n+\r\n+/* =========================================\r\n+   app.js — Credits, Navigation, Utilities\r\n+   ========================================= */\r\n+\r\n+const THEMES = {\r\n+  fire:    'th-fire',\r\n+  magic:   'th-magic',\r\n+  skull:   'th-skull',\r\n+  heart:   'th-heart',\r\n+  star:    'th-star',\r\n+  bolt:    'th-bolt',\r\n+  default: 'th-default'\r\n+};\r\n+\r\n+let credits  = parseInt(localStorage.getItem('ds_cr') || '0');\r\n+let selDeck  = null;\r\n+\r\n+const saveCr = () => localStorage.setItem('ds_cr', credits);\r\n+const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n+\r\n+/* ---- CREDIT DISPLAY ---- */\r\n+function updateCr() {\r\n+  document.getElementById('cr-disp').textContent = credits;\r\n+  const gcr = document.getElementById('g-cr');\r\n+  if (gcr) gcr.textContent = credits;\r\n+}\r\n+\r\n+/* ---- NAVIGATION ---- */\r\n+function goHome() {\r\n+  document.getElementById('home-screen').style.display  = '';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'none';\r\n+  renderDecks();\r\n+  updateCr();\r\n+}\r\n+\r\n+function showAdmin() {\r\n+  document.getElementById('home-screen').style.display  = 'none';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'block';\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n+  document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n+  document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n+document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n+}\r\n+\r\n+/* ---- DECK SELECTION (HOME) ---- */\r\n+function renderDecks() {\r\n+  const g = document.getElementById('deck-grid');\r\n+  g.innerHTML = '';\r\n+  DB.decks.filter(d => !d.hidden).forEach(d => {  // เพิ่ม .filter(d => !d.hidden)\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'deck-card' + (selDeck === d.id ? ' sel' : '');\r\n+    el.innerHTML = `\r\n+      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div class=\"d-name\">${d.name}</div>\r\n+      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n+      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n+      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n+    el.onclick = () => { selDeck = d.id; renderDecks(); };\r\n+    g.appendChild(el);\r\n+  });\r\n+  // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n+  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n+    selDeck = null;\r\n+  }\r\n+  document.getElementById('btn-play').disabled = !selDeck;\r\n+}\r\n+\r\n+/* ---- TOAST ---- */\r\n+const T_ICO = {\r\n+  success: 'fi-sr-check-circle',\r\n+  error:   'fi-sr-cross-circle',\r\n+  warning: 'fi-sr-triangle-warning',\r\n+  info:    'fi-sr-info'\r\n+};\r\n+const T_CLR = {\r\n+  success: '#4caf50',\r\n+  error:   'var(--red)',\r\n+  warning: 'var(--gold)',\r\n+  info:    'var(--text2)'\r\n+};\r\n+\r\n+function toast(msg, type = 'success') {\r\n+  const t   = document.getElementById('toast');\r\n+  const ico = document.getElementById('toast-ico');\r\n+  ico.className  = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n+  ico.style.color = T_CLR[type] || '#4caf50';\r\n+  document.getElementById('toast-msg').textContent = msg;\r\n+  t.classList.add('show');\r\n+  clearTimeout(t._t);\r\n+  t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n+}\r\n+\r\n+/* ---- TOPUP MODAL ---- */\r\n+function openTopup()  { document.getElementById('topup-overlay').classList.add('show'); }\r\n+function closeTopup() {\r\n+  document.getElementById('topup-overlay').classList.remove('show');\r\n+  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n+}\r\n+function selPkg(el)   { document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel')); el.classList.add('sel'); }\r\n+function freeCredit() { credits += 10; saveCr(); updateCr(); toast('รับ 10 เครดิตแล้ว!', 'success'); closeTopup(); }\r\n+\r\n+/* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n+['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n+  const el = document.getElementById(id);\r\n+  if (!el) return;\r\n+  el.addEventListener('click', function (e) {\r\n+    if (e.target !== this) return;\r\n+    if (id === 'topup-overlay')  closeTopup();\r\n+    else if (id === 'res-overlay') closeRes();\r\n+    else closeDeckForm();\r\n+  });\r\n+});\r\n+\r\n+/* ---- INIT ---- */\r\n+updateCr();\r\n+renderDecks();\r\n+function applyTestMode() {\r\n+  const isTest = DB.settings.testMode === 1;\r\n+  const banner = document.getElementById('test-banner');\r\n+  const btn    = document.getElementById('test-btn');\r\n+  if (banner) banner.style.display = isTest ? '' : 'none';\r\n+  if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n+}\r\n+\r\n+// เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n+applyTestMode();\r\n+\r\n // let deckCards   = [];\r\n // let revCount    = 0;\r\n // let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n // let stackIndex  = 0;\r\n"
                },
                {
                    "date": 1774396748979,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,477 @@\n+\r\n+/* =========================================\r\n+   app.js — Credits, Navigation, Utilities\r\n+   ========================================= */\r\n+\r\n+const THEMES = {\r\n+  fire:    'th-fire',\r\n+  magic:   'th-magic',\r\n+  skull:   'th-skull',\r\n+  heart:   'th-heart',\r\n+  star:    'th-star',\r\n+  bolt:    'th-bolt',\r\n+  default: 'th-default'\r\n+};\r\n+\r\n+let credits  = parseInt(localStorage.getItem('ds_cr') || '0');\r\n+let selDeck  = null;\r\n+\r\n+const saveCr = () => localStorage.setItem('ds_cr', credits);\r\n+const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n+\r\n+/* ---- CREDIT DISPLAY ---- */\r\n+function updateCr() {\r\n+  document.getElementById('cr-disp').textContent = credits;\r\n+  const gcr = document.getElementById('g-cr');\r\n+  if (gcr) gcr.textContent = credits;\r\n+}\r\n+\r\n+/* ---- NAVIGATION ---- */\r\n+function goHome() {\r\n+  document.getElementById('home-screen').style.display  = '';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'none';\r\n+  renderDecks();\r\n+  updateCr();\r\n+}\r\n+\r\n+function showAdmin() {\r\n+  document.getElementById('home-screen').style.display  = 'none';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'block';\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n+  document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n+  document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n+document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n+}\r\n+\r\n+/* ---- DECK SELECTION (HOME) ---- */\r\n+function renderDecks() {\r\n+  const g = document.getElementById('deck-grid');\r\n+  g.innerHTML = '';\r\n+  DB.decks.filter(d => !d.hidden).forEach(d => {  // เพิ่ม .filter(d => !d.hidden)\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'deck-card' + (selDeck === d.id ? ' sel' : '');\r\n+    el.innerHTML = `\r\n+      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div class=\"d-name\">${d.name}</div>\r\n+      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n+      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n+      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n+    el.onclick = () => { selDeck = d.id; renderDecks(); };\r\n+    g.appendChild(el);\r\n+  });\r\n+  // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n+  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n+    selDeck = null;\r\n+  }\r\n+  document.getElementById('btn-play').disabled = !selDeck;\r\n+}\r\n+\r\n+/* ---- TOAST ---- */\r\n+const T_ICO = {\r\n+  success: 'fi-sr-check-circle',\r\n+  error:   'fi-sr-cross-circle',\r\n+  warning: 'fi-sr-triangle-warning',\r\n+  info:    'fi-sr-info'\r\n+};\r\n+const T_CLR = {\r\n+  success: '#4caf50',\r\n+  error:   'var(--red)',\r\n+  warning: 'var(--gold)',\r\n+  info:    'var(--text2)'\r\n+};\r\n+\r\n+function toast(msg, type = 'success') {\r\n+  const t   = document.getElementById('toast');\r\n+  const ico = document.getElementById('toast-ico');\r\n+  ico.className  = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n+  ico.style.color = T_CLR[type] || '#4caf50';\r\n+  document.getElementById('toast-msg').textContent = msg;\r\n+  t.classList.add('show');\r\n+  clearTimeout(t._t);\r\n+  t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n+}\r\n+\r\n+/* ---- TOPUP MODAL ---- */\r\n+function openTopup()  { document.getElementById('topup-overlay').classList.add('show'); }\r\n+function closeTopup() {\r\n+  document.getElementById('topup-overlay').classList.remove('show');\r\n+  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n+}\r\n+function selPkg(el)   { document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel')); el.classList.add('sel'); }\r\n+function freeCredit() { credits += 10; saveCr(); updateCr(); toast('รับ 10 เครดิตแล้ว!', 'success'); closeTopup(); }\r\n+\r\n+/* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n+['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n+  const el = document.getElementById(id);\r\n+  if (!el) return;\r\n+  el.addEventListener('click', function (e) {\r\n+    if (e.target !== this) return;\r\n+    if (id === 'topup-overlay')  closeTopup();\r\n+    else if (id === 'res-overlay') closeRes();\r\n+    else closeDeckForm();\r\n+  });\r\n+});\r\n+\r\n+/* ---- INIT ---- */\r\n+updateCr();\r\n+renderDecks();\r\n+function applyTestMode() {\r\n+  const isTest = DB.settings.testMode === 1;\r\n+  const banner = document.getElementById('test-banner');\r\n+  const btn    = document.getElementById('test-btn');\r\n+  if (banner) banner.style.display = isTest ? '' : 'none';\r\n+  if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n+}\r\n+\r\n+function setLayout(mode) {\r\n+  layoutMode = mode;\r\n+  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+  \r\n+  // เพิ่มบรรทัดนี้\r\n+  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกอง' : 'แบบกระจาย';\r\n+  \r\n+  renderCards();\r\n+}\r\n+\r\n+// เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n+applyTestMode();\r\n+\r\n+// let deckCards   = [];\r\n+// let revCount    = 0;\r\n+// let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n+// let stackIndex  = 0;\r\n+// let showModal = true;\r\n+\r\n+// /* ---- ตรวจจับมือถือ ---- */\r\n+// function isMobile() {\r\n+//   return window.innerWidth <= 640;\r\n+// }\r\n+\r\n+// /* ---- LAYOUT TOGGLE ---- */\r\n+// function setLayout(mode) {\r\n+//   layoutMode = mode;\r\n+//   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+//   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+//   renderCards();\r\n+// }\r\n+\r\n+// function toggleModal() {\r\n+//   showModal = !showModal;\r\n+//   const btn = document.getElementById('lt-modal');\r\n+//   btn.classList.toggle('act', showModal);\r\n+//   btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n+//   toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n+// }\r\n+\r\n+// /* ---- START GAME ---- */\r\n+// function startGame() {\r\n+//   if (!selDeck) return;\r\n+//   const deck  = DB.decks.find(d => d.id === selDeck);\r\n+//   const cards = DB.cards[selDeck] || [];\r\n+\r\n+//   if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n+//   if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n+\r\n+//   deckCards  = [...cards].sort(() => Math.random() - .5);\r\n+//   revCount   = 0;\r\n+//   stackIndex = 0;\r\n+\r\n+//   document.getElementById('home-screen').style.display = 'none';\r\n+//   document.getElementById('game-screen').style.display = 'block';\r\n+\r\n+//   const th = getTh(deck);\r\n+//   const iw = document.getElementById('g-ico-wrap');\r\n+//   iw.className = 'g-deck-ico ' + th;\r\n+//   document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n+//   document.getElementById('g-nm').textContent      = deck.name;\r\n+//   document.getElementById('cost-show').textContent = deck.cost || 1;\r\n+\r\n+//   const rw = document.getElementById('res-ico-wrap');\r\n+//   rw.className = 'res-ico-wrap ' + th;\r\n+//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+\r\n+//   updRem();\r\n+//   updateCr();\r\n+//   renderCards();\r\n+// }\r\n+\r\n+// /* ---- REMAINING COUNT ---- */\r\n+// function updRem() {\r\n+//   const t = deckCards.length;\r\n+//   document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n+// }\r\n+\r\n+// /* ═══════════════════════════════════════════\r\n+//    โหมด 1 — SCATTER (กระจายเละ)\r\n+// ═══════════════════════════════════════════ */\r\n+// function renderScatter() {\r\n+//   const wrap = document.getElementById('cards-wrap');\r\n+//   wrap.innerHTML = '';\r\n+\r\n+//   const mobile = isMobile();\r\n+//   const deck     = DB.decks.find(d => d.id === selDeck);\r\n+//   const deckIcon = deck?.icon || 'fi-sr-layers';\r\n+//   const deckTheme = getTh(deck);\r\n+\r\n+//   // วัดพื้นที่จริงของ wrap\r\n+//   const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n+//   const CW = mobile ? 72 : 96;\r\n+//   const CH = mobile ? 104 : 138;\r\n+\r\n+//   // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n+//   const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n+//   const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n+//   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n+\r\n+//   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n+\r\n+//   const total = deckCards.length;\r\n+\r\n+//   const ZX = mobile ? 6 : 7;\r\n+//   const ZY = mobile ? 5 : 5;\r\n+//   const zW = W / ZX;\r\n+//   const zH = H / ZY;\r\n+\r\n+//   const zones = [];\r\n+//   for (let zy = 0; zy < ZY; zy++)\r\n+//     for (let zx = 0; zx < ZX; zx++)\r\n+//       zones.push([zx, zy]);\r\n+\r\n+//   for (let i = zones.length - 1; i > 0; i--) {\r\n+//     const j = Math.floor(Math.random() * (i + 1));\r\n+//     [zones[i], zones[j]] = [zones[j], zones[i]];\r\n+//   }\r\n+\r\n+//   const frag = document.createDocumentFragment();\r\n+//   const els  = [];\r\n+\r\n+//   for (let i = 0; i < total; i++) {\r\n+//     const [zx, zy] = zones[i % zones.length];\r\n+//     const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n+//     const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n+//     const ang = (Math.random() - .5) * 72;\r\n+//     const z   = Math.floor(Math.random() * total) + 1;\r\n+\r\n+//     const el = document.createElement('div');\r\n+//     el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n+//     el.dataset.i     = i;\r\n+//     el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n+//     el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+//     el.onclick       = () => flipCard(el, i, ang);\r\n+\r\n+//     frag.appendChild(el);\r\n+//     els.push(el);\r\n+//   }\r\n+\r\n+//   wrap.appendChild(frag);\r\n+\r\n+//   const delay = mobile ? 5 : 10;\r\n+//   els.forEach((el, i) => {\r\n+//     setTimeout(() => {\r\n+//       el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n+//       el.style.opacity    = '1';\r\n+//     }, i * delay);\r\n+//   });\r\n+// }\r\n+\r\n+// /* ═══════════════════════════════════════════\r\n+//    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n+// ═══════════════════════════════════════════ */\r\n+// function renderStack() {\r\n+//   const wrap = document.getElementById('cards-wrap');\r\n+//   wrap.innerHTML = '';\r\n+//   wrap.style.cssText = 'min-height:420px;position:relative;';\r\n+\r\n+//   wrap.innerHTML = [\r\n+//     '<div class=\"stack-layout\">',\r\n+//       '<div class=\"stack-section stack-center\">',\r\n+//         '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n+//           '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n+//           'กองไพ่',\r\n+//         '</div>',\r\n+//         '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n+//         '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n+//       '</div>',\r\n+//     '</div>'\r\n+//   ].join('');\r\n+\r\n+//   buildStackPile();\r\n+// }\r\n+\r\n+// function buildStackPile() {\r\n+//   const pileWrap = document.getElementById('stack-pile-wrap');\r\n+//   const countEl  = document.getElementById('stack-count');\r\n+//   if (!pileWrap) return;\r\n+\r\n+//   const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n+//   const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n+//   const deckTheme = getTh(deck);                            // เพิ่ม\r\n+//   pileWrap.innerHTML = '';\r\n+//   const remaining = deckCards.length - stackIndex;\r\n+//   if (countEl) countEl.textContent = remaining + ' ใบ';\r\n+\r\n+//   if (remaining <= 0) {\r\n+//     pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n+//       + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n+//       + 'หยิบครบแล้ว!</div>';\r\n+//     return;\r\n+//   }\r\n+\r\n+//   const showCount = Math.min(remaining, 6);\r\n+//   for (let i = showCount - 1; i >= 0; i--) {\r\n+//     const el  = document.createElement('div');\r\n+//     el.className = 'crd stack-crd';\r\n+//     const off = i * 2.5;\r\n+//     const ang = (i - showCount / 2) * 1.8;\r\n+//     el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n+//       + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n+//       + (i === 0\r\n+//         ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n+//         : 'cursor:default;');\r\n+//   el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+//     if (i === 0) el.onclick = () => flipTopCard(el);\r\n+//     pileWrap.appendChild(el);\r\n+//   }\r\n+// }\r\n+\r\n+// function flipTopCard(el) {\r\n+//   if (stackIndex >= deckCards.length) return;\r\n+//   const deck = DB.decks.find(d => d.id === selDeck);\r\n+//   const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+//   credits -= cost;\r\n+//   saveCr();\r\n+//   updateCr();\r\n+\r\n+//   el.style.animation     = '';\r\n+//   el.style.pointerEvents = 'none';\r\n+//   el.classList.add('flipping');\r\n+\r\n+//   const rect = el.getBoundingClientRect();\r\n+//   for (let i = 0; i < 8; i++) {\r\n+//     const s = document.createElement('div');\r\n+//     s.className = 'spk';\r\n+//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+//     document.body.appendChild(s);\r\n+//     setTimeout(() => s.remove(), 900);\r\n+//   }\r\n+\r\n+//   const cardData = deckCards[stackIndex];\r\n+//   stackIndex++;\r\n+//   revCount++;\r\n+//   updRem();\r\n+\r\n+//   setTimeout(() => {\r\n+//     el.classList.remove('flipping');\r\n+\r\n+//     // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n+//     el.classList.add('revealed');\r\n+//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n+//       + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n+//       + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n+//       + '</div>';\r\n+\r\n+//     // fade out และ remove\r\n+//     setTimeout(() => {\r\n+//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+//       el.style.opacity    = '0';\r\n+//       el.style.transform  = 'scale(0.6)';\r\n+//       setTimeout(() => el.remove(), 420);\r\n+//     }, 300);\r\n+\r\n+//     buildStackPile();\r\n+//     setTimeout(() => showRes(cardData, deck), 200);\r\n+//   }, 560);\r\n+// }\r\n+\r\n+// /* ═══════════════════════════════════════════\r\n+//    RENDER CARDS — เลือก mode\r\n+// ═══════════════════════════════════════════ */\r\n+// function renderCards() {\r\n+//   if (layoutMode === 'stack') renderStack();\r\n+//   else renderScatter();\r\n+// }\r\n+\r\n+// /* ═══════════════════════════════════════════\r\n+//    FLIP CARD — scatter mode\r\n+// ═══════════════════════════════════════════ */\r\n+// function flipCard(el, idx, ang) {\r\n+//   const deck = DB.decks.find(d => d.id === selDeck);\r\n+//   const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+//   if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n+//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+//   credits -= cost;\r\n+//   saveCr();\r\n+//   updateCr();\r\n+//   el.classList.add('flipping');\r\n+\r\n+//   const rect = el.getBoundingClientRect();\r\n+//   for (let i = 0; i < 8; i++) {\r\n+//     const s = document.createElement('div');\r\n+//     s.className = 'spk';\r\n+//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+//     document.body.appendChild(s);\r\n+//     setTimeout(() => s.remove(), 900);\r\n+//   }\r\n+\r\n+//   setTimeout(() => {\r\n+//     const card = deckCards[idx % deckCards.length];\r\n+//     el.classList.remove('flipping');\r\n+//     el.classList.add('revealed');\r\n+//     el.style.transform = 'rotate(' + ang + 'deg)';\r\n+//     el.style.zIndex    = '200';\r\n+//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n+//       + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n+//       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n+//       + '</div>';\r\n+//     revCount++;\r\n+//     updRem();\r\n+//     setTimeout(() => showRes(card, deck), 200);\r\n+\r\n+//     // fade out และ remove หลัง modal โผล่\r\n+//     setTimeout(() => {\r\n+//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+//       el.style.opacity    = '0';\r\n+//       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n+//       setTimeout(() => el.remove(), 420);\r\n+//     }, 800);\r\n+\r\n+//   }, 560);\r\n+// }\r\n+\r\n+// /* ---- RESULT MODAL ---- */\r\n+// function showRes(card, deck) {\r\n+//   const th = getTh(deck);\r\n+//   document.getElementById('res-dlbl').innerHTML =\r\n+//     '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n+//   document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n+//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+//   document.getElementById('res-cat').textContent = card.cat;\r\n+//   document.getElementById('res-txt').textContent  = card.text;\r\n+//   document.getElementById('res-overlay').classList.add('show');\r\n+// }\r\n+\r\n+// function closeRes() {\r\n+//   document.getElementById('res-overlay').classList.remove('show');\r\n+// }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774396784229,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -134,9 +134,9 @@\n   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n   \r\n   // เพิ่มบรรทัดนี้\r\n-  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกอง' : 'แบบกระจาย';\r\n+  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n   \r\n   renderCards();\r\n }\r\n \r\n@@ -473,471 +473,5 @@\n // }\r\n \r\n // function closeRes() {\r\n //   document.getElementById('res-overlay').classList.remove('show');\r\n-// }\n-\r\n-/* =========================================\r\n-   app.js — Credits, Navigation, Utilities\r\n-   ========================================= */\r\n-\r\n-const THEMES = {\r\n-  fire:    'th-fire',\r\n-  magic:   'th-magic',\r\n-  skull:   'th-skull',\r\n-  heart:   'th-heart',\r\n-  star:    'th-star',\r\n-  bolt:    'th-bolt',\r\n-  default: 'th-default'\r\n-};\r\n-\r\n-let credits  = parseInt(localStorage.getItem('ds_cr') || '0');\r\n-let selDeck  = null;\r\n-\r\n-const saveCr = () => localStorage.setItem('ds_cr', credits);\r\n-const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n-\r\n-/* ---- CREDIT DISPLAY ---- */\r\n-function updateCr() {\r\n-  document.getElementById('cr-disp').textContent = credits;\r\n-  const gcr = document.getElementById('g-cr');\r\n-  if (gcr) gcr.textContent = credits;\r\n-}\r\n-\r\n-/* ---- NAVIGATION ---- */\r\n-function goHome() {\r\n-  document.getElementById('home-screen').style.display  = '';\r\n-  document.getElementById('game-screen').style.display  = 'none';\r\n-  document.getElementById('admin-screen').style.display = 'none';\r\n-  renderDecks();\r\n-  updateCr();\r\n-}\r\n-\r\n-function showAdmin() {\r\n-  document.getElementById('home-screen').style.display  = 'none';\r\n-  document.getElementById('game-screen').style.display  = 'none';\r\n-  document.getElementById('admin-screen').style.display = 'block';\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n-  document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n-  document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n-document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n-}\r\n-\r\n-/* ---- DECK SELECTION (HOME) ---- */\r\n-function renderDecks() {\r\n-  const g = document.getElementById('deck-grid');\r\n-  g.innerHTML = '';\r\n-  DB.decks.filter(d => !d.hidden).forEach(d => {  // เพิ่ม .filter(d => !d.hidden)\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const el  = document.createElement('div');\r\n-    el.className = 'deck-card' + (selDeck === d.id ? ' sel' : '');\r\n-    el.innerHTML = `\r\n-      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div class=\"d-name\">${d.name}</div>\r\n-      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n-      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n-      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n-    el.onclick = () => { selDeck = d.id; renderDecks(); };\r\n-    g.appendChild(el);\r\n-  });\r\n-  // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n-  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n-    selDeck = null;\r\n-  }\r\n-  document.getElementById('btn-play').disabled = !selDeck;\r\n-}\r\n-\r\n-/* ---- TOAST ---- */\r\n-const T_ICO = {\r\n-  success: 'fi-sr-check-circle',\r\n-  error:   'fi-sr-cross-circle',\r\n-  warning: 'fi-sr-triangle-warning',\r\n-  info:    'fi-sr-info'\r\n-};\r\n-const T_CLR = {\r\n-  success: '#4caf50',\r\n-  error:   'var(--red)',\r\n-  warning: 'var(--gold)',\r\n-  info:    'var(--text2)'\r\n-};\r\n-\r\n-function toast(msg, type = 'success') {\r\n-  const t   = document.getElementById('toast');\r\n-  const ico = document.getElementById('toast-ico');\r\n-  ico.className  = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n-  ico.style.color = T_CLR[type] || '#4caf50';\r\n-  document.getElementById('toast-msg').textContent = msg;\r\n-  t.classList.add('show');\r\n-  clearTimeout(t._t);\r\n-  t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n-}\r\n-\r\n-/* ---- TOPUP MODAL ---- */\r\n-function openTopup()  { document.getElementById('topup-overlay').classList.add('show'); }\r\n-function closeTopup() {\r\n-  document.getElementById('topup-overlay').classList.remove('show');\r\n-  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n-}\r\n-function selPkg(el)   { document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel')); el.classList.add('sel'); }\r\n-function freeCredit() { credits += 10; saveCr(); updateCr(); toast('รับ 10 เครดิตแล้ว!', 'success'); closeTopup(); }\r\n-\r\n-/* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n-['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n-  const el = document.getElementById(id);\r\n-  if (!el) return;\r\n-  el.addEventListener('click', function (e) {\r\n-    if (e.target !== this) return;\r\n-    if (id === 'topup-overlay')  closeTopup();\r\n-    else if (id === 'res-overlay') closeRes();\r\n-    else closeDeckForm();\r\n-  });\r\n-});\r\n-\r\n-/* ---- INIT ---- */\r\n-updateCr();\r\n-renderDecks();\r\n-function applyTestMode() {\r\n-  const isTest = DB.settings.testMode === 1;\r\n-  const banner = document.getElementById('test-banner');\r\n-  const btn    = document.getElementById('test-btn');\r\n-  if (banner) banner.style.display = isTest ? '' : 'none';\r\n-  if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n-}\r\n-\r\n-// เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n-applyTestMode();\r\n-\r\n-// let deckCards   = [];\r\n-// let revCount    = 0;\r\n-// let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n-// let stackIndex  = 0;\r\n-// let showModal = true;\r\n-\r\n-// /* ---- ตรวจจับมือถือ ---- */\r\n-// function isMobile() {\r\n-//   return window.innerWidth <= 640;\r\n-// }\r\n-\r\n-// /* ---- LAYOUT TOGGLE ---- */\r\n-// function setLayout(mode) {\r\n-//   layoutMode = mode;\r\n-//   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-//   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-//   renderCards();\r\n-// }\r\n-\r\n-// function toggleModal() {\r\n-//   showModal = !showModal;\r\n-//   const btn = document.getElementById('lt-modal');\r\n-//   btn.classList.toggle('act', showModal);\r\n-//   btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n-//   toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n-// }\r\n-\r\n-// /* ---- START GAME ---- */\r\n-// function startGame() {\r\n-//   if (!selDeck) return;\r\n-//   const deck  = DB.decks.find(d => d.id === selDeck);\r\n-//   const cards = DB.cards[selDeck] || [];\r\n-\r\n-//   if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n-//   if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n-\r\n-//   deckCards  = [...cards].sort(() => Math.random() - .5);\r\n-//   revCount   = 0;\r\n-//   stackIndex = 0;\r\n-\r\n-//   document.getElementById('home-screen').style.display = 'none';\r\n-//   document.getElementById('game-screen').style.display = 'block';\r\n-\r\n-//   const th = getTh(deck);\r\n-//   const iw = document.getElementById('g-ico-wrap');\r\n-//   iw.className = 'g-deck-ico ' + th;\r\n-//   document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n-//   document.getElementById('g-nm').textContent      = deck.name;\r\n-//   document.getElementById('cost-show').textContent = deck.cost || 1;\r\n-\r\n-//   const rw = document.getElementById('res-ico-wrap');\r\n-//   rw.className = 'res-ico-wrap ' + th;\r\n-//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-\r\n-//   updRem();\r\n-//   updateCr();\r\n-//   renderCards();\r\n-// }\r\n-\r\n-// /* ---- REMAINING COUNT ---- */\r\n-// function updRem() {\r\n-//   const t = deckCards.length;\r\n-//   document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    โหมด 1 — SCATTER (กระจายเละ)\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderScatter() {\r\n-//   const wrap = document.getElementById('cards-wrap');\r\n-//   wrap.innerHTML = '';\r\n-\r\n-//   const mobile = isMobile();\r\n-//   const deck     = DB.decks.find(d => d.id === selDeck);\r\n-//   const deckIcon = deck?.icon || 'fi-sr-layers';\r\n-//   const deckTheme = getTh(deck);\r\n-\r\n-//   // วัดพื้นที่จริงของ wrap\r\n-//   const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n-//   const CW = mobile ? 72 : 96;\r\n-//   const CH = mobile ? 104 : 138;\r\n-\r\n-//   // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n-//   const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n-//   const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n-//   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n-\r\n-//   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n-\r\n-//   const total = deckCards.length;\r\n-\r\n-//   const ZX = mobile ? 6 : 7;\r\n-//   const ZY = mobile ? 5 : 5;\r\n-//   const zW = W / ZX;\r\n-//   const zH = H / ZY;\r\n-\r\n-//   const zones = [];\r\n-//   for (let zy = 0; zy < ZY; zy++)\r\n-//     for (let zx = 0; zx < ZX; zx++)\r\n-//       zones.push([zx, zy]);\r\n-\r\n-//   for (let i = zones.length - 1; i > 0; i--) {\r\n-//     const j = Math.floor(Math.random() * (i + 1));\r\n-//     [zones[i], zones[j]] = [zones[j], zones[i]];\r\n-//   }\r\n-\r\n-//   const frag = document.createDocumentFragment();\r\n-//   const els  = [];\r\n-\r\n-//   for (let i = 0; i < total; i++) {\r\n-//     const [zx, zy] = zones[i % zones.length];\r\n-//     const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n-//     const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n-//     const ang = (Math.random() - .5) * 72;\r\n-//     const z   = Math.floor(Math.random() * total) + 1;\r\n-\r\n-//     const el = document.createElement('div');\r\n-//     el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n-//     el.dataset.i     = i;\r\n-//     el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n-//     el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-//     el.onclick       = () => flipCard(el, i, ang);\r\n-\r\n-//     frag.appendChild(el);\r\n-//     els.push(el);\r\n-//   }\r\n-\r\n-//   wrap.appendChild(frag);\r\n-\r\n-//   const delay = mobile ? 5 : 10;\r\n-//   els.forEach((el, i) => {\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n-//       el.style.opacity    = '1';\r\n-//     }, i * delay);\r\n-//   });\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderStack() {\r\n-//   const wrap = document.getElementById('cards-wrap');\r\n-//   wrap.innerHTML = '';\r\n-//   wrap.style.cssText = 'min-height:420px;position:relative;';\r\n-\r\n-//   wrap.innerHTML = [\r\n-//     '<div class=\"stack-layout\">',\r\n-//       '<div class=\"stack-section stack-center\">',\r\n-//         '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n-//           '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n-//           'กองไพ่',\r\n-//         '</div>',\r\n-//         '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n-//         '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n-//       '</div>',\r\n-//     '</div>'\r\n-//   ].join('');\r\n-\r\n-//   buildStackPile();\r\n-// }\r\n-\r\n-// function buildStackPile() {\r\n-//   const pileWrap = document.getElementById('stack-pile-wrap');\r\n-//   const countEl  = document.getElementById('stack-count');\r\n-//   if (!pileWrap) return;\r\n-\r\n-//   const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n-//   const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n-//   const deckTheme = getTh(deck);                            // เพิ่ม\r\n-//   pileWrap.innerHTML = '';\r\n-//   const remaining = deckCards.length - stackIndex;\r\n-//   if (countEl) countEl.textContent = remaining + ' ใบ';\r\n-\r\n-//   if (remaining <= 0) {\r\n-//     pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n-//       + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n-//       + 'หยิบครบแล้ว!</div>';\r\n-//     return;\r\n-//   }\r\n-\r\n-//   const showCount = Math.min(remaining, 6);\r\n-//   for (let i = showCount - 1; i >= 0; i--) {\r\n-//     const el  = document.createElement('div');\r\n-//     el.className = 'crd stack-crd';\r\n-//     const off = i * 2.5;\r\n-//     const ang = (i - showCount / 2) * 1.8;\r\n-//     el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n-//       + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n-//       + (i === 0\r\n-//         ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n-//         : 'cursor:default;');\r\n-//   el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-//     if (i === 0) el.onclick = () => flipTopCard(el);\r\n-//     pileWrap.appendChild(el);\r\n-//   }\r\n-// }\r\n-\r\n-// function flipTopCard(el) {\r\n-//   if (stackIndex >= deckCards.length) return;\r\n-//   const deck = DB.decks.find(d => d.id === selDeck);\r\n-//   const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-//   credits -= cost;\r\n-//   saveCr();\r\n-//   updateCr();\r\n-\r\n-//   el.style.animation     = '';\r\n-//   el.style.pointerEvents = 'none';\r\n-//   el.classList.add('flipping');\r\n-\r\n-//   const rect = el.getBoundingClientRect();\r\n-//   for (let i = 0; i < 8; i++) {\r\n-//     const s = document.createElement('div');\r\n-//     s.className = 'spk';\r\n-//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-//     document.body.appendChild(s);\r\n-//     setTimeout(() => s.remove(), 900);\r\n-//   }\r\n-\r\n-//   const cardData = deckCards[stackIndex];\r\n-//   stackIndex++;\r\n-//   revCount++;\r\n-//   updRem();\r\n-\r\n-//   setTimeout(() => {\r\n-//     el.classList.remove('flipping');\r\n-\r\n-//     // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n-//     el.classList.add('revealed');\r\n-//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n-//       + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n-//       + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n-//       + '</div>';\r\n-\r\n-//     // fade out และ remove\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-//       el.style.opacity    = '0';\r\n-//       el.style.transform  = 'scale(0.6)';\r\n-//       setTimeout(() => el.remove(), 420);\r\n-//     }, 300);\r\n-\r\n-//     buildStackPile();\r\n-//     setTimeout(() => showRes(cardData, deck), 200);\r\n-//   }, 560);\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    RENDER CARDS — เลือก mode\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderCards() {\r\n-//   if (layoutMode === 'stack') renderStack();\r\n-//   else renderScatter();\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    FLIP CARD — scatter mode\r\n-// ═══════════════════════════════════════════ */\r\n-// function flipCard(el, idx, ang) {\r\n-//   const deck = DB.decks.find(d => d.id === selDeck);\r\n-//   const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-//   if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n-//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-//   credits -= cost;\r\n-//   saveCr();\r\n-//   updateCr();\r\n-//   el.classList.add('flipping');\r\n-\r\n-//   const rect = el.getBoundingClientRect();\r\n-//   for (let i = 0; i < 8; i++) {\r\n-//     const s = document.createElement('div');\r\n-//     s.className = 'spk';\r\n-//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-//     document.body.appendChild(s);\r\n-//     setTimeout(() => s.remove(), 900);\r\n-//   }\r\n-\r\n-//   setTimeout(() => {\r\n-//     const card = deckCards[idx % deckCards.length];\r\n-//     el.classList.remove('flipping');\r\n-//     el.classList.add('revealed');\r\n-//     el.style.transform = 'rotate(' + ang + 'deg)';\r\n-//     el.style.zIndex    = '200';\r\n-//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n-//       + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n-//       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n-//       + '</div>';\r\n-//     revCount++;\r\n-//     updRem();\r\n-//     setTimeout(() => showRes(card, deck), 200);\r\n-\r\n-//     // fade out และ remove หลัง modal โผล่\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-//       el.style.opacity    = '0';\r\n-//       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n-//       setTimeout(() => el.remove(), 420);\r\n-//     }, 800);\r\n-\r\n-//   }, 560);\r\n-// }\r\n-\r\n-// /* ---- RESULT MODAL ---- */\r\n-// function showRes(card, deck) {\r\n-//   const th = getTh(deck);\r\n-//   document.getElementById('res-dlbl').innerHTML =\r\n-//     '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n-//   document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n-//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-//   document.getElementById('res-cat').textContent = card.cat;\r\n-//   document.getElementById('res-txt').textContent  = card.text;\r\n-//   document.getElementById('res-overlay').classList.add('show');\r\n-// }\r\n-\r\n-// function closeRes() {\r\n-//   document.getElementById('res-overlay').classList.remove('show');\r\n // }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774397664396,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -28,8 +28,9 @@\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n   document.getElementById('home-screen').style.display  = '';\r\n+  document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   renderDecks();\r\n   updateCr();\r\n"
                },
                {
                    "date": 1774397935335,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,8 +31,9 @@\n   document.getElementById('home-screen').style.display  = '';\r\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n+    document.body.classList.remove('in-game');\r\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n"
                },
                {
                    "date": 1774401716524,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,9 +31,9 @@\n   document.getElementById('home-screen').style.display  = '';\r\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n-    document.body.classList.remove('in-game');\r\n+  document.body.classList.remove('in-game');\r\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n@@ -143,337 +143,4 @@\n }\r\n \r\n // เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n applyTestMode();\r\n-\r\n-// let deckCards   = [];\r\n-// let revCount    = 0;\r\n-// let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n-// let stackIndex  = 0;\r\n-// let showModal = true;\r\n-\r\n-// /* ---- ตรวจจับมือถือ ---- */\r\n-// function isMobile() {\r\n-//   return window.innerWidth <= 640;\r\n-// }\r\n-\r\n-// /* ---- LAYOUT TOGGLE ---- */\r\n-// function setLayout(mode) {\r\n-//   layoutMode = mode;\r\n-//   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-//   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-//   renderCards();\r\n-// }\r\n-\r\n-// function toggleModal() {\r\n-//   showModal = !showModal;\r\n-//   const btn = document.getElementById('lt-modal');\r\n-//   btn.classList.toggle('act', showModal);\r\n-//   btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n-//   toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n-// }\r\n-\r\n-// /* ---- START GAME ---- */\r\n-// function startGame() {\r\n-//   if (!selDeck) return;\r\n-//   const deck  = DB.decks.find(d => d.id === selDeck);\r\n-//   const cards = DB.cards[selDeck] || [];\r\n-\r\n-//   if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n-//   if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n-\r\n-//   deckCards  = [...cards].sort(() => Math.random() - .5);\r\n-//   revCount   = 0;\r\n-//   stackIndex = 0;\r\n-\r\n-//   document.getElementById('home-screen').style.display = 'none';\r\n-//   document.getElementById('game-screen').style.display = 'block';\r\n-\r\n-//   const th = getTh(deck);\r\n-//   const iw = document.getElementById('g-ico-wrap');\r\n-//   iw.className = 'g-deck-ico ' + th;\r\n-//   document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n-//   document.getElementById('g-nm').textContent      = deck.name;\r\n-//   document.getElementById('cost-show').textContent = deck.cost || 1;\r\n-\r\n-//   const rw = document.getElementById('res-ico-wrap');\r\n-//   rw.className = 'res-ico-wrap ' + th;\r\n-//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-\r\n-//   updRem();\r\n-//   updateCr();\r\n-//   renderCards();\r\n-// }\r\n-\r\n-// /* ---- REMAINING COUNT ---- */\r\n-// function updRem() {\r\n-//   const t = deckCards.length;\r\n-//   document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    โหมด 1 — SCATTER (กระจายเละ)\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderScatter() {\r\n-//   const wrap = document.getElementById('cards-wrap');\r\n-//   wrap.innerHTML = '';\r\n-\r\n-//   const mobile = isMobile();\r\n-//   const deck     = DB.decks.find(d => d.id === selDeck);\r\n-//   const deckIcon = deck?.icon || 'fi-sr-layers';\r\n-//   const deckTheme = getTh(deck);\r\n-\r\n-//   // วัดพื้นที่จริงของ wrap\r\n-//   const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n-//   const CW = mobile ? 72 : 96;\r\n-//   const CH = mobile ? 104 : 138;\r\n-\r\n-//   // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n-//   const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n-//   const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n-//   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n-\r\n-//   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n-\r\n-//   const total = deckCards.length;\r\n-\r\n-//   const ZX = mobile ? 6 : 7;\r\n-//   const ZY = mobile ? 5 : 5;\r\n-//   const zW = W / ZX;\r\n-//   const zH = H / ZY;\r\n-\r\n-//   const zones = [];\r\n-//   for (let zy = 0; zy < ZY; zy++)\r\n-//     for (let zx = 0; zx < ZX; zx++)\r\n-//       zones.push([zx, zy]);\r\n-\r\n-//   for (let i = zones.length - 1; i > 0; i--) {\r\n-//     const j = Math.floor(Math.random() * (i + 1));\r\n-//     [zones[i], zones[j]] = [zones[j], zones[i]];\r\n-//   }\r\n-\r\n-//   const frag = document.createDocumentFragment();\r\n-//   const els  = [];\r\n-\r\n-//   for (let i = 0; i < total; i++) {\r\n-//     const [zx, zy] = zones[i % zones.length];\r\n-//     const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n-//     const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n-//     const ang = (Math.random() - .5) * 72;\r\n-//     const z   = Math.floor(Math.random() * total) + 1;\r\n-\r\n-//     const el = document.createElement('div');\r\n-//     el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n-//     el.dataset.i     = i;\r\n-//     el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n-//     el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-//     el.onclick       = () => flipCard(el, i, ang);\r\n-\r\n-//     frag.appendChild(el);\r\n-//     els.push(el);\r\n-//   }\r\n-\r\n-//   wrap.appendChild(frag);\r\n-\r\n-//   const delay = mobile ? 5 : 10;\r\n-//   els.forEach((el, i) => {\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n-//       el.style.opacity    = '1';\r\n-//     }, i * delay);\r\n-//   });\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderStack() {\r\n-//   const wrap = document.getElementById('cards-wrap');\r\n-//   wrap.innerHTML = '';\r\n-//   wrap.style.cssText = 'min-height:420px;position:relative;';\r\n-\r\n-//   wrap.innerHTML = [\r\n-//     '<div class=\"stack-layout\">',\r\n-//       '<div class=\"stack-section stack-center\">',\r\n-//         '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n-//           '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n-//           'กองไพ่',\r\n-//         '</div>',\r\n-//         '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n-//         '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n-//       '</div>',\r\n-//     '</div>'\r\n-//   ].join('');\r\n-\r\n-//   buildStackPile();\r\n-// }\r\n-\r\n-// function buildStackPile() {\r\n-//   const pileWrap = document.getElementById('stack-pile-wrap');\r\n-//   const countEl  = document.getElementById('stack-count');\r\n-//   if (!pileWrap) return;\r\n-\r\n-//   const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n-//   const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n-//   const deckTheme = getTh(deck);                            // เพิ่ม\r\n-//   pileWrap.innerHTML = '';\r\n-//   const remaining = deckCards.length - stackIndex;\r\n-//   if (countEl) countEl.textContent = remaining + ' ใบ';\r\n-\r\n-//   if (remaining <= 0) {\r\n-//     pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n-//       + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n-//       + 'หยิบครบแล้ว!</div>';\r\n-//     return;\r\n-//   }\r\n-\r\n-//   const showCount = Math.min(remaining, 6);\r\n-//   for (let i = showCount - 1; i >= 0; i--) {\r\n-//     const el  = document.createElement('div');\r\n-//     el.className = 'crd stack-crd';\r\n-//     const off = i * 2.5;\r\n-//     const ang = (i - showCount / 2) * 1.8;\r\n-//     el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n-//       + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n-//       + (i === 0\r\n-//         ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n-//         : 'cursor:default;');\r\n-//   el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-//     if (i === 0) el.onclick = () => flipTopCard(el);\r\n-//     pileWrap.appendChild(el);\r\n-//   }\r\n-// }\r\n-\r\n-// function flipTopCard(el) {\r\n-//   if (stackIndex >= deckCards.length) return;\r\n-//   const deck = DB.decks.find(d => d.id === selDeck);\r\n-//   const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-//   credits -= cost;\r\n-//   saveCr();\r\n-//   updateCr();\r\n-\r\n-//   el.style.animation     = '';\r\n-//   el.style.pointerEvents = 'none';\r\n-//   el.classList.add('flipping');\r\n-\r\n-//   const rect = el.getBoundingClientRect();\r\n-//   for (let i = 0; i < 8; i++) {\r\n-//     const s = document.createElement('div');\r\n-//     s.className = 'spk';\r\n-//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-//     document.body.appendChild(s);\r\n-//     setTimeout(() => s.remove(), 900);\r\n-//   }\r\n-\r\n-//   const cardData = deckCards[stackIndex];\r\n-//   stackIndex++;\r\n-//   revCount++;\r\n-//   updRem();\r\n-\r\n-//   setTimeout(() => {\r\n-//     el.classList.remove('flipping');\r\n-\r\n-//     // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n-//     el.classList.add('revealed');\r\n-//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n-//       + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n-//       + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n-//       + '</div>';\r\n-\r\n-//     // fade out และ remove\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-//       el.style.opacity    = '0';\r\n-//       el.style.transform  = 'scale(0.6)';\r\n-//       setTimeout(() => el.remove(), 420);\r\n-//     }, 300);\r\n-\r\n-//     buildStackPile();\r\n-//     setTimeout(() => showRes(cardData, deck), 200);\r\n-//   }, 560);\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    RENDER CARDS — เลือก mode\r\n-// ═══════════════════════════════════════════ */\r\n-// function renderCards() {\r\n-//   if (layoutMode === 'stack') renderStack();\r\n-//   else renderScatter();\r\n-// }\r\n-\r\n-// /* ═══════════════════════════════════════════\r\n-//    FLIP CARD — scatter mode\r\n-// ═══════════════════════════════════════════ */\r\n-// function flipCard(el, idx, ang) {\r\n-//   const deck = DB.decks.find(d => d.id === selDeck);\r\n-//   const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-//   if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n-//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-//   credits -= cost;\r\n-//   saveCr();\r\n-//   updateCr();\r\n-//   el.classList.add('flipping');\r\n-\r\n-//   const rect = el.getBoundingClientRect();\r\n-//   for (let i = 0; i < 8; i++) {\r\n-//     const s = document.createElement('div');\r\n-//     s.className = 'spk';\r\n-//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-//     document.body.appendChild(s);\r\n-//     setTimeout(() => s.remove(), 900);\r\n-//   }\r\n-\r\n-//   setTimeout(() => {\r\n-//     const card = deckCards[idx % deckCards.length];\r\n-//     el.classList.remove('flipping');\r\n-//     el.classList.add('revealed');\r\n-//     el.style.transform = 'rotate(' + ang + 'deg)';\r\n-//     el.style.zIndex    = '200';\r\n-//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n-//       + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n-//       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n-//       + '</div>';\r\n-//     revCount++;\r\n-//     updRem();\r\n-//     setTimeout(() => showRes(card, deck), 200);\r\n-\r\n-//     // fade out และ remove หลัง modal โผล่\r\n-//     setTimeout(() => {\r\n-//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-//       el.style.opacity    = '0';\r\n-//       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n-//       setTimeout(() => el.remove(), 420);\r\n-//     }, 800);\r\n-\r\n-//   }, 560);\r\n-// }\r\n-\r\n-// /* ---- RESULT MODAL ---- */\r\n-// function showRes(card, deck) {\r\n-//   const th = getTh(deck);\r\n-//   document.getElementById('res-dlbl').innerHTML =\r\n-//     '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n-//   document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n-//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-//   document.getElementById('res-cat').textContent = card.cat;\r\n-//   document.getElementById('res-txt').textContent  = card.text;\r\n-//   document.getElementById('res-overlay').classList.add('show');\r\n-// }\r\n-\r\n-// function closeRes() {\r\n-//   document.getElementById('res-overlay').classList.remove('show');\r\n-// }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774401761199,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -32,8 +32,9 @@\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n+  history.pushState({}, '', '/');\r\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n"
                },
                {
                    "date": 1774401900821,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -32,9 +32,9 @@\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n-  history.pushState({}, '', '/');\r\n+  history.pushState({}, '', '#');\r\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n"
                },
                {
                    "date": 1774401960713,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -142,6 +142,18 @@\n   \r\n   renderCards();\r\n }\r\n \r\n+// อ่าน hash ตอนโหลดหน้า\r\n+window.addEventListener('load', function() {\r\n+  const hash = window.location.hash;\r\n+  if (hash.startsWith('#play/')) {\r\n+    const deckId = hash.replace('#play/', '');\r\n+    const deck = DB.decks.find(d => d.id === deckId);\r\n+    if (deck) {\r\n+      selDeck = deckId;\r\n+      startGame();\r\n+    }\r\n+  }\r\n+});\r\n // เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n applyTestMode();\r\n"
                },
                {
                    "date": 1774469858778,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -13,9 +13,9 @@\n   default: 'th-default'\r\n };\r\n \r\n let credits  = parseInt(localStorage.getItem('ds_cr') || '0');\r\n-let selDeck  = null;\r\n+let selDeck = [];\r\n \r\n const saveCr = () => localStorage.setItem('ds_cr', credits);\r\n const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n \r\n@@ -154,6 +154,7 @@\n       startGame();\r\n     }\r\n   }\r\n });\r\n+\r\n // เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n applyTestMode();\r\n"
                },
                {
                    "date": 1774469888094,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -64,9 +64,18 @@\n       <div class=\"d-name\">${d.name}</div>\r\n       <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n       ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n       <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n-    el.onclick = () => { selDeck = d.id; renderDecks(); };\r\n+    el.onclick = () => {\r\n+  if (selDeck.includes(d.id)) {\r\n+    selDeck = selDeck.filter(id => id !== d.id); // ถ้าเลือกอยู่แล้ว → ยกเลิก\r\n+  } else if (selDeck.length < 3) {\r\n+    selDeck.push(d.id); // เพิ่มได้ไม่เกิน 3\r\n+  } else {\r\n+    toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n+  }\r\n+  renderDecks();\r\n+};\r\n     g.appendChild(el);\r\n   });\r\n   // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n"
                },
                {
                    "date": 1774469914424,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -57,25 +57,25 @@\n   DB.decks.filter(d => !d.hidden).forEach(d => {  // เพิ่ม .filter(d => !d.hidden)\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n     const th  = getTh(d);\r\n     const el  = document.createElement('div');\r\n-    el.className = 'deck-card' + (selDeck === d.id ? ' sel' : '');\r\n+    el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n     el.innerHTML = `\r\n       <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n       <div class=\"d-name\">${d.name}</div>\r\n       <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n       ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n       <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n     el.onclick = () => {\r\n-  if (selDeck.includes(d.id)) {\r\n-    selDeck = selDeck.filter(id => id !== d.id); // ถ้าเลือกอยู่แล้ว → ยกเลิก\r\n-  } else if (selDeck.length < 3) {\r\n-    selDeck.push(d.id); // เพิ่มได้ไม่เกิน 3\r\n-  } else {\r\n-    toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n-  }\r\n-  renderDecks();\r\n-};\r\n+      if (selDeck.includes(d.id)) {\r\n+        selDeck = selDeck.filter(id => id !== d.id); // ถ้าเลือกอยู่แล้ว → ยกเลิก\r\n+      } else if (selDeck.length < 3) {\r\n+        selDeck.push(d.id); // เพิ่มได้ไม่เกิน 3\r\n+      } else {\r\n+        toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n+      }\r\n+      renderDecks();\r\n+    };\r\n     g.appendChild(el);\r\n   });\r\n   // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n"
                },
                {
                    "date": 1774469933641,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -80,9 +80,9 @@\n   // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n     selDeck = null;\r\n   }\r\n-  document.getElementById('btn-play').disabled = !selDeck;\r\n+  document.getElementById('btn-play').disabled = selDeck.length < 2;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n const T_ICO = {\r\n"
                },
                {
                    "date": 1774470081608,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -80,9 +80,9 @@\n   // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n     selDeck = null;\r\n   }\r\n-  document.getElementById('btn-play').disabled = selDeck.length < 2;\r\n+  document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n const T_ICO = {\r\n"
                },
                {
                    "date": 1774470501932,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,8 +27,9 @@\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n+  \r\n   document.getElementById('home-screen').style.display  = '';\r\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n"
                },
                {
                    "date": 1774470520075,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,9 +27,9 @@\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n-  \r\n+  selDeck = [];\r\n   document.getElementById('home-screen').style.display  = '';\r\n   document.getElementById('btn-admin').style.display = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n@@ -159,9 +159,9 @@\n   if (hash.startsWith('#play/')) {\r\n     const deckId = hash.replace('#play/', '');\r\n     const deck = DB.decks.find(d => d.id === deckId);\r\n     if (deck) {\r\n-      selDeck = deckId;\r\n+      selDeck = [deckId]; // แก้ตรงนี้\r\n       startGame();\r\n     }\r\n   }\r\n });\r\n"
                },
                {
                    "date": 1774472972483,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -39,8 +39,9 @@\n   updateCr();\r\n }\r\n \r\n function showAdmin() {\r\n+  history.pushState({}, '', '#admin');\r\n   document.getElementById('home-screen').style.display  = 'none';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'block';\r\n   renderAdmDecks();\r\n"
                },
                {
                    "date": 1774473056077,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -156,13 +156,15 @@\n \r\n // อ่าน hash ตอนโหลดหน้า\r\n window.addEventListener('load', function() {\r\n   const hash = window.location.hash;\r\n-  if (hash.startsWith('#play/')) {\r\n+  if (hash === '#admin') {\r\n+    showAdmin();\r\n+  } else if (hash.startsWith('#play/')) {\r\n     const deckId = hash.replace('#play/', '');\r\n     const deck = DB.decks.find(d => d.id === deckId);\r\n     if (deck) {\r\n-      selDeck = [deckId]; // แก้ตรงนี้\r\n+      selDeck = [deckId];\r\n       startGame();\r\n     }\r\n   }\r\n });\r\n"
                },
                {
                    "date": 1774473581256,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -49,8 +49,10 @@\n   document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n   document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n   document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n+document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n+document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n }\r\n \r\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n"
                },
                {
                    "date": 1774473586704,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -48,11 +48,11 @@\n   renderAdmSel();\r\n   document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n   document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n   document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n-document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n-document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n-document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n+  document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n+  document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n+  document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n }\r\n \r\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n"
                },
                {
                    "date": 1774473984295,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -91,9 +91,9 @@\n /* ---- TOAST ---- */\r\n const T_ICO = {\r\n   success: 'fi-sr-check-circle',\r\n   error:   'fi-sr-cross-circle',\r\n-  warning: 'fi-sr-triangle-warning',\r\n+  warning: 'fi-sr-bell-ring',\r\n   info:    'fi-sr-info'\r\n };\r\n const T_CLR = {\r\n   success: '#4caf50',\r\n"
                },
                {
                    "date": 1774530407762,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -113,9 +113,13 @@\n   t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n }\r\n \r\n /* ---- TOPUP MODAL ---- */\r\n-function openTopup()  { document.getElementById('topup-overlay').classList.add('show'); }\r\n+function openTopup() {\r\n+  document.getElementById('topup-overlay').classList.add('show');\r\n+  // เพิ่มบรรทัดนี้\r\n+  document.getElementById('topup-cr-disp').textContent = credits;\r\n+}\r\n function closeTopup() {\r\n   document.getElementById('topup-overlay').classList.remove('show');\r\n   document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n }\r\n"
                },
                {
                    "date": 1774565996300,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -39,8 +39,11 @@\n   updateCr();\r\n }\r\n \r\n function showAdmin() {\r\n+  if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n+  if (!isAdminLoggedIn()) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n+\r\n   history.pushState({}, '', '#admin');\r\n   document.getElementById('home-screen').style.display  = 'none';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'block';\r\n"
                },
                {
                    "date": 1774566478700,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -38,26 +38,25 @@\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n-function showAdmin() {\r\n+async function showAdmin() {\r\n   if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n-  if (!isAdminLoggedIn()) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n+  if (!(await isAdminLoggedIn())) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n \r\n   history.pushState({}, '', '#admin');\r\n   document.getElementById('home-screen').style.display  = 'none';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'block';\r\n   renderAdmDecks();\r\n   renderAdmSel();\r\n-  document.getElementById('st-cr').value   = DB.settings.startCredit || 10;\r\n-  document.getElementById('st-nm').value   = DB.settings.siteName || 'DRINKORDOOM';\r\n-  document.getElementById('st-test').value = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n-  document.getElementById('st-modal').value = DB.settings.showModal   ?? 1;  // เพิ่ม\r\n+  document.getElementById('st-cr').value    = DB.settings.startCredit || 10;\r\n+  document.getElementById('st-nm').value    = DB.settings.siteName || 'DRINKORDOOM';\r\n+  document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n+  document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n   document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n   document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n }\r\n-\r\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n   const g = document.getElementById('deck-grid');\r\n   g.innerHTML = '';\r\n"
                },
                {
                    "date": 1774566490786,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -55,8 +55,9 @@\n   document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n   document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n   document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n }\r\n+\r\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n   const g = document.getElementById('deck-grid');\r\n   g.innerHTML = '';\r\n@@ -162,19 +163,20 @@\n   renderCards();\r\n }\r\n \r\n // อ่าน hash ตอนโหลดหน้า\r\n-window.addEventListener('load', function() {\r\n+window.addEventListener('load', async function() {\r\n+  // รอ Supabase เช็ค session ให้เสร็จก่อน\r\n+  const { data } = await _sb.auth.getSession();\r\n+  currentUser = data.session?.user ?? null;\r\n+\r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n-    showAdmin();\r\n+    await showAdmin(); // await ด้วย\r\n   } else if (hash.startsWith('#play/')) {\r\n     const deckId = hash.replace('#play/', '');\r\n     const deck = DB.decks.find(d => d.id === deckId);\r\n-    if (deck) {\r\n-      selDeck = [deckId];\r\n-      startGame();\r\n-    }\r\n+    if (deck) { selDeck = [deckId]; startGame(); }\r\n   }\r\n });\r\n \r\n // เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n"
                },
                {
                    "date": 1774566543781,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -156,9 +156,8 @@\n   layoutMode = mode;\r\n   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n   \r\n-  // เพิ่มบรรทัดนี้\r\n   document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n   \r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774567474394,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,4 @@\n-\r\n /* =========================================\r\n    app.js — Credits, Navigation, Utilities\r\n    ========================================= */\r\n \r\n@@ -12,12 +11,13 @@\n   bolt:    'th-bolt',\r\n   default: 'th-default'\r\n };\r\n \r\n-let credits  = parseInt(localStorage.getItem('ds_cr') || '0');\r\n+let credits = 0; // โหลดจาก Supabase ตอน init\r\n let selDeck = [];\r\n \r\n-const saveCr = () => localStorage.setItem('ds_cr', credits);\r\n+// saveCr / updateCr ใช้ Supabase แทน localStorage\r\n+const saveCr = async () => { await API.saveCredits(credits); };\r\n const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n \r\n /* ---- CREDIT DISPLAY ---- */\r\n function updateCr() {\r\n@@ -25,13 +25,20 @@\n   const gcr = document.getElementById('g-cr');\r\n   if (gcr) gcr.textContent = credits;\r\n }\r\n \r\n+/* โหลดเครดิตจาก Supabase แล้วอัปเดต UI */\r\n+async function loadCredits() {\r\n+  if (!currentUser) { credits = 0; updateCr(); return; }\r\n+  credits = await API.getCredits();\r\n+  updateCr();\r\n+}\r\n+\r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n   selDeck = [];\r\n   document.getElementById('home-screen').style.display  = '';\r\n-  document.getElementById('btn-admin').style.display = '';\r\n+  document.getElementById('btn-admin').style.display    = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n@@ -54,15 +61,56 @@\n   document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n   document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n   document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n   document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n+\r\n+  // โหลด user list สำหรับแอดมิน\r\n+  loadAdminUserList();\r\n }\r\n \r\n+/* ---- ADMIN: USER CREDIT LIST ---- */\r\n+async function loadAdminUserList() {\r\n+  const container = document.getElementById('admin-user-list');\r\n+  if (!container) return;\r\n+\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+  const users = await API.getAllUsers();\r\n+\r\n+  if (!users.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบผู้ใช้</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  container.innerHTML = users.map(u => `\r\n+    <div class=\"user-cr-row\">\r\n+      <div class=\"ucr-info\">\r\n+        <span class=\"ucr-name\">${u.username}</span>\r\n+        ${u.is_admin ? '<span class=\"tag\" style=\"color:var(--red);\">admin</span>' : ''}\r\n+      </div>\r\n+      <div class=\"ucr-ctrl\">\r\n+        <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:90px;\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n+          <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n+}\r\n+\r\n+async function saveUserCredit(userId) {\r\n+  const input = document.getElementById('ucr-' + userId);\r\n+  const val   = parseInt(input.value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+  const ok = await API.adminSetCredits(userId, val);\r\n+  if (ok) toast('บันทึกเครดิตแล้ว', 'success');\r\n+  else    toast('เกิดข้อผิดพลาด', 'error');\r\n+}\r\n+\r\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n   const g = document.getElementById('deck-grid');\r\n   g.innerHTML = '';\r\n-  DB.decks.filter(d => !d.hidden).forEach(d => {  // เพิ่ม .filter(d => !d.hidden)\r\n+  DB.decks.filter(d => !d.hidden).forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n     const th  = getTh(d);\r\n     const el  = document.createElement('div');\r\n     el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n@@ -73,22 +121,19 @@\n       ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n       <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n     el.onclick = () => {\r\n       if (selDeck.includes(d.id)) {\r\n-        selDeck = selDeck.filter(id => id !== d.id); // ถ้าเลือกอยู่แล้ว → ยกเลิก\r\n+        selDeck = selDeck.filter(id => id !== d.id);\r\n       } else if (selDeck.length < 3) {\r\n-        selDeck.push(d.id); // เพิ่มได้ไม่เกิน 3\r\n+        selDeck.push(d.id);\r\n       } else {\r\n         toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n       }\r\n       renderDecks();\r\n     };\r\n     g.appendChild(el);\r\n   });\r\n-  // ถ้า deck ที่เลือกอยู่ถูกซ่อน ให้ reset\r\n-  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) {\r\n-    selDeck = null;\r\n-  }\r\n+  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n   document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n@@ -107,9 +152,9 @@\n \r\n function toast(msg, type = 'success') {\r\n   const t   = document.getElementById('toast');\r\n   const ico = document.getElementById('toast-ico');\r\n-  ico.className  = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n+  ico.className   = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n   ico.style.color = T_CLR[type] || '#4caf50';\r\n   document.getElementById('toast-msg').textContent = msg;\r\n   t.classList.add('show');\r\n   clearTimeout(t._t);\r\n@@ -118,33 +163,35 @@\n \r\n /* ---- TOPUP MODAL ---- */\r\n function openTopup() {\r\n   document.getElementById('topup-overlay').classList.add('show');\r\n-  // เพิ่มบรรทัดนี้\r\n   document.getElementById('topup-cr-disp').textContent = credits;\r\n }\r\n function closeTopup() {\r\n   document.getElementById('topup-overlay').classList.remove('show');\r\n   document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n }\r\n-function selPkg(el)   { document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel')); el.classList.add('sel'); }\r\n-function freeCredit() { credits += 10; saveCr(); updateCr(); toast('รับ 10 เครดิตแล้ว!', 'success'); closeTopup(); }\r\n+function selPkg(el) {\r\n+  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n+  el.classList.add('sel');\r\n+}\r\n \r\n /* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n ['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n   const el = document.getElementById(id);\r\n   if (!el) return;\r\n   el.addEventListener('click', function (e) {\r\n     if (e.target !== this) return;\r\n-    if (id === 'topup-overlay')  closeTopup();\r\n+    if (id === 'topup-overlay')    closeTopup();\r\n     else if (id === 'res-overlay') closeRes();\r\n-    else closeDeckForm();\r\n+    else                           closeDeckForm();\r\n   });\r\n });\r\n \r\n /* ---- INIT ---- */\r\n updateCr();\r\n renderDecks();\r\n+\r\n function applyTestMode() {\r\n   const isTest = DB.settings.testMode === 1;\r\n   const banner = document.getElementById('test-banner');\r\n   const btn    = document.getElementById('test-btn');\r\n@@ -155,28 +202,27 @@\n function setLayout(mode) {\r\n   layoutMode = mode;\r\n   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-  \r\n   document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n-  \r\n   renderCards();\r\n }\r\n \r\n-// อ่าน hash ตอนโหลดหน้า\r\n-window.addEventListener('load', async function() {\r\n-  // รอ Supabase เช็ค session ให้เสร็จก่อน\r\n+/* ---- LOAD: รอ Supabase session ก่อนเสมอ ---- */\r\n+window.addEventListener('load', async function () {\r\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n \r\n+  // โหลดเครดิตจาก Supabase\r\n+  await loadCredits();\r\n+\r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n-    await showAdmin(); // await ด้วย\r\n+    await showAdmin();\r\n   } else if (hash.startsWith('#play/')) {\r\n     const deckId = hash.replace('#play/', '');\r\n-    const deck = DB.decks.find(d => d.id === deckId);\r\n+    const deck   = DB.decks.find(d => d.id === deckId);\r\n     if (deck) { selDeck = [deckId]; startGame(); }\r\n   }\r\n });\r\n \r\n-// เพิ่มที่ด้านล่างสุด ต่อจาก renderDecks();\r\n-applyTestMode();\r\n+applyTestMode();\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774615141032,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,230 @@\n+/* =========================================\r\n+   app.js — Credits, Navigation, Utilities\r\n+   ========================================= */\r\n+\r\n+const THEMES = {\r\n+  fire:    'th-fire',\r\n+  magic:   'th-magic',\r\n+  skull:   'th-skull',\r\n+  heart:   'th-heart',\r\n+  star:    'th-star',\r\n+  bolt:    'th-bolt',\r\n+  default: 'th-default'\r\n+};\r\n+\r\n+let credits = 0; // โหลดจาก Supabase ตอน init\r\n+let selDeck = [];\r\n+\r\n+// saveCr / updateCr ใช้ Supabase แทน localStorage\r\n+const saveCr = async () => { await API.saveCredits(credits); };\r\n+const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n+\r\n+/* ---- CREDIT DISPLAY ---- */\r\n+function updateCr() {\r\n+  document.getElementById('cr-disp').textContent = credits;\r\n+  const gcr = document.getElementById('g-cr');\r\n+  if (gcr) gcr.textContent = credits;\r\n+}\r\n+\r\n+/* โหลดเครดิตจาก Supabase แล้วอัปเดต UI */\r\n+async function loadCredits() {\r\n+  if (!currentUser) { credits = 0; updateCr(); return; }\r\n+  credits = await API.getCredits();\r\n+  updateCr();\r\n+}\r\n+\r\n+/* ---- NAVIGATION ---- */\r\n+function goHome() {\r\n+  selDeck = [];\r\n+  document.getElementById('home-screen').style.display  = '';\r\n+  document.getElementById('btn-admin').style.display    = '';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'none';\r\n+  document.body.classList.remove('in-game');\r\n+  history.pushState({}, '', '#');\r\n+  renderDecks();\r\n+  updateCr();\r\n+}\r\n+\r\n+async function showAdmin() {\r\n+  if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n+  if (!(await isAdminLoggedIn())) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n+\r\n+  history.pushState({}, '', '#admin');\r\n+  document.getElementById('home-screen').style.display  = 'none';\r\n+  document.getElementById('game-screen').style.display  = 'none';\r\n+  document.getElementById('admin-screen').style.display = 'block';\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+setVal('st-cr',    DB.settings.startCredit || 10);\r\n+setVal('st-nm',    DB.settings.siteName || 'DRINKORDOOM');\r\n+// ... ต่อเหมือนเดิม\r\n+  document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n+  document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n+  document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n+  document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n+\r\n+  // โหลด user list สำหรับแอดมิน\r\n+  loadAdminUserList();\r\n+}\r\n+\r\n+/* ---- ADMIN: USER CREDIT LIST ---- */\r\n+async function loadAdminUserList() {\r\n+  const container = document.getElementById('admin-user-list');\r\n+  if (!container) return;\r\n+\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+  const users = await API.getAllUsers();\r\n+\r\n+  if (!users.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบผู้ใช้</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  container.innerHTML = users.map(u => `\r\n+    <div class=\"user-cr-row\">\r\n+      <div class=\"ucr-info\">\r\n+        <span class=\"ucr-name\">${u.username}</span>\r\n+        ${u.is_admin ? '<span class=\"tag\" style=\"color:var(--red);\">admin</span>' : ''}\r\n+      </div>\r\n+      <div class=\"ucr-ctrl\">\r\n+        <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:90px;\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n+          <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n+}\r\n+\r\n+async function saveUserCredit(userId) {\r\n+  const input = document.getElementById('ucr-' + userId);\r\n+  const val   = parseInt(input.value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+  const ok = await API.adminSetCredits(userId, val);\r\n+  if (ok) toast('บันทึกเครดิตแล้ว', 'success');\r\n+  else    toast('เกิดข้อผิดพลาด', 'error');\r\n+}\r\n+\r\n+/* ---- DECK SELECTION (HOME) ---- */\r\n+function renderDecks() {\r\n+  const g = document.getElementById('deck-grid');\r\n+  g.innerHTML = '';\r\n+  DB.decks.filter(d => !d.hidden).forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n+    el.innerHTML = `\r\n+      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div class=\"d-name\">${d.name}</div>\r\n+      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n+      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n+      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n+    el.onclick = () => {\r\n+      if (selDeck.includes(d.id)) {\r\n+        selDeck = selDeck.filter(id => id !== d.id);\r\n+      } else if (selDeck.length < 3) {\r\n+        selDeck.push(d.id);\r\n+      } else {\r\n+        toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n+      }\r\n+      renderDecks();\r\n+    };\r\n+    g.appendChild(el);\r\n+  });\r\n+  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n+  document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n+}\r\n+\r\n+/* ---- TOAST ---- */\r\n+const T_ICO = {\r\n+  success: 'fi-sr-check-circle',\r\n+  error:   'fi-sr-cross-circle',\r\n+  warning: 'fi-sr-bell-ring',\r\n+  info:    'fi-sr-info'\r\n+};\r\n+const T_CLR = {\r\n+  success: '#4caf50',\r\n+  error:   'var(--red)',\r\n+  warning: 'var(--gold)',\r\n+  info:    'var(--text2)'\r\n+};\r\n+\r\n+function toast(msg, type = 'success') {\r\n+  const t   = document.getElementById('toast');\r\n+  const ico = document.getElementById('toast-ico');\r\n+  ico.className   = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n+  ico.style.color = T_CLR[type] || '#4caf50';\r\n+  document.getElementById('toast-msg').textContent = msg;\r\n+  t.classList.add('show');\r\n+  clearTimeout(t._t);\r\n+  t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n+}\r\n+\r\n+/* ---- TOPUP MODAL ---- */\r\n+function openTopup() {\r\n+  document.getElementById('topup-overlay').classList.add('show');\r\n+  document.getElementById('topup-cr-disp').textContent = credits;\r\n+}\r\n+function closeTopup() {\r\n+  document.getElementById('topup-overlay').classList.remove('show');\r\n+  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n+}\r\n+function selPkg(el) {\r\n+  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n+  el.classList.add('sel');\r\n+}\r\n+\r\n+/* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n+['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n+  const el = document.getElementById(id);\r\n+  if (!el) return;\r\n+  el.addEventListener('click', function (e) {\r\n+    if (e.target !== this) return;\r\n+    if (id === 'topup-overlay')    closeTopup();\r\n+    else if (id === 'res-overlay') closeRes();\r\n+    else                           closeDeckForm();\r\n+  });\r\n+});\r\n+\r\n+/* ---- INIT ---- */\r\n+updateCr();\r\n+renderDecks();\r\n+\r\n+function applyTestMode() {\r\n+  const isTest = DB.settings.testMode === 1;\r\n+  const banner = document.getElementById('test-banner');\r\n+  const btn    = document.getElementById('test-btn');\r\n+  if (banner) banner.style.display = isTest ? '' : 'none';\r\n+  if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n+}\r\n+\r\n+function setLayout(mode) {\r\n+  layoutMode = mode;\r\n+  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n+  renderCards();\r\n+}\r\n+\r\n+/* ---- LOAD: รอ Supabase session ก่อนเสมอ ---- */\r\n+window.addEventListener('load', async function () {\r\n+  const { data } = await _sb.auth.getSession();\r\n+  currentUser = data.session?.user ?? null;\r\n+\r\n+  // โหลดเครดิตจาก Supabase\r\n+  await loadCredits();\r\n+\r\n+  const hash = window.location.hash;\r\n+  if (hash === '#admin') {\r\n+    await showAdmin();\r\n+  } else if (hash.startsWith('#play/')) {\r\n+    const deckId = hash.replace('#play/', '');\r\n+    const deck   = DB.decks.find(d => d.id === deckId);\r\n+    if (deck) { selDeck = [deckId]; startGame(); }\r\n+  }\r\n+});\r\n+\r\n+applyTestMode();\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774615155713,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -55,12 +55,12 @@\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'block';\r\n   renderAdmDecks();\r\n   renderAdmSel();\r\n-const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n-setVal('st-cr',    DB.settings.startCredit || 10);\r\n-setVal('st-nm',    DB.settings.siteName || 'DRINKORDOOM');\r\n-// ... ต่อเหมือนเดิม\r\n+  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+  setVal('st-cr',    DB.settings.startCredit || 10);\r\n+  setVal('st-nm',    DB.settings.siteName || 'DRINKORDOOM');\r\n+  // ... ต่อเหมือนเดิม\r\n   document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n   document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n   document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n   document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n@@ -165,9 +165,10 @@\n \r\n /* ---- TOPUP MODAL ---- */\r\n function openTopup() {\r\n   document.getElementById('topup-overlay').classList.add('show');\r\n-  document.getElementById('topup-cr-disp').textContent = credits;\r\n+  const el = document.getElementById('topup-cr-disp');\r\n+  if (el) el.textContent = credits;\r\n }\r\n function closeTopup() {\r\n   document.getElementById('topup-overlay').classList.remove('show');\r\n   document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n@@ -226,233 +227,5 @@\n     if (deck) { selDeck = [deckId]; startGame(); }\r\n   }\r\n });\r\n \r\n-applyTestMode();\n-/* =========================================\r\n-   app.js — Credits, Navigation, Utilities\r\n-   ========================================= */\r\n-\r\n-const THEMES = {\r\n-  fire:    'th-fire',\r\n-  magic:   'th-magic',\r\n-  skull:   'th-skull',\r\n-  heart:   'th-heart',\r\n-  star:    'th-star',\r\n-  bolt:    'th-bolt',\r\n-  default: 'th-default'\r\n-};\r\n-\r\n-let credits = 0; // โหลดจาก Supabase ตอน init\r\n-let selDeck = [];\r\n-\r\n-// saveCr / updateCr ใช้ Supabase แทน localStorage\r\n-const saveCr = async () => { await API.saveCredits(credits); };\r\n-const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n-\r\n-/* ---- CREDIT DISPLAY ---- */\r\n-function updateCr() {\r\n-  document.getElementById('cr-disp').textContent = credits;\r\n-  const gcr = document.getElementById('g-cr');\r\n-  if (gcr) gcr.textContent = credits;\r\n-}\r\n-\r\n-/* โหลดเครดิตจาก Supabase แล้วอัปเดต UI */\r\n-async function loadCredits() {\r\n-  if (!currentUser) { credits = 0; updateCr(); return; }\r\n-  credits = await API.getCredits();\r\n-  updateCr();\r\n-}\r\n-\r\n-/* ---- NAVIGATION ---- */\r\n-function goHome() {\r\n-  selDeck = [];\r\n-  document.getElementById('home-screen').style.display  = '';\r\n-  document.getElementById('btn-admin').style.display    = '';\r\n-  document.getElementById('game-screen').style.display  = 'none';\r\n-  document.getElementById('admin-screen').style.display = 'none';\r\n-  document.body.classList.remove('in-game');\r\n-  history.pushState({}, '', '#');\r\n-  renderDecks();\r\n-  updateCr();\r\n-}\r\n-\r\n-async function showAdmin() {\r\n-  if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n-  if (!(await isAdminLoggedIn())) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n-\r\n-  history.pushState({}, '', '#admin');\r\n-  document.getElementById('home-screen').style.display  = 'none';\r\n-  document.getElementById('game-screen').style.display  = 'none';\r\n-  document.getElementById('admin-screen').style.display = 'block';\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  document.getElementById('st-cr').value    = DB.settings.startCredit || 10;\r\n-  document.getElementById('st-nm').value    = DB.settings.siteName || 'DRINKORDOOM';\r\n-  document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n-  document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n-  document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n-  document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n-\r\n-  // โหลด user list สำหรับแอดมิน\r\n-  loadAdminUserList();\r\n-}\r\n-\r\n-/* ---- ADMIN: USER CREDIT LIST ---- */\r\n-async function loadAdminUserList() {\r\n-  const container = document.getElementById('admin-user-list');\r\n-  if (!container) return;\r\n-\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n-  const users = await API.getAllUsers();\r\n-\r\n-  if (!users.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบผู้ใช้</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  container.innerHTML = users.map(u => `\r\n-    <div class=\"user-cr-row\">\r\n-      <div class=\"ucr-info\">\r\n-        <span class=\"ucr-name\">${u.username}</span>\r\n-        ${u.is_admin ? '<span class=\"tag\" style=\"color:var(--red);\">admin</span>' : ''}\r\n-      </div>\r\n-      <div class=\"ucr-ctrl\">\r\n-        <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:90px;\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n-          <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('');\r\n-}\r\n-\r\n-async function saveUserCredit(userId) {\r\n-  const input = document.getElementById('ucr-' + userId);\r\n-  const val   = parseInt(input.value);\r\n-  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-  const ok = await API.adminSetCredits(userId, val);\r\n-  if (ok) toast('บันทึกเครดิตแล้ว', 'success');\r\n-  else    toast('เกิดข้อผิดพลาด', 'error');\r\n-}\r\n-\r\n-/* ---- DECK SELECTION (HOME) ---- */\r\n-function renderDecks() {\r\n-  const g = document.getElementById('deck-grid');\r\n-  g.innerHTML = '';\r\n-  DB.decks.filter(d => !d.hidden).forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const el  = document.createElement('div');\r\n-    el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n-    el.innerHTML = `\r\n-      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div class=\"d-name\">${d.name}</div>\r\n-      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n-      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n-      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n-    el.onclick = () => {\r\n-      if (selDeck.includes(d.id)) {\r\n-        selDeck = selDeck.filter(id => id !== d.id);\r\n-      } else if (selDeck.length < 3) {\r\n-        selDeck.push(d.id);\r\n-      } else {\r\n-        toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n-      }\r\n-      renderDecks();\r\n-    };\r\n-    g.appendChild(el);\r\n-  });\r\n-  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n-  document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n-}\r\n-\r\n-/* ---- TOAST ---- */\r\n-const T_ICO = {\r\n-  success: 'fi-sr-check-circle',\r\n-  error:   'fi-sr-cross-circle',\r\n-  warning: 'fi-sr-bell-ring',\r\n-  info:    'fi-sr-info'\r\n-};\r\n-const T_CLR = {\r\n-  success: '#4caf50',\r\n-  error:   'var(--red)',\r\n-  warning: 'var(--gold)',\r\n-  info:    'var(--text2)'\r\n-};\r\n-\r\n-function toast(msg, type = 'success') {\r\n-  const t   = document.getElementById('toast');\r\n-  const ico = document.getElementById('toast-ico');\r\n-  ico.className   = `fi ${T_ICO[type] || 'fi-sr-check-circle'}`;\r\n-  ico.style.color = T_CLR[type] || '#4caf50';\r\n-  document.getElementById('toast-msg').textContent = msg;\r\n-  t.classList.add('show');\r\n-  clearTimeout(t._t);\r\n-  t._t = setTimeout(() => t.classList.remove('show'), 2600);\r\n-}\r\n-\r\n-/* ---- TOPUP MODAL ---- */\r\n-function openTopup() {\r\n-  document.getElementById('topup-overlay').classList.add('show');\r\n-  document.getElementById('topup-cr-disp').textContent = credits;\r\n-}\r\n-function closeTopup() {\r\n-  document.getElementById('topup-overlay').classList.remove('show');\r\n-  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n-}\r\n-function selPkg(el) {\r\n-  document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n-  el.classList.add('sel');\r\n-}\r\n-\r\n-/* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n-['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n-  const el = document.getElementById(id);\r\n-  if (!el) return;\r\n-  el.addEventListener('click', function (e) {\r\n-    if (e.target !== this) return;\r\n-    if (id === 'topup-overlay')    closeTopup();\r\n-    else if (id === 'res-overlay') closeRes();\r\n-    else                           closeDeckForm();\r\n-  });\r\n-});\r\n-\r\n-/* ---- INIT ---- */\r\n-updateCr();\r\n-renderDecks();\r\n-\r\n-function applyTestMode() {\r\n-  const isTest = DB.settings.testMode === 1;\r\n-  const banner = document.getElementById('test-banner');\r\n-  const btn    = document.getElementById('test-btn');\r\n-  if (banner) banner.style.display = isTest ? '' : 'none';\r\n-  if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n-}\r\n-\r\n-function setLayout(mode) {\r\n-  layoutMode = mode;\r\n-  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n-  renderCards();\r\n-}\r\n-\r\n-/* ---- LOAD: รอ Supabase session ก่อนเสมอ ---- */\r\n-window.addEventListener('load', async function () {\r\n-  const { data } = await _sb.auth.getSession();\r\n-  currentUser = data.session?.user ?? null;\r\n-\r\n-  // โหลดเครดิตจาก Supabase\r\n-  await loadCredits();\r\n-\r\n-  const hash = window.location.hash;\r\n-  if (hash === '#admin') {\r\n-    await showAdmin();\r\n-  } else if (hash.startsWith('#play/')) {\r\n-    const deckId = hash.replace('#play/', '');\r\n-    const deck   = DB.decks.find(d => d.id === deckId);\r\n-    if (deck) { selDeck = [deckId]; startGame(); }\r\n-  }\r\n-});\r\n-\r\n applyTestMode();\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774652064334,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -49,8 +49,10 @@\n async function showAdmin() {\r\n   if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n   if (!(await isAdminLoggedIn())) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n \r\n+\r\n+  \r\n   history.pushState({}, '', '#admin');\r\n   document.getElementById('home-screen').style.display  = 'none';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'block';\r\n"
                },
                {
                    "date": 1774652379274,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -45,33 +45,9 @@\n   renderDecks();\r\n   updateCr();\r\n }\r\n \r\n-async function showAdmin() {\r\n-  if (!currentUser) { toast('กรุณาเข้าสู่ระบบก่อน', 'warning'); openLogin(); return; }\r\n-  if (!(await isAdminLoggedIn())) { toast('คุณไม่มีสิทธิ์เข้าหน้านี้', 'error'); return; }\r\n \r\n-\r\n-  \r\n-  history.pushState({}, '', '#admin');\r\n-  document.getElementById('home-screen').style.display  = 'none';\r\n-  document.getElementById('game-screen').style.display  = 'none';\r\n-  document.getElementById('admin-screen').style.display = 'block';\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n-  setVal('st-cr',    DB.settings.startCredit || 10);\r\n-  setVal('st-nm',    DB.settings.siteName || 'DRINKORDOOM');\r\n-  // ... ต่อเหมือนเดิม\r\n-  document.getElementById('st-test').value  = DB.settings.testMode !== undefined ? DB.settings.testMode : 1;\r\n-  document.getElementById('st-modal').value = DB.settings.showModal ?? 1;\r\n-  document.getElementById('st-cost').value  = DB.settings.defaultCost || 1;\r\n-  document.getElementById('st-topup').value = DB.settings.topupEnabled !== undefined ? DB.settings.topupEnabled : 1;\r\n-\r\n-  // โหลด user list สำหรับแอดมิน\r\n-  loadAdminUserList();\r\n-}\r\n-\r\n /* ---- ADMIN: USER CREDIT LIST ---- */\r\n async function loadAdminUserList() {\r\n   const container = document.getElementById('admin-user-list');\r\n   if (!container) return;\r\n"
                },
                {
                    "date": 1774655004123,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -36,15 +36,16 @@\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n   selDeck = [];\r\n   document.getElementById('home-screen').style.display  = '';\r\n-  document.getElementById('btn-admin').style.display    = '';\r\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n   renderDecks();\r\n   updateCr();\r\n+  // รีเช็ค admin button state\r\n+  _updateAuthUI();\r\n }\r\n \r\n \r\n /* ---- ADMIN: USER CREDIT LIST ---- */\r\n@@ -179,16 +180,8 @@\n   if (banner) banner.style.display = isTest ? '' : 'none';\r\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n-function setLayout(mode) {\r\n-  layoutMode = mode;\r\n-  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-  document.getElementById('g-layout-label').textContent = mode === 'stack' ? 'แบบกองไพ่' : 'แบบกระจาย';\r\n-  renderCards();\r\n-}\r\n-\r\n /* ---- LOAD: รอ Supabase session ก่อนเสมอ ---- */\r\n window.addEventListener('load', async function () {\r\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n"
                },
                {
                    "date": 1774656737386,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,11 +27,11 @@\n }\r\n \r\n /* โหลดเครดิตจาก Supabase แล้วอัปเดต UI */\r\n async function loadCredits() {\r\n-  if (!currentUser) { credits = 0; updateCr(); return; }\r\n+  if (!currentUser) { credits = 0;  return; }\r\n   credits = await API.getCredits();\r\n-  updateCr();\r\n+  \r\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n@@ -41,9 +41,9 @@\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n   renderDecks();\r\n-  updateCr();\r\n+  \r\n   // รีเช็ค admin button state\r\n   _updateAuthUI();\r\n }\r\n \r\n@@ -169,9 +169,9 @@\n   });\r\n });\r\n \r\n /* ---- INIT ---- */\r\n-updateCr();\r\n+\r\n renderDecks();\r\n \r\n function applyTestMode() {\r\n   const isTest = DB.settings.testMode === 1;\r\n"
                },
                {
                    "date": 1774656745686,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -40,10 +40,10 @@\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n-  renderDecks();\r\n   \r\n+  \r\n   // รีเช็ค admin button state\r\n   _updateAuthUI();\r\n }\r\n \r\n@@ -108,9 +108,9 @@\n         selDeck.push(d.id);\r\n       } else {\r\n         toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n       }\r\n-      renderDecks();\r\n+      \r\n     };\r\n     g.appendChild(el);\r\n   });\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n@@ -170,10 +170,10 @@\n });\r\n \r\n /* ---- INIT ---- */\r\n \r\n-renderDecks();\r\n \r\n+\r\n function applyTestMode() {\r\n   const isTest = DB.settings.testMode === 1;\r\n   const banner = document.getElementById('test-banner');\r\n   const btn    = document.getElementById('test-btn');\r\n"
                },
                {
                    "date": 1774656751652,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -198,5 +198,4 @@\n     if (deck) { selDeck = [deckId]; startGame(); }\r\n   }\r\n });\r\n \r\n-applyTestMode();\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774656764577,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -185,17 +185,18 @@\n window.addEventListener('load', async function () {\r\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n \r\n-  // โหลดเครดิตจาก Supabase\r\n   await loadCredits();\r\n+  applyTestMode(); // ✅ ย้ายมาไว้ในนี้\r\n+  renderDecks();   // ✅\r\n+  updateCr();      // ✅\r\n \r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n     await showAdmin();\r\n   } else if (hash.startsWith('#play/')) {\r\n     const deckId = hash.replace('#play/', '');\r\n-    const deck   = DB.decks.find(d => d.id === deckId);\r\n+    const deck = DB.decks.find(d => d.id === deckId);\r\n     if (deck) { selDeck = [deckId]; startGame(); }\r\n   }\r\n-});\r\n-\r\n+});\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774657101255,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -186,11 +186,8 @@\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n \r\n   await loadCredits();\r\n-  applyTestMode(); // ✅ ย้ายมาไว้ในนี้\r\n-  renderDecks();   // ✅\r\n-  updateCr();      // ✅\r\n \r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n     await showAdmin();\r\n"
                },
                {
                    "date": 1774657116348,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -180,14 +180,16 @@\n   if (banner) banner.style.display = isTest ? '' : 'none';\r\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n-/* ---- LOAD: รอ Supabase session ก่อนเสมอ ---- */\r\n window.addEventListener('load', async function () {\r\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n \r\n   await loadCredits();\r\n+  applyTestMode(); // ✅ ย้ายมาไว้ในนี้\r\n+  renderDecks();   // ✅\r\n+  updateCr();      // ✅\r\n \r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n     await showAdmin();\r\n"
                },
                {
                    "date": 1774657480552,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -108,9 +108,9 @@\n         selDeck.push(d.id);\r\n       } else {\r\n         toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n       }\r\n-      \r\n+      renderDecks();\r\n     };\r\n     g.appendChild(el);\r\n   });\r\n   if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n"
                },
                {
                    "date": 1774657496475,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -25,13 +25,13 @@\n   const gcr = document.getElementById('g-cr');\r\n   if (gcr) gcr.textContent = credits;\r\n }\r\n \r\n-/* โหลดเครดิตจาก Supabase แล้วอัปเดต UI */\r\n+// 2. loadCredits() — เพิ่ม updateCr()\r\n async function loadCredits() {\r\n-  if (!currentUser) { credits = 0;  return; }\r\n+  if (!currentUser) { credits = 0; updateCr(); return; }\r\n   credits = await API.getCredits();\r\n-  \r\n+  updateCr(); // ✅ เพิ่มตรงนี้\r\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n"
                },
                {
                    "date": 1774657551635,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -112,9 +112,10 @@\n       renderDecks();\r\n     };\r\n     g.appendChild(el);\r\n   });\r\n-  if (selDeck && DB.decks.find(d => d.id === selDeck)?.hidden) selDeck = null;\r\n+  // 3. แก้ selDeck check\r\n+if (selDeck.length && DB.decks.find(d => d.id === selDeck[0])?.hidden) selDeck = [];\r\n   document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n"
                },
                {
                    "date": 1774658471283,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -29,9 +29,9 @@\n // 2. loadCredits() — เพิ่ม updateCr()\r\n async function loadCredits() {\r\n   if (!currentUser) { credits = 0; updateCr(); return; }\r\n   credits = await API.getCredits();\r\n-  updateCr(); // ✅ เพิ่มตรงนี้\r\n+  updateCr(); // ✅ เพิ่ม\r\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n"
                },
                {
                    "date": 1774659250265,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -186,12 +186,15 @@\n   const { data } = await _sb.auth.getSession();\r\n   currentUser = data.session?.user ?? null;\r\n \r\n   await loadCredits();\r\n-  applyTestMode(); // ✅ ย้ายมาไว้ในนี้\r\n-  renderDecks();   // ✅\r\n-  updateCr();      // ✅\r\n+  applyTestMode();\r\n+  renderDecks();\r\n+  updateCr();\r\n \r\n+  // ✅ ถ้าไม่มี session และ onAuthStateChange ยังไม่ยิง → เปิด login เอง\r\n+  if (!currentUser) openLogin();\r\n+\r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n     await showAdmin();\r\n   } else if (hash.startsWith('#play/')) {\r\n"
                },
                {
                    "date": 1774659636700,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -182,19 +182,14 @@\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n window.addEventListener('load', async function () {\r\n-  const { data } = await _sb.auth.getSession();\r\n-  currentUser = data.session?.user ?? null;\r\n-\r\n-  await loadCredits();\r\n+  // INITIAL_SESSION จัดการ auth + credits แล้ว\r\n+  // window.load ทำแค่ render UI และ hash routing\r\n   applyTestMode();\r\n   renderDecks();\r\n   updateCr();\r\n \r\n-  // ✅ ถ้าไม่มี session และ onAuthStateChange ยังไม่ยิง → เปิด login เอง\r\n-  if (!currentUser) openLogin();\r\n-\r\n   const hash = window.location.hash;\r\n   if (hash === '#admin') {\r\n     await showAdmin();\r\n   } else if (hash.startsWith('#play/')) {\r\n"
                },
                {
                    "date": 1774659846706,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -182,10 +182,9 @@\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n window.addEventListener('load', async function () {\r\n-  // INITIAL_SESSION จัดการ auth + credits แล้ว\r\n-  // window.load ทำแค่ render UI และ hash routing\r\n+  // ❌ เอา getSession + loadCredits ออก — INITIAL_SESSION จัดการแล้ว\r\n   applyTestMode();\r\n   renderDecks();\r\n   updateCr();\r\n \r\n"
                },
                {
                    "date": 1774902711635,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -181,10 +181,9 @@\n   if (banner) banner.style.display = isTest ? '' : 'none';\r\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n-window.addEventListener('load', async function () {\r\n-  // ❌ เอา getSession + loadCredits ออก — INITIAL_SESSION จัดการแล้ว\r\n+window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n   updateCr();\r\n \r\n"
                },
                {
                    "date": 1774902740897,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -185,14 +185,7 @@\n window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n   updateCr();\r\n-\r\n-  const hash = window.location.hash;\r\n-  if (hash === '#admin') {\r\n-    await showAdmin();\r\n-  } else if (hash.startsWith('#play/')) {\r\n-    const deckId = hash.replace('#play/', '');\r\n-    const deck = DB.decks.find(d => d.id === deckId);\r\n-    if (deck) { selDeck = [deckId]; startGame(); }\r\n-  }\r\n+  // ❌ ลบ hash check ออกจากที่นี่ทั้งหมด\r\n+  // ✅ auth.js จะจัดการ hash หลัง INITIAL_SESSION fired แทน\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774903533961,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -40,12 +40,10 @@\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n-  \r\n-  \r\n-  // รีเช็ค admin button state\r\n-  _updateAuthUI();\r\n+  renderDecks();\r\n+  _updateAuthUI(); // จัดการ btn-admin ด้วย\r\n }\r\n \r\n \r\n /* ---- ADMIN: USER CREDIT LIST ---- */\r\n"
                },
                {
                    "date": 1774903544198,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -182,8 +182,7 @@\n \r\n window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n-  updateCr();\r\n   // ❌ ลบ hash check ออกจากที่นี่ทั้งหมด\r\n   // ✅ auth.js จะจัดการ hash หลัง INITIAL_SESSION fired แทน\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774903821141,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -40,10 +40,12 @@\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n-  renderDecks();\r\n-  _updateAuthUI(); // จัดการ btn-admin ด้วย\r\n+  \r\n+  \r\n+  // รีเช็ค admin button state\r\n+  _updateAuthUI();\r\n }\r\n \r\n \r\n /* ---- ADMIN: USER CREDIT LIST ---- */\r\n@@ -182,7 +184,7 @@\n \r\n window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n-  // ❌ ลบ hash check ออกจากที่นี่ทั้งหมด\r\n-  // ✅ auth.js จะจัดการ hash หลัง INITIAL_SESSION fired แทน\r\n+  // ❌ ไม่เรียก updateCr() ตรงนี้ — credits = 0 อยู่แล้ว\r\n+  // ✅ INITIAL_SESSION ใน auth.js จะเรียก loadCredits() แล้ว updateCr()\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774905728057,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,12 +11,11 @@\n   bolt:    'th-bolt',\r\n   default: 'th-default'\r\n };\r\n \r\n-let credits = 0; // โหลดจาก Supabase ตอน init\r\n+let credits = 0;\r\n let selDeck = [];\r\n \r\n-// saveCr / updateCr ใช้ Supabase แทน localStorage\r\n const saveCr = async () => { await API.saveCredits(credits); };\r\n const getTh  = (d) => THEMES[d.theme || 'default'] || 'th-default';\r\n \r\n /* ---- CREDIT DISPLAY ---- */\r\n@@ -25,13 +24,12 @@\n   const gcr = document.getElementById('g-cr');\r\n   if (gcr) gcr.textContent = credits;\r\n }\r\n \r\n-// 2. loadCredits() — เพิ่ม updateCr()\r\n async function loadCredits() {\r\n   if (!currentUser) { credits = 0; updateCr(); return; }\r\n   credits = await API.getCredits();\r\n-  updateCr(); // ✅ เพิ่ม\r\n+  updateCr();\r\n }\r\n \r\n /* ---- NAVIGATION ---- */\r\n function goHome() {\r\n@@ -40,15 +38,11 @@\n   document.getElementById('game-screen').style.display  = 'none';\r\n   document.getElementById('admin-screen').style.display = 'none';\r\n   document.body.classList.remove('in-game');\r\n   history.pushState({}, '', '#');\r\n-  \r\n-  \r\n-  // รีเช็ค admin button state\r\n   _updateAuthUI();\r\n }\r\n \r\n-\r\n /* ---- ADMIN: USER CREDIT LIST ---- */\r\n async function loadAdminUserList() {\r\n   const container = document.getElementById('admin-user-list');\r\n   if (!container) return;\r\n@@ -89,8 +83,15 @@\n /* ---- DECK SELECTION (HOME) ---- */\r\n function renderDecks() {\r\n   const g = document.getElementById('deck-grid');\r\n   g.innerHTML = '';\r\n+\r\n+  // [FIX #9] ตรวจ selDeck ก่อน render — ถ้า deck ที่เลือกถูกซ่อนไปแล้ว ล้างออก\r\n+  selDeck = selDeck.filter(id => {\r\n+    const d = DB.decks.find(d => d.id === id);\r\n+    return d && !d.hidden;\r\n+  });\r\n+\r\n   DB.decks.filter(d => !d.hidden).forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n     const th  = getTh(d);\r\n     const el  = document.createElement('div');\r\n@@ -112,10 +113,9 @@\n       renderDecks();\r\n     };\r\n     g.appendChild(el);\r\n   });\r\n-  // 3. แก้ selDeck check\r\n-if (selDeck.length && DB.decks.find(d => d.id === selDeck[0])?.hidden) selDeck = [];\r\n+\r\n   document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n@@ -152,11 +152,17 @@\n function closeTopup() {\r\n   document.getElementById('topup-overlay').classList.remove('show');\r\n   document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n }\r\n-function selPkg(el) {\r\n+\r\n+// [FIX #15] selPkg นิยามครั้งเดียวที่นี่ — ลบออกจาก payment.js แล้ว\r\n+function selPkg(el, amount) {\r\n   document.querySelectorAll('.topup-opt').forEach(o => o.classList.remove('sel'));\r\n   el.classList.add('sel');\r\n+  // selectedPackage จะถูก set ใน payment.js ผ่าน amount\r\n+  if (typeof PAYMENT_CONFIG !== 'undefined') {\r\n+    selectedPackage = PAYMENT_CONFIG.packages.find(p => p.coins === amount) || null;\r\n+  }\r\n }\r\n \r\n /* ---- OVERLAY CLOSE ON BACKDROP ---- */\r\n ['topup-overlay', 'res-overlay', 'deck-form-ov'].forEach(id => {\r\n@@ -170,11 +176,8 @@\n   });\r\n });\r\n \r\n /* ---- INIT ---- */\r\n-\r\n-\r\n-\r\n function applyTestMode() {\r\n   const isTest = DB.settings.testMode === 1;\r\n   const banner = document.getElementById('test-banner');\r\n   const btn    = document.getElementById('test-btn');\r\n@@ -184,7 +187,6 @@\n \r\n window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n-  // ❌ ไม่เรียก updateCr() ตรงนี้ — credits = 0 อยู่แล้ว\r\n-  // ✅ INITIAL_SESSION ใน auth.js จะเรียก loadCredits() แล้ว updateCr()\r\n+  // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775226327961,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -184,9 +184,9 @@\n   if (banner) banner.style.display = isTest ? '' : 'none';\r\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n-window.addEventListener('load', function () {\r\n-  applyTestMode();\r\n-  renderDecks();\r\n-  // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n-});\n\\ No newline at end of file\n+// window.addEventListener('load', function () {\r\n+//   applyTestMode();\r\n+//   renderDecks();\r\n+//   // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n+// });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775226535572,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -184,9 +184,9 @@\n   if (banner) banner.style.display = isTest ? '' : 'none';\r\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n-// window.addEventListener('load', function () {\r\n-//   applyTestMode();\r\n-//   renderDecks();\r\n-//   // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n-// });\n\\ No newline at end of file\n+window.addEventListener('load', function () {\r\n+  applyTestMode();\r\n+  renderDecks();\r\n+  // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n+});\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775226542523,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -185,8 +185,8 @@\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n window.addEventListener('load', function () {\r\n-  applyTestMode();\r\n+  // applyTestMode();\r\n   renderDecks();\r\n   // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775226550405,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -185,8 +185,8 @@\n   if (btn)    btn.style.display    = isTest ? '' : 'none';\r\n }\r\n \r\n window.addEventListener('load', function () {\r\n-  // applyTestMode();\r\n+  applyTestMode();\r\n   renderDecks();\r\n   // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775234924779,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -90,32 +90,57 @@\n     const d = DB.decks.find(d => d.id === id);\r\n     return d && !d.hidden;\r\n   });\r\n \r\n-  DB.decks.filter(d => !d.hidden).forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const el  = document.createElement('div');\r\n-    el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n-    el.innerHTML = `\r\n-      <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div class=\"d-name\">${d.name}</div>\r\n-      <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n-      ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n-      <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n-    el.onclick = () => {\r\n-      if (selDeck.includes(d.id)) {\r\n-        selDeck = selDeck.filter(id => id !== d.id);\r\n-      } else if (selDeck.length < 3) {\r\n-        selDeck.push(d.id);\r\n-      } else {\r\n-        toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n-      }\r\n-      renderDecks();\r\n-    };\r\n-    g.appendChild(el);\r\n+  const visibleDecks = DB.decks.filter(d => !d.hidden);\r\n+\r\n+  // จัดกลุ่มตาม category\r\n+  const groups = {};\r\n+  const ORDER  = [];\r\n+  visibleDecks.forEach(d => {\r\n+    const cat = d.category?.trim() || 'ทั่วไป';\r\n+    if (!groups[cat]) { groups[cat] = []; ORDER.push(cat); }\r\n+    groups[cat].push(d);\r\n   });\r\n \r\n+  ORDER.forEach(cat => {\r\n+    // Section header\r\n+    const header = document.createElement('div');\r\n+    header.className = 'deck-section-header';\r\n+    header.innerHTML = `<span>${cat}</span>`;\r\n+    g.appendChild(header);\r\n+\r\n+    // Row ของไพ่ในหมวดนี้\r\n+    const row = document.createElement('div');\r\n+    row.className = 'deck-section-row';\r\n+\r\n+    groups[cat].forEach(d => {\r\n+      const cnt = (DB.cards[d.id] || []).length;\r\n+      const th  = getTh(d);\r\n+      const el  = document.createElement('div');\r\n+      el.className = 'deck-card' + (selDeck.includes(d.id) ? ' sel' : '');\r\n+      el.innerHTML = `\r\n+        <div class=\"d-ico-wrap ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+        <div class=\"d-name\">${d.name}</div>\r\n+        <div class=\"d-cnt\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;opacity:.5;\"></i> ${cnt} ใบ</div>\r\n+        ${d.desc ? `<div class=\"d-desc\">${d.desc}</div>` : ''}\r\n+        <div class=\"d-cost\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1} เครดิต/ใบ</div>`;\r\n+      el.onclick = () => {\r\n+        if (selDeck.includes(d.id)) {\r\n+          selDeck = selDeck.filter(id => id !== d.id);\r\n+        } else if (selDeck.length < 3) {\r\n+          selDeck.push(d.id);\r\n+        } else {\r\n+          toast('เลือกได้สูงสุด 3 กองครับ', 'warning');\r\n+        }\r\n+        renderDecks();\r\n+      };\r\n+      row.appendChild(el);\r\n+    });\r\n+\r\n+    g.appendChild(row);\r\n+  });\r\n+\r\n   document.getElementById('btn-play').disabled = selDeck.length < 1;\r\n }\r\n \r\n /* ---- TOAST ---- */\r\n"
                },
                {
                    "date": 1775236373383,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -213,5 +213,9 @@\n window.addEventListener('load', function () {\r\n   applyTestMode();\r\n   renderDecks();\r\n   // credits โหลดจาก auth.js → INITIAL_SESSION → loadCredits() → updateCr()\r\n-});\n\\ No newline at end of file\n+// <<<<<<< HEAD\r\n+// });\r\n+// =======\r\n+// });\r\n+// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n"
                }
            ],
            "date": 1774366390638,
            "name": "Commit-0",
            "content": "// let deckCards   = [];\r\n// let revCount    = 0;\r\n// let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n// let stackIndex  = 0;\r\n// let showModal = true;\r\n\r\n// /* ---- ตรวจจับมือถือ ---- */\r\n// function isMobile() {\r\n//   return window.innerWidth <= 640;\r\n// }\r\n\r\n// /* ---- LAYOUT TOGGLE ---- */\r\n// function setLayout(mode) {\r\n//   layoutMode = mode;\r\n//   document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n//   document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n//   renderCards();\r\n// }\r\n\r\n// function toggleModal() {\r\n//   showModal = !showModal;\r\n//   const btn = document.getElementById('lt-modal');\r\n//   btn.classList.toggle('act', showModal);\r\n//   btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n//   toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n// }\r\n\r\n// /* ---- START GAME ---- */\r\n// function startGame() {\r\n//   if (!selDeck) return;\r\n//   const deck  = DB.decks.find(d => d.id === selDeck);\r\n//   const cards = DB.cards[selDeck] || [];\r\n\r\n//   if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n//   if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n\r\n//   deckCards  = [...cards].sort(() => Math.random() - .5);\r\n//   revCount   = 0;\r\n//   stackIndex = 0;\r\n\r\n//   document.getElementById('home-screen').style.display = 'none';\r\n//   document.getElementById('game-screen').style.display = 'block';\r\n\r\n//   const th = getTh(deck);\r\n//   const iw = document.getElementById('g-ico-wrap');\r\n//   iw.className = 'g-deck-ico ' + th;\r\n//   document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n//   document.getElementById('g-nm').textContent      = deck.name;\r\n//   document.getElementById('cost-show').textContent = deck.cost || 1;\r\n\r\n//   const rw = document.getElementById('res-ico-wrap');\r\n//   rw.className = 'res-ico-wrap ' + th;\r\n//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n\r\n//   updRem();\r\n//   updateCr();\r\n//   renderCards();\r\n// }\r\n\r\n// /* ---- REMAINING COUNT ---- */\r\n// function updRem() {\r\n//   const t = deckCards.length;\r\n//   document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n// }\r\n\r\n// /* ═══════════════════════════════════════════\r\n//    โหมด 1 — SCATTER (กระจายเละ)\r\n// ═══════════════════════════════════════════ */\r\n// function renderScatter() {\r\n//   const wrap = document.getElementById('cards-wrap');\r\n//   wrap.innerHTML = '';\r\n\r\n//   const mobile = isMobile();\r\n//   const deck     = DB.decks.find(d => d.id === selDeck);\r\n//   const deckIcon = deck?.icon || 'fi-sr-layers';\r\n//   const deckTheme = getTh(deck);\r\n\r\n//   // วัดพื้นที่จริงของ wrap\r\n//   const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n//   const CW = mobile ? 72 : 96;\r\n//   const CH = mobile ? 104 : 138;\r\n\r\n//   // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n//   const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n//   const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n//   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n\r\n//   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n\r\n//   const total = deckCards.length;\r\n\r\n//   const ZX = mobile ? 6 : 7;\r\n//   const ZY = mobile ? 5 : 5;\r\n//   const zW = W / ZX;\r\n//   const zH = H / ZY;\r\n\r\n//   const zones = [];\r\n//   for (let zy = 0; zy < ZY; zy++)\r\n//     for (let zx = 0; zx < ZX; zx++)\r\n//       zones.push([zx, zy]);\r\n\r\n//   for (let i = zones.length - 1; i > 0; i--) {\r\n//     const j = Math.floor(Math.random() * (i + 1));\r\n//     [zones[i], zones[j]] = [zones[j], zones[i]];\r\n//   }\r\n\r\n//   const frag = document.createDocumentFragment();\r\n//   const els  = [];\r\n\r\n//   for (let i = 0; i < total; i++) {\r\n//     const [zx, zy] = zones[i % zones.length];\r\n//     const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n//     const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n//     const ang = (Math.random() - .5) * 72;\r\n//     const z   = Math.floor(Math.random() * total) + 1;\r\n\r\n//     const el = document.createElement('div');\r\n//     el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n//     el.dataset.i     = i;\r\n//     el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n//     el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n//     el.onclick       = () => flipCard(el, i, ang);\r\n\r\n//     frag.appendChild(el);\r\n//     els.push(el);\r\n//   }\r\n\r\n//   wrap.appendChild(frag);\r\n\r\n//   const delay = mobile ? 5 : 10;\r\n//   els.forEach((el, i) => {\r\n//     setTimeout(() => {\r\n//       el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n//       el.style.opacity    = '1';\r\n//     }, i * delay);\r\n//   });\r\n// }\r\n\r\n// /* ═══════════════════════════════════════════\r\n//    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n// ═══════════════════════════════════════════ */\r\n// function renderStack() {\r\n//   const wrap = document.getElementById('cards-wrap');\r\n//   wrap.innerHTML = '';\r\n//   wrap.style.cssText = 'min-height:420px;position:relative;';\r\n\r\n//   wrap.innerHTML = [\r\n//     '<div class=\"stack-layout\">',\r\n//       '<div class=\"stack-section stack-center\">',\r\n//         '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n//           '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n//           'กองไพ่',\r\n//         '</div>',\r\n//         '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n//         '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n//       '</div>',\r\n//     '</div>'\r\n//   ].join('');\r\n\r\n//   buildStackPile();\r\n// }\r\n\r\n// function buildStackPile() {\r\n//   const pileWrap = document.getElementById('stack-pile-wrap');\r\n//   const countEl  = document.getElementById('stack-count');\r\n//   if (!pileWrap) return;\r\n\r\n//   const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n//   const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n//   const deckTheme = getTh(deck);                            // เพิ่ม\r\n//   pileWrap.innerHTML = '';\r\n//   const remaining = deckCards.length - stackIndex;\r\n//   if (countEl) countEl.textContent = remaining + ' ใบ';\r\n\r\n//   if (remaining <= 0) {\r\n//     pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n//       + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n//       + 'หยิบครบแล้ว!</div>';\r\n//     return;\r\n//   }\r\n\r\n//   const showCount = Math.min(remaining, 6);\r\n//   for (let i = showCount - 1; i >= 0; i--) {\r\n//     const el  = document.createElement('div');\r\n//     el.className = 'crd stack-crd';\r\n//     const off = i * 2.5;\r\n//     const ang = (i - showCount / 2) * 1.8;\r\n//     el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n//       + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n//       + (i === 0\r\n//         ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n//         : 'cursor:default;');\r\n//   el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n//     if (i === 0) el.onclick = () => flipTopCard(el);\r\n//     pileWrap.appendChild(el);\r\n//   }\r\n// }\r\n\r\n// function flipTopCard(el) {\r\n//   if (stackIndex >= deckCards.length) return;\r\n//   const deck = DB.decks.find(d => d.id === selDeck);\r\n//   const cost = deck ? (deck.cost || 1) : 1;\r\n\r\n//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n\r\n//   credits -= cost;\r\n//   saveCr();\r\n//   updateCr();\r\n\r\n//   el.style.animation     = '';\r\n//   el.style.pointerEvents = 'none';\r\n//   el.classList.add('flipping');\r\n\r\n//   const rect = el.getBoundingClientRect();\r\n//   for (let i = 0; i < 8; i++) {\r\n//     const s = document.createElement('div');\r\n//     s.className = 'spk';\r\n//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n//     document.body.appendChild(s);\r\n//     setTimeout(() => s.remove(), 900);\r\n//   }\r\n\r\n//   const cardData = deckCards[stackIndex];\r\n//   stackIndex++;\r\n//   revCount++;\r\n//   updRem();\r\n\r\n//   setTimeout(() => {\r\n//     el.classList.remove('flipping');\r\n\r\n//     // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n//     el.classList.add('revealed');\r\n//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n//       + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n//       + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n//       + '</div>';\r\n\r\n//     // fade out และ remove\r\n//     setTimeout(() => {\r\n//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n//       el.style.opacity    = '0';\r\n//       el.style.transform  = 'scale(0.6)';\r\n//       setTimeout(() => el.remove(), 420);\r\n//     }, 300);\r\n\r\n//     buildStackPile();\r\n//     setTimeout(() => showRes(cardData, deck), 200);\r\n//   }, 560);\r\n// }\r\n\r\n// /* ═══════════════════════════════════════════\r\n//    RENDER CARDS — เลือก mode\r\n// ═══════════════════════════════════════════ */\r\n// function renderCards() {\r\n//   if (layoutMode === 'stack') renderStack();\r\n//   else renderScatter();\r\n// }\r\n\r\n// /* ═══════════════════════════════════════════\r\n//    FLIP CARD — scatter mode\r\n// ═══════════════════════════════════════════ */\r\n// function flipCard(el, idx, ang) {\r\n//   const deck = DB.decks.find(d => d.id === selDeck);\r\n//   const cost = deck ? (deck.cost || 1) : 1;\r\n\r\n//   if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n//   if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n\r\n//   credits -= cost;\r\n//   saveCr();\r\n//   updateCr();\r\n//   el.classList.add('flipping');\r\n\r\n//   const rect = el.getBoundingClientRect();\r\n//   for (let i = 0; i < 8; i++) {\r\n//     const s = document.createElement('div');\r\n//     s.className = 'spk';\r\n//     s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n//       + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n//       + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n//       + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n//       + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n//     document.body.appendChild(s);\r\n//     setTimeout(() => s.remove(), 900);\r\n//   }\r\n\r\n//   setTimeout(() => {\r\n//     const card = deckCards[idx % deckCards.length];\r\n//     el.classList.remove('flipping');\r\n//     el.classList.add('revealed');\r\n//     el.style.transform = 'rotate(' + ang + 'deg)';\r\n//     el.style.zIndex    = '200';\r\n//     el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n//       + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n//       + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n//       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n//       + '</div>';\r\n//     revCount++;\r\n//     updRem();\r\n//     setTimeout(() => showRes(card, deck), 200);\r\n\r\n//     // fade out และ remove หลัง modal โผล่\r\n//     setTimeout(() => {\r\n//       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n//       el.style.opacity    = '0';\r\n//       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n//       setTimeout(() => el.remove(), 420);\r\n//     }, 800);\r\n\r\n//   }, 560);\r\n// }\r\n\r\n// /* ---- RESULT MODAL ---- */\r\n// function showRes(card, deck) {\r\n//   const th = getTh(deck);\r\n//   document.getElementById('res-dlbl').innerHTML =\r\n//     '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n//   document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n//   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n//   document.getElementById('res-cat').textContent = card.cat;\r\n//   document.getElementById('res-txt').textContent  = card.text;\r\n//   document.getElementById('res-overlay').classList.add('show');\r\n// }\r\n\r\n// function closeRes() {\r\n//   document.getElementById('res-overlay').classList.remove('show');\r\n// }"
        }
    ]
}