{
    "sourceFile": "js/admin.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 80,
            "patches": [
                {
                    "date": 1774366244454,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1774366433266,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,227 @@\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  document.getElementById('tc-' + name).classList.add('act');\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  const l = document.getElementById('adm-deck-list');\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+        <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+        <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+            <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+            <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+            ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+        </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+        <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+        <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value       = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML =\r\n+      `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value  = d.name;\r\n+    document.getElementById('df-icon').value  = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = d.theme || 'default';\r\n+    document.getElementById('df-desc').value  = d.desc  || '';\r\n+    document.getElementById('df-cost').value  = d.cost  || 1;\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML =\r\n+      `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closeDeckForm() {\r\n+  document.getElementById('deck-form-ov').classList.remove('show');\r\n+}\r\n+\r\n+function editDeck(id) { openDeckForm(id); }\r\n+\r\n+function saveDeck() {\r\n+  const name  = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon  = document.getElementById('df-icon').value.trim()  || 'fi-sr-flame';\r\n+  const theme = document.getElementById('df-theme').value        || 'default';\r\n+  const desc  = document.getElementById('df-desc').value.trim();\r\n+  const cost  = parseInt(document.getElementById('df-cost').value) || 1;\r\n+\r\n+  if (editDeckId) {\r\n+    const d = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(d, { name, icon, theme, desc, cost });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost });\r\n+    DB.cards[id] = [];\r\n+  }\r\n+  saveDB();\r\n+  closeDeckForm();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+}\r\n+\r\n+function delDeck(id) {\r\n+  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+  saveDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML =\r\n+      '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML =\r\n+    `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\">\r\n+      <i class=\"fi fi-sr-cross\"></i>\r\n+    </button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  DB.cards[id] = [];\r\n+  rows.forEach(r => {\r\n+    const ins = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) DB.cards[id].push({ cat, text });\r\n+  });\r\n+  saveDB();\r\n+  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName    = document.getElementById('st-nm').value             || 'DEALSHOT';\r\n+  DB.settings.testMode    = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal   = parseInt(document.getElementById('st-modal').value);\r\n+  saveDB();\r\n+  applyTestMode();  // เพิ่ม\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+/* ---- SPREADSHEET ---- */\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url').value.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+  toast('ฟีเจอร์นี้ต้องใช้ Backend — กำลังพัฒนา', 'info');\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href     = URL.createObjectURL(b);\r\n+  a.download = 'dealshot_data.json';\r\n+  a.click();\r\n+}\r\n+\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0];\r\n+  if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      DB = JSON.parse(ev.target.result);\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch { toast('ไฟล์ไม่ถูกต้อง', 'error'); }\r\n+  };\r\n+  r.readAsText(f);\r\n+}\r\n+\r\n+function resetData() {\r\n+  if (!confirm('รีเซ็ตข้อมูลทั้งหมดกลับค่าเริ่มต้น?')) return;\r\n+  resetDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('รีเซ็ตแล้ว', 'info');\r\n+}\r\n+\r\n+function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  d.hidden = d.hidden ? 0 : 1;\r\n+  saveDB();\r\n+  renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774473308347,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -12,8 +12,14 @@\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n+\r\n+  // อัพเดต stat\r\n+const deckEl = document.getElementById('stat-decks');\r\n+const cardEl = document.getElementById('stat-cards');\r\n+if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n   const l = document.getElementById('adm-deck-list');\r\n   l.innerHTML = '';\r\n   DB.decks.forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n"
                },
                {
                    "date": 1774473313788,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -13,13 +13,13 @@\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n \r\n-  // อัพเดต stat\r\n-const deckEl = document.getElementById('stat-decks');\r\n-const cardEl = document.getElementById('stat-cards');\r\n-if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+    // อัพเดต stat\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n   const l = document.getElementById('adm-deck-list');\r\n   l.innerHTML = '';\r\n   DB.decks.forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n"
                },
                {
                    "date": 1774473533082,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -12,9 +12,8 @@\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n-\r\n     // อัพเดต stat\r\n   const deckEl = document.getElementById('stat-decks');\r\n   const cardEl = document.getElementById('stat-cards');\r\n   if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n@@ -229,5 +228,21 @@\n   d.hidden = d.hidden ? 0 : 1;\r\n   saveDB();\r\n   renderAdmDecks();\r\n   toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+function adminSetCredit() {\r\n+  const val = parseInt(document.getElementById('st-set-cr').value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+  credits = val;\r\n+  saveCr();\r\n+  updateCr();\r\n+  toast('ตั้งเครดิตเป็น ' + val + ' แล้ว', 'success');\r\n+}\r\n+\r\n+function adminResetCredit() {\r\n+  credits = 0;\r\n+  saveCr();\r\n+  updateCr();\r\n+  toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774473554896,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -175,8 +175,10 @@\n   DB.settings.startCredit = parseInt(document.getElementById('st-cr').value)   || 10;\r\n   DB.settings.siteName    = document.getElementById('st-nm').value             || 'DEALSHOT';\r\n   DB.settings.testMode    = parseInt(document.getElementById('st-test').value);\r\n   DB.settings.showModal   = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n   saveDB();\r\n   applyTestMode();  // เพิ่ม\r\n   toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n }\r\n"
                },
                {
                    "date": 1774473809257,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,8 +8,9 @@\n function switchTab(name, idx) {\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   document.getElementById('tc-' + name).classList.add('act');\r\n+  const ids = ['decks','cards','settings','sheet','credit'];\r\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n"
                },
                {
                    "date": 1774568062797,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,14 +8,18 @@\n function switchTab(name, idx) {\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   document.getElementById('tc-' + name).classList.add('act');\r\n-  const ids = ['decks','cards','settings','sheet','credit'];\r\n+\r\n+  // โหลดข้อมูลเมื่อเปลี่ยน tab\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n-    // อัพเดต stat\r\n   const deckEl = document.getElementById('stat-decks');\r\n   const cardEl = document.getElementById('stat-cards');\r\n   if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n   if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n@@ -27,25 +31,25 @@\n     const r   = document.createElement('div');\r\n     r.className = 'drow';\r\n     r.innerHTML = `\r\n     <div class=\"drow-info\">\r\n-        <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-        <div>\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n         <div class=\"drow-nm\">${d.name}</div>\r\n         <div class=\"drow-meta\">\r\n-            <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-            <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-            ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n         </div>\r\n-        </div>\r\n+      </div>\r\n     </div>\r\n     <div class=\"drow-acts\">\r\n-        <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-        <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n     </div>`;\r\n     l.appendChild(r);\r\n   });\r\n }\r\n@@ -54,9 +58,9 @@\n   const s = document.getElementById('adm-deck-sel');\r\n   s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n   DB.decks.forEach(d => {\r\n     const o = document.createElement('option');\r\n-    o.value       = d.id;\r\n+    o.value = d.id;\r\n     o.textContent = d.name;\r\n     s.appendChild(o);\r\n   });\r\n }\r\n@@ -65,34 +69,567 @@\n function openDeckForm(id) {\r\n   editDeckId = id || null;\r\n   if (id) {\r\n     const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML =\r\n-      `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n     document.getElementById('df-name').value  = d.name;\r\n     document.getElementById('df-icon').value  = d.icon  || 'fi-sr-flame';\r\n     document.getElementById('df-theme').value = d.theme || 'default';\r\n     document.getElementById('df-desc').value  = d.desc  || '';\r\n     document.getElementById('df-cost').value  = d.cost  || 1;\r\n   } else {\r\n-    document.getElementById('df-title').innerHTML =\r\n-      `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n     ['df-name', 'df-desc'].forEach(i => document.getElementById(i).value = '');\r\n     document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n     document.getElementById('df-theme').value = 'fire';\r\n     document.getElementById('df-cost').value  = '1';\r\n   }\r\n   document.getElementById('deck-form-ov').classList.add('show');\r\n }\r\n \r\n-function closeDeckForm() {\r\n-  document.getElementById('deck-form-ov').classList.remove('show');\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n+function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon  = document.getElementById('df-icon').value.trim()  || 'fi-sr-flame';\r\n+  const theme = document.getElementById('df-theme').value        || 'default';\r\n+  const desc  = document.getElementById('df-desc').value.trim();\r\n+  const cost  = parseInt(document.getElementById('df-cost').value) || 1;\r\n+\r\n+  if (editDeckId) {\r\n+    const d = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(d, { name, icon, theme, desc, cost });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost });\r\n+    DB.cards[id] = [];\r\n+  }\r\n+  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n \r\n-function editDeck(id) { openDeckForm(id); }\r\n+function delDeck(id) {\r\n+  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+  saveDB(); renderAdmDecks(); renderAdmSel();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n \r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  DB.cards[id] = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) DB.cards[id].push({ cat, text });\r\n+  });\r\n+  saveDB();\r\n+  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+/* ---- SPREADSHEET ---- */\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url').value.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+  toast('ฟีเจอร์นี้ต้องใช้ Backend — กำลังพัฒนา', 'info');\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+}\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      DB = JSON.parse(ev.target.result); saveDB(); renderAdmDecks(); renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch { toast('ไฟล์ไม่ถูกต้อง', 'error'); }\r\n+  };\r\n+  r.readAsText(f);\r\n+}\r\n+function resetData() {\r\n+  if (!confirm('รีเซ็ตข้อมูลทั้งหมดกลับค่าเริ่มต้น?')) return;\r\n+  resetDB(); renderAdmDecks(); renderAdmSel();\r\n+  toast('รีเซ็ตแล้ว', 'info');\r\n+}\r\n+\r\n+function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  d.hidden = d.hidden ? 0 : 1; saveDB(); renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: USERS\r\n+   ========================================= */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  container.innerHTML = `\r\n+    <div class=\"adm-sec-hdr\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n+      <input class=\"fc\" id=\"user-search\" placeholder=\"🔍 ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n+    </div>\r\n+    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n+\r\n+  const users = await API.getAllUsers();\r\n+  window._adminUsers = users;\r\n+  renderUserTable(users);\r\n+}\r\n+\r\n+function filterUsers() {\r\n+  const q = document.getElementById('user-search').value.toLowerCase();\r\n+  const filtered = (window._adminUsers || []).filter(u => u.username.includes(q));\r\n+  renderUserTable(filtered);\r\n+}\r\n+\r\n+function renderUserTable(users) {\r\n+  const wrap = document.getElementById('user-table-wrap');\r\n+  if (!users.length) { wrap.innerHTML = '<p class=\"adm-empty\">ไม่พบผู้ใช้</p>'; return; }\r\n+  wrap.innerHTML = `\r\n+    <table class=\"adm-table\">\r\n+      <thead>\r\n+        <tr>\r\n+          <th>Username</th>\r\n+          <th>เครดิต</th>\r\n+          <th>สิทธิ์</th>\r\n+          <th>จัดการ</th>\r\n+        </tr>\r\n+      </thead>\r\n+      <tbody>\r\n+        ${users.map(u => `\r\n+          <tr id=\"urow-${u.id}\">\r\n+            <td>\r\n+              <span class=\"ucr-name\">${u.username}</span>\r\n+            </td>\r\n+            <td>\r\n+              <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:80px;\">\r\n+            </td>\r\n+            <td>\r\n+              <label class=\"toggle-wrap\">\r\n+                <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''} onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n+                <span class=\"toggle-label\">${u.is_admin ? '<span style=\"color:var(--red);\">Admin</span>' : 'User'}</span>\r\n+              </label>\r\n+            </td>\r\n+            <td style=\"display:flex;gap:6px;\">\r\n+              <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n+                <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+              </button>\r\n+              <button class=\"btn-sm btn-del\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n+                <i class=\"fi fi-sr-trash\"></i>\r\n+              </button>\r\n+            </td>\r\n+          </tr>\r\n+        `).join('')}\r\n+      </tbody>\r\n+    </table>`;\r\n+}\r\n+\r\n+async function saveUserCredit(userId) {\r\n+  const input = document.getElementById('ucr-' + userId);\r\n+  const val   = parseInt(input.value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+\r\n+  const ok = await API.adminSetCredits(userId, val);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+\r\n+  // บันทึก credit_history\r\n+  await _sb.from('credit_history').insert({\r\n+    user_id: userId,\r\n+    amount:  val,\r\n+    type:    'admin',\r\n+    note:    `Admin ตั้งเครดิตเป็น ${val}`\r\n+  });\r\n+\r\n+  toast('บันทึกเครดิตแล้ว', 'success');\r\n+}\r\n+\r\n+async function toggleAdmin(userId, isAdmin) {\r\n+  const { error } = await _sb\r\n+    .from('profiles')\r\n+    .update({ is_admin: isAdmin })\r\n+    .eq('id', userId);\r\n+\r\n+  // sync metadata ด้วย\r\n+  await _sb.rpc('set_admin_metadata', { uid: userId, admin: isAdmin }).catch(() => {});\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(isAdmin ? 'ตั้งเป็น Admin แล้ว' : 'ถอด Admin แล้ว', 'success');\r\n+}\r\n+\r\n+async function confirmDeleteUser(userId, username) {\r\n+  if (!confirm(`ลบ user \"${username}\" ออกจากระบบ?`)) return;\r\n+  const { error } = await _sb.from('profiles').delete().eq('id', userId);\r\n+  if (error) { toast('ลบไม่ได้', 'error'); return; }\r\n+  toast('ลบ user แล้ว', 'info');\r\n+  loadUserTab();\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: STATS\r\n+   ========================================= */\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n+\r\n+  // นับการเปิดไพ่แต่ละกอง\r\n+  const { data: deckStats } = await _sb\r\n+    .from('card_stats')\r\n+    .select('deck_id')\r\n+    .order('opened_at', { ascending: false });\r\n+\r\n+  // นับ user ทั้งหมด\r\n+  const { count: userCount } = await _sb\r\n+    .from('profiles')\r\n+    .select('*', { count: 'exact', head: true });\r\n+\r\n+  // นับยอดเครดิตรวม\r\n+  const { data: crData } = await _sb\r\n+    .from('credit_history')\r\n+    .select('amount, type')\r\n+    .eq('type', 'topup');\r\n+\r\n+  const totalTopup = (crData || []).reduce((s, r) => s + r.amount, 0);\r\n+\r\n+  // สรุปสถิติตามกอง\r\n+  const deckCount = {};\r\n+  (deckStats || []).forEach(r => {\r\n+    deckCount[r.deck_id] = (deckCount[r.deck_id] || 0) + 1;\r\n+  });\r\n+  const deckRows = Object.entries(deckCount)\r\n+    .sort((a, b) => b[1] - a[1])\r\n+    .map(([id, cnt]) => {\r\n+      const deck = DB.decks.find(d => d.id === id);\r\n+      return `<tr>\r\n+        <td>${deck ? deck.name : id}</td>\r\n+        <td><strong style=\"color:var(--gold);\">${cnt}</strong> ครั้ง</td>\r\n+        <td>\r\n+          <div class=\"stat-bar-wrap\">\r\n+            <div class=\"stat-bar-fill\" style=\"width:${Math.min(100, cnt * 5)}%\"></div>\r\n+          </div>\r\n+        </td>\r\n+      </tr>`;\r\n+    }).join('') || '<tr><td colspan=\"3\" style=\"color:var(--text3);\">ยังไม่มีข้อมูล</td></tr>';\r\n+\r\n+  container.innerHTML = `\r\n+    <div class=\"stats-bar\" style=\"margin-bottom:24px;\">\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">ผู้ใช้ทั้งหมด</div>\r\n+        <div class=\"stat-val gold\">${userCount || 0}</div>\r\n+      </div>\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">ไพ่ที่เปิดแล้ว</div>\r\n+        <div class=\"stat-val red\">${(deckStats || []).length}</div>\r\n+      </div>\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">เครดิตเติมรวม</div>\r\n+        <div class=\"stat-val green\">${totalTopup}</div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ยอดนิยม</h3>\r\n+      <table class=\"adm-table\">\r\n+        <thead><tr><th>กองไพ่</th><th>เปิดแล้ว</th><th>สัดส่วน</th></tr></thead>\r\n+        <tbody>${deckRows}</tbody>\r\n+      </table>\r\n+    </div>`;\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: CREDIT HISTORY\r\n+   ========================================= */\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('credit_history')\r\n+    .select('*, profiles(username)')\r\n+    .order('created_at', { ascending: false })\r\n+    .limit(100);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p class=\"adm-empty\">ยังไม่มีประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const typeLabel = { topup: '💳 เติมเงิน', free: '🎁 รับฟรี', admin: '🔧 Admin', spend: '🃏 ใช้ไพ่' };\r\n+  const typeColor = { topup: 'var(--gold)', free: '#4caf50', admin: 'var(--red)', spend: 'var(--text2)' };\r\n+\r\n+  container.innerHTML = `\r\n+    <table class=\"adm-table\">\r\n+      <thead>\r\n+        <tr><th>เวลา</th><th>User</th><th>ประเภท</th><th>เครดิต</th><th>หมายเหตุ</th></tr>\r\n+      </thead>\r\n+      <tbody>\r\n+        ${data.map(r => `\r\n+          <tr>\r\n+            <td style=\"color:var(--text3);font-size:.8rem;\">${new Date(r.created_at).toLocaleString('th-TH')}</td>\r\n+            <td>${r.profiles?.username || '-'}</td>\r\n+            <td><span style=\"color:${typeColor[r.type] || '#fff'}\">${typeLabel[r.type] || r.type}</span></td>\r\n+            <td><strong style=\"color:var(--gold);\">+${r.amount}</strong></td>\r\n+            <td style=\"color:var(--text3);font-size:.8rem;\">${r.note || '-'}</td>\r\n+          </tr>\r\n+        `).join('')}\r\n+      </tbody>\r\n+    </table>`;\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: ANNOUNCEMENTS\r\n+   ========================================= */\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+\r\n+  const { data: list } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  const rows = (list || []).map(a => `\r\n+    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n+      <div class=\"ann-info\">\r\n+        <span class=\"ann-type ann-${a.type}\">${a.type.toUpperCase()}</span>\r\n+        <strong>${a.title}</strong>\r\n+        <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n+        ${a.expires_at ? `<span style=\"color:var(--text3);font-size:.75rem;\">หมดอายุ: ${new Date(a.expires_at).toLocaleDateString('th-TH')}</span>` : ''}\r\n+      </div>\r\n+      <div class=\"ann-acts\">\r\n+        <span class=\"tag ${a.active ? '' : 'tag-off'}\">${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}</span>\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i> ${a.active ? 'ปิด' : 'เปิด'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce(${a.id})\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('') || '<p class=\"adm-empty\">ยังไม่มีประกาศ</p>';\r\n+\r\n+  container.innerHTML = `\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-bell\" style=\"color:var(--red);\"></i> เพิ่มประกาศ/โปรโมชั่น</h3>\r\n+      <div class=\"fg\"><label>หัวข้อ</label><input class=\"fc\" id=\"ann-title\" placeholder=\"เช่น โปรโมชั่นพิเศษ!\"></div>\r\n+      <div class=\"fg\"><label>ข้อความ</label><input class=\"fc\" id=\"ann-msg\" placeholder=\"รายละเอียด...\"></div>\r\n+      <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>ประเภท</label>\r\n+          <select class=\"fc\" id=\"ann-type\">\r\n+            <option value=\"info\">📢 ข้อมูล</option>\r\n+            <option value=\"promo\">🎉 โปรโมชั่น</option>\r\n+            <option value=\"warning\">⚠️ แจ้งเตือน</option>\r\n+            <option value=\"success\">✅ ข่าวดี</option>\r\n+          </select>\r\n+        </div>\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>แสดงเป็น Popup</label>\r\n+          <select class=\"fc\" id=\"ann-popup\">\r\n+            <option value=\"false\">ไม่ใช่ (banner เท่านั้น)</option>\r\n+            <option value=\"true\">ใช่ (popup ตอนเปิดเว็บ)</option>\r\n+          </select>\r\n+        </div>\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>วันหมดอายุ (ไม่บังคับ)</label>\r\n+          <input class=\"fc\" type=\"date\" id=\"ann-expires\">\r\n+        </div>\r\n+      </div>\r\n+      <button class=\"btn-save\" onclick=\"saveAnnounce()\"><i class=\"fi fi-sr-plus\"></i> เพิ่มประกาศ</button>\r\n+    </div>\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-list\" style=\"color:var(--red);\"></i> ประกาศทั้งหมด</h3>\r\n+      <div id=\"announce-list\">${rows}</div>\r\n+    </div>`;\r\n+}\r\n+\r\n+async function saveAnnounce() {\r\n+  const title     = document.getElementById('ann-title').value.trim();\r\n+  const message   = document.getElementById('ann-msg').value.trim();\r\n+  const type      = document.getElementById('ann-type').value;\r\n+  const showPopup = document.getElementById('ann-popup').value === 'true';\r\n+  const expiresAt = document.getElementById('ann-expires').value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type, show_popup: showPopup,\r\n+    expires_at: expiresAt ? new Date(expiresAt).toISOString() : null\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  toast(active ? 'เปิดประกาศแล้ว' : 'ปิดประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  await _sb.from('announcements').delete().eq('id', id);\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* =========================================\r\n+   CREDIT ADMIN (เดิม — ใช้ฟังก์ชันจาก api.js)\r\n+   ========================================= */\r\n+function adminSetCredit() {\r\n+  const val = parseInt(document.getElementById('st-set-cr').value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+  credits = val; saveCr(); updateCr();\r\n+  toast('ตั้งเครดิตเป็น ' + val + ' แล้ว', 'success');\r\n+}\r\n+\r\n+function adminResetCredit() {\r\n+  credits = 0; saveCr(); updateCr();\r\n+  toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n+}/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  document.getElementById('tc-' + name).classList.add('act');\r\n+\r\n+  // โหลดข้อมูลเมื่อเปลี่ยน tab\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+  const l = document.getElementById('adm-deck-list');\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value  = d.name;\r\n+    document.getElementById('df-icon').value  = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = d.theme || 'default';\r\n+    document.getElementById('df-desc').value  = d.desc  || '';\r\n+    document.getElementById('df-cost').value  = d.cost  || 1;\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n function saveDeck() {\r\n-  const name  = document.getElementById('df-name').value.trim();\r\n+  const name = document.getElementById('df-name').value.trim();\r\n   if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n   const icon  = document.getElementById('df-icon').value.trim()  || 'fi-sr-flame';\r\n   const theme = document.getElementById('df-theme').value        || 'default';\r\n   const desc  = document.getElementById('df-desc').value.trim();\r\n@@ -105,22 +642,17 @@\n     const id = 'd' + Date.now();\r\n     DB.decks.push({ id, name, icon, theme, desc, cost });\r\n     DB.cards[id] = [];\r\n   }\r\n-  saveDB();\r\n-  closeDeckForm();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n+  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n   toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n \r\n function delDeck(id) {\r\n   if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n   DB.decks   = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n-  saveDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n+  saveDB(); renderAdmDecks(); renderAdmSel();\r\n   toast('ลบกองไพ่แล้ว', 'info');\r\n }\r\n \r\n /* ---- CARD EDITOR ---- */\r\n@@ -128,15 +660,13 @@\n   const id = document.getElementById('adm-deck-sel').value;\r\n   const ed = document.getElementById('card-list-ed');\r\n   if (!id) {\r\n     ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML =\r\n-      '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n     return;\r\n   }\r\n   const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML =\r\n-    `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n   ed.innerHTML = '';\r\n   (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n }\r\n \r\n@@ -147,11 +677,9 @@\n   r.innerHTML = `\r\n     <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n     <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n     <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\">\r\n-      <i class=\"fi fi-sr-cross\"></i>\r\n-    </button>`;\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n   ed.appendChild(r);\r\n   r.querySelector('input:last-of-type').focus();\r\n }\r\n \r\n@@ -160,9 +688,9 @@\n   if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n   const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n   DB.cards[id] = [];\r\n   rows.forEach(r => {\r\n-    const ins = r.querySelectorAll('input');\r\n+    const ins  = r.querySelectorAll('input');\r\n     const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n     const text = ins[1].value.trim();\r\n     if (text) DB.cards[id].push({ cat, text });\r\n   });\r\n@@ -172,16 +700,15 @@\n }\r\n \r\n /* ---- SETTINGS ---- */\r\n function saveSettings() {\r\n-  DB.settings.startCredit = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName    = document.getElementById('st-nm').value             || 'DEALSHOT';\r\n-  DB.settings.testMode    = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal   = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n   DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n   DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  saveDB();\r\n-  applyTestMode();  // เพิ่ม\r\n+  saveDB(); applyTestMode();\r\n   toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n }\r\n \r\n /* ---- SPREADSHEET ---- */\r\n@@ -194,58 +721,357 @@\n /* ---- EXPORT / IMPORT ---- */\r\n function exportData() {\r\n   const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n   const a = document.createElement('a');\r\n-  a.href     = URL.createObjectURL(b);\r\n-  a.download = 'dealshot_data.json';\r\n-  a.click();\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n }\r\n-\r\n function importData() { document.getElementById('imp-file').click(); }\r\n-\r\n function handleImport(e) {\r\n-  const f = e.target.files[0];\r\n-  if (!f) return;\r\n+  const f = e.target.files[0]; if (!f) return;\r\n   const r = new FileReader();\r\n   r.onload = ev => {\r\n     try {\r\n-      DB = JSON.parse(ev.target.result);\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n+      DB = JSON.parse(ev.target.result); saveDB(); renderAdmDecks(); renderAdmSel();\r\n       toast('Import สำเร็จ', 'success');\r\n     } catch { toast('ไฟล์ไม่ถูกต้อง', 'error'); }\r\n   };\r\n   r.readAsText(f);\r\n }\r\n-\r\n function resetData() {\r\n   if (!confirm('รีเซ็ตข้อมูลทั้งหมดกลับค่าเริ่มต้น?')) return;\r\n-  resetDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n+  resetDB(); renderAdmDecks(); renderAdmSel();\r\n   toast('รีเซ็ตแล้ว', 'info');\r\n }\r\n \r\n function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n-  d.hidden = d.hidden ? 0 : 1;\r\n-  saveDB();\r\n-  renderAdmDecks();\r\n+  d.hidden = d.hidden ? 0 : 1; saveDB(); renderAdmDecks();\r\n   toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n }\r\n \r\n+/* =========================================\r\n+   TAB: USERS\r\n+   ========================================= */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  container.innerHTML = `\r\n+    <div class=\"adm-sec-hdr\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n+      <input class=\"fc\" id=\"user-search\" placeholder=\"🔍 ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n+    </div>\r\n+    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n+\r\n+  const users = await API.getAllUsers();\r\n+  window._adminUsers = users;\r\n+  renderUserTable(users);\r\n+}\r\n+\r\n+function filterUsers() {\r\n+  const q = document.getElementById('user-search').value.toLowerCase();\r\n+  const filtered = (window._adminUsers || []).filter(u => u.username.includes(q));\r\n+  renderUserTable(filtered);\r\n+}\r\n+\r\n+function renderUserTable(users) {\r\n+  const wrap = document.getElementById('user-table-wrap');\r\n+  if (!users.length) { wrap.innerHTML = '<p class=\"adm-empty\">ไม่พบผู้ใช้</p>'; return; }\r\n+  wrap.innerHTML = `\r\n+    <table class=\"adm-table\">\r\n+      <thead>\r\n+        <tr>\r\n+          <th>Username</th>\r\n+          <th>เครดิต</th>\r\n+          <th>สิทธิ์</th>\r\n+          <th>จัดการ</th>\r\n+        </tr>\r\n+      </thead>\r\n+      <tbody>\r\n+        ${users.map(u => `\r\n+          <tr id=\"urow-${u.id}\">\r\n+            <td>\r\n+              <span class=\"ucr-name\">${u.username}</span>\r\n+            </td>\r\n+            <td>\r\n+              <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:80px;\">\r\n+            </td>\r\n+            <td>\r\n+              <label class=\"toggle-wrap\">\r\n+                <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''} onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n+                <span class=\"toggle-label\">${u.is_admin ? '<span style=\"color:var(--red);\">Admin</span>' : 'User'}</span>\r\n+              </label>\r\n+            </td>\r\n+            <td style=\"display:flex;gap:6px;\">\r\n+              <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n+                <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+              </button>\r\n+              <button class=\"btn-sm btn-del\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n+                <i class=\"fi fi-sr-trash\"></i>\r\n+              </button>\r\n+            </td>\r\n+          </tr>\r\n+        `).join('')}\r\n+      </tbody>\r\n+    </table>`;\r\n+}\r\n+\r\n+async function saveUserCredit(userId) {\r\n+  const input = document.getElementById('ucr-' + userId);\r\n+  const val   = parseInt(input.value);\r\n+  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n+\r\n+  const ok = await API.adminSetCredits(userId, val);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+\r\n+  // บันทึก credit_history\r\n+  await _sb.from('credit_history').insert({\r\n+    user_id: userId,\r\n+    amount:  val,\r\n+    type:    'admin',\r\n+    note:    `Admin ตั้งเครดิตเป็น ${val}`\r\n+  });\r\n+\r\n+  toast('บันทึกเครดิตแล้ว', 'success');\r\n+}\r\n+\r\n+async function toggleAdmin(userId, isAdmin) {\r\n+  const { error } = await _sb\r\n+    .from('profiles')\r\n+    .update({ is_admin: isAdmin })\r\n+    .eq('id', userId);\r\n+\r\n+  // sync metadata ด้วย\r\n+  await _sb.rpc('set_admin_metadata', { uid: userId, admin: isAdmin }).catch(() => {});\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(isAdmin ? 'ตั้งเป็น Admin แล้ว' : 'ถอด Admin แล้ว', 'success');\r\n+}\r\n+\r\n+async function confirmDeleteUser(userId, username) {\r\n+  if (!confirm(`ลบ user \"${username}\" ออกจากระบบ?`)) return;\r\n+  const { error } = await _sb.from('profiles').delete().eq('id', userId);\r\n+  if (error) { toast('ลบไม่ได้', 'error'); return; }\r\n+  toast('ลบ user แล้ว', 'info');\r\n+  loadUserTab();\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: STATS\r\n+   ========================================= */\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n+\r\n+  // นับการเปิดไพ่แต่ละกอง\r\n+  const { data: deckStats } = await _sb\r\n+    .from('card_stats')\r\n+    .select('deck_id')\r\n+    .order('opened_at', { ascending: false });\r\n+\r\n+  // นับ user ทั้งหมด\r\n+  const { count: userCount } = await _sb\r\n+    .from('profiles')\r\n+    .select('*', { count: 'exact', head: true });\r\n+\r\n+  // นับยอดเครดิตรวม\r\n+  const { data: crData } = await _sb\r\n+    .from('credit_history')\r\n+    .select('amount, type')\r\n+    .eq('type', 'topup');\r\n+\r\n+  const totalTopup = (crData || []).reduce((s, r) => s + r.amount, 0);\r\n+\r\n+  // สรุปสถิติตามกอง\r\n+  const deckCount = {};\r\n+  (deckStats || []).forEach(r => {\r\n+    deckCount[r.deck_id] = (deckCount[r.deck_id] || 0) + 1;\r\n+  });\r\n+  const deckRows = Object.entries(deckCount)\r\n+    .sort((a, b) => b[1] - a[1])\r\n+    .map(([id, cnt]) => {\r\n+      const deck = DB.decks.find(d => d.id === id);\r\n+      return `<tr>\r\n+        <td>${deck ? deck.name : id}</td>\r\n+        <td><strong style=\"color:var(--gold);\">${cnt}</strong> ครั้ง</td>\r\n+        <td>\r\n+          <div class=\"stat-bar-wrap\">\r\n+            <div class=\"stat-bar-fill\" style=\"width:${Math.min(100, cnt * 5)}%\"></div>\r\n+          </div>\r\n+        </td>\r\n+      </tr>`;\r\n+    }).join('') || '<tr><td colspan=\"3\" style=\"color:var(--text3);\">ยังไม่มีข้อมูล</td></tr>';\r\n+\r\n+  container.innerHTML = `\r\n+    <div class=\"stats-bar\" style=\"margin-bottom:24px;\">\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">ผู้ใช้ทั้งหมด</div>\r\n+        <div class=\"stat-val gold\">${userCount || 0}</div>\r\n+      </div>\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">ไพ่ที่เปิดแล้ว</div>\r\n+        <div class=\"stat-val red\">${(deckStats || []).length}</div>\r\n+      </div>\r\n+      <div class=\"stat-card\">\r\n+        <div class=\"stat-label\">เครดิตเติมรวม</div>\r\n+        <div class=\"stat-val green\">${totalTopup}</div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ยอดนิยม</h3>\r\n+      <table class=\"adm-table\">\r\n+        <thead><tr><th>กองไพ่</th><th>เปิดแล้ว</th><th>สัดส่วน</th></tr></thead>\r\n+        <tbody>${deckRows}</tbody>\r\n+      </table>\r\n+    </div>`;\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: CREDIT HISTORY\r\n+   ========================================= */\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('credit_history')\r\n+    .select('*, profiles(username)')\r\n+    .order('created_at', { ascending: false })\r\n+    .limit(100);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p class=\"adm-empty\">ยังไม่มีประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const typeLabel = { topup: '💳 เติมเงิน', free: '🎁 รับฟรี', admin: '🔧 Admin', spend: '🃏 ใช้ไพ่' };\r\n+  const typeColor = { topup: 'var(--gold)', free: '#4caf50', admin: 'var(--red)', spend: 'var(--text2)' };\r\n+\r\n+  container.innerHTML = `\r\n+    <table class=\"adm-table\">\r\n+      <thead>\r\n+        <tr><th>เวลา</th><th>User</th><th>ประเภท</th><th>เครดิต</th><th>หมายเหตุ</th></tr>\r\n+      </thead>\r\n+      <tbody>\r\n+        ${data.map(r => `\r\n+          <tr>\r\n+            <td style=\"color:var(--text3);font-size:.8rem;\">${new Date(r.created_at).toLocaleString('th-TH')}</td>\r\n+            <td>${r.profiles?.username || '-'}</td>\r\n+            <td><span style=\"color:${typeColor[r.type] || '#fff'}\">${typeLabel[r.type] || r.type}</span></td>\r\n+            <td><strong style=\"color:var(--gold);\">+${r.amount}</strong></td>\r\n+            <td style=\"color:var(--text3);font-size:.8rem;\">${r.note || '-'}</td>\r\n+          </tr>\r\n+        `).join('')}\r\n+      </tbody>\r\n+    </table>`;\r\n+}\r\n+\r\n+/* =========================================\r\n+   TAB: ANNOUNCEMENTS\r\n+   ========================================= */\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+\r\n+  const { data: list } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  const rows = (list || []).map(a => `\r\n+    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n+      <div class=\"ann-info\">\r\n+        <span class=\"ann-type ann-${a.type}\">${a.type.toUpperCase()}</span>\r\n+        <strong>${a.title}</strong>\r\n+        <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n+        ${a.expires_at ? `<span style=\"color:var(--text3);font-size:.75rem;\">หมดอายุ: ${new Date(a.expires_at).toLocaleDateString('th-TH')}</span>` : ''}\r\n+      </div>\r\n+      <div class=\"ann-acts\">\r\n+        <span class=\"tag ${a.active ? '' : 'tag-off'}\">${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}</span>\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i> ${a.active ? 'ปิด' : 'เปิด'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce(${a.id})\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('') || '<p class=\"adm-empty\">ยังไม่มีประกาศ</p>';\r\n+\r\n+  container.innerHTML = `\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-bell\" style=\"color:var(--red);\"></i> เพิ่มประกาศ/โปรโมชั่น</h3>\r\n+      <div class=\"fg\"><label>หัวข้อ</label><input class=\"fc\" id=\"ann-title\" placeholder=\"เช่น โปรโมชั่นพิเศษ!\"></div>\r\n+      <div class=\"fg\"><label>ข้อความ</label><input class=\"fc\" id=\"ann-msg\" placeholder=\"รายละเอียด...\"></div>\r\n+      <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>ประเภท</label>\r\n+          <select class=\"fc\" id=\"ann-type\">\r\n+            <option value=\"info\">📢 ข้อมูล</option>\r\n+            <option value=\"promo\">🎉 โปรโมชั่น</option>\r\n+            <option value=\"warning\">⚠️ แจ้งเตือน</option>\r\n+            <option value=\"success\">✅ ข่าวดี</option>\r\n+          </select>\r\n+        </div>\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>แสดงเป็น Popup</label>\r\n+          <select class=\"fc\" id=\"ann-popup\">\r\n+            <option value=\"false\">ไม่ใช่ (banner เท่านั้น)</option>\r\n+            <option value=\"true\">ใช่ (popup ตอนเปิดเว็บ)</option>\r\n+          </select>\r\n+        </div>\r\n+        <div class=\"fg\" style=\"flex:1;\">\r\n+          <label>วันหมดอายุ (ไม่บังคับ)</label>\r\n+          <input class=\"fc\" type=\"date\" id=\"ann-expires\">\r\n+        </div>\r\n+      </div>\r\n+      <button class=\"btn-save\" onclick=\"saveAnnounce()\"><i class=\"fi fi-sr-plus\"></i> เพิ่มประกาศ</button>\r\n+    </div>\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-list\" style=\"color:var(--red);\"></i> ประกาศทั้งหมด</h3>\r\n+      <div id=\"announce-list\">${rows}</div>\r\n+    </div>`;\r\n+}\r\n+\r\n+async function saveAnnounce() {\r\n+  const title     = document.getElementById('ann-title').value.trim();\r\n+  const message   = document.getElementById('ann-msg').value.trim();\r\n+  const type      = document.getElementById('ann-type').value;\r\n+  const showPopup = document.getElementById('ann-popup').value === 'true';\r\n+  const expiresAt = document.getElementById('ann-expires').value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type, show_popup: showPopup,\r\n+    expires_at: expiresAt ? new Date(expiresAt).toISOString() : null\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  toast(active ? 'เปิดประกาศแล้ว' : 'ปิดประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  await _sb.from('announcements').delete().eq('id', id);\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* =========================================\r\n+   CREDIT ADMIN (เดิม — ใช้ฟังก์ชันจาก api.js)\r\n+   ========================================= */\r\n function adminSetCredit() {\r\n   const val = parseInt(document.getElementById('st-set-cr').value);\r\n   if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-  credits = val;\r\n-  saveCr();\r\n-  updateCr();\r\n+  credits = val; saveCr(); updateCr();\r\n   toast('ตั้งเครดิตเป็น ' + val + ' แล้ว', 'success');\r\n }\r\n \r\n function adminResetCredit() {\r\n-  credits = 0;\r\n-  saveCr();\r\n-  updateCr();\r\n+  credits = 0; saveCr(); updateCr();\r\n   toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774615089254,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,11 +7,11 @@\n /* ---- TABS ---- */\r\n function switchTab(name, idx) {\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  document.getElementById('tc-' + name).classList.add('act');\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');  // ← เพิ่ม null check\r\n \r\n-  // โหลดข้อมูลเมื่อเปลี่ยน tab\r\n   if (name === 'users')    loadUserTab();\r\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n@@ -535,543 +535,5 @@\n \r\n function adminResetCredit() {\r\n   credits = 0; saveCr(); updateCr();\r\n   toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n-}/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n-\r\n-let editDeckId = null;\r\n-\r\n-/* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  document.getElementById('tc-' + name).classList.add('act');\r\n-\r\n-  // โหลดข้อมูลเมื่อเปลี่ยน tab\r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-}\r\n-\r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-  const l = document.getElementById('adm-deck-list');\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n-}\r\n-\r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n-}\r\n-\r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value  = d.name;\r\n-    document.getElementById('df-icon').value  = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = d.theme || 'default';\r\n-    document.getElementById('df-desc').value  = d.desc  || '';\r\n-    document.getElementById('df-cost').value  = d.cost  || 1;\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n-\r\n-function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon  = document.getElementById('df-icon').value.trim()  || 'fi-sr-flame';\r\n-  const theme = document.getElementById('df-theme').value        || 'default';\r\n-  const desc  = document.getElementById('df-desc').value.trim();\r\n-  const cost  = parseInt(document.getElementById('df-cost').value) || 1;\r\n-\r\n-  if (editDeckId) {\r\n-    const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost });\r\n-    DB.cards[id] = [];\r\n-  }\r\n-  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n-}\r\n-\r\n-function delDeck(id) {\r\n-  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n-  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-  saveDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n-}\r\n-\r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n-}\r\n-\r\n-function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  DB.cards[id] = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) DB.cards[id].push({ cat, text });\r\n-  });\r\n-  saveDB();\r\n-  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n-  renderAdmDecks();\r\n-}\r\n-\r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n-}\r\n-\r\n-/* ---- SPREADSHEET ---- */\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url').value.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-  toast('ฟีเจอร์นี้ต้องใช้ Backend — กำลังพัฒนา', 'info');\r\n-}\r\n-\r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n-}\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      DB = JSON.parse(ev.target.result); saveDB(); renderAdmDecks(); renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch { toast('ไฟล์ไม่ถูกต้อง', 'error'); }\r\n-  };\r\n-  r.readAsText(f);\r\n-}\r\n-function resetData() {\r\n-  if (!confirm('รีเซ็ตข้อมูลทั้งหมดกลับค่าเริ่มต้น?')) return;\r\n-  resetDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('รีเซ็ตแล้ว', 'info');\r\n-}\r\n-\r\n-function toggleDeck(id) {\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  d.hidden = d.hidden ? 0 : 1; saveDB(); renderAdmDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: USERS\r\n-   ========================================= */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  container.innerHTML = `\r\n-    <div class=\"adm-sec-hdr\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      <input class=\"fc\" id=\"user-search\" placeholder=\"🔍 ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n-    </div>\r\n-    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n-\r\n-  const users = await API.getAllUsers();\r\n-  window._adminUsers = users;\r\n-  renderUserTable(users);\r\n-}\r\n-\r\n-function filterUsers() {\r\n-  const q = document.getElementById('user-search').value.toLowerCase();\r\n-  const filtered = (window._adminUsers || []).filter(u => u.username.includes(q));\r\n-  renderUserTable(filtered);\r\n-}\r\n-\r\n-function renderUserTable(users) {\r\n-  const wrap = document.getElementById('user-table-wrap');\r\n-  if (!users.length) { wrap.innerHTML = '<p class=\"adm-empty\">ไม่พบผู้ใช้</p>'; return; }\r\n-  wrap.innerHTML = `\r\n-    <table class=\"adm-table\">\r\n-      <thead>\r\n-        <tr>\r\n-          <th>Username</th>\r\n-          <th>เครดิต</th>\r\n-          <th>สิทธิ์</th>\r\n-          <th>จัดการ</th>\r\n-        </tr>\r\n-      </thead>\r\n-      <tbody>\r\n-        ${users.map(u => `\r\n-          <tr id=\"urow-${u.id}\">\r\n-            <td>\r\n-              <span class=\"ucr-name\">${u.username}</span>\r\n-            </td>\r\n-            <td>\r\n-              <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:80px;\">\r\n-            </td>\r\n-            <td>\r\n-              <label class=\"toggle-wrap\">\r\n-                <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''} onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n-                <span class=\"toggle-label\">${u.is_admin ? '<span style=\"color:var(--red);\">Admin</span>' : 'User'}</span>\r\n-              </label>\r\n-            </td>\r\n-            <td style=\"display:flex;gap:6px;\">\r\n-              <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n-                <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n-              </button>\r\n-              <button class=\"btn-sm btn-del\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n-                <i class=\"fi fi-sr-trash\"></i>\r\n-              </button>\r\n-            </td>\r\n-          </tr>\r\n-        `).join('')}\r\n-      </tbody>\r\n-    </table>`;\r\n-}\r\n-\r\n-async function saveUserCredit(userId) {\r\n-  const input = document.getElementById('ucr-' + userId);\r\n-  const val   = parseInt(input.value);\r\n-  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-\r\n-  const ok = await API.adminSetCredits(userId, val);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-\r\n-  // บันทึก credit_history\r\n-  await _sb.from('credit_history').insert({\r\n-    user_id: userId,\r\n-    amount:  val,\r\n-    type:    'admin',\r\n-    note:    `Admin ตั้งเครดิตเป็น ${val}`\r\n-  });\r\n-\r\n-  toast('บันทึกเครดิตแล้ว', 'success');\r\n-}\r\n-\r\n-async function toggleAdmin(userId, isAdmin) {\r\n-  const { error } = await _sb\r\n-    .from('profiles')\r\n-    .update({ is_admin: isAdmin })\r\n-    .eq('id', userId);\r\n-\r\n-  // sync metadata ด้วย\r\n-  await _sb.rpc('set_admin_metadata', { uid: userId, admin: isAdmin }).catch(() => {});\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(isAdmin ? 'ตั้งเป็น Admin แล้ว' : 'ถอด Admin แล้ว', 'success');\r\n-}\r\n-\r\n-async function confirmDeleteUser(userId, username) {\r\n-  if (!confirm(`ลบ user \"${username}\" ออกจากระบบ?`)) return;\r\n-  const { error } = await _sb.from('profiles').delete().eq('id', userId);\r\n-  if (error) { toast('ลบไม่ได้', 'error'); return; }\r\n-  toast('ลบ user แล้ว', 'info');\r\n-  loadUserTab();\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: STATS\r\n-   ========================================= */\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n-\r\n-  // นับการเปิดไพ่แต่ละกอง\r\n-  const { data: deckStats } = await _sb\r\n-    .from('card_stats')\r\n-    .select('deck_id')\r\n-    .order('opened_at', { ascending: false });\r\n-\r\n-  // นับ user ทั้งหมด\r\n-  const { count: userCount } = await _sb\r\n-    .from('profiles')\r\n-    .select('*', { count: 'exact', head: true });\r\n-\r\n-  // นับยอดเครดิตรวม\r\n-  const { data: crData } = await _sb\r\n-    .from('credit_history')\r\n-    .select('amount, type')\r\n-    .eq('type', 'topup');\r\n-\r\n-  const totalTopup = (crData || []).reduce((s, r) => s + r.amount, 0);\r\n-\r\n-  // สรุปสถิติตามกอง\r\n-  const deckCount = {};\r\n-  (deckStats || []).forEach(r => {\r\n-    deckCount[r.deck_id] = (deckCount[r.deck_id] || 0) + 1;\r\n-  });\r\n-  const deckRows = Object.entries(deckCount)\r\n-    .sort((a, b) => b[1] - a[1])\r\n-    .map(([id, cnt]) => {\r\n-      const deck = DB.decks.find(d => d.id === id);\r\n-      return `<tr>\r\n-        <td>${deck ? deck.name : id}</td>\r\n-        <td><strong style=\"color:var(--gold);\">${cnt}</strong> ครั้ง</td>\r\n-        <td>\r\n-          <div class=\"stat-bar-wrap\">\r\n-            <div class=\"stat-bar-fill\" style=\"width:${Math.min(100, cnt * 5)}%\"></div>\r\n-          </div>\r\n-        </td>\r\n-      </tr>`;\r\n-    }).join('') || '<tr><td colspan=\"3\" style=\"color:var(--text3);\">ยังไม่มีข้อมูล</td></tr>';\r\n-\r\n-  container.innerHTML = `\r\n-    <div class=\"stats-bar\" style=\"margin-bottom:24px;\">\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">ผู้ใช้ทั้งหมด</div>\r\n-        <div class=\"stat-val gold\">${userCount || 0}</div>\r\n-      </div>\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">ไพ่ที่เปิดแล้ว</div>\r\n-        <div class=\"stat-val red\">${(deckStats || []).length}</div>\r\n-      </div>\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">เครดิตเติมรวม</div>\r\n-        <div class=\"stat-val green\">${totalTopup}</div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ยอดนิยม</h3>\r\n-      <table class=\"adm-table\">\r\n-        <thead><tr><th>กองไพ่</th><th>เปิดแล้ว</th><th>สัดส่วน</th></tr></thead>\r\n-        <tbody>${deckRows}</tbody>\r\n-      </table>\r\n-    </div>`;\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: CREDIT HISTORY\r\n-   ========================================= */\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('credit_history')\r\n-    .select('*, profiles(username)')\r\n-    .order('created_at', { ascending: false })\r\n-    .limit(100);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p class=\"adm-empty\">ยังไม่มีประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const typeLabel = { topup: '💳 เติมเงิน', free: '🎁 รับฟรี', admin: '🔧 Admin', spend: '🃏 ใช้ไพ่' };\r\n-  const typeColor = { topup: 'var(--gold)', free: '#4caf50', admin: 'var(--red)', spend: 'var(--text2)' };\r\n-\r\n-  container.innerHTML = `\r\n-    <table class=\"adm-table\">\r\n-      <thead>\r\n-        <tr><th>เวลา</th><th>User</th><th>ประเภท</th><th>เครดิต</th><th>หมายเหตุ</th></tr>\r\n-      </thead>\r\n-      <tbody>\r\n-        ${data.map(r => `\r\n-          <tr>\r\n-            <td style=\"color:var(--text3);font-size:.8rem;\">${new Date(r.created_at).toLocaleString('th-TH')}</td>\r\n-            <td>${r.profiles?.username || '-'}</td>\r\n-            <td><span style=\"color:${typeColor[r.type] || '#fff'}\">${typeLabel[r.type] || r.type}</span></td>\r\n-            <td><strong style=\"color:var(--gold);\">+${r.amount}</strong></td>\r\n-            <td style=\"color:var(--text3);font-size:.8rem;\">${r.note || '-'}</td>\r\n-          </tr>\r\n-        `).join('')}\r\n-      </tbody>\r\n-    </table>`;\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: ANNOUNCEMENTS\r\n-   ========================================= */\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-\r\n-  const { data: list } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n-\r\n-  const rows = (list || []).map(a => `\r\n-    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n-      <div class=\"ann-info\">\r\n-        <span class=\"ann-type ann-${a.type}\">${a.type.toUpperCase()}</span>\r\n-        <strong>${a.title}</strong>\r\n-        <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n-        ${a.expires_at ? `<span style=\"color:var(--text3);font-size:.75rem;\">หมดอายุ: ${new Date(a.expires_at).toLocaleDateString('th-TH')}</span>` : ''}\r\n-      </div>\r\n-      <div class=\"ann-acts\">\r\n-        <span class=\"tag ${a.active ? '' : 'tag-off'}\">${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}</span>\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i> ${a.active ? 'ปิด' : 'เปิด'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce(${a.id})\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('') || '<p class=\"adm-empty\">ยังไม่มีประกาศ</p>';\r\n-\r\n-  container.innerHTML = `\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-bell\" style=\"color:var(--red);\"></i> เพิ่มประกาศ/โปรโมชั่น</h3>\r\n-      <div class=\"fg\"><label>หัวข้อ</label><input class=\"fc\" id=\"ann-title\" placeholder=\"เช่น โปรโมชั่นพิเศษ!\"></div>\r\n-      <div class=\"fg\"><label>ข้อความ</label><input class=\"fc\" id=\"ann-msg\" placeholder=\"รายละเอียด...\"></div>\r\n-      <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>ประเภท</label>\r\n-          <select class=\"fc\" id=\"ann-type\">\r\n-            <option value=\"info\">📢 ข้อมูล</option>\r\n-            <option value=\"promo\">🎉 โปรโมชั่น</option>\r\n-            <option value=\"warning\">⚠️ แจ้งเตือน</option>\r\n-            <option value=\"success\">✅ ข่าวดี</option>\r\n-          </select>\r\n-        </div>\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>แสดงเป็น Popup</label>\r\n-          <select class=\"fc\" id=\"ann-popup\">\r\n-            <option value=\"false\">ไม่ใช่ (banner เท่านั้น)</option>\r\n-            <option value=\"true\">ใช่ (popup ตอนเปิดเว็บ)</option>\r\n-          </select>\r\n-        </div>\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>วันหมดอายุ (ไม่บังคับ)</label>\r\n-          <input class=\"fc\" type=\"date\" id=\"ann-expires\">\r\n-        </div>\r\n-      </div>\r\n-      <button class=\"btn-save\" onclick=\"saveAnnounce()\"><i class=\"fi fi-sr-plus\"></i> เพิ่มประกาศ</button>\r\n-    </div>\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-list\" style=\"color:var(--red);\"></i> ประกาศทั้งหมด</h3>\r\n-      <div id=\"announce-list\">${rows}</div>\r\n-    </div>`;\r\n-}\r\n-\r\n-async function saveAnnounce() {\r\n-  const title     = document.getElementById('ann-title').value.trim();\r\n-  const message   = document.getElementById('ann-msg').value.trim();\r\n-  const type      = document.getElementById('ann-type').value;\r\n-  const showPopup = document.getElementById('ann-popup').value === 'true';\r\n-  const expiresAt = document.getElementById('ann-expires').value || null;\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type, show_popup: showPopup,\r\n-    expires_at: expiresAt ? new Date(expiresAt).toISOString() : null\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function toggleAnnounce(id, active) {\r\n-  await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  toast(active ? 'เปิดประกาศแล้ว' : 'ปิดประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  await _sb.from('announcements').delete().eq('id', id);\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-/* =========================================\r\n-   CREDIT ADMIN (เดิม — ใช้ฟังก์ชันจาก api.js)\r\n-   ========================================= */\r\n-function adminSetCredit() {\r\n-  const val = parseInt(document.getElementById('st-set-cr').value);\r\n-  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-  credits = val; saveCr(); updateCr();\r\n-  toast('ตั้งเครดิตเป็น ' + val + ' แล้ว', 'success');\r\n-}\r\n-\r\n-function adminResetCredit() {\r\n-  credits = 0; saveCr(); updateCr();\r\n-  toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774615325613,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -402,9 +402,14 @@\n     container.innerHTML = '<p class=\"adm-empty\">ยังไม่มีประวัติ</p>';\r\n     return;\r\n   }\r\n \r\n-  const typeLabel = { topup: '💳 เติมเงิน', free: '🎁 รับฟรี', admin: '🔧 Admin', spend: '🃏 ใช้ไพ่' };\r\n+  const typeLabel = {\r\n+  topup: '<i class=\"fi fi-sr-credit-card\"></i> เติมเงิน',\r\n+  free:  '<i class=\"fi fi-sr-gift\"></i> รับฟรี',\r\n+  admin: '<i class=\"fi fi-sr-settings\"></i> Admin',\r\n+  spend: '<i class=\"fi fi-sr-layers\"></i> ใช้ไพ่'\r\n+};\r\n   const typeColor = { topup: 'var(--gold)', free: '#4caf50', admin: 'var(--red)', spend: 'var(--text2)' };\r\n \r\n   container.innerHTML = `\r\n     <table class=\"adm-table\">\r\n"
                },
                {
                    "date": 1774615357264,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -469,12 +469,12 @@\n       <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n           <label>ประเภท</label>\r\n           <select class=\"fc\" id=\"ann-type\">\r\n-            <option value=\"info\">📢 ข้อมูล</option>\r\n-            <option value=\"promo\">🎉 โปรโมชั่น</option>\r\n-            <option value=\"warning\">⚠️ แจ้งเตือน</option>\r\n-            <option value=\"success\">✅ ข่าวดี</option>\r\n+`<option value=\"info\">ข้อมูล</option>\r\n+ <option value=\"promo\">โปรโมชั่น</option>\r\n+ <option value=\"warning\">แจ้งเตือน</option>\r\n+ <option value=\"success\">ข่าวดี</option>`\r\n           </select>\r\n         </div>\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n           <label>แสดงเป็น Popup</label>\r\n"
                },
                {
                    "date": 1774615391051,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -469,12 +469,12 @@\n       <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n           <label>ประเภท</label>\r\n           <select class=\"fc\" id=\"ann-type\">\r\n-`<option value=\"info\">ข้อมูล</option>\r\n- <option value=\"promo\">โปรโมชั่น</option>\r\n- <option value=\"warning\">แจ้งเตือน</option>\r\n- <option value=\"success\">ข่าวดี</option>`\r\n+            <option value=\"info\">ข้อมูล</option>\r\n+            <option value=\"promo\">โปรโมชั่น</option>\r\n+            <option value=\"warning\">แจ้งเตือน</option>\r\n+            <option value=\"success\">ข่าวดี</option>\r\n           </select>\r\n         </div>\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n           <label>แสดงเป็น Popup</label>\r\n"
                },
                {
                    "date": 1774615410191,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -443,9 +443,9 @@\n \r\n   const rows = (list || []).map(a => `\r\n     <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n       <div class=\"ann-info\">\r\n-        <span class=\"ann-type ann-${a.type}\">${a.type.toUpperCase()}</span>\r\n+        <span class=\"ann-type ann-${a.type}\"><i class=\"fi ${typeIcon[a.type]}\"></i> ${a.type}</span>\r\n         <strong>${a.title}</strong>\r\n         <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n         ${a.expires_at ? `<span style=\"color:var(--text3);font-size:.75rem;\">หมดอายุ: ${new Date(a.expires_at).toLocaleDateString('th-TH')}</span>` : ''}\r\n       </div>\r\n"
                },
                {
                    "date": 1774615449874,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -441,9 +441,15 @@\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n \r\n   const rows = (list || []).map(a => `\r\n-    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n+    const typeIcon = {\r\n+  info:    'fi-sr-info',\r\n+  promo:   'fi-sr-star',\r\n+  warning: 'fi-sr-triangle-warning',\r\n+  success: 'fi-sr-check-circle'\r\n+};\r\n+\r\n       <div class=\"ann-info\">\r\n         <span class=\"ann-type ann-${a.type}\"><i class=\"fi ${typeIcon[a.type]}\"></i> ${a.type}</span>\r\n         <strong>${a.title}</strong>\r\n         <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n"
                },
                {
                    "date": 1774615457364,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -441,15 +441,9 @@\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n \r\n   const rows = (list || []).map(a => `\r\n-    const typeIcon = {\r\n-  info:    'fi-sr-info',\r\n-  promo:   'fi-sr-star',\r\n-  warning: 'fi-sr-triangle-warning',\r\n-  success: 'fi-sr-check-circle'\r\n-};\r\n-\r\n+    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n       <div class=\"ann-info\">\r\n         <span class=\"ann-type ann-${a.type}\"><i class=\"fi ${typeIcon[a.type]}\"></i> ${a.type}</span>\r\n         <strong>${a.title}</strong>\r\n         <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n"
                },
                {
                    "date": 1774615485264,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -440,8 +440,15 @@\n     .from('announcements')\r\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n \r\n+    const typeIcon = {\r\n+  info:    'fi-sr-info',\r\n+  promo:   'fi-sr-star',\r\n+  warning: 'fi-sr-triangle-warning',\r\n+  success: 'fi-sr-check-circle'\r\n+};\r\n+\r\n   const rows = (list || []).map(a => `\r\n     <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n       <div class=\"ann-info\">\r\n         <span class=\"ann-type ann-${a.type}\"><i class=\"fi ${typeIcon[a.type]}\"></i> ${a.type}</span>\r\n"
                },
                {
                    "date": 1774615517692,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -217,9 +217,11 @@\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n     <div class=\"adm-sec-hdr\">\r\n       <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      <input class=\"fc\" id=\"user-search\" placeholder=\"🔍 ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n+      // แทนที่บรรทัด input search\r\n+`<input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" \r\n+  oninput=\"filterUsers()\" style=\"width:220px;\">`\r\n     </div>\r\n     <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n@@ -441,13 +443,13 @@\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n \r\n     const typeIcon = {\r\n-  info:    'fi-sr-info',\r\n-  promo:   'fi-sr-star',\r\n-  warning: 'fi-sr-triangle-warning',\r\n-  success: 'fi-sr-check-circle'\r\n-};\r\n+        info:    'fi-sr-info',\r\n+        promo:   'fi-sr-star',\r\n+        warning: 'fi-sr-triangle-warning',\r\n+        success: 'fi-sr-check-circle'\r\n+      };\r\n \r\n   const rows = (list || []).map(a => `\r\n     <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n       <div class=\"ann-info\">\r\n"
                },
                {
                    "date": 1774615612297,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -217,11 +217,9 @@\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n     <div class=\"adm-sec-hdr\">\r\n       <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      // แทนที่บรรทัด input search\r\n-`<input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" \r\n-  oninput=\"filterUsers()\" style=\"width:220px;\">`\r\n+      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n     </div>\r\n     <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n"
                },
                {
                    "date": 1774615760491,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,13 +215,12 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n-    <div class=\"adm-sec-hdr\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n-    </div>\r\n-    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n+<div style=\"position:relative;width:220px;\">\r\n+  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n+  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n+</div>\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n   renderUserTable(users);\r\n"
                },
                {
                    "date": 1774615772148,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,12 +215,13 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n-<div style=\"position:relative;width:220px;\">\r\n-  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n-  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n-</div>\r\n+    <div class=\"adm-sec-hdr\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n+      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n+    </div>\r\n+    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n   renderUserTable(users);\r\n"
                },
                {
                    "date": 1774615777484,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,13 +215,12 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n-    <div class=\"adm-sec-hdr\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n-    </div>\r\n-    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n+<div style=\"position:relative;width:220px;\">\r\n+  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n+  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n+</div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n   renderUserTable(users);\r\n"
                },
                {
                    "date": 1774615846988,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,12 +215,13 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n-<div style=\"position:relative;width:220px;\">\r\n-  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n-  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n-</div>`;\r\n+    <div class=\"adm-sec-hdr\">\r\n+      <h3><i class=\"fi fi-sr-search\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n+      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n+    </div>\r\n+    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n   renderUserTable(users);\r\n"
                },
                {
                    "date": 1774615874617,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,12 +215,12 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n-    <div class=\"adm-sec-hdr\">\r\n-      <h3><i class=\"fi fi-sr-search\" style=\"color:var(--red);\"></i> ผู้ใช้ทั้งหมด</h3>\r\n-      <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:220px;\">\r\n-    </div>\r\n+<div style=\"position:relative;width:220px;\">\r\n+  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n+  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n+</div>\r\n     <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n"
                },
                {
                    "date": 1774615883838,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,12 +215,14 @@\n    ========================================= */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n+    <div class=\"adm-sec-hdr\">\r\n <div style=\"position:relative;width:220px;\">\r\n   <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n   <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n </div>\r\n+    </div>\r\n     <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n   window._adminUsers = users;\r\n"
                },
                {
                    "date": 1774616457226,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -216,12 +216,12 @@\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   container.innerHTML = `\r\n     <div class=\"adm-sec-hdr\">\r\n-<div style=\"position:relative;width:220px;\">\r\n-  <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n-  <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n-</div>\r\n+      <div style=\"position:relative;width:220px;\">\r\n+        <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n+        <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n+      </div>\r\n     </div>\r\n     <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n \r\n   const users = await API.getAllUsers();\r\n@@ -436,65 +436,75 @@\n    TAB: ANNOUNCEMENTS\r\n    ========================================= */\r\n async function loadAnnounceTab() {\r\n   const container = document.getElementById('tc-announce');\r\n-\r\n+ \r\n   const { data: list } = await _sb\r\n     .from('announcements')\r\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n-\r\n-    const typeIcon = {\r\n-        info:    'fi-sr-info',\r\n-        promo:   'fi-sr-star',\r\n-        warning: 'fi-sr-triangle-warning',\r\n-        success: 'fi-sr-check-circle'\r\n-      };\r\n-\r\n+ \r\n+  const typeIcon  = { info: 'fi-sr-info', promo: 'fi-sr-star', warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle' };\r\n+  const typeLabel = { info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี' };\r\n+  const typeColor = { info: '#60c0ff', promo: 'var(--gold)', warning: 'orange', success: '#4caf50' };\r\n+ \r\n   const rows = (list || []).map(a => `\r\n     <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n-      <div class=\"ann-info\">\r\n-        <span class=\"ann-type ann-${a.type}\"><i class=\"fi ${typeIcon[a.type]}\"></i> ${a.type}</span>\r\n-        <strong>${a.title}</strong>\r\n-        <span style=\"color:var(--text3);font-size:.8rem;\">${a.message}</span>\r\n-        ${a.expires_at ? `<span style=\"color:var(--text3);font-size:.75rem;\">หมดอายุ: ${new Date(a.expires_at).toLocaleDateString('th-TH')}</span>` : ''}\r\n+      <div class=\"ann-left\">\r\n+        <div class=\"ann-icon-wrap ann-${a.type}\">\r\n+          <i class=\"fi ${typeIcon[a.type] || 'fi-sr-info'}\"></i>\r\n+        </div>\r\n       </div>\r\n+      <div class=\"ann-body\">\r\n+        <div class=\"ann-meta\">\r\n+          <span class=\"ann-type-badge ann-${a.type}\">\r\n+            <i class=\"fi ${typeIcon[a.type]}\"></i> ${typeLabel[a.type] || a.type}\r\n+          </span>\r\n+          <span class=\"ann-status ${a.active ? 'ann-on' : 'ann-off'}\">\r\n+            <i class=\"fi fi-sr-${a.active ? 'eye' : 'eye-crossed'}\"></i>\r\n+            ${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-popup-badge\"><i class=\"fi fi-sr-window\"></i> Popup</span>' : ''}\r\n+        </div>\r\n+        <div class=\"ann-title-text\">${a.title}</div>\r\n+        <div class=\"ann-message-text\">${a.message}</div>\r\n+        ${a.expires_at ? `<div class=\"ann-expires\"><i class=\"fi fi-sr-clock\"></i> หมดอายุ ${new Date(a.expires_at).toLocaleDateString('th-TH')}</div>` : ''}\r\n+      </div>\r\n       <div class=\"ann-acts\">\r\n-        <span class=\"tag ${a.active ? '' : 'tag-off'}\">${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}</span>\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i> ${a.active ? 'ปิด' : 'เปิด'}\r\n+        <button class=\"btn-sm ${a.active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n         </button>\r\n         <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce(${a.id})\">\r\n           <i class=\"fi fi-sr-trash\"></i>\r\n         </button>\r\n       </div>\r\n     </div>\r\n-  `).join('') || '<p class=\"adm-empty\">ยังไม่มีประกาศ</p>';\r\n-\r\n+  `).join('') || '<p class=\"adm-empty\"><i class=\"fi fi-sr-bell-slash\"></i> ยังไม่มีประกาศ</p>';\r\n+ \r\n   container.innerHTML = `\r\n     <div class=\"sheet-sec\">\r\n       <h3><i class=\"fi fi-sr-bell\" style=\"color:var(--red);\"></i> เพิ่มประกาศ/โปรโมชั่น</h3>\r\n       <div class=\"fg\"><label>หัวข้อ</label><input class=\"fc\" id=\"ann-title\" placeholder=\"เช่น โปรโมชั่นพิเศษ!\"></div>\r\n       <div class=\"fg\"><label>ข้อความ</label><input class=\"fc\" id=\"ann-msg\" placeholder=\"รายละเอียด...\"></div>\r\n       <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>ประเภท</label>\r\n+          <label><i class=\"fi fi-sr-tag\"></i> ประเภท</label>\r\n           <select class=\"fc\" id=\"ann-type\">\r\n             <option value=\"info\">ข้อมูล</option>\r\n             <option value=\"promo\">โปรโมชั่น</option>\r\n             <option value=\"warning\">แจ้งเตือน</option>\r\n             <option value=\"success\">ข่าวดี</option>\r\n           </select>\r\n         </div>\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>แสดงเป็น Popup</label>\r\n+          <label><i class=\"fi fi-sr-window\"></i> แสดงเป็น Popup</label>\r\n           <select class=\"fc\" id=\"ann-popup\">\r\n-            <option value=\"false\">ไม่ใช่ (banner เท่านั้น)</option>\r\n-            <option value=\"true\">ใช่ (popup ตอนเปิดเว็บ)</option>\r\n+            <option value=\"false\">ไม่ใช่ (banner)</option>\r\n+            <option value=\"true\">ใช่ (popup)</option>\r\n           </select>\r\n         </div>\r\n         <div class=\"fg\" style=\"flex:1;\">\r\n-          <label>วันหมดอายุ (ไม่บังคับ)</label>\r\n+          <label><i class=\"fi fi-sr-clock\"></i> วันหมดอายุ (ไม่บังคับ)</label>\r\n           <input class=\"fc\" type=\"date\" id=\"ann-expires\">\r\n         </div>\r\n       </div>\r\n       <button class=\"btn-save\" onclick=\"saveAnnounce()\"><i class=\"fi fi-sr-plus\"></i> เพิ่มประกาศ</button>\r\n"
                },
                {
                    "date": 1774648155617,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -236,46 +236,57 @@\n }\r\n \r\n function renderUserTable(users) {\r\n   const wrap = document.getElementById('user-table-wrap');\r\n-  if (!users.length) { wrap.innerHTML = '<p class=\"adm-empty\">ไม่พบผู้ใช้</p>'; return; }\r\n+  if (!users.length) {\r\n+    wrap.innerHTML = '<p class=\"adm-empty\"><i class=\"fi fi-sr-users\"></i> ไม่พบผู้ใช้</p>';\r\n+    return;\r\n+  }\r\n   wrap.innerHTML = `\r\n-    <table class=\"adm-table\">\r\n-      <thead>\r\n-        <tr>\r\n-          <th>Username</th>\r\n-          <th>เครดิต</th>\r\n-          <th>สิทธิ์</th>\r\n-          <th>จัดการ</th>\r\n-        </tr>\r\n-      </thead>\r\n-      <tbody>\r\n-        ${users.map(u => `\r\n-          <tr id=\"urow-${u.id}\">\r\n-            <td>\r\n-              <span class=\"ucr-name\">${u.username}</span>\r\n-            </td>\r\n-            <td>\r\n-              <input class=\"fc ucr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\" style=\"width:80px;\">\r\n-            </td>\r\n-            <td>\r\n-              <label class=\"toggle-wrap\">\r\n+    <div class=\"user-cards\">\r\n+      ${users.map(u => `\r\n+        <div class=\"user-card\" id=\"urow-${u.id}\">\r\n+          <div class=\"uc-left\">\r\n+            <div class=\"uc-avatar ${u.is_admin ? 'uc-admin' : ''}\">\r\n+              <i class=\"fi fi-sr-${u.is_admin ? 'shield-check' : 'user'}\"></i>\r\n+            </div>\r\n+            <div class=\"uc-info\">\r\n+              <div class=\"uc-name\">${u.username}</div>\r\n+              <div class=\"uc-role\">\r\n+                ${u.is_admin\r\n+                  ? '<span class=\"uc-badge-admin\"><i class=\"fi fi-sr-shield-check\"></i> Admin</span>'\r\n+                  : '<span class=\"uc-badge-user\"><i class=\"fi fi-sr-user\"></i> User</span>'}\r\n+              </div>\r\n+            </div>\r\n+          </div>\r\n+ \r\n+          <div class=\"uc-credit-wrap\">\r\n+            <div class=\"uc-credit-label\"><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิต</div>\r\n+            <div class=\"uc-credit-row\">\r\n+              <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', -10)\"><i class=\"fi fi-sr-minus\"></i></button>\r\n+              <input class=\"uc-cr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\">\r\n+              <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', 10)\"><i class=\"fi fi-sr-plus\"></i></button>\r\n+            </div>\r\n+          </div>\r\n+ \r\n+          <div class=\"uc-actions\">\r\n+            <button class=\"uc-save-btn\" onclick=\"saveUserCredit('${u.id}')\">\r\n+              <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+            </button>\r\n+            <div class=\"uc-toggle-row\">\r\n+              <label class=\"uc-toggle\">\r\n                 <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''} onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n-                <span class=\"toggle-label\">${u.is_admin ? '<span style=\"color:var(--red);\">Admin</span>' : 'User'}</span>\r\n+                <span class=\"uc-toggle-track\"></span>\r\n               </label>\r\n-            </td>\r\n-            <td style=\"display:flex;gap:6px;\">\r\n-              <button class=\"btn-sm btn-edit\" onclick=\"saveUserCredit('${u.id}')\">\r\n-                <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n-              </button>\r\n-              <button class=\"btn-sm btn-del\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n-                <i class=\"fi fi-sr-trash\"></i>\r\n-              </button>\r\n-            </td>\r\n-          </tr>\r\n-        `).join('')}\r\n-      </tbody>\r\n-    </table>`;\r\n+              <span class=\"uc-toggle-label\">Admin</span>\r\n+            </div>\r\n+            <button class=\"uc-del-btn\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n+              <i class=\"fi fi-sr-trash\"></i>\r\n+            </button>\r\n+          </div>\r\n+        </div>\r\n+      `).join('')}\r\n+    </div>`;\r\n }\r\n \r\n async function saveUserCredit(userId) {\r\n   const input = document.getElementById('ucr-' + userId);\r\n"
                },
                {
                    "date": 1774648171857,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -286,8 +286,14 @@\n         </div>\r\n       `).join('')}\r\n     </div>`;\r\n }\r\n+function adjustCredit(userId, delta) {\r\n+  const input = document.getElementById('ucr-' + userId);\r\n+  const val   = Math.max(0, (parseInt(input.value) || 0) + delta);\r\n+  input.value = val;\r\n+}\r\n+ \r\n \r\n async function saveUserCredit(userId) {\r\n   const input = document.getElementById('ucr-' + userId);\r\n   const val   = parseInt(input.value);\r\n"
                },
                {
                    "date": 1774651408198,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -240,54 +240,77 @@\n   if (!users.length) {\r\n     wrap.innerHTML = '<p class=\"adm-empty\"><i class=\"fi fi-sr-users\"></i> ไม่พบผู้ใช้</p>';\r\n     return;\r\n   }\r\n-  wrap.innerHTML = `\r\n-    <div class=\"user-cards\">\r\n-      ${users.map(u => `\r\n-        <div class=\"user-card\" id=\"urow-${u.id}\">\r\n-          <div class=\"uc-left\">\r\n-            <div class=\"uc-avatar ${u.is_admin ? 'uc-admin' : ''}\">\r\n+  wrap.innerHTML = `<div class=\"user-cards\">\r\n+    ${users.map(u => {\r\n+      const lastSeen = u.last_seen\r\n+        ? new Date(u.last_seen).toLocaleString('th-TH', { dateStyle: 'short', timeStyle: 'short' })\r\n+        : 'ไม่ทราบ';\r\n+      const isOnline = u.last_seen && (Date.now() - new Date(u.last_seen)) < 5 * 60 * 1000;\r\n+      return `\r\n+      <div class=\"user-card ${u.is_banned ? 'uc-banned' : ''}\" id=\"urow-${u.id}\">\r\n+        <div class=\"uc-avatar ${u.is_admin ? 'uc-admin' : ''}\">\r\n+          <i class=\"fi fi-sr-${u.is_admin ? 'shield-check' : 'user'}\"></i>\r\n+        </div>\r\n+        <div class=\"uc-info\">\r\n+          <div class=\"uc-name\">\r\n+            ${u.username}\r\n+            <span class=\"badge ${u.is_admin ? 'uc-badge-admin' : 'uc-badge-user'}\">\r\n               <i class=\"fi fi-sr-${u.is_admin ? 'shield-check' : 'user'}\"></i>\r\n-            </div>\r\n-            <div class=\"uc-info\">\r\n-              <div class=\"uc-name\">${u.username}</div>\r\n-              <div class=\"uc-role\">\r\n-                ${u.is_admin\r\n-                  ? '<span class=\"uc-badge-admin\"><i class=\"fi fi-sr-shield-check\"></i> Admin</span>'\r\n-                  : '<span class=\"uc-badge-user\"><i class=\"fi fi-sr-user\"></i> User</span>'}\r\n-              </div>\r\n-            </div>\r\n+              ${u.is_admin ? 'Admin' : 'User'}\r\n+            </span>\r\n+            ${u.is_banned ? '<span class=\"badge uc-banned-tag\"><i class=\"fi fi-sr-ban\"></i> แบน</span>' : ''}\r\n           </div>\r\n- \r\n-          <div class=\"uc-credit-wrap\">\r\n-            <div class=\"uc-credit-label\"><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิต</div>\r\n-            <div class=\"uc-credit-row\">\r\n-              <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', -10)\"><i class=\"fi fi-sr-minus\"></i></button>\r\n-              <input class=\"uc-cr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\">\r\n-              <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', 10)\"><i class=\"fi fi-sr-plus\"></i></button>\r\n-            </div>\r\n+          <div class=\"uc-meta\">\r\n+            <span class=\"uc-lastseen ${isOnline ? 'on' : ''}\">\r\n+              <span class=\"uc-dot ${isOnline ? 'on' : ''}\"></span>\r\n+              ${isOnline ? 'ออนไลน์อยู่' : lastSeen}\r\n+            </span>\r\n           </div>\r\n- \r\n-          <div class=\"uc-actions\">\r\n-            <button class=\"uc-save-btn\" onclick=\"saveUserCredit('${u.id}')\">\r\n-              <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+        </div>\r\n+\r\n+        <div class=\"uc-credit-wrap\">\r\n+          <div class=\"uc-credit-label\">\r\n+            <i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิต\r\n+          </div>\r\n+          <div class=\"uc-credit-row\">\r\n+            <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', -10)\">\r\n+              <i class=\"fi fi-sr-minus\"></i>\r\n             </button>\r\n-            <div class=\"uc-toggle-row\">\r\n-              <label class=\"uc-toggle\">\r\n-                <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''} onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n-                <span class=\"uc-toggle-track\"></span>\r\n-              </label>\r\n-              <span class=\"uc-toggle-label\">Admin</span>\r\n-            </div>\r\n-            <button class=\"uc-del-btn\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n-              <i class=\"fi fi-sr-trash\"></i>\r\n+            <input class=\"uc-cr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\">\r\n+            <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', 10)\">\r\n+              <i class=\"fi fi-sr-plus\"></i>\r\n             </button>\r\n           </div>\r\n         </div>\r\n-      `).join('')}\r\n-    </div>`;\r\n+\r\n+        <div class=\"uc-actions\">\r\n+          <button class=\"uc-save-btn\" onclick=\"saveUserCredit('${u.id}')\">\r\n+            <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n+          </button>\r\n+          <div class=\"uc-toggle-row\">\r\n+            <label class=\"uc-toggle\">\r\n+              <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''}\r\n+                onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n+              <span class=\"uc-toggle-track\"></span>\r\n+            </label>\r\n+            <span class=\"uc-toggle-label\">Admin</span>\r\n+          </div>\r\n+          <button class=\"uc-ban-btn ${u.is_banned ? 'uc-ban-active' : ''}\"\r\n+            onclick=\"toggleBan('${u.id}', ${!u.is_banned})\">\r\n+            <i class=\"fi fi-sr-${u.is_banned ? 'check' : 'ban'}\"></i>\r\n+            ${u.is_banned ? 'ปลดแบน' : 'แบน'}\r\n+          </button>\r\n+          <button class=\"uc-del-btn\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n+            <i class=\"fi fi-sr-trash\"></i>\r\n+          </button>\r\n+        </div>\r\n+      </div>`\r\n+    }).join('')}\r\n+  </div>`;\r\n }\r\n+\r\n function adjustCredit(userId, delta) {\r\n   const input = document.getElementById('ucr-' + userId);\r\n   const val   = Math.max(0, (parseInt(input.value) || 0) + delta);\r\n   input.value = val;\r\n"
                },
                {
                    "date": 1774651422485,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -355,8 +355,17 @@\n   if (error) { toast('ลบไม่ได้', 'error'); return; }\r\n   toast('ลบ user แล้ว', 'info');\r\n   loadUserTab();\r\n }\r\n+async function toggleBan(userId, ban) {\r\n+  const { error } = await _sb\r\n+    .from('profiles')\r\n+    .update({ is_banned: ban })\r\n+    .eq('id', userId);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(ban ? 'แบน user แล้ว' : 'ปลดแบนแล้ว', ban ? 'warning' : 'success');\r\n+  loadUserTab();\r\n+}\r\n \r\n /* =========================================\r\n    TAB: STATS\r\n    ========================================= */\r\n"
                },
                {
                    "date": 1774905755301,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,9 +8,9 @@\n function switchTab(name, idx) {\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');  // ← เพิ่ม null check\r\n+  if (target) target.classList.add('act');\r\n \r\n   if (name === 'users')    loadUserTab();\r\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n@@ -172,13 +172,24 @@\n   saveDB(); applyTestMode();\r\n   toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n }\r\n \r\n-/* ---- SPREADSHEET ---- */\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n function connectSheet() {\r\n-  const url = document.getElementById('sheet-url').value.trim();\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n   if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-  toast('ฟีเจอร์นี้ต้องใช้ Backend — กำลังพัฒนา', 'info');\r\n+\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n+\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n }\r\n \r\n /* ---- EXPORT / IMPORT ---- */\r\n function exportData() {\r\n@@ -186,427 +197,45 @@\n   const a = document.createElement('a');\r\n   a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n }\r\n function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n function handleImport(e) {\r\n   const f = e.target.files[0]; if (!f) return;\r\n   const r = new FileReader();\r\n   r.onload = ev => {\r\n     try {\r\n-      DB = JSON.parse(ev.target.result); saveDB(); renderAdmDecks(); renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch { toast('ไฟล์ไม่ถูกต้อง', 'error'); }\r\n-  };\r\n-  r.readAsText(f);\r\n-}\r\n-function resetData() {\r\n-  if (!confirm('รีเซ็ตข้อมูลทั้งหมดกลับค่าเริ่มต้น?')) return;\r\n-  resetDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('รีเซ็ตแล้ว', 'info');\r\n-}\r\n+      const parsed = JSON.parse(ev.target.result);\r\n \r\n-function toggleDeck(id) {\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  d.hidden = d.hidden ? 0 : 1; saveDB(); renderAdmDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n-}\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n \r\n-/* =========================================\r\n-   TAB: USERS\r\n-   ========================================= */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  container.innerHTML = `\r\n-    <div class=\"adm-sec-hdr\">\r\n-      <div style=\"position:relative;width:220px;\">\r\n-        <i class=\"fi fi-sr-search\" style=\"position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--text3);pointer-events:none;\"></i>\r\n-        <input class=\"fc\" id=\"user-search\" placeholder=\"ค้นหา username...\" oninput=\"filterUsers()\" style=\"width:100%;padding-left:32px;\">\r\n-      </div>\r\n-    </div>\r\n-    <div id=\"user-table-wrap\"><p class=\"adm-loading\">กำลังโหลด...</p></div>`;\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n \r\n-  const users = await API.getAllUsers();\r\n-  window._adminUsers = users;\r\n-  renderUserTable(users);\r\n-}\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n \r\n-function filterUsers() {\r\n-  const q = document.getElementById('user-search').value.toLowerCase();\r\n-  const filtered = (window._adminUsers || []).filter(u => u.username.includes(q));\r\n-  renderUserTable(filtered);\r\n-}\r\n-\r\n-function renderUserTable(users) {\r\n-  const wrap = document.getElementById('user-table-wrap');\r\n-  if (!users.length) {\r\n-    wrap.innerHTML = '<p class=\"adm-empty\"><i class=\"fi fi-sr-users\"></i> ไม่พบผู้ใช้</p>';\r\n-    return;\r\n-  }\r\n-  wrap.innerHTML = `<div class=\"user-cards\">\r\n-    ${users.map(u => {\r\n-      const lastSeen = u.last_seen\r\n-        ? new Date(u.last_seen).toLocaleString('th-TH', { dateStyle: 'short', timeStyle: 'short' })\r\n-        : 'ไม่ทราบ';\r\n-      const isOnline = u.last_seen && (Date.now() - new Date(u.last_seen)) < 5 * 60 * 1000;\r\n-      return `\r\n-      <div class=\"user-card ${u.is_banned ? 'uc-banned' : ''}\" id=\"urow-${u.id}\">\r\n-        <div class=\"uc-avatar ${u.is_admin ? 'uc-admin' : ''}\">\r\n-          <i class=\"fi fi-sr-${u.is_admin ? 'shield-check' : 'user'}\"></i>\r\n-        </div>\r\n-        <div class=\"uc-info\">\r\n-          <div class=\"uc-name\">\r\n-            ${u.username}\r\n-            <span class=\"badge ${u.is_admin ? 'uc-badge-admin' : 'uc-badge-user'}\">\r\n-              <i class=\"fi fi-sr-${u.is_admin ? 'shield-check' : 'user'}\"></i>\r\n-              ${u.is_admin ? 'Admin' : 'User'}\r\n-            </span>\r\n-            ${u.is_banned ? '<span class=\"badge uc-banned-tag\"><i class=\"fi fi-sr-ban\"></i> แบน</span>' : ''}\r\n-          </div>\r\n-          <div class=\"uc-meta\">\r\n-            <span class=\"uc-lastseen ${isOnline ? 'on' : ''}\">\r\n-              <span class=\"uc-dot ${isOnline ? 'on' : ''}\"></span>\r\n-              ${isOnline ? 'ออนไลน์อยู่' : lastSeen}\r\n-            </span>\r\n-          </div>\r\n-        </div>\r\n-\r\n-        <div class=\"uc-credit-wrap\">\r\n-          <div class=\"uc-credit-label\">\r\n-            <i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิต\r\n-          </div>\r\n-          <div class=\"uc-credit-row\">\r\n-            <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', -10)\">\r\n-              <i class=\"fi fi-sr-minus\"></i>\r\n-            </button>\r\n-            <input class=\"uc-cr-input\" type=\"number\" value=\"${u.credits}\" min=\"0\" id=\"ucr-${u.id}\">\r\n-            <button class=\"uc-cr-btn\" onclick=\"adjustCredit('${u.id}', 10)\">\r\n-              <i class=\"fi fi-sr-plus\"></i>\r\n-            </button>\r\n-          </div>\r\n-        </div>\r\n-\r\n-        <div class=\"uc-actions\">\r\n-          <button class=\"uc-save-btn\" onclick=\"saveUserCredit('${u.id}')\">\r\n-            <i class=\"fi fi-sr-disk\"></i> บันทึก\r\n-          </button>\r\n-          <div class=\"uc-toggle-row\">\r\n-            <label class=\"uc-toggle\">\r\n-              <input type=\"checkbox\" ${u.is_admin ? 'checked' : ''}\r\n-                onchange=\"toggleAdmin('${u.id}', this.checked)\">\r\n-              <span class=\"uc-toggle-track\"></span>\r\n-            </label>\r\n-            <span class=\"uc-toggle-label\">Admin</span>\r\n-          </div>\r\n-          <button class=\"uc-ban-btn ${u.is_banned ? 'uc-ban-active' : ''}\"\r\n-            onclick=\"toggleBan('${u.id}', ${!u.is_banned})\">\r\n-            <i class=\"fi fi-sr-${u.is_banned ? 'check' : 'ban'}\"></i>\r\n-            ${u.is_banned ? 'ปลดแบน' : 'แบน'}\r\n-          </button>\r\n-          <button class=\"uc-del-btn\" onclick=\"confirmDeleteUser('${u.id}', '${u.username}')\">\r\n-            <i class=\"fi fi-sr-trash\"></i>\r\n-          </button>\r\n-        </div>\r\n-      </div>`\r\n-    }).join('')}\r\n-  </div>`;\r\n-}\r\n-\r\n-function adjustCredit(userId, delta) {\r\n-  const input = document.getElementById('ucr-' + userId);\r\n-  const val   = Math.max(0, (parseInt(input.value) || 0) + delta);\r\n-  input.value = val;\r\n-}\r\n- \r\n-\r\n-async function saveUserCredit(userId) {\r\n-  const input = document.getElementById('ucr-' + userId);\r\n-  const val   = parseInt(input.value);\r\n-  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-\r\n-  const ok = await API.adminSetCredits(userId, val);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-\r\n-  // บันทึก credit_history\r\n-  await _sb.from('credit_history').insert({\r\n-    user_id: userId,\r\n-    amount:  val,\r\n-    type:    'admin',\r\n-    note:    `Admin ตั้งเครดิตเป็น ${val}`\r\n-  });\r\n-\r\n-  toast('บันทึกเครดิตแล้ว', 'success');\r\n-}\r\n-\r\n-async function toggleAdmin(userId, isAdmin) {\r\n-  const { error } = await _sb\r\n-    .from('profiles')\r\n-    .update({ is_admin: isAdmin })\r\n-    .eq('id', userId);\r\n-\r\n-  // sync metadata ด้วย\r\n-  await _sb.rpc('set_admin_metadata', { uid: userId, admin: isAdmin }).catch(() => {});\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(isAdmin ? 'ตั้งเป็น Admin แล้ว' : 'ถอด Admin แล้ว', 'success');\r\n-}\r\n-\r\n-async function confirmDeleteUser(userId, username) {\r\n-  if (!confirm(`ลบ user \"${username}\" ออกจากระบบ?`)) return;\r\n-  const { error } = await _sb.from('profiles').delete().eq('id', userId);\r\n-  if (error) { toast('ลบไม่ได้', 'error'); return; }\r\n-  toast('ลบ user แล้ว', 'info');\r\n-  loadUserTab();\r\n-}\r\n-async function toggleBan(userId, ban) {\r\n-  const { error } = await _sb\r\n-    .from('profiles')\r\n-    .update({ is_banned: ban })\r\n-    .eq('id', userId);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(ban ? 'แบน user แล้ว' : 'ปลดแบนแล้ว', ban ? 'warning' : 'success');\r\n-  loadUserTab();\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: STATS\r\n-   ========================================= */\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n-\r\n-  // นับการเปิดไพ่แต่ละกอง\r\n-  const { data: deckStats } = await _sb\r\n-    .from('card_stats')\r\n-    .select('deck_id')\r\n-    .order('opened_at', { ascending: false });\r\n-\r\n-  // นับ user ทั้งหมด\r\n-  const { count: userCount } = await _sb\r\n-    .from('profiles')\r\n-    .select('*', { count: 'exact', head: true });\r\n-\r\n-  // นับยอดเครดิตรวม\r\n-  const { data: crData } = await _sb\r\n-    .from('credit_history')\r\n-    .select('amount, type')\r\n-    .eq('type', 'topup');\r\n-\r\n-  const totalTopup = (crData || []).reduce((s, r) => s + r.amount, 0);\r\n-\r\n-  // สรุปสถิติตามกอง\r\n-  const deckCount = {};\r\n-  (deckStats || []).forEach(r => {\r\n-    deckCount[r.deck_id] = (deckCount[r.deck_id] || 0) + 1;\r\n-  });\r\n-  const deckRows = Object.entries(deckCount)\r\n-    .sort((a, b) => b[1] - a[1])\r\n-    .map(([id, cnt]) => {\r\n-      const deck = DB.decks.find(d => d.id === id);\r\n-      return `<tr>\r\n-        <td>${deck ? deck.name : id}</td>\r\n-        <td><strong style=\"color:var(--gold);\">${cnt}</strong> ครั้ง</td>\r\n-        <td>\r\n-          <div class=\"stat-bar-wrap\">\r\n-            <div class=\"stat-bar-fill\" style=\"width:${Math.min(100, cnt * 5)}%\"></div>\r\n-          </div>\r\n-        </td>\r\n-      </tr>`;\r\n-    }).join('') || '<tr><td colspan=\"3\" style=\"color:var(--text3);\">ยังไม่มีข้อมูล</td></tr>';\r\n-\r\n-  container.innerHTML = `\r\n-    <div class=\"stats-bar\" style=\"margin-bottom:24px;\">\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">ผู้ใช้ทั้งหมด</div>\r\n-        <div class=\"stat-val gold\">${userCount || 0}</div>\r\n-      </div>\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">ไพ่ที่เปิดแล้ว</div>\r\n-        <div class=\"stat-val red\">${(deckStats || []).length}</div>\r\n-      </div>\r\n-      <div class=\"stat-card\">\r\n-        <div class=\"stat-label\">เครดิตเติมรวม</div>\r\n-        <div class=\"stat-val green\">${totalTopup}</div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ยอดนิยม</h3>\r\n-      <table class=\"adm-table\">\r\n-        <thead><tr><th>กองไพ่</th><th>เปิดแล้ว</th><th>สัดส่วน</th></tr></thead>\r\n-        <tbody>${deckRows}</tbody>\r\n-      </table>\r\n-    </div>`;\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: CREDIT HISTORY\r\n-   ========================================= */\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  container.innerHTML = '<p class=\"adm-loading\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('credit_history')\r\n-    .select('*, profiles(username)')\r\n-    .order('created_at', { ascending: false })\r\n-    .limit(100);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p class=\"adm-empty\">ยังไม่มีประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const typeLabel = {\r\n-  topup: '<i class=\"fi fi-sr-credit-card\"></i> เติมเงิน',\r\n-  free:  '<i class=\"fi fi-sr-gift\"></i> รับฟรี',\r\n-  admin: '<i class=\"fi fi-sr-settings\"></i> Admin',\r\n-  spend: '<i class=\"fi fi-sr-layers\"></i> ใช้ไพ่'\r\n-};\r\n-  const typeColor = { topup: 'var(--gold)', free: '#4caf50', admin: 'var(--red)', spend: 'var(--text2)' };\r\n-\r\n-  container.innerHTML = `\r\n-    <table class=\"adm-table\">\r\n-      <thead>\r\n-        <tr><th>เวลา</th><th>User</th><th>ประเภท</th><th>เครดิต</th><th>หมายเหตุ</th></tr>\r\n-      </thead>\r\n-      <tbody>\r\n-        ${data.map(r => `\r\n-          <tr>\r\n-            <td style=\"color:var(--text3);font-size:.8rem;\">${new Date(r.created_at).toLocaleString('th-TH')}</td>\r\n-            <td>${r.profiles?.username || '-'}</td>\r\n-            <td><span style=\"color:${typeColor[r.type] || '#fff'}\">${typeLabel[r.type] || r.type}</span></td>\r\n-            <td><strong style=\"color:var(--gold);\">+${r.amount}</strong></td>\r\n-            <td style=\"color:var(--text3);font-size:.8rem;\">${r.note || '-'}</td>\r\n-          </tr>\r\n-        `).join('')}\r\n-      </tbody>\r\n-    </table>`;\r\n-}\r\n-\r\n-/* =========================================\r\n-   TAB: ANNOUNCEMENTS\r\n-   ========================================= */\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n- \r\n-  const { data: list } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n- \r\n-  const typeIcon  = { info: 'fi-sr-info', promo: 'fi-sr-star', warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle' };\r\n-  const typeLabel = { info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี' };\r\n-  const typeColor = { info: '#60c0ff', promo: 'var(--gold)', warning: 'orange', success: '#4caf50' };\r\n- \r\n-  const rows = (list || []).map(a => `\r\n-    <div class=\"announce-row ${a.active ? '' : 'inactive'}\">\r\n-      <div class=\"ann-left\">\r\n-        <div class=\"ann-icon-wrap ann-${a.type}\">\r\n-          <i class=\"fi ${typeIcon[a.type] || 'fi-sr-info'}\"></i>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"ann-body\">\r\n-        <div class=\"ann-meta\">\r\n-          <span class=\"ann-type-badge ann-${a.type}\">\r\n-            <i class=\"fi ${typeIcon[a.type]}\"></i> ${typeLabel[a.type] || a.type}\r\n-          </span>\r\n-          <span class=\"ann-status ${a.active ? 'ann-on' : 'ann-off'}\">\r\n-            <i class=\"fi fi-sr-${a.active ? 'eye' : 'eye-crossed'}\"></i>\r\n-            ${a.active ? 'เปิดอยู่' : 'ปิดอยู่'}\r\n-          </span>\r\n-          ${a.show_popup ? '<span class=\"ann-popup-badge\"><i class=\"fi fi-sr-window\"></i> Popup</span>' : ''}\r\n-        </div>\r\n-        <div class=\"ann-title-text\">${a.title}</div>\r\n-        <div class=\"ann-message-text\">${a.message}</div>\r\n-        ${a.expires_at ? `<div class=\"ann-expires\"><i class=\"fi fi-sr-clock\"></i> หมดอายุ ${new Date(a.expires_at).toLocaleDateString('th-TH')}</div>` : ''}\r\n-      </div>\r\n-      <div class=\"ann-acts\">\r\n-        <button class=\"btn-sm ${a.active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleAnnounce(${a.id}, ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce(${a.id})\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('') || '<p class=\"adm-empty\"><i class=\"fi fi-sr-bell-slash\"></i> ยังไม่มีประกาศ</p>';\r\n- \r\n-  container.innerHTML = `\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-bell\" style=\"color:var(--red);\"></i> เพิ่มประกาศ/โปรโมชั่น</h3>\r\n-      <div class=\"fg\"><label>หัวข้อ</label><input class=\"fc\" id=\"ann-title\" placeholder=\"เช่น โปรโมชั่นพิเศษ!\"></div>\r\n-      <div class=\"fg\"><label>ข้อความ</label><input class=\"fc\" id=\"ann-msg\" placeholder=\"รายละเอียด...\"></div>\r\n-      <div style=\"display:flex;gap:12px;flex-wrap:wrap;\">\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label><i class=\"fi fi-sr-tag\"></i> ประเภท</label>\r\n-          <select class=\"fc\" id=\"ann-type\">\r\n-            <option value=\"info\">ข้อมูล</option>\r\n-            <option value=\"promo\">โปรโมชั่น</option>\r\n-            <option value=\"warning\">แจ้งเตือน</option>\r\n-            <option value=\"success\">ข่าวดี</option>\r\n-          </select>\r\n-        </div>\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label><i class=\"fi fi-sr-window\"></i> แสดงเป็น Popup</label>\r\n-          <select class=\"fc\" id=\"ann-popup\">\r\n-            <option value=\"false\">ไม่ใช่ (banner)</option>\r\n-            <option value=\"true\">ใช่ (popup)</option>\r\n-          </select>\r\n-        </div>\r\n-        <div class=\"fg\" style=\"flex:1;\">\r\n-          <label><i class=\"fi fi-sr-clock\"></i> วันหมดอายุ (ไม่บังคับ)</label>\r\n-          <input class=\"fc\" type=\"date\" id=\"ann-expires\">\r\n-        </div>\r\n-      </div>\r\n-      <button class=\"btn-save\" onclick=\"saveAnnounce()\"><i class=\"fi fi-sr-plus\"></i> เพิ่มประกาศ</button>\r\n-    </div>\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-list\" style=\"color:var(--red);\"></i> ประกาศทั้งหมด</h3>\r\n-      <div id=\"announce-list\">${rows}</div>\r\n-    </div>`;\r\n-}\r\n-\r\n-async function saveAnnounce() {\r\n-  const title     = document.getElementById('ann-title').value.trim();\r\n-  const message   = document.getElementById('ann-msg').value.trim();\r\n-  const type      = document.getElementById('ann-type').value;\r\n-  const showPopup = document.getElementById('ann-popup').value === 'true';\r\n-  const expiresAt = document.getElementById('ann-expires').value || null;\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type, show_popup: showPopup,\r\n-    expires_at: expiresAt ? new Date(expiresAt).toISOString() : null\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function toggleAnnounce(id, active) {\r\n-  await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  toast(active ? 'เปิดประกาศแล้ว' : 'ปิดประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  await _sb.from('announcements').delete().eq('id', id);\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-/* =========================================\r\n-   CREDIT ADMIN (เดิม — ใช้ฟังก์ชันจาก api.js)\r\n-   ========================================= */\r\n-function adminSetCredit() {\r\n-  const val = parseInt(document.getElementById('st-set-cr').value);\r\n-  if (isNaN(val) || val < 0) { toast('ใส่จำนวนให้ถูกต้อง', 'error'); return; }\r\n-  credits = val; saveCr(); updateCr();\r\n-  toast('ตั้งเครดิตเป็น ' + val + ' แล้ว', 'success');\r\n-}\r\n-\r\n-function adminResetCredit() {\r\n-  credits = 0; saveCr(); updateCr();\r\n-  toast('รีเซ็ตเครดิตเป็น 0 แล้ว', 'success');\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775215768275,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,8 +16,103 @@\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n }\r\n \r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+}\r\n+\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+  const totalUsers = document.getElementById('stat-users');\r\n+  if (totalUsers) {\r\n+    const users = await API.getAllUsers();\r\n+    totalUsers.textContent = users.length;\r\n+  }\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('credit_history')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false })\r\n+    .limit(100);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  container.innerHTML = data.map(h => `\r\n+    <div class=\"drow\" style=\"font-size:.85rem;\">\r\n+      <div class=\"drow-info\" style=\"gap:10px;\">\r\n+        <div>\r\n+          <div style=\"color:var(--text1);\">${h.note || h.type}</div>\r\n+          <div style=\"color:var(--text3);font-size:.75rem;\">${new Date(h.created_at).toLocaleString('th-TH')}</div>\r\n+        </div>\r\n+      </div>\r\n+      <div style=\"color:${h.amount > 0 ? 'var(--gold)' : 'var(--red)'}; font-weight:600;\">\r\n+        ${h.amount > 0 ? '+' : ''}${h.amount} เครดิต\r\n+      </div>\r\n+    </div>`).join('');\r\n+}\r\n+\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n+\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  listEl.innerHTML = data.map(a => `\r\n+    <div class=\"drow\">\r\n+      <div class=\"drow-info\">\r\n+        <div>\r\n+          <div class=\"drow-nm\">${a.title || '(ไม่มีชื่อ)'}</div>\r\n+          <div class=\"drow-meta\">\r\n+            <span class=\"tag\">${a.type || 'info'}</span>\r\n+            <span class=\"tag\" style=\"color:${a.active ? 'var(--gold)' : 'var(--text3)'};\">\r\n+              ${a.active ? 'เผยแพร่อยู่' : 'ซ่อนอยู่'}\r\n+            </span>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"drow-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-eye${a.active ? '-crossed' : ''}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+      </div>\r\n+    </div>`).join('');\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n   const deckEl = document.getElementById('stat-decks');\r\n   const cardEl = document.getElementById('stat-cards');\r\n"
                },
                {
                    "date": 1775234917455,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -165,16 +165,17 @@\n   editDeckId = id || null;\r\n   if (id) {\r\n     const d = DB.decks.find(x => x.id === id);\r\n     document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value  = d.name;\r\n-    document.getElementById('df-icon').value  = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = d.theme || 'default';\r\n-    document.getElementById('df-desc').value  = d.desc  || '';\r\n-    document.getElementById('df-cost').value  = d.cost  || 1;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n   } else {\r\n     document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc'].forEach(i => document.getElementById(i).value = '');\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n     document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n     document.getElementById('df-theme').value = 'fire';\r\n     document.getElementById('df-cost').value  = '1';\r\n   }\r\n@@ -186,19 +187,20 @@\n \r\n function saveDeck() {\r\n   const name = document.getElementById('df-name').value.trim();\r\n   if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon  = document.getElementById('df-icon').value.trim()  || 'fi-sr-flame';\r\n-  const theme = document.getElementById('df-theme').value        || 'default';\r\n-  const desc  = document.getElementById('df-desc').value.trim();\r\n-  const cost  = parseInt(document.getElementById('df-cost').value) || 1;\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n \r\n   if (editDeckId) {\r\n     const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost });\r\n+    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n   } else {\r\n     const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost });\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n     DB.cards[id] = [];\r\n   }\r\n   saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n   toast('บันทึกกองไพ่แล้ว', 'success');\r\n"
                },
                {
                    "date": 1775236356847,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -334,5 +334,9 @@\n   };\r\n   r.readAsText(f);\r\n   // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n   e.target.value = '';\r\n-}\n\\ No newline at end of file\n+// <<<<<<< HEAD\r\n+// }\r\n+// =======\r\n+// }\r\n+// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n"
                },
                {
                    "date": 1775252572027,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -26,13 +26,15 @@\n \r\n async function loadStatsTab() {\r\n   const container = document.getElementById('tc-stats');\r\n   if (!container) return;\r\n+\r\n+  // โหลด user count\r\n+  const users = await API.getAllUsers();\r\n   const totalUsers = document.getElementById('stat-users');\r\n-  if (totalUsers) {\r\n-    const users = await API.getAllUsers();\r\n-    totalUsers.textContent = users.length;\r\n-  }\r\n+  if (totalUsers) totalUsers.textContent = users.length;\r\n+\r\n+  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n   renderAdmDecks();\r\n }\r\n \r\n async function loadHistoryTab() {\r\n@@ -335,8 +337,8 @@\n   r.readAsText(f);\r\n   // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n   e.target.value = '';\r\n // <<<<<<< HEAD\r\n-// }\r\n+}\r\n // =======\r\n // }\r\n // >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n"
                },
                {
                    "date": 1775252607239,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -115,13 +115,17 @@\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n+function renderAdmDecks() {\r\n   const deckEl = document.getElementById('stat-decks');\r\n   const cardEl = document.getElementById('stat-cards');\r\n   if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n   if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-  const l = document.getElementById('adm-deck-list');\r\n+\r\n+  // หา deck list ทั้งจาก tab decks และ tab stats\r\n+  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n+  if (!l) return;\r\n   l.innerHTML = '';\r\n   DB.decks.forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n     const th  = getTh(d);\r\n"
                },
                {
                    "date": 1775252715047,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,382 @@\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');\r\n+\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+}\r\n+\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+\r\n+  // โหลด user count\r\n+  const users = await API.getAllUsers();\r\n+  const totalUsers = document.getElementById('stat-users');\r\n+  if (totalUsers) totalUsers.textContent = users.length;\r\n+\r\n+  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('credit_history')\r\n+    .select('*, profiles(username)')\r\n+    .order('created_at', { ascending: false })\r\n+    .limit(100);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const TYPE_ICO = {\r\n+    free:     'fi-sr-gift',\r\n+    purchase: 'fi-sr-shopping-cart',\r\n+    deduct:   'fi-sr-layers',\r\n+    admin:    'fi-sr-shield-check',\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    free:     'รับฟรี',\r\n+    purchase: 'ซื้อเครดิต',\r\n+    deduct:   'ใช้ไพ่',\r\n+    admin:    'แอดมินเติม',\r\n+  };\r\n+\r\n+  container.innerHTML = data.map(h => {\r\n+    const isPlus  = h.amount > 0;\r\n+    const typeKey = h.type || 'deduct';\r\n+    const ico     = TYPE_ICO[typeKey]  || 'fi-sr-coins';\r\n+    const label   = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = h.profiles?.username || h.user_id?.slice(0,8) || '?';\r\n+    const date    = new Date(h.created_at);\r\n+    const dateStr = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n+\r\n+    return `\r\n+    <div class=\"hist-row\">\r\n+      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n+        <i class=\"fi ${ico}\"></i>\r\n+      </div>\r\n+      <div class=\"hist-info\">\r\n+        <div class=\"hist-top\">\r\n+          <span class=\"hist-note\">${h.note || label}</span>\r\n+          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n+        </div>\r\n+        <div class=\"hist-sub\">\r\n+          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n+        ${isPlus ? '+' : ''}${h.amount}\r\n+        <span class=\"hist-unit\">เครดิต</span>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n+\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  listEl.innerHTML = data.map(a => `\r\n+    <div class=\"drow\">\r\n+      <div class=\"drow-info\">\r\n+        <div>\r\n+          <div class=\"drow-nm\">${a.title || '(ไม่มีชื่อ)'}</div>\r\n+          <div class=\"drow-meta\">\r\n+            <span class=\"tag\">${a.type || 'info'}</span>\r\n+            <span class=\"tag\" style=\"color:${a.active ? 'var(--gold)' : 'var(--text3)'};\">\r\n+              ${a.active ? 'เผยแพร่อยู่' : 'ซ่อนอยู่'}\r\n+            </span>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"drow-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-eye${a.active ? '-crossed' : ''}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+      </div>\r\n+    </div>`).join('');\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+\r\n+  // หา deck list ทั้งจาก tab decks และ tab stats\r\n+  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n+function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n+\r\n+  if (editDeckId) {\r\n+    const d = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n+    DB.cards[id] = [];\r\n+  }\r\n+  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+}\r\n+\r\n+function delDeck(id) {\r\n+  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+  saveDB(); renderAdmDecks(); renderAdmSel();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  DB.cards[id] = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) DB.cards[id].push({ cat, text });\r\n+  });\r\n+  saveDB();\r\n+  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n+\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+}\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      const parsed = JSON.parse(ev.target.result);\r\n+\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n+\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n+\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n+\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n+// <<<<<<< HEAD\r\n+}\r\n+// =======\r\n+// }\r\n+// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n"
                },
                {
                    "date": 1775252871713,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -376,355 +376,30 @@\n   // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n   e.target.value = '';\r\n // <<<<<<< HEAD\r\n }\r\n-// =======\r\n-// }\r\n-// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n-/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n \r\n-let editDeckId = null;\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n \r\n-/* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title,\r\n+    message,\r\n+    type,\r\n+    show_popup: popup,\r\n+    active: true,\r\n+  });\r\n \r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-}\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n \r\n-/* ---- TAB LOADERS ---- */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  if (!container) return;\r\n-  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n-}\r\n-\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  if (!container) return;\r\n-\r\n-  // โหลด user count\r\n-  const users = await API.getAllUsers();\r\n-  const totalUsers = document.getElementById('stat-users');\r\n-  if (totalUsers) totalUsers.textContent = users.length;\r\n-\r\n-  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n-  renderAdmDecks();\r\n-}\r\n-\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('credit_history')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false })\r\n-    .limit(100);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  container.innerHTML = data.map(h => `\r\n-    <div class=\"drow\" style=\"font-size:.85rem;\">\r\n-      <div class=\"drow-info\" style=\"gap:10px;\">\r\n-        <div>\r\n-          <div style=\"color:var(--text1);\">${h.note || h.type}</div>\r\n-          <div style=\"color:var(--text3);font-size:.75rem;\">${new Date(h.created_at).toLocaleString('th-TH')}</div>\r\n-        </div>\r\n-      </div>\r\n-      <div style=\"color:${h.amount > 0 ? 'var(--gold)' : 'var(--red)'}; font-weight:600;\">\r\n-        ${h.amount > 0 ? '+' : ''}${h.amount} เครดิต\r\n-      </div>\r\n-    </div>`).join('');\r\n-}\r\n-\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-  if (!container) return;\r\n-  const listEl = document.getElementById('announce-list');\r\n-  if (!listEl) return;\r\n-\r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n-\r\n-  if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประกาศ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  listEl.innerHTML = data.map(a => `\r\n-    <div class=\"drow\">\r\n-      <div class=\"drow-info\">\r\n-        <div>\r\n-          <div class=\"drow-nm\">${a.title || '(ไม่มีชื่อ)'}</div>\r\n-          <div class=\"drow-meta\">\r\n-            <span class=\"tag\">${a.type || 'info'}</span>\r\n-            <span class=\"tag\" style=\"color:${a.active ? 'var(--gold)' : 'var(--text3)'};\">\r\n-              ${a.active ? 'เผยแพร่อยู่' : 'ซ่อนอยู่'}\r\n-            </span>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"drow-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-eye${a.active ? '-crossed' : ''}\"></i>\r\n-          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n-        </button>\r\n-      </div>\r\n-    </div>`).join('');\r\n-}\r\n-\r\n-async function toggleAnnounce(id, active) {\r\n-  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n   loadAnnounceTab();\r\n }\r\n-\r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-function renderAdmDecks() {\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-\r\n-  // หา deck list ทั้งจาก tab decks และ tab stats\r\n-  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n-}\r\n-\r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n-}\r\n-\r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value     = d.name;\r\n-    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value    = d.theme || 'default';\r\n-    document.getElementById('df-desc').value     = d.desc  || '';\r\n-    document.getElementById('df-cost').value     = d.cost  || 1;\r\n-    document.getElementById('df-category').value = d.category || '';\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n-\r\n-function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n-  const theme    = document.getElementById('df-theme').value            || 'default';\r\n-  const desc     = document.getElementById('df-desc').value.trim();\r\n-  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n-  const category = document.getElementById('df-category').value.trim() || '';\r\n-\r\n-  if (editDeckId) {\r\n-    const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n-    DB.cards[id] = [];\r\n-  }\r\n-  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n-}\r\n-\r\n-function delDeck(id) {\r\n-  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n-  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-  saveDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n-}\r\n-\r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n-}\r\n-\r\n-function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  DB.cards[id] = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) DB.cards[id].push({ cat, text });\r\n-  });\r\n-  saveDB();\r\n-  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n-  renderAdmDecks();\r\n-}\r\n-\r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n-}\r\n-\r\n-// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url')?.value?.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-\r\n-  API.setSheetUrl(url);\r\n-  toast('กำลังดึงข้อมูล...', 'info');\r\n-\r\n-  API.syncFromSheets(url)\r\n-    .then(({ decks, cards }) => {\r\n-      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      renderDecks();\r\n-    })\r\n-    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n-}\r\n-\r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n-}\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n-\r\n-/* [FIX #6] validate JSON structure ก่อน import */\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      const parsed = JSON.parse(ev.target.result);\r\n-\r\n-      // validate required fields\r\n-      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n-      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n-      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n-\r\n-      // validate deck structure\r\n-      for (const d of parsed.decks) {\r\n-        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n-        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n-      }\r\n-\r\n-      // validate cards structure\r\n-      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n-        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n-        for (const c of cards) {\r\n-          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n-        }\r\n-      }\r\n-\r\n-      DB = parsed;\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch (err) {\r\n-      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n-    }\r\n-  };\r\n-  r.readAsText(f);\r\n-  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n-  e.target.value = '';\r\n-// <<<<<<< HEAD\r\n-}\r\n // =======\r\n // }\r\n // >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n"
                },
                {
                    "date": 1775253095207,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -100,49 +100,107 @@\n       </div>\r\n     </div>`;\r\n   }).join('');\r\n }\r\n-\r\n async function loadAnnounceTab() {\r\n   const container = document.getElementById('tc-announce');\r\n   if (!container) return;\r\n   const listEl = document.getElementById('announce-list');\r\n   if (!listEl) return;\r\n \r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n \r\n   const { data, error } = await _sb\r\n     .from('announcements')\r\n     .select('*')\r\n     .order('created_at', { ascending: false });\r\n \r\n   if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบประกาศ</p>';\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n     return;\r\n   }\r\n \r\n-  listEl.innerHTML = data.map(a => `\r\n-    <div class=\"drow\">\r\n-      <div class=\"drow-info\">\r\n-        <div>\r\n-          <div class=\"drow-nm\">${a.title || '(ไม่มีชื่อ)'}</div>\r\n-          <div class=\"drow-meta\">\r\n-            <span class=\"tag\">${a.type || 'info'}</span>\r\n-            <span class=\"tag\" style=\"color:${a.active ? 'var(--gold)' : 'var(--text3)'};\">\r\n-              ${a.active ? 'เผยแพร่อยู่' : 'ซ่อนอยู่'}\r\n-            </span>\r\n-          </div>\r\n+  const TYPE_ICO = {\r\n+    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n+    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n+  };\r\n+\r\n+  listEl.innerHTML = data.map(a => {\r\n+    const t       = a.type || 'info';\r\n+    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n+    const expires = a.expires_at\r\n+      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n+      : null;\r\n+    return `\r\n+    <div class=\"ann-row\">\r\n+      <div class=\"ann-row-ico ann-ico-${t}\">\r\n+        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n+      </div>\r\n+      <div class=\"ann-row-info\">\r\n+        <div class=\"ann-row-top\">\r\n+          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n+          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n+          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n+            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n+            ${a.active ? 'เผยแพร่' : 'draft'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n         </div>\r\n+        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n+        <div class=\"ann-row-sub\">\r\n+          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n+          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n+        </div>\r\n       </div>\r\n-      <div class=\"drow-acts\">\r\n+      <div class=\"ann-row-acts\">\r\n         <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-eye${a.active ? '-crossed' : ''}\"></i>\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n           ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n         </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n       </div>\r\n-    </div>`).join('');\r\n+    </div>`;\r\n+  }).join('');\r\n }\r\n \r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+  const active  = document.getElementById('ann-active')?.value === '1';\r\n+  const expires = document.getElementById('ann-expires')?.value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type,\r\n+    show_popup: popup,\r\n+    active,\r\n+    expires_at: expires ? new Date(expires).toISOString() : null,\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  document.getElementById('ann-expires').value = '';\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n async function toggleAnnounce(id, active) {\r\n   const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n   if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n   toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n"
                },
                {
                    "date": 1775289729187,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,585 @@\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');\r\n+\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+}\r\n+\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+\r\n+  // โหลด user count\r\n+  const users = await API.getAllUsers();\r\n+  const totalUsers = document.getElementById('stat-users');\r\n+  if (totalUsers) totalUsers.textContent = users.length;\r\n+\r\n+  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('credit_history')\r\n+    .select('*, profiles(username)')\r\n+    .order('created_at', { ascending: false })\r\n+    .limit(100);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const TYPE_ICO = {\r\n+    free:     'fi-sr-gift',\r\n+    purchase: 'fi-sr-shopping-cart',\r\n+    deduct:   'fi-sr-layers',\r\n+    admin:    'fi-sr-shield-check',\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    free:     'รับฟรี',\r\n+    purchase: 'ซื้อเครดิต',\r\n+    deduct:   'ใช้ไพ่',\r\n+    admin:    'แอดมินเติม',\r\n+  };\r\n+\r\n+  container.innerHTML = data.map(h => {\r\n+    const isPlus  = h.amount > 0;\r\n+    const typeKey = h.type || 'deduct';\r\n+    const ico     = TYPE_ICO[typeKey]  || 'fi-sr-coins';\r\n+    const label   = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = h.profiles?.username || h.user_id?.slice(0,8) || '?';\r\n+    const date    = new Date(h.created_at);\r\n+    const dateStr = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n+\r\n+    return `\r\n+    <div class=\"hist-row\">\r\n+      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n+        <i class=\"fi ${ico}\"></i>\r\n+      </div>\r\n+      <div class=\"hist-info\">\r\n+        <div class=\"hist-top\">\r\n+          <span class=\"hist-note\">${h.note || label}</span>\r\n+          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n+        </div>\r\n+        <div class=\"hist-sub\">\r\n+          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n+        ${isPlus ? '+' : ''}${h.amount}\r\n+        <span class=\"hist-unit\">เครดิต</span>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n+\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const TYPE_ICO = {\r\n+    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n+    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n+  };\r\n+\r\n+  listEl.innerHTML = data.map(a => {\r\n+    const t       = a.type || 'info';\r\n+    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n+    const expires = a.expires_at\r\n+      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n+      : null;\r\n+    return `\r\n+    <div class=\"ann-row\">\r\n+      <div class=\"ann-row-ico ann-ico-${t}\">\r\n+        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n+      </div>\r\n+      <div class=\"ann-row-info\">\r\n+        <div class=\"ann-row-top\">\r\n+          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n+          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n+          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n+            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n+            ${a.active ? 'เผยแพร่' : 'draft'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n+        </div>\r\n+        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n+        <div class=\"ann-row-sub\">\r\n+          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n+          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"ann-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+  const active  = document.getElementById('ann-active')?.value === '1';\r\n+  const expires = document.getElementById('ann-expires')?.value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type,\r\n+    show_popup: popup,\r\n+    active,\r\n+    expires_at: expires ? new Date(expires).toISOString() : null,\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  document.getElementById('ann-expires').value = '';\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+\r\n+  // หา deck list ทั้งจาก tab decks และ tab stats\r\n+  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n+function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n+\r\n+  if (editDeckId) {\r\n+    const d = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n+    DB.cards[id] = [];\r\n+  }\r\n+  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+}\r\n+\r\n+function delDeck(id) {\r\n+  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+  saveDB(); renderAdmDecks(); renderAdmSel();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  DB.cards[id] = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) DB.cards[id].push({ cat, text });\r\n+  });\r\n+  saveDB();\r\n+  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+  renderAdmDecks();\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n+\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+}\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      const parsed = JSON.parse(ev.target.result);\r\n+\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n+\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n+\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n+\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n+// <<<<<<< HEAD\r\n+}\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title,\r\n+    message,\r\n+    type,\r\n+    show_popup: popup,\r\n+    active: true,\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  loadAnnounceTab();\r\n+}\r\n+// =======\r\n+// }\r\n+// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n+/* ---- PACKAGE MANAGER ---- */\r\n+let editPkgId = null;\r\n+\r\n+async function loadPkgList() {\r\n+  const el = document.getElementById('pkg-list');\r\n+  if (!el) return;\r\n+  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('packages')\r\n+    .select('*')\r\n+    .order('sort_order', { ascending: true });\r\n+\r\n+  if (error || !data?.length) {\r\n+    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  el.innerHTML = data.map(p => `\r\n+    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n+      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n+        <i class=\"fi fi-sr-coins\"></i>\r\n+      </div>\r\n+      <div class=\"pkg-row-info\">\r\n+        <div class=\"pkg-row-top\">\r\n+          <span class=\"pkg-row-name\">${p.name}</span>\r\n+          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n+          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n+        </div>\r\n+        <div class=\"pkg-row-detail\">\r\n+          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span>ลำดับ ${p.sort_order}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"pkg-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n+          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n+        </button>\r\n+        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n+          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n+}\r\n+\r\n+function openPkgForm(id = null) {\r\n+  editPkgId = id;\r\n+  if (id) {\r\n+    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n+      if (!data) return;\r\n+      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n+      document.getElementById('pf-name').value     = data.name;\r\n+      document.getElementById('pf-coins').value    = data.coins;\r\n+      document.getElementById('pf-bonus').value    = data.bonus;\r\n+      document.getElementById('pf-price').value    = data.price;\r\n+      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n+      document.getElementById('pf-order').value    = data.sort_order;\r\n+      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n+    });\r\n+  } else {\r\n+    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n+    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n+    document.getElementById('pf-bonus').value    = '0';\r\n+    document.getElementById('pf-featured').value = '0';\r\n+    document.getElementById('pf-order').value    = '0';\r\n+    document.getElementById('pf-active').value   = '1';\r\n+  }\r\n+  document.getElementById('pkg-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closePkgForm() {\r\n+  document.getElementById('pkg-form-ov').classList.remove('show');\r\n+  editPkgId = null;\r\n+}\r\n+\r\n+async function savePkg() {\r\n+  const name     = document.getElementById('pf-name').value.trim();\r\n+  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n+  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n+  const price    = parseInt(document.getElementById('pf-price').value);\r\n+  const featured = document.getElementById('pf-featured').value === '1';\r\n+  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n+  const active   = document.getElementById('pf-active').value === '1';\r\n+\r\n+  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n+\r\n+  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n+\r\n+  const { error } = editPkgId\r\n+    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n+    : await _sb.from('packages').insert(payload);\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n+  closePkgForm();\r\n+  loadPkgList();\r\n+  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n+}\r\n+\r\n+async function togglePkg(id, active) {\r\n+  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n+  loadPkgList();\r\n+}\r\n+\r\n+async function deletePkg(id) {\r\n+  if (!confirm('ลบแพ็กเกจนี้?')) return;\r\n+  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบแพ็กเกจแล้ว', 'info');\r\n+  loadPkgList();\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775289740667,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,8 +5,9 @@\n let editDeckId = null;\r\n \r\n /* ---- TABS ---- */\r\n function switchTab(name, idx) {\r\n+  if (name === 'settings') loadPkgList();\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   const target = document.getElementById('tc-' + name);\r\n   if (target) target.classList.add('act');\r\n@@ -581,468 +582,5 @@\n   const { error } = await _sb.from('packages').delete().eq('id', id);\r\n   if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n   toast('ลบแพ็กเกจแล้ว', 'info');\r\n   loadPkgList();\r\n-}\n-/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n-\r\n-let editDeckId = null;\r\n-\r\n-/* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');\r\n-\r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-}\r\n-\r\n-/* ---- TAB LOADERS ---- */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  if (!container) return;\r\n-  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n-}\r\n-\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  if (!container) return;\r\n-\r\n-  // โหลด user count\r\n-  const users = await API.getAllUsers();\r\n-  const totalUsers = document.getElementById('stat-users');\r\n-  if (totalUsers) totalUsers.textContent = users.length;\r\n-\r\n-  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n-  renderAdmDecks();\r\n-}\r\n-\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('credit_history')\r\n-    .select('*, profiles(username)')\r\n-    .order('created_at', { ascending: false })\r\n-    .limit(100);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const TYPE_ICO = {\r\n-    free:     'fi-sr-gift',\r\n-    purchase: 'fi-sr-shopping-cart',\r\n-    deduct:   'fi-sr-layers',\r\n-    admin:    'fi-sr-shield-check',\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    free:     'รับฟรี',\r\n-    purchase: 'ซื้อเครดิต',\r\n-    deduct:   'ใช้ไพ่',\r\n-    admin:    'แอดมินเติม',\r\n-  };\r\n-\r\n-  container.innerHTML = data.map(h => {\r\n-    const isPlus  = h.amount > 0;\r\n-    const typeKey = h.type || 'deduct';\r\n-    const ico     = TYPE_ICO[typeKey]  || 'fi-sr-coins';\r\n-    const label   = TYPE_LABEL[typeKey] || typeKey;\r\n-    const username = h.profiles?.username || h.user_id?.slice(0,8) || '?';\r\n-    const date    = new Date(h.created_at);\r\n-    const dateStr = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n-    const timeStr = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n-\r\n-    return `\r\n-    <div class=\"hist-row\">\r\n-      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n-        <i class=\"fi ${ico}\"></i>\r\n-      </div>\r\n-      <div class=\"hist-info\">\r\n-        <div class=\"hist-top\">\r\n-          <span class=\"hist-note\">${h.note || label}</span>\r\n-          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n-        </div>\r\n-        <div class=\"hist-sub\">\r\n-          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n-        ${isPlus ? '+' : ''}${h.amount}\r\n-        <span class=\"hist-unit\">เครดิต</span>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-  if (!container) return;\r\n-  const listEl = document.getElementById('announce-list');\r\n-  if (!listEl) return;\r\n-\r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n-\r\n-  if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const TYPE_ICO = {\r\n-    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n-    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n-  };\r\n-\r\n-  listEl.innerHTML = data.map(a => {\r\n-    const t       = a.type || 'info';\r\n-    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n-    const expires = a.expires_at\r\n-      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n-      : null;\r\n-    return `\r\n-    <div class=\"ann-row\">\r\n-      <div class=\"ann-row-ico ann-ico-${t}\">\r\n-        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n-      </div>\r\n-      <div class=\"ann-row-info\">\r\n-        <div class=\"ann-row-top\">\r\n-          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n-          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n-          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n-            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n-            ${a.active ? 'เผยแพร่' : 'draft'}\r\n-          </span>\r\n-          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n-        </div>\r\n-        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n-        <div class=\"ann-row-sub\">\r\n-          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n-          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"ann-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n-          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-\r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n-  const active  = document.getElementById('ann-active')?.value === '1';\r\n-  const expires = document.getElementById('ann-expires')?.value || null;\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type,\r\n-    show_popup: popup,\r\n-    active,\r\n-    expires_at: expires ? new Date(expires).toISOString() : null,\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  document.getElementById('ann-expires').value = '';\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-async function toggleAnnounce(id, active) {\r\n-  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-\r\n-  // หา deck list ทั้งจาก tab decks และ tab stats\r\n-  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n-}\r\n-\r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n-}\r\n-\r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value     = d.name;\r\n-    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value    = d.theme || 'default';\r\n-    document.getElementById('df-desc').value     = d.desc  || '';\r\n-    document.getElementById('df-cost').value     = d.cost  || 1;\r\n-    document.getElementById('df-category').value = d.category || '';\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n-\r\n-function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n-  const theme    = document.getElementById('df-theme').value            || 'default';\r\n-  const desc     = document.getElementById('df-desc').value.trim();\r\n-  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n-  const category = document.getElementById('df-category').value.trim() || '';\r\n-\r\n-  if (editDeckId) {\r\n-    const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n-    DB.cards[id] = [];\r\n-  }\r\n-  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n-}\r\n-\r\n-function delDeck(id) {\r\n-  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n-  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-  saveDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n-}\r\n-\r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n-}\r\n-\r\n-function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  DB.cards[id] = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) DB.cards[id].push({ cat, text });\r\n-  });\r\n-  saveDB();\r\n-  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n-  renderAdmDecks();\r\n-}\r\n-\r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n-}\r\n-\r\n-// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url')?.value?.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-\r\n-  API.setSheetUrl(url);\r\n-  toast('กำลังดึงข้อมูล...', 'info');\r\n-\r\n-  API.syncFromSheets(url)\r\n-    .then(({ decks, cards }) => {\r\n-      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      renderDecks();\r\n-    })\r\n-    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n-}\r\n-\r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n-}\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n-\r\n-/* [FIX #6] validate JSON structure ก่อน import */\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      const parsed = JSON.parse(ev.target.result);\r\n-\r\n-      // validate required fields\r\n-      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n-      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n-      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n-\r\n-      // validate deck structure\r\n-      for (const d of parsed.decks) {\r\n-        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n-        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n-      }\r\n-\r\n-      // validate cards structure\r\n-      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n-        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n-        for (const c of cards) {\r\n-          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n-        }\r\n-      }\r\n-\r\n-      DB = parsed;\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch (err) {\r\n-      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n-    }\r\n-  };\r\n-  r.readAsText(f);\r\n-  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n-  e.target.value = '';\r\n-// <<<<<<< HEAD\r\n-}\r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title,\r\n-    message,\r\n-    type,\r\n-    show_popup: popup,\r\n-    active: true,\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  loadAnnounceTab();\r\n-}\r\n-// =======\r\n-// }\r\n-// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\n\\ No newline at end of file\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775290312196,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,9 +5,8 @@\n let editDeckId = null;\r\n \r\n /* ---- TABS ---- */\r\n function switchTab(name, idx) {\r\n-  if (name === 'settings') loadPkgList();\r\n   document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   const target = document.getElementById('tc-' + name);\r\n   if (target) target.classList.add('act');\r\n@@ -15,8 +14,9 @@\n   if (name === 'users')    loadUserTab();\r\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n+  if (name === 'settings') loadPkgList();\r\n }\r\n \r\n /* ---- TAB LOADERS ---- */\r\n async function loadUserTab() {\r\n@@ -36,47 +36,46 @@\n \r\n   // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n   renderAdmDecks();\r\n }\r\n-\r\n async function loadHistoryTab() {\r\n   const container = document.getElementById('tc-history');\r\n   if (!container) return;\r\n   container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n \r\n-  const { data, error } = await _sb\r\n-    .from('credit_history')\r\n-    .select('*, profiles(username)')\r\n-    .order('created_at', { ascending: false })\r\n-    .limit(100);\r\n+  // ดึง history และ users พร้อมกัน\r\n+  const [{ data, error }, { data: users }] = await Promise.all([\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n+    _sb.from('profiles').select('id, username')\r\n+  ]);\r\n \r\n   if (error || !data?.length) {\r\n     container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n     return;\r\n   }\r\n \r\n+  // สร้าง map id → username\r\n+  const userMap = {};\r\n+  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n+\r\n   const TYPE_ICO = {\r\n-    free:     'fi-sr-gift',\r\n-    purchase: 'fi-sr-shopping-cart',\r\n-    deduct:   'fi-sr-layers',\r\n-    admin:    'fi-sr-shield-check',\r\n+    free: 'fi-sr-gift', purchase: 'fi-sr-shopping-cart',\r\n+    deduct: 'fi-sr-layers', admin: 'fi-sr-shield-check',\r\n   };\r\n   const TYPE_LABEL = {\r\n-    free:     'รับฟรี',\r\n-    purchase: 'ซื้อเครดิต',\r\n-    deduct:   'ใช้ไพ่',\r\n-    admin:    'แอดมินเติม',\r\n+    free: 'รับฟรี', purchase: 'ซื้อเครดิต',\r\n+    deduct: 'ใช้ไพ่', admin: 'แอดมินเติม',\r\n   };\r\n \r\n   container.innerHTML = data.map(h => {\r\n-    const isPlus  = h.amount > 0;\r\n-    const typeKey = h.type || 'deduct';\r\n-    const ico     = TYPE_ICO[typeKey]  || 'fi-sr-coins';\r\n-    const label   = TYPE_LABEL[typeKey] || typeKey;\r\n-    const username = h.profiles?.username || h.user_id?.slice(0,8) || '?';\r\n-    const date    = new Date(h.created_at);\r\n-    const dateStr = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n-    const timeStr = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n+    const isPlus   = h.amount > 0;\r\n+    const typeKey  = h.type || 'deduct';\r\n+    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n+    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n+    const date     = new Date(h.created_at);\r\n+    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n \r\n     return `\r\n     <div class=\"hist-row\">\r\n       <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n"
                },
                {
                    "date": 1775290613578,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -14,9 +14,10 @@\n   if (name === 'users')    loadUserTab();\r\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n-  if (name === 'settings') loadPkgList();\r\n+  if (name === 'settings') { loadSettingsTab(); }\r\n+  if (name === 'sheet')    {} // static content\r\n }\r\n \r\n /* ---- TAB LOADERS ---- */\r\n async function loadUserTab() {\r\n@@ -433,36 +434,24 @@\n   };\r\n   r.readAsText(f);\r\n   // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n   e.target.value = '';\r\n-// <<<<<<< HEAD\r\n }\r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+/* ---- SETTINGS TAB ---- */\r\n+async function loadSettingsTab() {\r\n+  // โหลด packages list ในแท็บตั้งค่า\r\n+  if (typeof loadPkgList === 'function') await loadPkgList();\r\n \r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+  // sync ค่า settings fields\r\n+  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n+  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n+  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n+  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n+  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n+  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n+}\r\n \r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title,\r\n-    message,\r\n-    type,\r\n-    show_popup: popup,\r\n-    active: true,\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  loadAnnounceTab();\r\n-}\r\n-// =======\r\n-// }\r\n-// >>>>>>> 35c8474b63db1f7722e0f8c3738916b5f76e4a23\r\n /* ---- PACKAGE MANAGER ---- */\r\n let editPkgId = null;\r\n \r\n async function loadPkgList() {\r\n"
                },
                {
                    "date": 1775290991686,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -15,29 +15,57 @@\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n   if (name === 'settings') { loadSettingsTab(); }\r\n+  if (name === 'cards')    { renderAdmSel(); }\r\n   if (name === 'sheet')    {} // static content\r\n }\r\n \r\n /* ---- TAB LOADERS ---- */\r\n async function loadUserTab() {\r\n   const container = document.getElementById('tc-users');\r\n   if (!container) return;\r\n+  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n   if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n }\r\n \r\n async function loadStatsTab() {\r\n   const container = document.getElementById('tc-stats');\r\n   if (!container) return;\r\n \r\n-  // โหลด user count\r\n   const users = await API.getAllUsers();\r\n-  const totalUsers = document.getElementById('stat-users');\r\n-  if (totalUsers) totalUsers.textContent = users.length;\r\n+  const elUsers = document.getElementById('stat-users');\r\n+  if (elUsers) elUsers.textContent = users.length;\r\n \r\n-  // อัปเดต stat cards ที่อยู่ใน stats-bar ด้านบน\r\n-  renderAdmDecks();\r\n+  // stat-decks-s และ stat-cards-s อยู่ใน tc-stats\r\n+  const elDecks = document.getElementById('stat-decks-s');\r\n+  const elCards = document.getElementById('stat-cards-s');\r\n+  if (elDecks) elDecks.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (elCards) elCards.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+\r\n+  // render deck list ใน stats tab\r\n+  const l = document.getElementById('adm-deck-list-stats');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.hidden ? '<span class=\"tag\" style=\"color:var(--text3);\">ซ่อน</span>' : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n }\r\n async function loadHistoryTab() {\r\n   const container = document.getElementById('tc-history');\r\n   if (!container) return;\r\n@@ -210,15 +238,16 @@\n }\r\n \r\n /* ---- DECK LIST ---- */\r\n function renderAdmDecks() {\r\n+  // update stats-bar (ด้านบนสุดของ admin panel)\r\n   const deckEl = document.getElementById('stat-decks');\r\n   const cardEl = document.getElementById('stat-cards');\r\n   if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n   if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n \r\n-  // หา deck list ทั้งจาก tab decks และ tab stats\r\n-  const l = document.getElementById('adm-deck-list') || document.getElementById('adm-deck-list-stats');\r\n+  // render deck list ใน tab กองไพ่\r\n+  const l = document.getElementById('adm-deck-list');\r\n   if (!l) return;\r\n   l.innerHTML = '';\r\n   DB.decks.forEach(d => {\r\n     const cnt = (DB.cards[d.id] || []).length;\r\n@@ -248,8 +277,16 @@\n     l.appendChild(r);\r\n   });\r\n }\r\n \r\n+function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  if (!d) return;\r\n+  d.hidden = !d.hidden;\r\n+  saveDB(); renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n function renderAdmSel() {\r\n   const s = document.getElementById('adm-deck-sel');\r\n   s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n   DB.decks.forEach(d => {\r\n"
                },
                {
                    "date": 1775291254788,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -94,9 +94,9 @@\n     free: 'รับฟรี', purchase: 'ซื้อเครดิต',\r\n     deduct: 'ใช้ไพ่', admin: 'แอดมินเติม',\r\n   };\r\n \r\n-  container.innerHTML = data.map(h => {\r\n+  const rows = data.map(h => {\r\n     const isPlus   = h.amount > 0;\r\n     const typeKey  = h.type || 'deduct';\r\n     const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n     const label    = TYPE_LABEL[typeKey] || typeKey;\r\n@@ -128,8 +128,10 @@\n         <span class=\"hist-unit\">เครดิต</span>\r\n       </div>\r\n     </div>`;\r\n   }).join('');\r\n+\r\n+  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n }\r\n async function loadAnnounceTab() {\r\n   const container = document.getElementById('tc-announce');\r\n   if (!container) return;\r\n"
                },
                {
                    "date": 1775332586842,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -279,14 +279,20 @@\n     l.appendChild(r);\r\n   });\r\n }\r\n \r\n-function toggleDeck(id) {\r\n+async function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n-  saveDB(); renderAdmDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+  const ok = await saveDeckToDB(d);\r\n+  if (ok) {\r\n+    renderAdmDecks();\r\n+    toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+  } else {\r\n+    d.hidden = !d.hidden; // rollback\r\n+    toast('เกิดข้อผิดพลาด ไม่สามารถบันทึกได้', 'error');\r\n+  }\r\n }\r\n \r\n function renderAdmSel() {\r\n   const s = document.getElementById('adm-deck-sel');\r\n"
                },
                {
                    "date": 1775332803536,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,42 +30,241 @@\n \r\n async function loadStatsTab() {\r\n   const container = document.getElementById('tc-stats');\r\n   if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n \r\n-  const users = await API.getAllUsers();\r\n-  const elUsers = document.getElementById('stat-users');\r\n-  if (elUsers) elUsers.textContent = users.length;\r\n+  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n+  const [\r\n+    users,\r\n+    { data: history },\r\n+    { data: packages }\r\n+  ] = await Promise.all([\r\n+    API.getAllUsers(),\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n+    _sb.from('packages').select('*').eq('is_active', true)\r\n+  ]);\r\n \r\n-  // stat-decks-s และ stat-cards-s อยู่ใน tc-stats\r\n-  const elDecks = document.getElementById('stat-decks-s');\r\n-  const elCards = document.getElementById('stat-cards-s');\r\n-  if (elDecks) elDecks.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (elCards) elCards.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+  const now        = new Date();\r\n+  const weekAgo    = new Date(now - 7  * 86400000);\r\n+  const monthAgo   = new Date(now - 30 * 86400000);\r\n \r\n-  // render deck list ใน stats tab\r\n-  const l = document.getElementById('adm-deck-list-stats');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.hidden ? '<span class=\"tag\" style=\"color:var(--text3);\">ซ่อน</span>' : ''}\r\n+  // ---- ผู้ใช้ ----\r\n+  const totalUsers    = users.length;\r\n+  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n+  const adminUsers    = users.filter(u => u.is_admin).length;\r\n+  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n+  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n+  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n+  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n+\r\n+  // ---- กองไพ่ & ไพ่ ----\r\n+  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n+  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n+  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n+\r\n+  // top deck by card count\r\n+  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n+\r\n+  // ---- เครดิต / ประวัติ ----\r\n+  const hist         = history || [];\r\n+  const purchases    = hist.filter(h => h.type === 'purchase');\r\n+  const frees        = hist.filter(h => h.type === 'free');\r\n+  const deducts      = hist.filter(h => h.type === 'deduct');\r\n+  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n+  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n+\r\n+  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n+  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n+    const pkg = (packages || []).find(p => {\r\n+      const total = p.coins + p.bonus;\r\n+      return total === h.amount;\r\n+    });\r\n+    return s + (pkg ? pkg.price : 0);\r\n+  }, 0);\r\n+\r\n+  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n+  const newThisWeek  = users.filter(u => {\r\n+    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n+    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n+  }).length;\r\n+\r\n+  // ---- สร้าง HTML ----\r\n+  container.innerHTML = `\r\n+    <!-- ROW 1: ตัวเลขหลัก -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n+          </div>\r\n         </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n+            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n+            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n+            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n+          </div>\r\n+        </div>\r\n       </div>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n+    </div>\r\n+\r\n+    <!-- ROW 2: เครดิต -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n+            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 3: กองไพ่ -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 4: Top Decks -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${decksSorted.slice(0, 6).map((d, i) => {\r\n+          const cnt    = (DB.cards[d.id] || []).length;\r\n+          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n+          const pct    = Math.round(cnt / maxCnt * 100);\r\n+          const th     = getTh(d);\r\n+          return `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;min-width:0;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n+                ${d.name}\r\n+                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n+              </div>\r\n+              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n+                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n+            </div>\r\n+          </div>`;\r\n+        }).join('')}\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 5: Top Users by Credits -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n+              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;\">\r\n+                ${u.username}\r\n+                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n+                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n+              </div>\r\n+              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n+                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n+              ${(u.credits||0).toLocaleString()}\r\n+              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n+            </div>\r\n+          </div>`).join('')}\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+\r\n+  // update legacy stat elements ด้านบน (stat bar)\r\n+  const elUsers = document.getElementById('stat-users');\r\n+  if (elUsers) elUsers.textContent = totalUsers;\r\n }\r\n async function loadHistoryTab() {\r\n   const container = document.getElementById('tc-history');\r\n   if (!container) return;\r\n@@ -279,20 +478,14 @@\n     l.appendChild(r);\r\n   });\r\n }\r\n \r\n-async function toggleDeck(id) {\r\n+function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n-  const ok = await saveDeckToDB(d);\r\n-  if (ok) {\r\n-    renderAdmDecks();\r\n-    toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n-  } else {\r\n-    d.hidden = !d.hidden; // rollback\r\n-    toast('เกิดข้อผิดพลาด ไม่สามารถบันทึกได้', 'error');\r\n-  }\r\n+  saveDB(); renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n }\r\n \r\n function renderAdmSel() {\r\n   const s = document.getElementById('adm-deck-sel');\r\n"
                },
                {
                    "date": 1775333939553,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,812 +1,866 @@\n-/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n+/* ========================================\r\n+   admin.css — Admin Panel Styles\r\n+   ======================================== */\r\n \r\n-let editDeckId = null;\r\n+#admin-screen { display: none; min-height: 100vh; padding: 90px 36px 40px; }\r\n \r\n+.adm-hdr { display: flex; align-items: center; justify-content: space-between; margin-bottom: 36px; }\r\n+.adm-ttl { font-family: \"Kanit\", sans-serif; font-weight: 900; font-size: 1.85rem; letter-spacing: 4px; display: flex; align-items: center; gap: 10px; }\r\n+.adm-ttl .r { color: var(--red); }\r\n+.adm-sub { color: var(--text2); font-size: .82rem; margin-top: 3px; }\r\n+\r\n /* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');\r\n+.tabs {\r\n+  display: flex; gap: 3px;\r\n+  background: #111; padding: 4px; border-radius: 10px;\r\n+  border: 1px solid #1e1e1e; margin-bottom: 28px;\r\n+}\r\n+.tab {\r\n+  padding: 9px 20px; border-radius: 7px; border: none;\r\n+  background: transparent; color: var(--text2);\r\n+  font-family: \"Kanit\", sans-serif; font-weight: 600; font-size: .84rem;\r\n+  cursor: pointer; transition: .2s;\r\n+  display: flex; align-items: center; gap: 7px;\r\n+}\r\n+.tab.act { background: var(--red); color: #fff; }\r\n+.tab-content     { display: none; }\r\n+.tab-content.act { display: block; }\r\n \r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-  if (name === 'settings') { loadSettingsTab(); }\r\n-  if (name === 'cards')    { renderAdmSel(); }\r\n-  if (name === 'sheet')    {} // static content\r\n+/* ---- DECK LIST ---- */\r\n+.deck-list-adm { display: flex; flex-direction: column; gap: 13px; margin-bottom: 22px; }\r\n+\r\n+.drow {\r\n+  background: #111; border: 1px solid #1e1e1e; border-radius: 11px;\r\n+  padding: 18px 22px;\r\n+  display: flex; align-items: center; justify-content: space-between; gap: 14px;\r\n }\r\n+.drow-info  { display: flex; align-items: center; gap: 12px; flex: 1; }\r\n+.drow-ico   { width: 44px; height: 44px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 1.25rem; flex-shrink: 0; }\r\n+.drow-nm    { font-weight: 700; font-size: .95rem; }\r\n+.drow-meta  { color: var(--text2); font-size: .78rem; display: flex; align-items: center; gap: 10px; margin-top: 3px; flex-wrap: wrap; }\r\n+.drow-meta .tag { display: flex; align-items: center; gap: 4px; }\r\n+.drow-acts  { display: flex; gap: 7px; }\r\n \r\n-/* ---- TAB LOADERS ---- */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  if (!container) return;\r\n-  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n-  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+/* ---- SMALL BUTTONS ---- */\r\n+.btn-sm {\r\n+  padding: 7px 13px; border-radius: 6px; border: none;\r\n+  font-family: \"Kanit\", sans-serif; font-size: .78rem;\r\n+  cursor: pointer; font-weight: 600; transition: .2s;\r\n+  display: flex; align-items: center; gap: 5px;\r\n }\r\n+.btn-edit           { background: #1e1e1e; border: 1px solid #2e2e2e; color: var(--text2); }\r\n+.btn-edit:hover     { border-color: var(--text2); color: #fff; }\r\n+.btn-del            { background: rgba(229,9,20,.08); border: 1px solid rgba(229,9,20,.25); color: var(--red); }\r\n+.btn-del:hover      { background: var(--red); color: #fff; }\r\n+.btn-add            { background: var(--red); color: #fff; border: none; padding: 10px 22px; border-radius: 8px; font-family: \"Kanit\", sans-serif; font-weight: 700; font-size: .88rem; cursor: pointer; display: flex; align-items: center; gap: 7px; }\r\n+.btn-add:hover      { background: var(--red3); }\r\n \r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n+/* ---- SHEET SECTION ---- */\r\n+.sheet-sec { background: #111; border: 1px solid #1e1e1e; border-radius: 12px; padding: 24px; margin-bottom: 20px; }\r\n+.sheet-sec h3 { font-size: .95rem; font-weight: 700; margin-bottom: 6px; display: flex; align-items: center; gap: 8px; }\r\n+.sheet-sec p  { color: var(--text2); font-size: .82rem; margin-bottom: 14px; }\r\n \r\n-  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n-  const [\r\n-    users,\r\n-    { data: history },\r\n-    { data: packages }\r\n-  ] = await Promise.all([\r\n-    API.getAllUsers(),\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n-    _sb.from('packages').select('*').eq('is_active', true)\r\n-  ]);\r\n+.sheet-row { display: flex; gap: 9px; }\r\n+.sh-inp {\r\n+  flex: 1; background: #1a1a1a; border: 1px solid #2e2e2e;\r\n+  border-radius: 8px; padding: 9px 13px;\r\n+  color: #fff; font-family: \"Kanit\", sans-serif; font-size: .88rem; outline: none;\r\n+}\r\n+.sh-inp:focus { border-color: var(--red); }\r\n \r\n-  const now        = new Date();\r\n-  const weekAgo    = new Date(now - 7  * 86400000);\r\n-  const monthAgo   = new Date(now - 30 * 86400000);\r\n+.btn-conn {\r\n+  background: var(--red); color: #fff; border: none;\r\n+  padding: 9px 18px; border-radius: 8px;\r\n+  font-family: \"Kanit\", sans-serif; font-weight: 700;\r\n+  cursor: pointer; white-space: nowrap;\r\n+  display: inline-flex; align-items: center; gap: 6px;\r\n+}\r\n+.btn-conn:hover       { background: var(--red3); }\r\n+.btn-conn.ghost       { background: #1a1a1a; border: 1px solid #2e2e2e; color: var(--text2); }\r\n+.btn-conn.ghost:hover { border-color: var(--text2); color: #fff; background: #222; }\r\n \r\n-  // ---- ผู้ใช้ ----\r\n-  const totalUsers    = users.length;\r\n-  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n-  const adminUsers    = users.filter(u => u.is_admin).length;\r\n-  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n-  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n-  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n-  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n+/* ---- CARD EDITOR ---- */\r\n+.card-editor-box { background: #111; border: 1px solid #1e1e1e; border-radius: 12px; padding: 22px; }\r\n+.card-editor-box h3 { margin-bottom: 14px; font-size: .95rem; display: flex; align-items: center; gap: 8px; }\r\n \r\n-  // ---- กองไพ่ & ไพ่ ----\r\n-  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n-  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n-  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n+.card-list-ed {\r\n+  display: flex; flex-direction: column; gap: 9px;\r\n+  max-height: 380px; overflow-y: auto; margin-bottom: 14px; padding-right: 4px;\r\n+}\r\n+.card-list-ed::-webkit-scrollbar       { width: 3px; }\r\n+.card-list-ed::-webkit-scrollbar-thumb { background: var(--red2); border-radius: 4px; }\r\n \r\n-  // top deck by card count\r\n-  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n+.ced-row {\r\n+  display: flex; gap: 8px; align-items: center;\r\n+  background: #1a1a1a; border: 1px solid #252525; border-radius: 8px; padding: 9px 12px;\r\n+}\r\n+.ced-row input   { background: transparent; border: none; color: #fff; font-family: \"Kanit\", sans-serif; font-size: .88rem; flex: 1; outline: none; }\r\n+.ced-row .ci     { width: 110px; flex: none; color: var(--text2); }\r\n+.ced-row .del-btn {\r\n+  background: none; border: none; color: var(--text3); cursor: pointer;\r\n+  font-size: .82rem; width: 24px; height: 24px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  border-radius: 4px; transition: .2s;\r\n+}\r\n+.ced-row .del-btn:hover { color: var(--red); background: rgba(229,9,20,.1); }\r\n \r\n-  // ---- เครดิต / ประวัติ ----\r\n-  const hist         = history || [];\r\n-  const purchases    = hist.filter(h => h.type === 'purchase');\r\n-  const frees        = hist.filter(h => h.type === 'free');\r\n-  const deducts      = hist.filter(h => h.type === 'deduct');\r\n-  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n-  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n+.btn-add-card {\r\n+  background: transparent; border: 1px dashed #2e2e2e; color: var(--text2);\r\n+  padding: 9px; border-radius: 8px; width: 100%;\r\n+  font-family: \"Kanit\", sans-serif; cursor: pointer; transition: .2s; font-size: .85rem;\r\n+  display: flex; align-items: center; justify-content: center; gap: 7px;\r\n+}\r\n+.btn-add-card:hover { border-color: #444; color: #fff; }\r\n \r\n-  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n-  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n-    const pkg = (packages || []).find(p => {\r\n-      const total = p.coins + p.bonus;\r\n-      return total === h.amount;\r\n-    });\r\n-    return s + (pkg ? pkg.price : 0);\r\n-  }, 0);\r\n+/* ---- FORM FIELDS ---- */\r\n+.fg { margin-bottom: 14px; }\r\n+.fg label { display: flex; align-items: center; gap: 6px; font-size: .78rem; color: var(--text2); margin-bottom: 5px; }\r\n+.fc {\r\n+  width: 100%; background: #1a1a1a; border: 1px solid #2e2e2e;\r\n+  border-radius: 8px; padding: 9px 12px;\r\n+  color: #fff; font-family: \"Kanit\", sans-serif; font-size: .9rem; outline: none;\r\n+  transition: border-color .2s;\r\n+}\r\n+.fc:focus { border-color: var(--red); }\r\n \r\n-  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n-  const newThisWeek  = users.filter(u => {\r\n-    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n-    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n-  }).length;\r\n+/* ---- DECK FORM OVERLAY ---- */\r\n+.form-overlay { position: fixed; inset: 0; background: rgba(0,0,0,.88); z-index: 700; display: none; align-items: center; justify-content: center; backdrop-filter: blur(6px); }\r\n+.form-overlay.show { display: flex; }\r\n \r\n-  // ---- สร้าง HTML ----\r\n-  container.innerHTML = `\r\n-    <!-- ROW 1: ตัวเลขหลัก -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n-            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n-            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n-            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n+.form-box { background: #181818; border: 1px solid #272727; border-radius: 16px; padding: 34px; max-width: 400px; width: 90%; position: relative; }\r\n+.form-box h3 { font-size: 1.15rem; font-weight: 700; margin-bottom: 18px; display: flex; align-items: center; gap: 9px; }\r\n \r\n-    <!-- ROW 2: เครดิต -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n-            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n+.form-x {\r\n+  position: absolute; top: 12px; right: 14px;\r\n+  background: none; border: none; color: var(--text2); cursor: pointer;\r\n+  width: 28px; height: 28px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  border-radius: 50%; transition: .2s; font-size: .9rem;\r\n+}\r\n+.form-x:hover { background: #222; color: #fff; }\r\n \r\n-    <!-- ROW 3: กองไพ่ -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n+.form-acts  { display: flex; gap: 8px; justify-content: flex-end; margin-top: 18px; }\r\n+.btn-cancel { background: transparent; border: 1px solid #2e2e2e; color: var(--text2); padding: 9px 18px; border-radius: 8px; font-family: \"Kanit\", sans-serif; cursor: pointer; display: flex; align-items: center; gap: 6px; }\r\n+.btn-save   { background: var(--red); color: #fff; border: none; padding: 9px 22px; border-radius: 8px; font-family: \"Kanit\", sans-serif; font-weight: 700; cursor: pointer; display: flex; align-items: center; gap: 6px; }\r\n \r\n-    <!-- ROW 4: Top Decks -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${decksSorted.slice(0, 6).map((d, i) => {\r\n-          const cnt    = (DB.cards[d.id] || []).length;\r\n-          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n-          const pct    = Math.round(cnt / maxCnt * 100);\r\n-          const th     = getTh(d);\r\n-          return `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n-              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;min-width:0;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n-                ${d.name}\r\n-                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n-              </div>\r\n-              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n-                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n-            </div>\r\n-          </div>`;\r\n-        }).join('')}\r\n-      </div>\r\n-    </div>\r\n+/* ---- RESPONSIVE ---- */\r\n+@media (max-width: 580px) {\r\n+  #admin-screen { padding: 88px 14px 30px; }\r\n+  .tabs { flex-wrap: wrap; }\r\n+}\r\n \r\n-    <!-- ROW 5: Top Users by Credits -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n-              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;\">\r\n-                ${u.username}\r\n-                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n-                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n-              </div>\r\n-              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n-                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n-              ${(u.credits||0).toLocaleString()}\r\n-              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n-            </div>\r\n-          </div>`).join('')}\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n+.btn-visible { background: rgba(76,175,80,.12); color: #4caf50; border-color: rgba(76,175,80,.3); }\r\n+.btn-hidden  { background: rgba(255,255,255,.05); color: var(--text3); border-color: #333; }\r\n \r\n-  // update legacy stat elements ด้านบน (stat bar)\r\n-  const elUsers = document.getElementById('stat-users');\r\n-  if (elUsers) elUsers.textContent = totalUsers;\r\n+.stats-bar {\r\n+  display: flex; gap: 14px; flex-wrap: wrap; margin-bottom: 24px;\r\n }\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n+.stat-card {\r\n+  flex: 1; min-width: 140px;\r\n+  background: #161616; border: 1px solid #252525; border-radius: 12px;\r\n+  padding: 18px 20px;\r\n+}\r\n+.stat-label { color: var(--text2); font-size: .78rem; margin-bottom: 8px; }\r\n+.stat-val { font-size: 1.8rem; font-weight: 800; line-height: 1; }\r\n+.stat-val.gold  { color: var(--gold); }\r\n+.stat-val.green { color: #4caf50; }\r\n+.stat-val.red   { color: var(--red); }\r\n+.stat-val.white { color: #fff; }\r\n+.stat-sub { color: var(--text3); font-size: .72rem; margin-top: 6px; }\r\n \r\n-  // ดึง history และ users พร้อมกัน\r\n-  const [{ data, error }, { data: users }] = await Promise.all([\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n-    _sb.from('profiles').select('id, username')\r\n-  ]);\r\n+.user-cr-row { display:flex; justify-content:space-between; align-items:center; padding:10px 0; border-bottom:1px solid var(--border); }\r\n+.ucr-info { display:flex; align-items:center; gap:8px; }\r\n+.ucr-name { font-weight:600; }\r\n+.ucr-ctrl { display:flex; align-items:center; gap:8px; }\r\n \r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n-    return;\r\n-  }\r\n+.adm-sec-hdr { display:flex; justify-content:space-between; align-items:center; margin-bottom:16px; flex-wrap:wrap; gap:10px; }\r\n+.adm-loading { color:var(--text3); font-size:.85rem; padding:20px 0; }\r\n+.adm-empty   { color:var(--text3); font-size:.85rem; padding:20px 0; text-align:center; }\r\n+ \r\n+/* Table */\r\n+.adm-table { width:100%; border-collapse:collapse; font-size:.88rem; }\r\n+.adm-table th { text-align:left; color:var(--text3); font-size:.75rem; text-transform:uppercase; letter-spacing:.05em; padding:8px 12px; border-bottom:1px solid var(--border); }\r\n+.adm-table td { padding:10px 12px; border-bottom:1px solid #1e1e1e; vertical-align:middle; }\r\n+.adm-table tr:hover td { background:#1a1a1a; }\r\n+ \r\n+/* Stat bar */\r\n+.stat-bar-wrap { background:#1a1a1a; border-radius:4px; height:6px; width:120px; }\r\n+.stat-bar-fill { background:var(--red); border-radius:4px; height:6px; transition:width .4s; }\r\n+ \r\n+/* Announce */\r\n+.announce-row { display:flex; justify-content:space-between; align-items:flex-start; gap:12px; padding:14px 0; border-bottom:1px solid var(--border); }\r\n+.announce-row.inactive { opacity:.45; }\r\n+.ann-info { display:flex; flex-direction:column; gap:4px; }\r\n+.ann-acts { display:flex; align-items:center; gap:6px; flex-shrink:0; }\r\n+.ann-type { font-size:.7rem; font-weight:700; letter-spacing:.08em; padding:2px 8px; border-radius:4px; }\r\n+.ann-info  { color:#60c0ff; background:rgba(96,192,255,.12); }\r\n+.ann-promo { color:var(--gold); background:rgba(255,200,0,.12); }\r\n+.ann-warning { color:orange; background:rgba(255,165,0,.12); }\r\n+.ann-success { color:#4caf50; background:rgba(76,175,80,.12); }\r\n+.tag-off { opacity:.4; }\r\n+ \r\n+/* Toggle */\r\n+.toggle-wrap { display:flex; align-items:center; gap:6px; cursor:pointer; font-size:.85rem; }\r\n \r\n-  // สร้าง map id → username\r\n-  const userMap = {};\r\n-  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n \r\n-  const TYPE_ICO = {\r\n-    free: 'fi-sr-gift', purchase: 'fi-sr-shopping-cart',\r\n-    deduct: 'fi-sr-layers', admin: 'fi-sr-shield-check',\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    free: 'รับฟรี', purchase: 'ซื้อเครดิต',\r\n-    deduct: 'ใช้ไพ่', admin: 'แอดมินเติม',\r\n-  };\r\n+.announce-row {\r\n+  display: flex;\r\n+  align-items: flex-start;\r\n+  gap: 14px;\r\n+  padding: 16px 0;\r\n+  border-bottom: 1px solid var(--border);\r\n+}\r\n+.announce-row.inactive { opacity: .4; }\r\n+ \r\n+.ann-left { flex-shrink: 0; }\r\n+.ann-icon-wrap {\r\n+  width: 40px; height: 40px;\r\n+  border-radius: 10px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  font-size: 1.1rem;\r\n+}\r\n+.ann-icon-wrap.ann-info    { background: rgba(96,192,255,.15); color: #60c0ff; }\r\n+.ann-icon-wrap.ann-promo   { background: rgba(255,200,0,.15);  color: var(--gold); }\r\n+.ann-icon-wrap.ann-warning { background: rgba(255,165,0,.15);  color: orange; }\r\n+.ann-icon-wrap.ann-success { background: rgba(76,175,80,.15);  color: #4caf50; }\r\n+ \r\n+.ann-body { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4px; }\r\n+.ann-meta { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }\r\n+ \r\n+.ann-type-badge {\r\n+  display: inline-flex; align-items: center; gap: 4px;\r\n+  font-size: .72rem; font-weight: 700; letter-spacing: .06em;\r\n+  padding: 2px 10px; border-radius: 20px;\r\n+}\r\n+.ann-type-badge.ann-info    { background: rgba(96,192,255,.12); color: #60c0ff; }\r\n+.ann-type-badge.ann-promo   { background: rgba(255,200,0,.12);  color: var(--gold); }\r\n+.ann-type-badge.ann-warning { background: rgba(255,165,0,.12);  color: orange; }\r\n+.ann-type-badge.ann-success { background: rgba(76,175,80,.12);  color: #4caf50; }\r\n+ \r\n+.ann-status { font-size: .72rem; display: inline-flex; align-items: center; gap: 4px; }\r\n+.ann-on  { color: #4caf50; }\r\n+.ann-off { color: var(--text3); }\r\n+ \r\n+ \r\n+ \r\n+.ann-acts { display: flex; flex-direction: column; gap: 6px; flex-shrink: 0; }\r\n \r\n-  const rows = data.map(h => {\r\n-    const isPlus   = h.amount > 0;\r\n-    const typeKey  = h.type || 'deduct';\r\n-    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n-    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n-    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n-    const date     = new Date(h.created_at);\r\n-    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n-    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n \r\n-    return `\r\n-    <div class=\"hist-row\">\r\n-      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n-        <i class=\"fi ${ico}\"></i>\r\n-      </div>\r\n-      <div class=\"hist-info\">\r\n-        <div class=\"hist-top\">\r\n-          <span class=\"hist-note\">${h.note || label}</span>\r\n-          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n-        </div>\r\n-        <div class=\"hist-sub\">\r\n-          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n-        ${isPlus ? '+' : ''}${h.amount}\r\n-        <span class=\"hist-unit\">เครดิต</span>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n \r\n-  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n+\r\n+/* =========================================\r\n+   Announcement Popup v3 — CLEAN\r\n+   ========================================= */\r\n+\r\n+.ann-popup-box {\r\n+  width: 92%;\r\n+  max-width: 360px;\r\n+  background: linear-gradient(160deg, #1c1c1c 0%, #111 100%);\r\n+  border: 1px solid #2a2a2a;\r\n+  border-radius: 20px;\r\n+  overflow: hidden;\r\n+  box-shadow:\r\n+    0 0 0 1px rgba(255,255,255,.03),\r\n+    0 32px 80px rgba(0,0,0,.9),\r\n+    0 0 60px rgba(229,9,20,.08);\r\n+  animation: annPop .4s cubic-bezier(.34,1.4,.64,1);\r\n }\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-  if (!container) return;\r\n-  const listEl = document.getElementById('announce-list');\r\n-  if (!listEl) return;\r\n \r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n+@keyframes annPop {\r\n+  from { transform: scale(.88) translateY(20px); opacity: 0; }\r\n+  to   { transform: scale(1)   translateY(0);    opacity: 1; }\r\n+}\r\n \r\n-  const { data, error } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n+/* top accent */\r\n+.ann-top-bar { height: 3px; }\r\n+.ann-top-bar.ann-bar-info    { background: linear-gradient(90deg,transparent,#60c0ff,transparent); }\r\n+.ann-top-bar.ann-bar-promo   { background: linear-gradient(90deg,transparent,#f5c518,transparent); }\r\n+.ann-top-bar.ann-bar-warning { background: linear-gradient(90deg,transparent,#ffa726,transparent); }\r\n+.ann-top-bar.ann-bar-success { background: linear-gradient(90deg,transparent,#66bb6a,transparent); }\r\n \r\n-  if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n-    return;\r\n-  }\r\n+/* hero icon */\r\n+.ann-hero {\r\n+  display: flex; justify-content: center;\r\n+  padding: 28px 0 4px;\r\n+}\r\n+.ann-hero-info    { color: #60c0ff; }\r\n+.ann-hero-promo   { color: #f5c518; }\r\n+.ann-hero-warning { color: #ffa726; }\r\n+.ann-hero-success { color: #66bb6a; }\r\n \r\n-  const TYPE_ICO = {\r\n-    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n-    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n-  };\r\n+.ann-hero-ring {\r\n+  width: 70px; height: 70px; border-radius: 50%;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  font-size: 1.7rem; position: relative;\r\n+}\r\n+.ann-hero-info    .ann-hero-ring { background: rgba(96,192,255,.12); }\r\n+.ann-hero-promo   .ann-hero-ring { background: rgba(245,197,24,.12); }\r\n+.ann-hero-warning .ann-hero-ring { background: rgba(255,167,38,.12); }\r\n+.ann-hero-success .ann-hero-ring { background: rgba(102,187,106,.12); }\r\n \r\n-  listEl.innerHTML = data.map(a => {\r\n-    const t       = a.type || 'info';\r\n-    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n-    const expires = a.expires_at\r\n-      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n-      : null;\r\n-    return `\r\n-    <div class=\"ann-row\">\r\n-      <div class=\"ann-row-ico ann-ico-${t}\">\r\n-        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n-      </div>\r\n-      <div class=\"ann-row-info\">\r\n-        <div class=\"ann-row-top\">\r\n-          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n-          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n-          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n-            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n-            ${a.active ? 'เผยแพร่' : 'draft'}\r\n-          </span>\r\n-          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n-        </div>\r\n-        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n-        <div class=\"ann-row-sub\">\r\n-          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n-          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"ann-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n-          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n+.ann-hero-ring::before {\r\n+  content:\"\"; position:absolute; inset:-5px;\r\n+  border-radius:50%; border:1.5px solid currentColor; opacity:.18;\r\n }\r\n+.ann-hero-ring::after {\r\n+  content:\"\"; position:absolute; inset:-11px;\r\n+  border-radius:50%; border:1px solid currentColor; opacity:.07;\r\n+}\r\n \r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n-  const active  = document.getElementById('ann-active')?.value === '1';\r\n-  const expires = document.getElementById('ann-expires')?.value || null;\r\n+/* content */\r\n+.ann-content {\r\n+  padding: 14px 24px 18px;\r\n+  display: flex; flex-direction: column;\r\n+  align-items: center; text-align: center; gap: 8px;\r\n+}\r\n \r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+/* popup badge — ใช้ class .ann-popup-badge เพื่อไม่ชนกับ .ann-badge ใน admin list */\r\n+.ann-popup-badge {\r\n+  display: inline-flex; align-items: center; gap: 5px;\r\n+  font-size: .7rem; font-weight: 700; letter-spacing: .08em;\r\n+  padding: 3px 12px; border-radius: 20px; border: 1px solid currentColor;\r\n+}\r\n+.ann-popup-badge.ann-info    { color: #60c0ff; background: rgba(96,192,255,.08); }\r\n+.ann-popup-badge.ann-promo   { color: #f5c518; background: rgba(245,197,24,.08); }\r\n+.ann-popup-badge.ann-warning { color: #ffa726; background: rgba(255,167,38,.08); }\r\n+.ann-popup-badge.ann-success { color: #66bb6a; background: rgba(102,187,106,.08); }\r\n \r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type,\r\n-    show_popup: popup,\r\n-    active,\r\n-    expires_at: expires ? new Date(expires).toISOString() : null,\r\n-  });\r\n+.ann-title {\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-size: 1.3rem; font-weight: 800;\r\n+  color: #fff; line-height: 1.25; letter-spacing: .01em;\r\n+}\r\n+.ann-msg { font-size: .875rem; color: #888; line-height: 1.75; }\r\n \r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+.ann-expires {\r\n+  display: inline-flex; align-items: center; gap: 6px;\r\n+  font-size: .72rem; color: #555;\r\n+  background: #161616; border: 1px solid #222;\r\n+  border-radius: 6px; padding: 4px 10px; margin-top: 2px;\r\n+}\r\n \r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  document.getElementById('ann-expires').value = '';\r\n-  loadAnnounceTab();\r\n+/* footer */\r\n+.ann-footer {\r\n+  display: flex; align-items: center; justify-content: space-between;\r\n+  padding: 12px 16px 16px;\r\n+  border-top: 1px solid #1e1e1e; gap: 12px;\r\n }\r\n \r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n+.ann-brand {\r\n+  display: flex; flex-direction: column;\r\n+  border-left: 2.5px solid #e50914;\r\n+  padding-left: 10px; flex-shrink: 0; gap: 1px;\r\n }\r\n+.ann-brand-name {\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-weight: 900; font-size: .88rem; line-height: 1;\r\n+}\r\n+.ann-brand-sub { font-size: .54rem; color: #555; letter-spacing: .18em; }\r\n \r\n-async function toggleAnnounce(id, active) {\r\n-  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n+.ann-btn {\r\n+  flex: 1;\r\n+  display: flex; align-items: center; justify-content: center; gap: 7px;\r\n+  padding: 11px 16px; border: none; border-radius: 10px;\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-size: .9rem; font-weight: 700; color: #111;\r\n+  cursor: pointer; transition: filter .15s, transform .1s;\r\n }\r\n+.ann-btn-info    { background: #60c0ff; }\r\n+.ann-btn-promo   { background: #f5c518; }\r\n+.ann-btn-warning { background: #ffa726; }\r\n+.ann-btn-success { background: #66bb6a; }\r\n+.ann-btn:hover  { filter: brightness(1.12); }\r\n+.ann-btn:active { transform: scale(.97); }\r\n \r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-  // update stats-bar (ด้านบนสุดของ admin panel)\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n \r\n-  // render deck list ใน tab กองไพ่\r\n-  const l = document.getElementById('adm-deck-list');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n+/* ---- ADMIN USER LIST ---- */\r\n+#admin-user-list {\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 8px;\r\n }\r\n \r\n-function toggleDeck(id) {\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  if (!d) return;\r\n-  d.hidden = !d.hidden;\r\n-  saveDB(); renderAdmDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+.ucr-row {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 14px;\r\n+  padding: 14px 16px;\r\n+  background: linear-gradient(135deg, #141414, #111);\r\n+  border: 1px solid #1e1e1e;\r\n+  border-radius: 12px;\r\n+  transition: border-color .2s;\r\n+  justify-content: flex-start; /* เพิ่มบรรทัดนี้ */\r\n }\r\n+.ucr-row:hover { border-color: #2a2a2a; }\r\n+.ucr-row.ucr-banned {\r\n+  border-color: rgba(229,9,20,.2);\r\n+  background: linear-gradient(135deg, #1a0808, #110505);\r\n+  opacity: .75;\r\n+}\r\n \r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n+.ucr-avatar {\r\n+  width: 38px; height: 38px;\r\n+  border-radius: 50%;\r\n+  background: rgba(255,255,255,.06);\r\n+  border: 1px solid #2a2a2a;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  color: var(--text3);\r\n+  font-size: .9rem;\r\n+  flex-shrink: 0;\r\n }\r\n+.ucr-av-admin {\r\n+  background: rgba(229,9,20,.12);\r\n+  border-color: rgba(229,9,20,.3);\r\n+  color: var(--red);\r\n+}\r\n \r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value     = d.name;\r\n-    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value    = d.theme || 'default';\r\n-    document.getElementById('df-desc').value     = d.desc  || '';\r\n-    document.getElementById('df-cost').value     = d.cost  || 1;\r\n-    document.getElementById('df-category').value = d.category || '';\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n+.ucr-info {\r\n+  flex: 1;\r\n+  min-width: 0;\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 4px;\r\n+  align-items: flex-start; /* เปลี่ยนจาก center เป็น flex-start */\r\n+  text-align: left;        /* เพิ่มบรรทัดนี้ */\r\n }\r\n \r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n+.ucr-top {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  flex-wrap: wrap;\r\n+  justify-content: flex-start; /* เพิ่มบรรทัดนี้ */\r\n+}\r\n+.ucr-name {\r\n+  font-weight: 700;\r\n+  font-size: .92rem;\r\n+  color: var(--text);\r\n+}\r\n+.ucr-sub {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 5px;\r\n+  color: var(--text3);\r\n+  font-size: .72rem;\r\n+}\r\n \r\n-function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n-  const theme    = document.getElementById('df-theme').value            || 'default';\r\n-  const desc     = document.getElementById('df-desc').value.trim();\r\n-  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n-  const category = document.getElementById('df-category').value.trim() || '';\r\n+.ucr-badge {\r\n+  display: inline-flex;\r\n+  align-items: center;\r\n+  gap: 4px;\r\n+  padding: 2px 8px;\r\n+  border-radius: 20px;\r\n+  font-size: .68rem;\r\n+  font-weight: 700;\r\n+}\r\n+.ucr-badge-admin {\r\n+  background: rgba(229,9,20,.12);\r\n+  border: 1px solid rgba(229,9,20,.3);\r\n+  color: var(--red);\r\n+}\r\n+.ucr-badge-ban {\r\n+  background: rgba(255,152,0,.1);\r\n+  border: 1px solid rgba(255,152,0,.3);\r\n+  color: #ffa726;\r\n+}\r\n \r\n-  if (editDeckId) {\r\n-    const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n-    DB.cards[id] = [];\r\n-  }\r\n-  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+.ucr-ctrl {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  flex-shrink: 0;\r\n }\r\n+.ucr-credit-wrap {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 6px;\r\n+  background: #0d0d0d;\r\n+  border: 1px solid #222;\r\n+  border-radius: 8px;\r\n+  padding: 0 10px;\r\n+}\r\n+.ucr-input {\r\n+  width: 80px !important;\r\n+  background: transparent !important;\r\n+  border: none !important;\r\n+  text-align: right;\r\n+  color: var(--gold) !important;\r\n+  font-weight: 700;\r\n+  padding: 8px 0 !important;\r\n+}\r\n+.ucr-input:focus { outline: none; }\r\n \r\n-function delDeck(id) {\r\n-  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n-  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-  saveDB(); renderAdmDecks(); renderAdmSel();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n+.btn-ban {\r\n+  background: rgba(229,9,20,.1);\r\n+  border: 1px solid rgba(229,9,20,.3);\r\n+  color: var(--red);\r\n+  padding: 6px 10px;\r\n+  border-radius: 6px;\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-size: .78rem;\r\n+  cursor: pointer;\r\n+  display: flex; align-items: center; gap: 5px;\r\n+  transition: .2s;\r\n+  white-space: nowrap;\r\n }\r\n+.btn-ban:hover { background: rgba(229,9,20,.2); border-color: var(--red); }\r\n \r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+.btn-unban {\r\n+  background: rgba(76,175,80,.1);\r\n+  border: 1px solid rgba(76,175,80,.3);\r\n+  color: #4caf50;\r\n+  padding: 6px 10px;\r\n+  border-radius: 6px;\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-size: .78rem;\r\n+  cursor: pointer;\r\n+  display: flex; align-items: center; gap: 5px;\r\n+  transition: .2s;\r\n+  white-space: nowrap;\r\n }\r\n+.btn-unban:hover { background: rgba(76,175,80,.2); border-color: #4caf50; }\r\n+/* ---- HISTORY TAB ---- */\r\n+#tc-history.act {\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 6px;\r\n+}\r\n \r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n+.hist-row {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 14px;\r\n+  padding: 14px 16px;\r\n+  background: #111;\r\n+  border: 1px solid #1e1e1e;\r\n+  border-radius: 12px;\r\n+  transition: border-color .2s;\r\n }\r\n+.hist-row:hover { border-color: #2a2a2a; }\r\n \r\n-function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  DB.cards[id] = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) DB.cards[id].push({ cat, text });\r\n-  });\r\n-  saveDB();\r\n-  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n-  renderAdmDecks();\r\n+.hist-ico {\r\n+  width: 38px; height: 38px;\r\n+  border-radius: 10px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  font-size: .9rem;\r\n+  flex-shrink: 0;\r\n }\r\n+.hist-ico-plus  { background: rgba(245,197,24,.1); border: 1px solid rgba(245,197,24,.25); color: var(--gold); }\r\n+.hist-ico-minus { background: rgba(229,9,20,.1);   border: 1px solid rgba(229,9,20,.25);   color: var(--red); }\r\n \r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+.hist-info {\r\n+  flex: 1;\r\n+  min-width: 0;\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 5px;\r\n }\r\n \r\n-// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url')?.value?.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+.hist-top {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  flex-wrap: wrap;\r\n+}\r\n \r\n-  API.setSheetUrl(url);\r\n-  toast('กำลังดึงข้อมูล...', 'info');\r\n+.hist-note {\r\n+  font-weight: 700;\r\n+  font-size: .88rem;\r\n+  color: var(--text);\r\n+  white-space: nowrap;\r\n+  overflow: hidden;\r\n+  text-overflow: ellipsis;\r\n+  max-width: 280px;\r\n+}\r\n \r\n-  API.syncFromSheets(url)\r\n-    .then(({ decks, cards }) => {\r\n-      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      renderDecks();\r\n-    })\r\n-    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n+.hist-type-badge {\r\n+  font-size: .65rem;\r\n+  font-weight: 700;\r\n+  padding: 2px 8px;\r\n+  border-radius: 20px;\r\n+  white-space: nowrap;\r\n }\r\n+.hist-type-free     { background: rgba(76,175,80,.12);  border: 1px solid rgba(76,175,80,.3);  color: #4caf50; }\r\n+.hist-type-purchase { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.3); color: var(--gold); }\r\n+.hist-type-deduct   { background: rgba(229,9,20,.1);    border: 1px solid rgba(229,9,20,.25);  color: var(--red); }\r\n+.hist-type-admin    { background: rgba(156,39,176,.12); border: 1px solid rgba(156,39,176,.3); color: #ce93d8; }\r\n \r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+.hist-sub {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 6px;\r\n+  color: var(--text3);\r\n+  font-size: .72rem;\r\n+  flex-wrap: wrap;\r\n }\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n+.hist-user { color: var(--text2); font-weight: 600; display: flex; align-items: center; gap: 4px; }\r\n+.hist-date, .hist-time { display: flex; align-items: center; gap: 4px; }\r\n+.hist-sep  { color: #333; }\r\n \r\n-/* [FIX #6] validate JSON structure ก่อน import */\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      const parsed = JSON.parse(ev.target.result);\r\n+.hist-amount {\r\n+  font-weight: 900;\r\n+  font-size: 1.05rem;\r\n+  text-align: right;\r\n+  flex-shrink: 0;\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  align-items: flex-end;\r\n+  gap: 1px;\r\n+}\r\n+.hist-plus  { color: var(--gold); }\r\n+.hist-minus { color: var(--red); }\r\n+.hist-unit  { font-size: .65rem; font-weight: 400; color: var(--text3); }\r\n \r\n-      // validate required fields\r\n-      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n-      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n-      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n+.ann-form-box {\r\n+  background: #111;\r\n+  border: 1px solid #1e1e1e;\r\n+  border-radius: 12px;\r\n+  padding: 20px;\r\n+  margin-bottom: 20px;\r\n+}\r\n \r\n-      // validate deck structure\r\n-      for (const d of parsed.decks) {\r\n-        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n-        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n-      }\r\n+#announce-list {\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 8px;\r\n+}\r\n \r\n-      // validate cards structure\r\n-      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n-        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n-        for (const c of cards) {\r\n-          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n-        }\r\n-      }\r\n+/* ---- ANNOUNCE FORM ---- */\r\n+.ann-form-box {\r\n+  background: linear-gradient(135deg, #141414, #111);\r\n+  border: 1px solid #1e1e1e;\r\n+  border-radius: 16px;\r\n+  padding: 24px;\r\n+  margin-bottom: 20px;\r\n+  position: relative;\r\n+  overflow: hidden;\r\n+}\r\n+.ann-form-box::before {\r\n+  content: \"\";\r\n+  position: absolute;\r\n+  top: 0; left: 0; right: 0;\r\n+  height: 2px;\r\n+  background: linear-gradient(90deg, transparent, var(--red), transparent);\r\n+}\r\n \r\n-      DB = parsed;\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch (err) {\r\n-      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n-    }\r\n-  };\r\n-  r.readAsText(f);\r\n-  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n-  e.target.value = '';\r\n+.ann-form-header {\r\n+  margin-bottom: 20px;\r\n }\r\n-/* ---- SETTINGS TAB ---- */\r\n-async function loadSettingsTab() {\r\n-  // โหลด packages list ในแท็บตั้งค่า\r\n-  if (typeof loadPkgList === 'function') await loadPkgList();\r\n+.ann-form-title {\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-weight: 800;\r\n+  font-size: 1rem;\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  color: var(--text);\r\n+}\r\n \r\n-  // sync ค่า settings fields\r\n-  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n-  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n-  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n-  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n-  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n-  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n-  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n+.ann-form-grid {\r\n+  display: grid;\r\n+  grid-template-columns: 1fr 1fr;\r\n+  gap: 14px;\r\n }\r\n+.ann-fg-full {\r\n+  grid-column: 1 / -1;\r\n+}\r\n \r\n-/* ---- PACKAGE MANAGER ---- */\r\n-let editPkgId = null;\r\n+.ann-form-actions {\r\n+  display: flex;\r\n+  gap: 10px;\r\n+  margin-top: 18px;\r\n+  align-items: center;\r\n+}\r\n \r\n-async function loadPkgList() {\r\n-  const el = document.getElementById('pkg-list');\r\n-  if (!el) return;\r\n-  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+/* ---- ANNOUNCE LIST ---- */\r\n+.ann-list-header {\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-weight: 800;\r\n+  font-size: .85rem;\r\n+  letter-spacing: 2px;\r\n+  color: var(--text3);\r\n+  text-transform: uppercase;\r\n+  margin-bottom: 10px;\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+}\r\n \r\n-  const { data, error } = await _sb\r\n-    .from('packages')\r\n-    .select('*')\r\n-    .order('sort_order', { ascending: true });\r\n+#announce-list {\r\n+  display: flex;\r\n+  flex-direction: column;\r\n+  gap: 8px;\r\n+}\r\n \r\n-  if (error || !data?.length) {\r\n-    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n-    return;\r\n-  }\r\n+.ann-row {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 14px;\r\n+  padding: 16px;\r\n+  background: #111;\r\n+  border: 1px solid #1e1e1e;\r\n+  border-radius: 12px;\r\n+  transition: border-color .2s;\r\n+}\r\n+.ann-row:hover { border-color: #2a2a2a; }\r\n \r\n-  el.innerHTML = data.map(p => `\r\n-    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n-      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n-        <i class=\"fi fi-sr-coins\"></i>\r\n-      </div>\r\n-      <div class=\"pkg-row-info\">\r\n-        <div class=\"pkg-row-top\">\r\n-          <span class=\"pkg-row-name\">${p.name}</span>\r\n-          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n-          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n-        </div>\r\n-        <div class=\"pkg-row-detail\">\r\n-          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span>ลำดับ ${p.sort_order}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"pkg-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n-          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n-        </button>\r\n-        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n-          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('');\r\n+.ann-row-ico {\r\n+  width: 42px; height: 42px;\r\n+  border-radius: 10px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  font-size: 1rem;\r\n+  flex-shrink: 0;\r\n }\r\n+.ann-ico-info    { background: rgba(33,150,243,.1);  border: 1px solid rgba(33,150,243,.25);  color: #42a5f5; }\r\n+.ann-ico-promo   { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.25);  color: var(--gold); }\r\n+.ann-ico-warning { background: rgba(255,152,0,.1);   border: 1px solid rgba(255,152,0,.25);   color: #ffa726; }\r\n+.ann-ico-success { background: rgba(76,175,80,.1);   border: 1px solid rgba(76,175,80,.25);   color: #4caf50; }\r\n \r\n-function openPkgForm(id = null) {\r\n-  editPkgId = id;\r\n-  if (id) {\r\n-    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n-      if (!data) return;\r\n-      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n-      document.getElementById('pf-name').value     = data.name;\r\n-      document.getElementById('pf-coins').value    = data.coins;\r\n-      document.getElementById('pf-bonus').value    = data.bonus;\r\n-      document.getElementById('pf-price').value    = data.price;\r\n-      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n-      document.getElementById('pf-order').value    = data.sort_order;\r\n-      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n-    });\r\n-  } else {\r\n-    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n-    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n-    document.getElementById('pf-bonus').value    = '0';\r\n-    document.getElementById('pf-featured').value = '0';\r\n-    document.getElementById('pf-order').value    = '0';\r\n-    document.getElementById('pf-active').value   = '1';\r\n-  }\r\n-  document.getElementById('pkg-form-ov').classList.add('show');\r\n+.ann-row-info { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 5px; }\r\n+\r\n+.ann-row-top {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  flex-wrap: wrap;\r\n }\r\n+.ann-row-title {\r\n+  font-weight: 700;\r\n+  font-size: .92rem;\r\n+  color: var(--text);\r\n+}\r\n \r\n-function closePkgForm() {\r\n-  document.getElementById('pkg-form-ov').classList.remove('show');\r\n-  editPkgId = null;\r\n+.ann-badge {\r\n+  font-size: .65rem;\r\n+  font-weight: 700;\r\n+  padding: 2px 8px;\r\n+  border-radius: 20px;\r\n+  white-space: nowrap;\r\n }\r\n+.ann-badge-active  { background: rgba(76,175,80,.12);  border: 1px solid rgba(76,175,80,.3);  color: #4caf50; }\r\n+.ann-badge-draft   { background: rgba(255,255,255,.05); border: 1px solid #2a2a2a; color: var(--text3); }\r\n+.ann-badge-info    { background: rgba(33,150,243,.1);  border: 1px solid rgba(33,150,243,.25); color: #42a5f5; }\r\n+.ann-badge-promo   { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.25); color: var(--gold); }\r\n+.ann-badge-warning { background: rgba(255,152,0,.1);   border: 1px solid rgba(255,152,0,.25);  color: #ffa726; }\r\n+.ann-badge-success { background: rgba(76,175,80,.1);   border: 1px solid rgba(76,175,80,.25);  color: #4caf50; }\r\n \r\n-async function savePkg() {\r\n-  const name     = document.getElementById('pf-name').value.trim();\r\n-  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n-  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n-  const price    = parseInt(document.getElementById('pf-price').value);\r\n-  const featured = document.getElementById('pf-featured').value === '1';\r\n-  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n-  const active   = document.getElementById('pf-active').value === '1';\r\n+.ann-row-msg {\r\n+  font-size: .8rem;\r\n+  color: var(--text2);\r\n+  white-space: nowrap;\r\n+  overflow: hidden;\r\n+  text-overflow: ellipsis;\r\n+  max-width: 500px;\r\n+}\r\n \r\n-  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n+.ann-row-sub {\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 8px;\r\n+  color: var(--text3);\r\n+  font-size: .72rem;\r\n+  flex-wrap: wrap;\r\n+}\r\n+.ann-row-sub span { display: flex; align-items: center; gap: 4px; }\r\n+.ann-row-sub .sep { color: #2a2a2a; }\r\n \r\n-  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n+.ann-row-acts {\r\n+  display: flex;\r\n+  gap: 6px;\r\n+  flex-shrink: 0;\r\n+}\r\n+/* ---- PACKAGE LIST ---- */\r\n+#pkg-list { display: flex; flex-direction: column; gap: 8px; }\r\n \r\n-  const { error } = editPkgId\r\n-    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n-    : await _sb.from('packages').insert(payload);\r\n+.pkg-row {\r\n+  display: flex; align-items: center; gap: 14px;\r\n+  padding: 14px 16px;\r\n+  background: #111; border: 1px solid #1e1e1e; border-radius: 12px;\r\n+  transition: border-color .2s;\r\n+}\r\n+.pkg-row:hover { border-color: #2a2a2a; }\r\n+.pkg-row.pkg-inactive { opacity: .55; }\r\n \r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+.pkg-row-ico {\r\n+  width: 38px; height: 38px; border-radius: 10px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  background: rgba(245,197,24,.1); border: 1px solid rgba(245,197,24,.25);\r\n+  color: var(--gold); font-size: .9rem; flex-shrink: 0;\r\n+}\r\n+.pkg-ico-featured {\r\n+  background: rgba(229,9,20,.12); border-color: rgba(229,9,20,.3); color: var(--red);\r\n+}\r\n \r\n-  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n-  closePkgForm();\r\n-  loadPkgList();\r\n-  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n+.pkg-row-info { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4px; }\r\n+.pkg-row-top  { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }\r\n+.pkg-row-name { font-weight: 700; font-size: .92rem; color: var(--text); }\r\n+\r\n+.pkg-row-detail {\r\n+  display: flex; align-items: center; gap: 6px;\r\n+  color: var(--text3); font-size: .75rem; flex-wrap: wrap;\r\n }\r\n+.pkg-sep { color: #2a2a2a; }\r\n \r\n-async function togglePkg(id, active) {\r\n-  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n-  loadPkgList();\r\n+.pkg-badge {\r\n+  font-size: .65rem; font-weight: 700; padding: 2px 8px;\r\n+  border-radius: 20px; white-space: nowrap;\r\n }\r\n+.pkg-badge-featured { background: rgba(229,9,20,.12); border: 1px solid rgba(229,9,20,.3); color: var(--red); }\r\n+.pkg-badge-off      { background: rgba(255,255,255,.05); border: 1px solid #2a2a2a; color: var(--text3); }\r\n \r\n-async function deletePkg(id) {\r\n-  if (!confirm('ลบแพ็กเกจนี้?')) return;\r\n-  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบแพ็กเกจแล้ว', 'info');\r\n-  loadPkgList();\r\n+.pkg-row-acts { display: flex; gap: 6px; flex-shrink: 0; }\r\n+/* =========================================\r\n+   Stats Tab — stat-card2 & stats-grid-4\r\n+   ========================================= */\r\n+.stats-grid-4 {\r\n+  display: grid;\r\n+  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));\r\n+  gap: 12px;\r\n+  margin-top: 12px;\r\n+}\r\n+\r\n+.stat-card2 {\r\n+  background: #141414;\r\n+  border: 1px solid #222;\r\n+  border-radius: 12px;\r\n+  padding: 14px 16px;\r\n+  display: flex;\r\n+  align-items: center;\r\n+  gap: 12px;\r\n+  transition: border-color .2s;\r\n+}\r\n+.stat-card2:hover { border-color: #333; }\r\n+\r\n+.sc2-ico {\r\n+  width: 40px; height: 40px;\r\n+  border-radius: 10px;\r\n+  display: flex; align-items: center; justify-content: center;\r\n+  font-size: 1rem;\r\n+  flex-shrink: 0;\r\n+}\r\n+\r\n+.sc2-body { display: flex; flex-direction: column; gap: 2px; min-width: 0; }\r\n+\r\n+.sc2-val {\r\n+  font-family: \"Kanit\", sans-serif;\r\n+  font-weight: 900;\r\n+  font-size: 1.5rem;\r\n+  line-height: 1;\r\n+  color: #fff;\r\n+  white-space: nowrap;\r\n+  overflow: hidden;\r\n+  text-overflow: ellipsis;\r\n+}\r\n+\r\n+.sc2-lbl {\r\n+  font-size: .72rem;\r\n+  color: var(--text3);\r\n+  white-space: nowrap;\r\n+  overflow: hidden;\r\n+  text-overflow: ellipsis;\r\n+}\r\n+\r\n+@media (max-width: 580px) {\r\n+  .stats-grid-4 { grid-template-columns: repeat(2, 1fr); }\r\n+  .sc2-val { font-size: 1.2rem; }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775335668247,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,866 +1,822 @@\n-/* ========================================\r\n-   admin.css — Admin Panel Styles\r\n-   ======================================== */\r\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n \r\n-#admin-screen { display: none; min-height: 100vh; padding: 90px 36px 40px; }\r\n+let editDeckId = null;\r\n \r\n-.adm-hdr { display: flex; align-items: center; justify-content: space-between; margin-bottom: 36px; }\r\n-.adm-ttl { font-family: \"Kanit\", sans-serif; font-weight: 900; font-size: 1.85rem; letter-spacing: 4px; display: flex; align-items: center; gap: 10px; }\r\n-.adm-ttl .r { color: var(--red); }\r\n-.adm-sub { color: var(--text2); font-size: .82rem; margin-top: 3px; }\r\n-\r\n /* ---- TABS ---- */\r\n-.tabs {\r\n-  display: flex; gap: 3px;\r\n-  background: #111; padding: 4px; border-radius: 10px;\r\n-  border: 1px solid #1e1e1e; margin-bottom: 28px;\r\n-}\r\n-.tab {\r\n-  padding: 9px 20px; border-radius: 7px; border: none;\r\n-  background: transparent; color: var(--text2);\r\n-  font-family: \"Kanit\", sans-serif; font-weight: 600; font-size: .84rem;\r\n-  cursor: pointer; transition: .2s;\r\n-  display: flex; align-items: center; gap: 7px;\r\n-}\r\n-.tab.act { background: var(--red); color: #fff; }\r\n-.tab-content     { display: none; }\r\n-.tab-content.act { display: block; }\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');\r\n \r\n-/* ---- DECK LIST ---- */\r\n-.deck-list-adm { display: flex; flex-direction: column; gap: 13px; margin-bottom: 22px; }\r\n-\r\n-.drow {\r\n-  background: #111; border: 1px solid #1e1e1e; border-radius: 11px;\r\n-  padding: 18px 22px;\r\n-  display: flex; align-items: center; justify-content: space-between; gap: 14px;\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+  if (name === 'settings') { loadSettingsTab(); }\r\n+  if (name === 'cards')    { renderAdmSel(); }\r\n+  if (name === 'sheet')    {} // static content\r\n }\r\n-.drow-info  { display: flex; align-items: center; gap: 12px; flex: 1; }\r\n-.drow-ico   { width: 44px; height: 44px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 1.25rem; flex-shrink: 0; }\r\n-.drow-nm    { font-weight: 700; font-size: .95rem; }\r\n-.drow-meta  { color: var(--text2); font-size: .78rem; display: flex; align-items: center; gap: 10px; margin-top: 3px; flex-wrap: wrap; }\r\n-.drow-meta .tag { display: flex; align-items: center; gap: 4px; }\r\n-.drow-acts  { display: flex; gap: 7px; }\r\n \r\n-/* ---- SMALL BUTTONS ---- */\r\n-.btn-sm {\r\n-  padding: 7px 13px; border-radius: 6px; border: none;\r\n-  font-family: \"Kanit\", sans-serif; font-size: .78rem;\r\n-  cursor: pointer; font-weight: 600; transition: .2s;\r\n-  display: flex; align-items: center; gap: 5px;\r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n }\r\n-.btn-edit           { background: #1e1e1e; border: 1px solid #2e2e2e; color: var(--text2); }\r\n-.btn-edit:hover     { border-color: var(--text2); color: #fff; }\r\n-.btn-del            { background: rgba(229,9,20,.08); border: 1px solid rgba(229,9,20,.25); color: var(--red); }\r\n-.btn-del:hover      { background: var(--red); color: #fff; }\r\n-.btn-add            { background: var(--red); color: #fff; border: none; padding: 10px 22px; border-radius: 8px; font-family: \"Kanit\", sans-serif; font-weight: 700; font-size: .88rem; cursor: pointer; display: flex; align-items: center; gap: 7px; }\r\n-.btn-add:hover      { background: var(--red3); }\r\n \r\n-/* ---- SHEET SECTION ---- */\r\n-.sheet-sec { background: #111; border: 1px solid #1e1e1e; border-radius: 12px; padding: 24px; margin-bottom: 20px; }\r\n-.sheet-sec h3 { font-size: .95rem; font-weight: 700; margin-bottom: 6px; display: flex; align-items: center; gap: 8px; }\r\n-.sheet-sec p  { color: var(--text2); font-size: .82rem; margin-bottom: 14px; }\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n \r\n-.sheet-row { display: flex; gap: 9px; }\r\n-.sh-inp {\r\n-  flex: 1; background: #1a1a1a; border: 1px solid #2e2e2e;\r\n-  border-radius: 8px; padding: 9px 13px;\r\n-  color: #fff; font-family: \"Kanit\", sans-serif; font-size: .88rem; outline: none;\r\n-}\r\n-.sh-inp:focus { border-color: var(--red); }\r\n+  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n+  const [\r\n+    users,\r\n+    { data: history },\r\n+    { data: packages }\r\n+  ] = await Promise.all([\r\n+    API.getAllUsers(),\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n+    _sb.from('packages').select('*').eq('is_active', true)\r\n+  ]);\r\n \r\n-.btn-conn {\r\n-  background: var(--red); color: #fff; border: none;\r\n-  padding: 9px 18px; border-radius: 8px;\r\n-  font-family: \"Kanit\", sans-serif; font-weight: 700;\r\n-  cursor: pointer; white-space: nowrap;\r\n-  display: inline-flex; align-items: center; gap: 6px;\r\n-}\r\n-.btn-conn:hover       { background: var(--red3); }\r\n-.btn-conn.ghost       { background: #1a1a1a; border: 1px solid #2e2e2e; color: var(--text2); }\r\n-.btn-conn.ghost:hover { border-color: var(--text2); color: #fff; background: #222; }\r\n+  const now        = new Date();\r\n+  const weekAgo    = new Date(now - 7  * 86400000);\r\n+  const monthAgo   = new Date(now - 30 * 86400000);\r\n \r\n-/* ---- CARD EDITOR ---- */\r\n-.card-editor-box { background: #111; border: 1px solid #1e1e1e; border-radius: 12px; padding: 22px; }\r\n-.card-editor-box h3 { margin-bottom: 14px; font-size: .95rem; display: flex; align-items: center; gap: 8px; }\r\n+  // ---- ผู้ใช้ ----\r\n+  const totalUsers    = users.length;\r\n+  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n+  const adminUsers    = users.filter(u => u.is_admin).length;\r\n+  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n+  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n+  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n+  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n \r\n-.card-list-ed {\r\n-  display: flex; flex-direction: column; gap: 9px;\r\n-  max-height: 380px; overflow-y: auto; margin-bottom: 14px; padding-right: 4px;\r\n-}\r\n-.card-list-ed::-webkit-scrollbar       { width: 3px; }\r\n-.card-list-ed::-webkit-scrollbar-thumb { background: var(--red2); border-radius: 4px; }\r\n+  // ---- กองไพ่ & ไพ่ ----\r\n+  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n+  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n+  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n \r\n-.ced-row {\r\n-  display: flex; gap: 8px; align-items: center;\r\n-  background: #1a1a1a; border: 1px solid #252525; border-radius: 8px; padding: 9px 12px;\r\n-}\r\n-.ced-row input   { background: transparent; border: none; color: #fff; font-family: \"Kanit\", sans-serif; font-size: .88rem; flex: 1; outline: none; }\r\n-.ced-row .ci     { width: 110px; flex: none; color: var(--text2); }\r\n-.ced-row .del-btn {\r\n-  background: none; border: none; color: var(--text3); cursor: pointer;\r\n-  font-size: .82rem; width: 24px; height: 24px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  border-radius: 4px; transition: .2s;\r\n-}\r\n-.ced-row .del-btn:hover { color: var(--red); background: rgba(229,9,20,.1); }\r\n+  // top deck by card count\r\n+  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n \r\n-.btn-add-card {\r\n-  background: transparent; border: 1px dashed #2e2e2e; color: var(--text2);\r\n-  padding: 9px; border-radius: 8px; width: 100%;\r\n-  font-family: \"Kanit\", sans-serif; cursor: pointer; transition: .2s; font-size: .85rem;\r\n-  display: flex; align-items: center; justify-content: center; gap: 7px;\r\n-}\r\n-.btn-add-card:hover { border-color: #444; color: #fff; }\r\n+  // ---- เครดิต / ประวัติ ----\r\n+  const hist         = history || [];\r\n+  const purchases    = hist.filter(h => h.type === 'purchase');\r\n+  const frees        = hist.filter(h => h.type === 'free');\r\n+  const deducts      = hist.filter(h => h.type === 'deduct');\r\n+  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n+  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n \r\n-/* ---- FORM FIELDS ---- */\r\n-.fg { margin-bottom: 14px; }\r\n-.fg label { display: flex; align-items: center; gap: 6px; font-size: .78rem; color: var(--text2); margin-bottom: 5px; }\r\n-.fc {\r\n-  width: 100%; background: #1a1a1a; border: 1px solid #2e2e2e;\r\n-  border-radius: 8px; padding: 9px 12px;\r\n-  color: #fff; font-family: \"Kanit\", sans-serif; font-size: .9rem; outline: none;\r\n-  transition: border-color .2s;\r\n-}\r\n-.fc:focus { border-color: var(--red); }\r\n+  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n+  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n+    const pkg = (packages || []).find(p => {\r\n+      const total = p.coins + p.bonus;\r\n+      return total === h.amount;\r\n+    });\r\n+    return s + (pkg ? pkg.price : 0);\r\n+  }, 0);\r\n \r\n-/* ---- DECK FORM OVERLAY ---- */\r\n-.form-overlay { position: fixed; inset: 0; background: rgba(0,0,0,.88); z-index: 700; display: none; align-items: center; justify-content: center; backdrop-filter: blur(6px); }\r\n-.form-overlay.show { display: flex; }\r\n+  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n+  const newThisWeek  = users.filter(u => {\r\n+    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n+    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n+  }).length;\r\n \r\n-.form-box { background: #181818; border: 1px solid #272727; border-radius: 16px; padding: 34px; max-width: 400px; width: 90%; position: relative; }\r\n-.form-box h3 { font-size: 1.15rem; font-weight: 700; margin-bottom: 18px; display: flex; align-items: center; gap: 9px; }\r\n+  // ---- สร้าง HTML ----\r\n+  container.innerHTML = `\r\n+    <!-- ROW 1: ตัวเลขหลัก -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n+            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n+            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n+            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n \r\n-.form-x {\r\n-  position: absolute; top: 12px; right: 14px;\r\n-  background: none; border: none; color: var(--text2); cursor: pointer;\r\n-  width: 28px; height: 28px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  border-radius: 50%; transition: .2s; font-size: .9rem;\r\n-}\r\n-.form-x:hover { background: #222; color: #fff; }\r\n+    <!-- ROW 2: เครดิต -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n+            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n \r\n-.form-acts  { display: flex; gap: 8px; justify-content: flex-end; margin-top: 18px; }\r\n-.btn-cancel { background: transparent; border: 1px solid #2e2e2e; color: var(--text2); padding: 9px 18px; border-radius: 8px; font-family: \"Kanit\", sans-serif; cursor: pointer; display: flex; align-items: center; gap: 6px; }\r\n-.btn-save   { background: var(--red); color: #fff; border: none; padding: 9px 22px; border-radius: 8px; font-family: \"Kanit\", sans-serif; font-weight: 700; cursor: pointer; display: flex; align-items: center; gap: 6px; }\r\n+    <!-- ROW 3: กองไพ่ -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n \r\n-/* ---- RESPONSIVE ---- */\r\n-@media (max-width: 580px) {\r\n-  #admin-screen { padding: 88px 14px 30px; }\r\n-  .tabs { flex-wrap: wrap; }\r\n-}\r\n+    <!-- ROW 4: Top Decks -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${decksSorted.slice(0, 6).map((d, i) => {\r\n+          const cnt    = (DB.cards[d.id] || []).length;\r\n+          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n+          const pct    = Math.round(cnt / maxCnt * 100);\r\n+          const th     = getTh(d);\r\n+          return `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;min-width:0;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n+                ${d.name}\r\n+                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n+              </div>\r\n+              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n+                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n+            </div>\r\n+          </div>`;\r\n+        }).join('')}\r\n+      </div>\r\n+    </div>\r\n \r\n-.btn-visible { background: rgba(76,175,80,.12); color: #4caf50; border-color: rgba(76,175,80,.3); }\r\n-.btn-hidden  { background: rgba(255,255,255,.05); color: var(--text3); border-color: #333; }\r\n+    <!-- ROW 5: Top Users by Credits -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n+              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;\">\r\n+                ${u.username}\r\n+                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n+                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n+              </div>\r\n+              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n+                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n+              ${(u.credits||0).toLocaleString()}\r\n+              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n+            </div>\r\n+          </div>`).join('')}\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n \r\n-.stats-bar {\r\n-  display: flex; gap: 14px; flex-wrap: wrap; margin-bottom: 24px;\r\n+  // update legacy stat elements ด้านบน (stat bar)\r\n+  const elUsers = document.getElementById('stat-users');\r\n+  if (elUsers) elUsers.textContent = totalUsers;\r\n }\r\n-.stat-card {\r\n-  flex: 1; min-width: 140px;\r\n-  background: #161616; border: 1px solid #252525; border-radius: 12px;\r\n-  padding: 18px 20px;\r\n-}\r\n-.stat-label { color: var(--text2); font-size: .78rem; margin-bottom: 8px; }\r\n-.stat-val { font-size: 1.8rem; font-weight: 800; line-height: 1; }\r\n-.stat-val.gold  { color: var(--gold); }\r\n-.stat-val.green { color: #4caf50; }\r\n-.stat-val.red   { color: var(--red); }\r\n-.stat-val.white { color: #fff; }\r\n-.stat-sub { color: var(--text3); font-size: .72rem; margin-top: 6px; }\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n \r\n-.user-cr-row { display:flex; justify-content:space-between; align-items:center; padding:10px 0; border-bottom:1px solid var(--border); }\r\n-.ucr-info { display:flex; align-items:center; gap:8px; }\r\n-.ucr-name { font-weight:600; }\r\n-.ucr-ctrl { display:flex; align-items:center; gap:8px; }\r\n+  // ดึง history และ users พร้อมกัน\r\n+  const [{ data, error }, { data: users }] = await Promise.all([\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n+    _sb.from('profiles').select('id, username')\r\n+  ]);\r\n \r\n-.adm-sec-hdr { display:flex; justify-content:space-between; align-items:center; margin-bottom:16px; flex-wrap:wrap; gap:10px; }\r\n-.adm-loading { color:var(--text3); font-size:.85rem; padding:20px 0; }\r\n-.adm-empty   { color:var(--text3); font-size:.85rem; padding:20px 0; text-align:center; }\r\n- \r\n-/* Table */\r\n-.adm-table { width:100%; border-collapse:collapse; font-size:.88rem; }\r\n-.adm-table th { text-align:left; color:var(--text3); font-size:.75rem; text-transform:uppercase; letter-spacing:.05em; padding:8px 12px; border-bottom:1px solid var(--border); }\r\n-.adm-table td { padding:10px 12px; border-bottom:1px solid #1e1e1e; vertical-align:middle; }\r\n-.adm-table tr:hover td { background:#1a1a1a; }\r\n- \r\n-/* Stat bar */\r\n-.stat-bar-wrap { background:#1a1a1a; border-radius:4px; height:6px; width:120px; }\r\n-.stat-bar-fill { background:var(--red); border-radius:4px; height:6px; transition:width .4s; }\r\n- \r\n-/* Announce */\r\n-.announce-row { display:flex; justify-content:space-between; align-items:flex-start; gap:12px; padding:14px 0; border-bottom:1px solid var(--border); }\r\n-.announce-row.inactive { opacity:.45; }\r\n-.ann-info { display:flex; flex-direction:column; gap:4px; }\r\n-.ann-acts { display:flex; align-items:center; gap:6px; flex-shrink:0; }\r\n-.ann-type { font-size:.7rem; font-weight:700; letter-spacing:.08em; padding:2px 8px; border-radius:4px; }\r\n-.ann-info  { color:#60c0ff; background:rgba(96,192,255,.12); }\r\n-.ann-promo { color:var(--gold); background:rgba(255,200,0,.12); }\r\n-.ann-warning { color:orange; background:rgba(255,165,0,.12); }\r\n-.ann-success { color:#4caf50; background:rgba(76,175,80,.12); }\r\n-.tag-off { opacity:.4; }\r\n- \r\n-/* Toggle */\r\n-.toggle-wrap { display:flex; align-items:center; gap:6px; cursor:pointer; font-size:.85rem; }\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n \r\n+  // สร้าง map id → username\r\n+  const userMap = {};\r\n+  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n \r\n-.announce-row {\r\n-  display: flex;\r\n-  align-items: flex-start;\r\n-  gap: 14px;\r\n-  padding: 16px 0;\r\n-  border-bottom: 1px solid var(--border);\r\n-}\r\n-.announce-row.inactive { opacity: .4; }\r\n- \r\n-.ann-left { flex-shrink: 0; }\r\n-.ann-icon-wrap {\r\n-  width: 40px; height: 40px;\r\n-  border-radius: 10px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  font-size: 1.1rem;\r\n-}\r\n-.ann-icon-wrap.ann-info    { background: rgba(96,192,255,.15); color: #60c0ff; }\r\n-.ann-icon-wrap.ann-promo   { background: rgba(255,200,0,.15);  color: var(--gold); }\r\n-.ann-icon-wrap.ann-warning { background: rgba(255,165,0,.15);  color: orange; }\r\n-.ann-icon-wrap.ann-success { background: rgba(76,175,80,.15);  color: #4caf50; }\r\n- \r\n-.ann-body { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4px; }\r\n-.ann-meta { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }\r\n- \r\n-.ann-type-badge {\r\n-  display: inline-flex; align-items: center; gap: 4px;\r\n-  font-size: .72rem; font-weight: 700; letter-spacing: .06em;\r\n-  padding: 2px 10px; border-radius: 20px;\r\n-}\r\n-.ann-type-badge.ann-info    { background: rgba(96,192,255,.12); color: #60c0ff; }\r\n-.ann-type-badge.ann-promo   { background: rgba(255,200,0,.12);  color: var(--gold); }\r\n-.ann-type-badge.ann-warning { background: rgba(255,165,0,.12);  color: orange; }\r\n-.ann-type-badge.ann-success { background: rgba(76,175,80,.12);  color: #4caf50; }\r\n- \r\n-.ann-status { font-size: .72rem; display: inline-flex; align-items: center; gap: 4px; }\r\n-.ann-on  { color: #4caf50; }\r\n-.ann-off { color: var(--text3); }\r\n- \r\n- \r\n- \r\n-.ann-acts { display: flex; flex-direction: column; gap: 6px; flex-shrink: 0; }\r\n+  const TYPE_ICO = {\r\n+    free: 'fi-sr-gift', purchase: 'fi-sr-shopping-cart',\r\n+    deduct: 'fi-sr-layers', admin: 'fi-sr-shield-check',\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    free: 'รับฟรี', purchase: 'ซื้อเครดิต',\r\n+    deduct: 'ใช้ไพ่', admin: 'แอดมินเติม',\r\n+  };\r\n \r\n+  const rows = data.map(h => {\r\n+    const isPlus   = h.amount > 0;\r\n+    const typeKey  = h.type || 'deduct';\r\n+    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n+    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n+    const date     = new Date(h.created_at);\r\n+    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n \r\n+    return `\r\n+    <div class=\"hist-row\">\r\n+      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n+        <i class=\"fi ${ico}\"></i>\r\n+      </div>\r\n+      <div class=\"hist-info\">\r\n+        <div class=\"hist-top\">\r\n+          <span class=\"hist-note\">${h.note || label}</span>\r\n+          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n+        </div>\r\n+        <div class=\"hist-sub\">\r\n+          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n+        ${isPlus ? '+' : ''}${h.amount}\r\n+        <span class=\"hist-unit\">เครดิต</span>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n \r\n-\r\n-/* =========================================\r\n-   Announcement Popup v3 — CLEAN\r\n-   ========================================= */\r\n-\r\n-.ann-popup-box {\r\n-  width: 92%;\r\n-  max-width: 360px;\r\n-  background: linear-gradient(160deg, #1c1c1c 0%, #111 100%);\r\n-  border: 1px solid #2a2a2a;\r\n-  border-radius: 20px;\r\n-  overflow: hidden;\r\n-  box-shadow:\r\n-    0 0 0 1px rgba(255,255,255,.03),\r\n-    0 32px 80px rgba(0,0,0,.9),\r\n-    0 0 60px rgba(229,9,20,.08);\r\n-  animation: annPop .4s cubic-bezier(.34,1.4,.64,1);\r\n+  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n }\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n \r\n-@keyframes annPop {\r\n-  from { transform: scale(.88) translateY(20px); opacity: 0; }\r\n-  to   { transform: scale(1)   translateY(0);    opacity: 1; }\r\n-}\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n \r\n-/* top accent */\r\n-.ann-top-bar { height: 3px; }\r\n-.ann-top-bar.ann-bar-info    { background: linear-gradient(90deg,transparent,#60c0ff,transparent); }\r\n-.ann-top-bar.ann-bar-promo   { background: linear-gradient(90deg,transparent,#f5c518,transparent); }\r\n-.ann-top-bar.ann-bar-warning { background: linear-gradient(90deg,transparent,#ffa726,transparent); }\r\n-.ann-top-bar.ann-bar-success { background: linear-gradient(90deg,transparent,#66bb6a,transparent); }\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n \r\n-/* hero icon */\r\n-.ann-hero {\r\n-  display: flex; justify-content: center;\r\n-  padding: 28px 0 4px;\r\n-}\r\n-.ann-hero-info    { color: #60c0ff; }\r\n-.ann-hero-promo   { color: #f5c518; }\r\n-.ann-hero-warning { color: #ffa726; }\r\n-.ann-hero-success { color: #66bb6a; }\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n \r\n-.ann-hero-ring {\r\n-  width: 70px; height: 70px; border-radius: 50%;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  font-size: 1.7rem; position: relative;\r\n-}\r\n-.ann-hero-info    .ann-hero-ring { background: rgba(96,192,255,.12); }\r\n-.ann-hero-promo   .ann-hero-ring { background: rgba(245,197,24,.12); }\r\n-.ann-hero-warning .ann-hero-ring { background: rgba(255,167,38,.12); }\r\n-.ann-hero-success .ann-hero-ring { background: rgba(102,187,106,.12); }\r\n+  const TYPE_ICO = {\r\n+    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n+    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n+  };\r\n \r\n-.ann-hero-ring::before {\r\n-  content:\"\"; position:absolute; inset:-5px;\r\n-  border-radius:50%; border:1.5px solid currentColor; opacity:.18;\r\n+  listEl.innerHTML = data.map(a => {\r\n+    const t       = a.type || 'info';\r\n+    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n+    const expires = a.expires_at\r\n+      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n+      : null;\r\n+    return `\r\n+    <div class=\"ann-row\">\r\n+      <div class=\"ann-row-ico ann-ico-${t}\">\r\n+        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n+      </div>\r\n+      <div class=\"ann-row-info\">\r\n+        <div class=\"ann-row-top\">\r\n+          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n+          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n+          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n+            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n+            ${a.active ? 'เผยแพร่' : 'draft'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n+        </div>\r\n+        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n+        <div class=\"ann-row-sub\">\r\n+          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n+          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"ann-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n }\r\n-.ann-hero-ring::after {\r\n-  content:\"\"; position:absolute; inset:-11px;\r\n-  border-radius:50%; border:1px solid currentColor; opacity:.07;\r\n-}\r\n \r\n-/* content */\r\n-.ann-content {\r\n-  padding: 14px 24px 18px;\r\n-  display: flex; flex-direction: column;\r\n-  align-items: center; text-align: center; gap: 8px;\r\n-}\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+  const active  = document.getElementById('ann-active')?.value === '1';\r\n+  const expires = document.getElementById('ann-expires')?.value || null;\r\n \r\n-/* popup badge — ใช้ class .ann-popup-badge เพื่อไม่ชนกับ .ann-badge ใน admin list */\r\n-.ann-popup-badge {\r\n-  display: inline-flex; align-items: center; gap: 5px;\r\n-  font-size: .7rem; font-weight: 700; letter-spacing: .08em;\r\n-  padding: 3px 12px; border-radius: 20px; border: 1px solid currentColor;\r\n-}\r\n-.ann-popup-badge.ann-info    { color: #60c0ff; background: rgba(96,192,255,.08); }\r\n-.ann-popup-badge.ann-promo   { color: #f5c518; background: rgba(245,197,24,.08); }\r\n-.ann-popup-badge.ann-warning { color: #ffa726; background: rgba(255,167,38,.08); }\r\n-.ann-popup-badge.ann-success { color: #66bb6a; background: rgba(102,187,106,.08); }\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n \r\n-.ann-title {\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-size: 1.3rem; font-weight: 800;\r\n-  color: #fff; line-height: 1.25; letter-spacing: .01em;\r\n-}\r\n-.ann-msg { font-size: .875rem; color: #888; line-height: 1.75; }\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type,\r\n+    show_popup: popup,\r\n+    active,\r\n+    expires_at: expires ? new Date(expires).toISOString() : null,\r\n+  });\r\n \r\n-.ann-expires {\r\n-  display: inline-flex; align-items: center; gap: 6px;\r\n-  font-size: .72rem; color: #555;\r\n-  background: #161616; border: 1px solid #222;\r\n-  border-radius: 6px; padding: 4px 10px; margin-top: 2px;\r\n-}\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n \r\n-/* footer */\r\n-.ann-footer {\r\n-  display: flex; align-items: center; justify-content: space-between;\r\n-  padding: 12px 16px 16px;\r\n-  border-top: 1px solid #1e1e1e; gap: 12px;\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  document.getElementById('ann-expires').value = '';\r\n+  loadAnnounceTab();\r\n }\r\n \r\n-.ann-brand {\r\n-  display: flex; flex-direction: column;\r\n-  border-left: 2.5px solid #e50914;\r\n-  padding-left: 10px; flex-shrink: 0; gap: 1px;\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n }\r\n-.ann-brand-name {\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-weight: 900; font-size: .88rem; line-height: 1;\r\n-}\r\n-.ann-brand-sub { font-size: .54rem; color: #555; letter-spacing: .18em; }\r\n \r\n-.ann-btn {\r\n-  flex: 1;\r\n-  display: flex; align-items: center; justify-content: center; gap: 7px;\r\n-  padding: 11px 16px; border: none; border-radius: 10px;\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-size: .9rem; font-weight: 700; color: #111;\r\n-  cursor: pointer; transition: filter .15s, transform .1s;\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n }\r\n-.ann-btn-info    { background: #60c0ff; }\r\n-.ann-btn-promo   { background: #f5c518; }\r\n-.ann-btn-warning { background: #ffa726; }\r\n-.ann-btn-success { background: #66bb6a; }\r\n-.ann-btn:hover  { filter: brightness(1.12); }\r\n-.ann-btn:active { transform: scale(.97); }\r\n \r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  // update stats-bar (ด้านบนสุดของ admin panel)\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n \r\n-/* ---- ADMIN USER LIST ---- */\r\n-#admin-user-list {\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 8px;\r\n+  // render deck list ใน tab กองไพ่\r\n+  const l = document.getElementById('adm-deck-list');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n }\r\n \r\n-.ucr-row {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 14px;\r\n-  padding: 14px 16px;\r\n-  background: linear-gradient(135deg, #141414, #111);\r\n-  border: 1px solid #1e1e1e;\r\n-  border-radius: 12px;\r\n-  transition: border-color .2s;\r\n-  justify-content: flex-start; /* เพิ่มบรรทัดนี้ */\r\n-}\r\n-.ucr-row:hover { border-color: #2a2a2a; }\r\n-.ucr-row.ucr-banned {\r\n-  border-color: rgba(229,9,20,.2);\r\n-  background: linear-gradient(135deg, #1a0808, #110505);\r\n-  opacity: .75;\r\n-}\r\n+async function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  if (!d) return;\r\n+  d.hidden = !d.hidden;\r\n \r\n-.ucr-avatar {\r\n-  width: 38px; height: 38px;\r\n-  border-radius: 50%;\r\n-  background: rgba(255,255,255,.06);\r\n-  border: 1px solid #2a2a2a;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  color: var(--text3);\r\n-  font-size: .9rem;\r\n-  flex-shrink: 0;\r\n-}\r\n-.ucr-av-admin {\r\n-  background: rgba(229,9,20,.12);\r\n-  border-color: rgba(229,9,20,.3);\r\n-  color: var(--red);\r\n-}\r\n+  // บันทึกไป Supabase ด้วย\r\n+  const ok = await saveDeckToDB(d);\r\n+  if (!ok) {\r\n+    // ถ้า error ให้คืนค่าเดิม\r\n+    d.hidden = !d.hidden;\r\n+    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n+    return;\r\n+  }\r\n \r\n-.ucr-info {\r\n-  flex: 1;\r\n-  min-width: 0;\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 4px;\r\n-  align-items: flex-start; /* เปลี่ยนจาก center เป็น flex-start */\r\n-  text-align: left;        /* เพิ่มบรรทัดนี้ */\r\n+  renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'success');\r\n }\r\n \r\n-.ucr-top {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  flex-wrap: wrap;\r\n-  justify-content: flex-start; /* เพิ่มบรรทัดนี้ */\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n }\r\n-.ucr-name {\r\n-  font-weight: 700;\r\n-  font-size: .92rem;\r\n-  color: var(--text);\r\n-}\r\n-.ucr-sub {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 5px;\r\n-  color: var(--text3);\r\n-  font-size: .72rem;\r\n-}\r\n \r\n-.ucr-badge {\r\n-  display: inline-flex;\r\n-  align-items: center;\r\n-  gap: 4px;\r\n-  padding: 2px 8px;\r\n-  border-radius: 20px;\r\n-  font-size: .68rem;\r\n-  font-weight: 700;\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n }\r\n-.ucr-badge-admin {\r\n-  background: rgba(229,9,20,.12);\r\n-  border: 1px solid rgba(229,9,20,.3);\r\n-  color: var(--red);\r\n-}\r\n-.ucr-badge-ban {\r\n-  background: rgba(255,152,0,.1);\r\n-  border: 1px solid rgba(255,152,0,.3);\r\n-  color: #ffa726;\r\n-}\r\n \r\n-.ucr-ctrl {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  flex-shrink: 0;\r\n-}\r\n-.ucr-credit-wrap {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 6px;\r\n-  background: #0d0d0d;\r\n-  border: 1px solid #222;\r\n-  border-radius: 8px;\r\n-  padding: 0 10px;\r\n-}\r\n-.ucr-input {\r\n-  width: 80px !important;\r\n-  background: transparent !important;\r\n-  border: none !important;\r\n-  text-align: right;\r\n-  color: var(--gold) !important;\r\n-  font-weight: 700;\r\n-  padding: 8px 0 !important;\r\n-}\r\n-.ucr-input:focus { outline: none; }\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n \r\n-.btn-ban {\r\n-  background: rgba(229,9,20,.1);\r\n-  border: 1px solid rgba(229,9,20,.3);\r\n-  color: var(--red);\r\n-  padding: 6px 10px;\r\n-  border-radius: 6px;\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-size: .78rem;\r\n-  cursor: pointer;\r\n-  display: flex; align-items: center; gap: 5px;\r\n-  transition: .2s;\r\n-  white-space: nowrap;\r\n-}\r\n-.btn-ban:hover { background: rgba(229,9,20,.2); border-color: var(--red); }\r\n+function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n \r\n-.btn-unban {\r\n-  background: rgba(76,175,80,.1);\r\n-  border: 1px solid rgba(76,175,80,.3);\r\n-  color: #4caf50;\r\n-  padding: 6px 10px;\r\n-  border-radius: 6px;\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-size: .78rem;\r\n-  cursor: pointer;\r\n-  display: flex; align-items: center; gap: 5px;\r\n-  transition: .2s;\r\n-  white-space: nowrap;\r\n+  if (editDeckId) {\r\n+    const d = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n+    DB.cards[id] = [];\r\n+  }\r\n+  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n-.btn-unban:hover { background: rgba(76,175,80,.2); border-color: #4caf50; }\r\n-/* ---- HISTORY TAB ---- */\r\n-#tc-history.act {\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 6px;\r\n-}\r\n \r\n-.hist-row {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 14px;\r\n-  padding: 14px 16px;\r\n-  background: #111;\r\n-  border: 1px solid #1e1e1e;\r\n-  border-radius: 12px;\r\n-  transition: border-color .2s;\r\n+function delDeck(id) {\r\n+  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+  saveDB(); renderAdmDecks(); renderAdmSel();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n }\r\n-.hist-row:hover { border-color: #2a2a2a; }\r\n \r\n-.hist-ico {\r\n-  width: 38px; height: 38px;\r\n-  border-radius: 10px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  font-size: .9rem;\r\n-  flex-shrink: 0;\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n }\r\n-.hist-ico-plus  { background: rgba(245,197,24,.1); border: 1px solid rgba(245,197,24,.25); color: var(--gold); }\r\n-.hist-ico-minus { background: rgba(229,9,20,.1);   border: 1px solid rgba(229,9,20,.25);   color: var(--red); }\r\n \r\n-.hist-info {\r\n-  flex: 1;\r\n-  min-width: 0;\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 5px;\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n }\r\n \r\n-.hist-top {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  flex-wrap: wrap;\r\n+function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  DB.cards[id] = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) DB.cards[id].push({ cat, text });\r\n+  });\r\n+  saveDB();\r\n+  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+  renderAdmDecks();\r\n }\r\n \r\n-.hist-note {\r\n-  font-weight: 700;\r\n-  font-size: .88rem;\r\n-  color: var(--text);\r\n-  white-space: nowrap;\r\n-  overflow: hidden;\r\n-  text-overflow: ellipsis;\r\n-  max-width: 280px;\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n }\r\n \r\n-.hist-type-badge {\r\n-  font-size: .65rem;\r\n-  font-weight: 700;\r\n-  padding: 2px 8px;\r\n-  border-radius: 20px;\r\n-  white-space: nowrap;\r\n-}\r\n-.hist-type-free     { background: rgba(76,175,80,.12);  border: 1px solid rgba(76,175,80,.3);  color: #4caf50; }\r\n-.hist-type-purchase { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.3); color: var(--gold); }\r\n-.hist-type-deduct   { background: rgba(229,9,20,.1);    border: 1px solid rgba(229,9,20,.25);  color: var(--red); }\r\n-.hist-type-admin    { background: rgba(156,39,176,.12); border: 1px solid rgba(156,39,176,.3); color: #ce93d8; }\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n \r\n-.hist-sub {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 6px;\r\n-  color: var(--text3);\r\n-  font-size: .72rem;\r\n-  flex-wrap: wrap;\r\n-}\r\n-.hist-user { color: var(--text2); font-weight: 600; display: flex; align-items: center; gap: 4px; }\r\n-.hist-date, .hist-time { display: flex; align-items: center; gap: 4px; }\r\n-.hist-sep  { color: #333; }\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n \r\n-.hist-amount {\r\n-  font-weight: 900;\r\n-  font-size: 1.05rem;\r\n-  text-align: right;\r\n-  flex-shrink: 0;\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  align-items: flex-end;\r\n-  gap: 1px;\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n }\r\n-.hist-plus  { color: var(--gold); }\r\n-.hist-minus { color: var(--red); }\r\n-.hist-unit  { font-size: .65rem; font-weight: 400; color: var(--text3); }\r\n \r\n-.ann-form-box {\r\n-  background: #111;\r\n-  border: 1px solid #1e1e1e;\r\n-  border-radius: 12px;\r\n-  padding: 20px;\r\n-  margin-bottom: 20px;\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n }\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n \r\n-#announce-list {\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 8px;\r\n-}\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      const parsed = JSON.parse(ev.target.result);\r\n \r\n-/* ---- ANNOUNCE FORM ---- */\r\n-.ann-form-box {\r\n-  background: linear-gradient(135deg, #141414, #111);\r\n-  border: 1px solid #1e1e1e;\r\n-  border-radius: 16px;\r\n-  padding: 24px;\r\n-  margin-bottom: 20px;\r\n-  position: relative;\r\n-  overflow: hidden;\r\n-}\r\n-.ann-form-box::before {\r\n-  content: \"\";\r\n-  position: absolute;\r\n-  top: 0; left: 0; right: 0;\r\n-  height: 2px;\r\n-  background: linear-gradient(90deg, transparent, var(--red), transparent);\r\n-}\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n \r\n-.ann-form-header {\r\n-  margin-bottom: 20px;\r\n-}\r\n-.ann-form-title {\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-weight: 800;\r\n-  font-size: 1rem;\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  color: var(--text);\r\n-}\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n \r\n-.ann-form-grid {\r\n-  display: grid;\r\n-  grid-template-columns: 1fr 1fr;\r\n-  gap: 14px;\r\n-}\r\n-.ann-fg-full {\r\n-  grid-column: 1 / -1;\r\n-}\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n \r\n-.ann-form-actions {\r\n-  display: flex;\r\n-  gap: 10px;\r\n-  margin-top: 18px;\r\n-  align-items: center;\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n }\r\n+/* ---- SETTINGS TAB ---- */\r\n+async function loadSettingsTab() {\r\n+  // โหลด packages list ในแท็บตั้งค่า\r\n+  if (typeof loadPkgList === 'function') await loadPkgList();\r\n \r\n-/* ---- ANNOUNCE LIST ---- */\r\n-.ann-list-header {\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-weight: 800;\r\n-  font-size: .85rem;\r\n-  letter-spacing: 2px;\r\n-  color: var(--text3);\r\n-  text-transform: uppercase;\r\n-  margin-bottom: 10px;\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n+  // sync ค่า settings fields\r\n+  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n+  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n+  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n+  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n+  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n+  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n }\r\n \r\n-#announce-list {\r\n-  display: flex;\r\n-  flex-direction: column;\r\n-  gap: 8px;\r\n-}\r\n+/* ---- PACKAGE MANAGER ---- */\r\n+let editPkgId = null;\r\n \r\n-.ann-row {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 14px;\r\n-  padding: 16px;\r\n-  background: #111;\r\n-  border: 1px solid #1e1e1e;\r\n-  border-radius: 12px;\r\n-  transition: border-color .2s;\r\n-}\r\n-.ann-row:hover { border-color: #2a2a2a; }\r\n+async function loadPkgList() {\r\n+  const el = document.getElementById('pkg-list');\r\n+  if (!el) return;\r\n+  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n \r\n-.ann-row-ico {\r\n-  width: 42px; height: 42px;\r\n-  border-radius: 10px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  font-size: 1rem;\r\n-  flex-shrink: 0;\r\n-}\r\n-.ann-ico-info    { background: rgba(33,150,243,.1);  border: 1px solid rgba(33,150,243,.25);  color: #42a5f5; }\r\n-.ann-ico-promo   { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.25);  color: var(--gold); }\r\n-.ann-ico-warning { background: rgba(255,152,0,.1);   border: 1px solid rgba(255,152,0,.25);   color: #ffa726; }\r\n-.ann-ico-success { background: rgba(76,175,80,.1);   border: 1px solid rgba(76,175,80,.25);   color: #4caf50; }\r\n+  const { data, error } = await _sb\r\n+    .from('packages')\r\n+    .select('*')\r\n+    .order('sort_order', { ascending: true });\r\n \r\n-.ann-row-info { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 5px; }\r\n+  if (error || !data?.length) {\r\n+    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n+    return;\r\n+  }\r\n \r\n-.ann-row-top {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  flex-wrap: wrap;\r\n+  el.innerHTML = data.map(p => `\r\n+    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n+      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n+        <i class=\"fi fi-sr-coins\"></i>\r\n+      </div>\r\n+      <div class=\"pkg-row-info\">\r\n+        <div class=\"pkg-row-top\">\r\n+          <span class=\"pkg-row-name\">${p.name}</span>\r\n+          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n+          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n+        </div>\r\n+        <div class=\"pkg-row-detail\">\r\n+          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span>ลำดับ ${p.sort_order}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"pkg-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n+          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n+        </button>\r\n+        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n+          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n }\r\n-.ann-row-title {\r\n-  font-weight: 700;\r\n-  font-size: .92rem;\r\n-  color: var(--text);\r\n-}\r\n \r\n-.ann-badge {\r\n-  font-size: .65rem;\r\n-  font-weight: 700;\r\n-  padding: 2px 8px;\r\n-  border-radius: 20px;\r\n-  white-space: nowrap;\r\n+function openPkgForm(id = null) {\r\n+  editPkgId = id;\r\n+  if (id) {\r\n+    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n+      if (!data) return;\r\n+      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n+      document.getElementById('pf-name').value     = data.name;\r\n+      document.getElementById('pf-coins').value    = data.coins;\r\n+      document.getElementById('pf-bonus').value    = data.bonus;\r\n+      document.getElementById('pf-price').value    = data.price;\r\n+      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n+      document.getElementById('pf-order').value    = data.sort_order;\r\n+      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n+    });\r\n+  } else {\r\n+    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n+    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n+    document.getElementById('pf-bonus').value    = '0';\r\n+    document.getElementById('pf-featured').value = '0';\r\n+    document.getElementById('pf-order').value    = '0';\r\n+    document.getElementById('pf-active').value   = '1';\r\n+  }\r\n+  document.getElementById('pkg-form-ov').classList.add('show');\r\n }\r\n-.ann-badge-active  { background: rgba(76,175,80,.12);  border: 1px solid rgba(76,175,80,.3);  color: #4caf50; }\r\n-.ann-badge-draft   { background: rgba(255,255,255,.05); border: 1px solid #2a2a2a; color: var(--text3); }\r\n-.ann-badge-info    { background: rgba(33,150,243,.1);  border: 1px solid rgba(33,150,243,.25); color: #42a5f5; }\r\n-.ann-badge-promo   { background: rgba(245,197,24,.1);  border: 1px solid rgba(245,197,24,.25); color: var(--gold); }\r\n-.ann-badge-warning { background: rgba(255,152,0,.1);   border: 1px solid rgba(255,152,0,.25);  color: #ffa726; }\r\n-.ann-badge-success { background: rgba(76,175,80,.1);   border: 1px solid rgba(76,175,80,.25);  color: #4caf50; }\r\n \r\n-.ann-row-msg {\r\n-  font-size: .8rem;\r\n-  color: var(--text2);\r\n-  white-space: nowrap;\r\n-  overflow: hidden;\r\n-  text-overflow: ellipsis;\r\n-  max-width: 500px;\r\n+function closePkgForm() {\r\n+  document.getElementById('pkg-form-ov').classList.remove('show');\r\n+  editPkgId = null;\r\n }\r\n \r\n-.ann-row-sub {\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 8px;\r\n-  color: var(--text3);\r\n-  font-size: .72rem;\r\n-  flex-wrap: wrap;\r\n-}\r\n-.ann-row-sub span { display: flex; align-items: center; gap: 4px; }\r\n-.ann-row-sub .sep { color: #2a2a2a; }\r\n+async function savePkg() {\r\n+  const name     = document.getElementById('pf-name').value.trim();\r\n+  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n+  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n+  const price    = parseInt(document.getElementById('pf-price').value);\r\n+  const featured = document.getElementById('pf-featured').value === '1';\r\n+  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n+  const active   = document.getElementById('pf-active').value === '1';\r\n \r\n-.ann-row-acts {\r\n-  display: flex;\r\n-  gap: 6px;\r\n-  flex-shrink: 0;\r\n-}\r\n-/* ---- PACKAGE LIST ---- */\r\n-#pkg-list { display: flex; flex-direction: column; gap: 8px; }\r\n+  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n \r\n-.pkg-row {\r\n-  display: flex; align-items: center; gap: 14px;\r\n-  padding: 14px 16px;\r\n-  background: #111; border: 1px solid #1e1e1e; border-radius: 12px;\r\n-  transition: border-color .2s;\r\n-}\r\n-.pkg-row:hover { border-color: #2a2a2a; }\r\n-.pkg-row.pkg-inactive { opacity: .55; }\r\n+  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n \r\n-.pkg-row-ico {\r\n-  width: 38px; height: 38px; border-radius: 10px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  background: rgba(245,197,24,.1); border: 1px solid rgba(245,197,24,.25);\r\n-  color: var(--gold); font-size: .9rem; flex-shrink: 0;\r\n-}\r\n-.pkg-ico-featured {\r\n-  background: rgba(229,9,20,.12); border-color: rgba(229,9,20,.3); color: var(--red);\r\n-}\r\n+  const { error } = editPkgId\r\n+    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n+    : await _sb.from('packages').insert(payload);\r\n \r\n-.pkg-row-info { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4px; }\r\n-.pkg-row-top  { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }\r\n-.pkg-row-name { font-weight: 700; font-size: .92rem; color: var(--text); }\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n \r\n-.pkg-row-detail {\r\n-  display: flex; align-items: center; gap: 6px;\r\n-  color: var(--text3); font-size: .75rem; flex-wrap: wrap;\r\n+  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n+  closePkgForm();\r\n+  loadPkgList();\r\n+  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n }\r\n-.pkg-sep { color: #2a2a2a; }\r\n \r\n-.pkg-badge {\r\n-  font-size: .65rem; font-weight: 700; padding: 2px 8px;\r\n-  border-radius: 20px; white-space: nowrap;\r\n+async function togglePkg(id, active) {\r\n+  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n+  loadPkgList();\r\n }\r\n-.pkg-badge-featured { background: rgba(229,9,20,.12); border: 1px solid rgba(229,9,20,.3); color: var(--red); }\r\n-.pkg-badge-off      { background: rgba(255,255,255,.05); border: 1px solid #2a2a2a; color: var(--text3); }\r\n \r\n-.pkg-row-acts { display: flex; gap: 6px; flex-shrink: 0; }\r\n-/* =========================================\r\n-   Stats Tab — stat-card2 & stats-grid-4\r\n-   ========================================= */\r\n-.stats-grid-4 {\r\n-  display: grid;\r\n-  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));\r\n-  gap: 12px;\r\n-  margin-top: 12px;\r\n-}\r\n-\r\n-.stat-card2 {\r\n-  background: #141414;\r\n-  border: 1px solid #222;\r\n-  border-radius: 12px;\r\n-  padding: 14px 16px;\r\n-  display: flex;\r\n-  align-items: center;\r\n-  gap: 12px;\r\n-  transition: border-color .2s;\r\n-}\r\n-.stat-card2:hover { border-color: #333; }\r\n-\r\n-.sc2-ico {\r\n-  width: 40px; height: 40px;\r\n-  border-radius: 10px;\r\n-  display: flex; align-items: center; justify-content: center;\r\n-  font-size: 1rem;\r\n-  flex-shrink: 0;\r\n-}\r\n-\r\n-.sc2-body { display: flex; flex-direction: column; gap: 2px; min-width: 0; }\r\n-\r\n-.sc2-val {\r\n-  font-family: \"Kanit\", sans-serif;\r\n-  font-weight: 900;\r\n-  font-size: 1.5rem;\r\n-  line-height: 1;\r\n-  color: #fff;\r\n-  white-space: nowrap;\r\n-  overflow: hidden;\r\n-  text-overflow: ellipsis;\r\n-}\r\n-\r\n-.sc2-lbl {\r\n-  font-size: .72rem;\r\n-  color: var(--text3);\r\n-  white-space: nowrap;\r\n-  overflow: hidden;\r\n-  text-overflow: ellipsis;\r\n-}\r\n-\r\n-@media (max-width: 580px) {\r\n-  .stats-grid-4 { grid-template-columns: repeat(2, 1fr); }\r\n-  .sc2-val { font-size: 1.2rem; }\r\n+async function deletePkg(id) {\r\n+  if (!confirm('ลบแพ็กเกจนี้?')) return;\r\n+  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบแพ็กเกจแล้ว', 'info');\r\n+  loadPkgList();\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775335871198,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -483,18 +483,18 @@\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n \r\n-  // บันทึกไป Supabase ด้วย\r\n   const ok = await saveDeckToDB(d);\r\n   if (!ok) {\r\n-    // ถ้า error ให้คืนค่าเดิม\r\n-    d.hidden = !d.hidden;\r\n+    d.hidden = !d.hidden; // คืนค่าเดิม\r\n     toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n     return;\r\n   }\r\n \r\n-  renderAdmDecks();\r\n+  await initDB();        // reload จาก Supabase ให้ข้อมูลตรง\r\n+  renderAdmDecks();      // admin เห็นทุก deck\r\n+  renderDecks();         // หน้าเล่นเห็นเฉพาะที่ไม่ซ่อน\r\n   toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'success');\r\n }\r\n \r\n function renderAdmSel() {\r\n"
                },
                {
                    "date": 1775336099115,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -478,24 +478,14 @@\n     l.appendChild(r);\r\n   });\r\n }\r\n \r\n-async function toggleDeck(id) {\r\n+function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n-\r\n-  const ok = await saveDeckToDB(d);\r\n-  if (!ok) {\r\n-    d.hidden = !d.hidden; // คืนค่าเดิม\r\n-    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n-    return;\r\n-  }\r\n-\r\n-  await initDB();        // reload จาก Supabase ให้ข้อมูลตรง\r\n-  renderAdmDecks();      // admin เห็นทุก deck\r\n-  renderDecks();         // หน้าเล่นเห็นเฉพาะที่ไม่ซ่อน\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'success');\r\n+  saveDB(); renderAdmDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n }\r\n \r\n function renderAdmSel() {\r\n   const s = document.getElementById('adm-deck-sel');\r\n@@ -532,26 +522,36 @@\n \r\n function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n function editDeck(id)    { openDeckForm(id); }\r\n \r\n-function saveDeck() {\r\n+async function saveDeck() {\r\n   const name = document.getElementById('df-name').value.trim();\r\n   if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n   const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n   const theme    = document.getElementById('df-theme').value            || 'default';\r\n   const desc     = document.getElementById('df-desc').value.trim();\r\n   const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n   const category = document.getElementById('df-category').value.trim() || '';\r\n \r\n+  let deck;\r\n   if (editDeckId) {\r\n-    const d = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(d, { name, icon, theme, desc, cost, category });\r\n+    deck = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(deck, { name, icon, theme, desc, cost, category });\r\n   } else {\r\n     const id = 'd' + Date.now();\r\n-    DB.decks.push({ id, name, icon, theme, desc, cost, category });\r\n-    DB.cards[id] = [];\r\n+    deck = { id, name, icon, theme, desc, cost, category, hidden: false };\r\n+    DB.decks.push(deck);\r\n+    DB.cards[deck.id] = [];\r\n   }\r\n-  saveDB(); closeDeckForm(); renderAdmDecks(); renderAdmSel();\r\n+\r\n+  const ok = await saveDeckToDB(deck);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB();\r\n+  closeDeckForm();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n   toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n \r\n function delDeck(id) {\r\n"
                },
                {
                    "date": 1775336250519,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -589,22 +589,28 @@\n   ed.appendChild(r);\r\n   r.querySelector('input:last-of-type').focus();\r\n }\r\n \r\n-function saveCards() {\r\n+async function saveCards() {\r\n   const id = document.getElementById('adm-deck-sel').value;\r\n   if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n   const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  DB.cards[id] = [];\r\n+  const cards = [];\r\n   rows.forEach(r => {\r\n     const ins  = r.querySelectorAll('input');\r\n     const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n     const text = ins[1].value.trim();\r\n-    if (text) DB.cards[id].push({ cat, text });\r\n+    if (text) cards.push({ cat, text });\r\n   });\r\n-  saveDB();\r\n-  toast(`บันทึก ${DB.cards[id].length} ใบแล้ว`, 'success');\r\n+\r\n+  // บันทึกไป Supabase\r\n+  const ok = await saveCardsToDB(id, cards);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB(); // reload จาก Supabase\r\n   renderAdmDecks();\r\n+  toast(`บันทึก ${cards.length} ใบแล้ว`, 'success');\r\n }\r\n \r\n /* ---- SETTINGS ---- */\r\n function saveSettings() {\r\n"
                },
                {
                    "date": 1775339926170,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -478,13 +478,23 @@\n     l.appendChild(r);\r\n   });\r\n }\r\n \r\n-function toggleDeck(id) {\r\n+async function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n-  saveDB(); renderAdmDecks();\r\n+\r\n+  // บันทึกไป Supabase จริง\r\n+  const { error } = await _sb.from('decks').update({ hidden: d.hidden }).eq('id', id);\r\n+  if (error) {\r\n+    d.hidden = !d.hidden; // rollback\r\n+    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n+    return;\r\n+  }\r\n+\r\n+  renderAdmDecks();\r\n+  renderDecks();\r\n   toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n }\r\n \r\n function renderAdmSel() {\r\n@@ -553,13 +563,22 @@\n   renderDecks();\r\n   toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n \r\n-function delDeck(id) {\r\n+async function delDeck(id) {\r\n   if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n-  DB.decks   = DB.decks.filter(d => d.id !== id);\r\n+\r\n+  // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n+  const ok = await deleteDeckFromDB(id);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // อัปเดต memory cache\r\n+  DB.decks = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n-  saveDB(); renderAdmDecks(); renderAdmSel();\r\n+\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n   toast('ลบกองไพ่แล้ว', 'info');\r\n }\r\n \r\n /* ---- CARD EDITOR ---- */\r\n"
                },
                {
                    "date": 1775418802174,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -608,8 +608,123 @@\n   ed.appendChild(r);\r\n   r.querySelector('input:last-of-type').focus();\r\n }\r\n \r\n+/* ---- BULK PASTE: แปลง textarea → หลาย row ---- */\r\n+function openBulkPaste() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  let ov = document.getElementById('bulk-paste-ov');\r\n+  if (!ov) {\r\n+    ov = document.createElement('div');\r\n+    ov.id = 'bulk-paste-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n+      z-index:700;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(6px);\r\n+    `;\r\n+    ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n+    document.body.appendChild(ov);\r\n+  }\r\n+\r\n+  ov.innerHTML = `\r\n+    <div style=\"\r\n+      background:linear-gradient(145deg,#181818,#101010);\r\n+      border:1px solid #2a2a2a;border-radius:20px;\r\n+      padding:28px 24px;max-width:520px;width:94%;\r\n+      position:relative;\r\n+    \">\r\n+      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+        border-radius:20px 20px 0 0;\"></div>\r\n+\r\n+      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n+        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n+      </div>\r\n+      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n+        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ: <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n+        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n+      </div>\r\n+\r\n+      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n+        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n+        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\"\r\n+          style=\"flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n+                 color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;\">\r\n+      </div>\r\n+\r\n+      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n+        width:100%;height:220px;\r\n+        background:#0d0d0d;border:1px solid #2a2a2a;\r\n+        border-radius:10px;color:#fff;padding:12px;\r\n+        font-family:'Kanit',sans-serif;font-size:.88rem;line-height:1.7;\r\n+        resize:vertical;outline:none;\r\n+      \"></textarea>\r\n+\r\n+      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n+\r\n+      <div style=\"display:flex;gap:10px;\">\r\n+        <button onclick=\"closeBulkPaste()\" style=\"\r\n+          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        \">ยกเลิก</button>\r\n+        <button onclick=\"applyBulkPaste()\" style=\"\r\n+          flex:2;background:var(--red);border:none;color:#fff;\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n+          display:flex;align-items:center;justify-content:center;gap:8px;\r\n+        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+\r\n+  ov.style.display = 'flex';\r\n+\r\n+  // live preview count\r\n+  const ta = ov.querySelector('#bulk-textarea');\r\n+  const pc = ov.querySelector('#bulk-preview-count');\r\n+  ta.addEventListener('input', () => {\r\n+    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n+    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n+  });\r\n+  ta.focus();\r\n+}\r\n+\r\n+function closeBulkPaste() {\r\n+  const ov = document.getElementById('bulk-paste-ov');\r\n+  if (ov) ov.style.display = 'none';\r\n+}\r\n+\r\n+function applyBulkPaste() {\r\n+  const ta      = document.getElementById('bulk-textarea');\r\n+  const defCat  = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n+  const lines   = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n+\r\n+  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n+\r\n+  lines.forEach(line => {\r\n+    if (line.includes('|')) {\r\n+      const [cat, ...rest] = line.split('|');\r\n+      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n+    } else {\r\n+      addCRow(line, defCat);\r\n+    }\r\n+  });\r\n+\r\n+  closeBulkPaste();\r\n+  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n+}\r\n+\r\n+/* ---- MULTI ADD: เพิ่มช่องว่างหลายช่องพร้อมกัน ---- */\r\n+function addMultiRows(n = 5) {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  for (let i = 0; i < n; i++) addCRow();\r\n+  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n+}\r\n+\r\n async function saveCards() {\r\n   const id = document.getElementById('adm-deck-sel').value;\r\n   if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n \r\n"
                },
                {
                    "date": 1775419274127,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,8 +16,9 @@\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n   if (name === 'settings') { loadSettingsTab(); }\r\n   if (name === 'cards')    { renderAdmSel(); }\r\n+  if (name === 'search')   { initCardSearch(); }\r\n   if (name === 'sheet')    {} // static content\r\n }\r\n \r\n /* ---- TAB LOADERS ---- */\r\n@@ -472,8 +473,9 @@\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n       </button>\r\n       <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm\" style=\"background:rgba(245,197,24,.08);border:1px solid rgba(245,197,24,.2);color:var(--gold);\" onclick=\"duplicateDeck('${d.id}')\"><i class=\"fi fi-sr-copy-alt\"></i> ก็อปปี้</button>\r\n       <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n     </div>`;\r\n     l.appendChild(r);\r\n   });\r\n@@ -608,123 +610,8 @@\n   ed.appendChild(r);\r\n   r.querySelector('input:last-of-type').focus();\r\n }\r\n \r\n-/* ---- BULK PASTE: แปลง textarea → หลาย row ---- */\r\n-function openBulkPaste() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-\r\n-  let ov = document.getElementById('bulk-paste-ov');\r\n-  if (!ov) {\r\n-    ov = document.createElement('div');\r\n-    ov.id = 'bulk-paste-ov';\r\n-    ov.style.cssText = `\r\n-      position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n-      z-index:700;display:flex;align-items:center;justify-content:center;\r\n-      backdrop-filter:blur(6px);\r\n-    `;\r\n-    ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n-    document.body.appendChild(ov);\r\n-  }\r\n-\r\n-  ov.innerHTML = `\r\n-    <div style=\"\r\n-      background:linear-gradient(145deg,#181818,#101010);\r\n-      border:1px solid #2a2a2a;border-radius:20px;\r\n-      padding:28px 24px;max-width:520px;width:94%;\r\n-      position:relative;\r\n-    \">\r\n-      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-        border-radius:20px 20px 0 0;\"></div>\r\n-\r\n-      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n-        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n-      </div>\r\n-      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n-        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ: <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n-        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n-      </div>\r\n-\r\n-      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n-        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n-        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\"\r\n-          style=\"flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n-                 color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;\">\r\n-      </div>\r\n-\r\n-      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n-        width:100%;height:220px;\r\n-        background:#0d0d0d;border:1px solid #2a2a2a;\r\n-        border-radius:10px;color:#fff;padding:12px;\r\n-        font-family:'Kanit',sans-serif;font-size:.88rem;line-height:1.7;\r\n-        resize:vertical;outline:none;\r\n-      \"></textarea>\r\n-\r\n-      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n-\r\n-      <div style=\"display:flex;gap:10px;\">\r\n-        <button onclick=\"closeBulkPaste()\" style=\"\r\n-          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-        \">ยกเลิก</button>\r\n-        <button onclick=\"applyBulkPaste()\" style=\"\r\n-          flex:2;background:var(--red);border:none;color:#fff;\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n-          display:flex;align-items:center;justify-content:center;gap:8px;\r\n-        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n-\r\n-  ov.style.display = 'flex';\r\n-\r\n-  // live preview count\r\n-  const ta = ov.querySelector('#bulk-textarea');\r\n-  const pc = ov.querySelector('#bulk-preview-count');\r\n-  ta.addEventListener('input', () => {\r\n-    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n-    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n-  });\r\n-  ta.focus();\r\n-}\r\n-\r\n-function closeBulkPaste() {\r\n-  const ov = document.getElementById('bulk-paste-ov');\r\n-  if (ov) ov.style.display = 'none';\r\n-}\r\n-\r\n-function applyBulkPaste() {\r\n-  const ta      = document.getElementById('bulk-textarea');\r\n-  const defCat  = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n-  const lines   = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n-\r\n-  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n-\r\n-  lines.forEach(line => {\r\n-    if (line.includes('|')) {\r\n-      const [cat, ...rest] = line.split('|');\r\n-      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n-    } else {\r\n-      addCRow(line, defCat);\r\n-    }\r\n-  });\r\n-\r\n-  closeBulkPaste();\r\n-  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n-}\r\n-\r\n-/* ---- MULTI ADD: เพิ่มช่องว่างหลายช่องพร้อมกัน ---- */\r\n-function addMultiRows(n = 5) {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  for (let i = 0; i < n; i++) addCRow();\r\n-  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n-}\r\n-\r\n async function saveCards() {\r\n   const id = document.getElementById('adm-deck-sel').value;\r\n   if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n \r\n@@ -958,5 +845,159 @@\n   const { error } = await _sb.from('packages').delete().eq('id', id);\r\n   if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n   toast('ลบแพ็กเกจแล้ว', 'info');\r\n   loadPkgList();\r\n+}\r\n+/* =========================================\r\n+   DECK DUPLICATOR\r\n+   ========================================= */\r\n+async function duplicateDeck(srcId) {\r\n+  const src = DB.decks.find(d => d.id === srcId);\r\n+  if (!src) return;\r\n+\r\n+  const newName = prompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n+  if (!newName?.trim()) return;\r\n+\r\n+  const newId   = 'd' + Date.now();\r\n+  const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n+\r\n+  // บันทึก deck ใหม่\r\n+  const deckOk = await saveDeckToDB(newDeck);\r\n+  if (!deckOk) { toast('เกิดข้อผิดพลาด ก็อปกองไพ่ไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // ก็อปไพ่ทั้งหมด\r\n+  const srcCards = DB.cards[srcId] || [];\r\n+  if (srcCards.length) {\r\n+    const cardOk = await saveCardsToDB(newId, srcCards);\r\n+    if (!cardOk) { toast('ก็อปกองไพ่สำเร็จ แต่ไพ่บางส่วนอาจไม่ครบ', 'warning'); }\r\n+  }\r\n+\r\n+  await initDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast(`ก็อปปี้ \"${newName.trim()}\" สำเร็จ! ${srcCards.length} ใบ`, 'success');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CARD SEARCH & FILTER\r\n+   ========================================= */\r\n+let _csShowDup = false;\r\n+\r\n+function initCardSearch() {\r\n+  // populate deck dropdown\r\n+  const deckSel = document.getElementById('cs-deck');\r\n+  if (!deckSel) return;\r\n+  deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id; o.textContent = d.name;\r\n+    deckSel.appendChild(o);\r\n+  });\r\n+\r\n+  // populate category dropdown (unique cats across all decks)\r\n+  const catSel = document.getElementById('cs-cat');\r\n+  if (catSel) {\r\n+    const cats = new Set();\r\n+    Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n+    catSel.innerHTML = '<option value=\"\">ทุกหมวด</option>';\r\n+    [...cats].sort().forEach(cat => {\r\n+      const o = document.createElement('option');\r\n+      o.value = cat; o.textContent = cat;\r\n+      catSel.appendChild(o);\r\n+    });\r\n+  }\r\n+\r\n+  _csShowDup = false;\r\n+  const dupBtn = document.getElementById('cs-dup-btn');\r\n+  if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n+\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function toggleDupFilter() {\r\n+  _csShowDup = !_csShowDup;\r\n+  const btn = document.getElementById('cs-dup-btn');\r\n+  if (btn) {\r\n+    btn.style.background = _csShowDup\r\n+      ? 'rgba(245,197,24,.25)' : 'rgba(245,197,24,.08)';\r\n+    btn.style.borderColor = _csShowDup ? 'var(--gold)' : 'rgba(245,197,24,.25)';\r\n+  }\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function runCardSearch() {\r\n+  const query    = (document.getElementById('cs-query')?.value || '').trim().toLowerCase();\r\n+  const deckId   = document.getElementById('cs-deck')?.value  || '';\r\n+  const catFilt  = document.getElementById('cs-cat')?.value   || '';\r\n+  const results  = document.getElementById('cs-results');\r\n+  const summary  = document.getElementById('cs-summary');\r\n+  if (!results) return;\r\n+\r\n+  // รวมไพ่ทั้งหมด + แนบ deck info\r\n+  let all = [];\r\n+  DB.decks.forEach(d => {\r\n+    (DB.cards[d.id] || []).forEach((c, i) => {\r\n+      all.push({ deck: d, cat: c.cat || 'ทั่วไป', text: c.text, idx: i });\r\n+    });\r\n+  });\r\n+\r\n+  // filter by deck\r\n+  if (deckId) all = all.filter(c => c.deck.id === deckId);\r\n+  // filter by category\r\n+  if (catFilt) all = all.filter(c => c.cat === catFilt);\r\n+  // filter by search query\r\n+  if (query) all = all.filter(c => c.text.toLowerCase().includes(query));\r\n+\r\n+  // filter duplicates (same text ใน deck เดียวกัน หรือข้ามกองก็ได้)\r\n+  if (_csShowDup) {\r\n+    const textCount = {};\r\n+    all.forEach(c => { textCount[c.text] = (textCount[c.text] || 0) + 1; });\r\n+    all = all.filter(c => textCount[c.text] > 1);\r\n+  }\r\n+\r\n+  // summary\r\n+  const totalShown = all.length;\r\n+  summary.textContent = totalShown\r\n+    ? `พบ ${totalShown} ใบ${_csShowDup ? ' (ข้อความซ้ำ)' : ''}`\r\n+    : 'ไม่พบไพ่ที่ตรงกัน';\r\n+\r\n+  if (!all.length) {\r\n+    results.innerHTML = `\r\n+      <div style=\"text-align:center;padding:40px;color:var(--text3);\">\r\n+        <i class=\"fi fi-sr-search\" style=\"font-size:2rem;opacity:.3;\"></i>\r\n+        <div style=\"margin-top:10px;font-size:.85rem;\">ไม่พบไพ่</div>\r\n+      </div>`;\r\n+    return;\r\n+  }\r\n+\r\n+  // highlight matching text\r\n+  function hl(text) {\r\n+    if (!query) return text;\r\n+    const re = new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')})`, 'gi');\r\n+    return text.replace(re, '<mark style=\"background:rgba(229,9,20,.3);color:#fff;border-radius:2px;padding:0 2px;\">$1</mark>');\r\n+  }\r\n+\r\n+  results.innerHTML = all.map(c => {\r\n+    const th = getTh(c.deck);\r\n+    return `\r\n+    <div style=\"\r\n+      display:flex;align-items:center;gap:12px;\r\n+      background:#161616;border:1px solid #1e1e1e;\r\n+      border-radius:10px;padding:10px 14px;\r\n+      transition:border-color .2s;\r\n+    \" onmouseover=\"this.style.borderColor='#2a2a2a'\" onmouseout=\"this.style.borderColor='#1e1e1e'\">\r\n+      <div class=\"drow-ico ${th}\" style=\"width:32px;height:32px;font-size:.78rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+        <i class=\"fi ${c.deck.icon || 'fi-sr-layers'}\"></i>\r\n+      </div>\r\n+      <div style=\"flex:1;min-width:0;\">\r\n+        <div style=\"font-size:.88rem;line-height:1.4;\">${hl(c.text)}</div>\r\n+        <div style=\"display:flex;align-items:center;gap:8px;margin-top:4px;\">\r\n+          <span style=\"font-size:.72rem;color:var(--text3);\">${c.deck.name}</span>\r\n+          <span style=\"color:#333;font-size:.6rem;\">·</span>\r\n+          <span style=\"font-size:.7rem;color:var(--text2);background:#1a1a1a;border:1px solid #2a2a2a;\r\n+                       border-radius:20px;padding:1px 8px;\">${c.cat}</span>\r\n+        </div>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775419887782,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -566,9 +566,10 @@\n   toast('บันทึกกองไพ่แล้ว', 'success');\r\n }\r\n \r\n async function delDeck(id) {\r\n-  if (!confirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย')) return;\r\n+  const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n+  if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n   const ok = await deleteDeckFromDB(id);\r\n   if (!ok) { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n"
                },
                {
                    "date": 1775419924832,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -570,9 +570,9 @@\n   const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n   if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n-  const ok = await deleteDeckFromDB(id);\r\n+  const ok1= await deleteDeckFromDB(id);\r\n   if (!ok) { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n \r\n   // อัปเดต memory cache\r\n   DB.decks = DB.decks.filter(d => d.id !== id);\r\n"
                },
                {
                    "date": 1775419930232,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -571,9 +571,9 @@\n   if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n   const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+  if (!ok1{ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n \r\n   // อัปเดต memory cache\r\n   DB.decks = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n"
                },
                {
                    "date": 1775419936982,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -571,9 +571,9 @@\n   if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n   const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok1{ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+  if (!ok)=toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n \r\n   // อัปเดต memory cache\r\n   DB.decks = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n"
                },
                {
                    "date": 1775419947840,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -571,9 +571,9 @@\n   if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n   const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok)=toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+  if (!ok1 { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n \r\n   // อัปเดต memory cache\r\n   DB.decks = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n"
                },
                {
                    "date": 1775419993645,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -571,9 +571,9 @@\n   if (!ok) return;\r\n \r\n   // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n   const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok1 { toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+  if (!ok1){ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n \r\n   // อัปเดต memory cache\r\n   DB.decks = DB.decks.filter(d => d.id !== id);\r\n   delete DB.cards[id];\r\n@@ -841,9 +841,10 @@\n   loadPkgList();\r\n }\r\n \r\n async function deletePkg(id) {\r\n-  if (!confirm('ลบแพ็กเกจนี้?')) return;\r\n+  const ok = await customConfirm('ลบแพ็กเกจนี้?');\r\n+  if (!ok) return;\r\n   const { error } = await _sb.from('packages').delete().eq('id', id);\r\n   if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n   toast('ลบแพ็กเกจแล้ว', 'info');\r\n   loadPkgList();\r\n"
                },
                {
                    "date": 1775420189544,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1001,5 +1001,14 @@\n         </div>\r\n       </div>\r\n     </div>`;\r\n   }).join('');\r\n+}\r\n+\r\n+async function confirmReset() {\r\n+  const ok = await customConfirm('รีเซ็ตข้อมูลทั้งหมด?');\r\n+  if (!ok) return;\r\n+  resetDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('รีเซ็ตแล้ว', 'info');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775476236736,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -855,9 +855,9 @@\n async function duplicateDeck(srcId) {\r\n   const src = DB.decks.find(d => d.id === srcId);\r\n   if (!src) return;\r\n \r\n-  const newName = prompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n+const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n   if (!newName?.trim()) return;\r\n \r\n   const newId   = 'd' + Date.now();\r\n   const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n"
                },
                {
                    "date": 1775476253248,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -855,9 +855,9 @@\n async function duplicateDeck(srcId) {\r\n   const src = DB.decks.find(d => d.id === srcId);\r\n   if (!src) return;\r\n \r\n-const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n+  const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n   if (!newName?.trim()) return;\r\n \r\n   const newId   = 'd' + Date.now();\r\n   const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n@@ -1010,5 +1010,207 @@\n   resetDB();\r\n   renderAdmDecks();\r\n   renderAdmSel();\r\n   toast('รีเซ็ตแล้ว', 'info');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CUSTOM CONFIRM / PROMPT DIALOGS\r\n+   ========================================= */\r\n+function customConfirm(message) {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-confirm-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-confirm-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:320px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:1rem;font-weight:600;\r\n+          margin-bottom:22px;line-height:1.5;color:#fff;\" id=\"cc-msg\"></div>\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cc-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cc-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ยืนยัน</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cc-msg').textContent = message;\r\n+    ov.querySelector('#cc-ok').onclick     = () => { ov.remove(); resolve(true);  };\r\n+    ov.querySelector('#cc-cancel').onclick = () => { ov.remove(); resolve(false); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(false); } });\r\n+    document.body.appendChild(ov);\r\n+  });\r\n+}\r\n+\r\n+function customPrompt(message, defaultValue = '') {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-prompt-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-prompt-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:340px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:.95rem;font-weight:600;\r\n+          margin-bottom:14px;color:#fff;\" id=\"cp-msg\"></div>\r\n+        <input id=\"cp-input\" type=\"text\" style=\"\r\n+          width:100%;background:#151515;border:1px solid #2a2a2a;border-radius:8px;\r\n+          color:#fff;padding:10px 12px;font-family:'Kanit',sans-serif;font-size:.95rem;\r\n+          outline:none;box-sizing:border-box;margin-bottom:18px;\r\n+        \">\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cp-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cp-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ตกลง</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cp-msg').textContent = message;\r\n+    const inp = ov.querySelector('#cp-input');\r\n+    inp.value = defaultValue;\r\n+    const confirm = () => { ov.remove(); resolve(inp.value.trim() || null); };\r\n+    ov.querySelector('#cp-ok').onclick     = confirm;\r\n+    ov.querySelector('#cp-cancel').onclick = () => { ov.remove(); resolve(null); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(null); } });\r\n+    document.body.appendChild(ov);\r\n+    setTimeout(() => { inp.focus(); inp.select(); }, 50);\r\n+    inp.addEventListener('keydown', e => {\r\n+      if (e.key === 'Enter') confirm();\r\n+      if (e.key === 'Escape') { ov.remove(); resolve(null); }\r\n+    });\r\n+  });\r\n+}\r\n+\r\n+/* =========================================\r\n+   BULK PASTE & MULTI ADD\r\n+   ========================================= */\r\n+function addMultiRows(n = 5) {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  for (let i = 0; i < n; i++) addCRow();\r\n+  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n+}\r\n+\r\n+function openBulkPaste() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+  const ov = document.createElement('div');\r\n+  ov.id = 'bulk-paste-ov';\r\n+  ov.style.cssText = `\r\n+    position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n+    z-index:700;display:flex;align-items:center;justify-content:center;\r\n+    backdrop-filter:blur(6px);\r\n+  `;\r\n+  ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n+  ov.innerHTML = `\r\n+    <div style=\"\r\n+      background:linear-gradient(145deg,#181818,#101010);\r\n+      border:1px solid #2a2a2a;border-radius:20px;\r\n+      padding:28px 24px;max-width:520px;width:94%;position:relative;\r\n+    \">\r\n+      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+        border-radius:20px 20px 0 0;\"></div>\r\n+      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n+        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n+      </div>\r\n+      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n+        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ:\r\n+        <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n+        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n+      </div>\r\n+      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n+        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n+        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\" style=\"\r\n+          flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n+          color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;outline:none;\">\r\n+      </div>\r\n+      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n+        width:100%;height:220px;background:#0d0d0d;border:1px solid #2a2a2a;border-radius:10px;\r\n+        color:#fff;padding:12px;font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        line-height:1.7;resize:vertical;outline:none;box-sizing:border-box;\r\n+      \"></textarea>\r\n+      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n+      <div style=\"display:flex;gap:10px;\">\r\n+        <button onclick=\"closeBulkPaste()\" style=\"\r\n+          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        \">ยกเลิก</button>\r\n+        <button onclick=\"applyBulkPaste()\" style=\"\r\n+          flex:2;background:var(--red);border:none;color:#fff;\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n+          display:flex;align-items:center;justify-content:center;gap:8px;\r\n+        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+  document.body.appendChild(ov);\r\n+  const ta = ov.querySelector('#bulk-textarea');\r\n+  const pc = ov.querySelector('#bulk-preview-count');\r\n+  ta.addEventListener('input', () => {\r\n+    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n+    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n+  });\r\n+  ta.focus();\r\n+}\r\n+\r\n+function closeBulkPaste() {\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+}\r\n+\r\n+function applyBulkPaste() {\r\n+  const ta     = document.getElementById('bulk-textarea');\r\n+  const defCat = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n+  const lines  = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n+  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n+  lines.forEach(line => {\r\n+    if (line.includes('|')) {\r\n+      const [cat, ...rest] = line.split('|');\r\n+      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n+    } else {\r\n+      addCRow(line, defCat);\r\n+    }\r\n+  });\r\n+  closeBulkPaste();\r\n+  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775510571337,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -635,14 +635,15 @@\n }\r\n \r\n /* ---- SETTINGS ---- */\r\n function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)   || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value             || 'DRINKORDOOM';\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)    || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value              || 'DRINKORDOOM';\r\n   DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n   DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value) || 1;\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value)  || 1;\r\n   DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  DB.settings.deckLayout   = document.getElementById('st-layout').value || 'auto'; // ← เพิ่ม\r\n   saveDB(); applyTestMode();\r\n   toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n }\r\n \r\n"
                },
                {
                    "date": 1775621513045,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,1223 @@\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');\r\n+\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+  if (name === 'settings') { loadSettingsTab(); }\r\n+  if (name === 'cards')    { renderAdmSel(); }\r\n+  if (name === 'search')   { initCardSearch(); }\r\n+  if (name === 'sheet')    {} // static content\r\n+}\r\n+\r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+}\r\n+\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n+\r\n+  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n+  const [\r\n+    users,\r\n+    { data: history },\r\n+    { data: packages }\r\n+  ] = await Promise.all([\r\n+    API.getAllUsers(),\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n+    _sb.from('packages').select('*').eq('is_active', true)\r\n+  ]);\r\n+\r\n+  const now        = new Date();\r\n+  const weekAgo    = new Date(now - 7  * 86400000);\r\n+  const monthAgo   = new Date(now - 30 * 86400000);\r\n+\r\n+  // ---- ผู้ใช้ ----\r\n+  const totalUsers    = users.length;\r\n+  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n+  const adminUsers    = users.filter(u => u.is_admin).length;\r\n+  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n+  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n+  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n+  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n+\r\n+  // ---- กองไพ่ & ไพ่ ----\r\n+  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n+  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n+  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n+\r\n+  // top deck by card count\r\n+  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n+\r\n+  // ---- เครดิต / ประวัติ ----\r\n+  const hist         = history || [];\r\n+  const purchases    = hist.filter(h => h.type === 'purchase');\r\n+  const frees        = hist.filter(h => h.type === 'free');\r\n+  const deducts      = hist.filter(h => h.type === 'deduct');\r\n+  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n+  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n+\r\n+  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n+  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n+    const pkg = (packages || []).find(p => {\r\n+      const total = p.coins + p.bonus;\r\n+      return total === h.amount;\r\n+    });\r\n+    return s + (pkg ? pkg.price : 0);\r\n+  }, 0);\r\n+\r\n+  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n+  const newThisWeek  = users.filter(u => {\r\n+    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n+    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n+  }).length;\r\n+\r\n+  // ---- สร้าง HTML ----\r\n+  container.innerHTML = `\r\n+    <!-- ROW 1: ตัวเลขหลัก -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n+            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n+            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n+            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 2: เครดิต -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n+            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 3: กองไพ่ -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 4: Top Decks -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${decksSorted.slice(0, 6).map((d, i) => {\r\n+          const cnt    = (DB.cards[d.id] || []).length;\r\n+          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n+          const pct    = Math.round(cnt / maxCnt * 100);\r\n+          const th     = getTh(d);\r\n+          return `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;min-width:0;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n+                ${d.name}\r\n+                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n+              </div>\r\n+              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n+                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n+            </div>\r\n+          </div>`;\r\n+        }).join('')}\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 5: Top Users by Credits -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n+              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;\">\r\n+                ${u.username}\r\n+                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n+                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n+              </div>\r\n+              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n+                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n+              ${(u.credits||0).toLocaleString()}\r\n+              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n+            </div>\r\n+          </div>`).join('')}\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+\r\n+  // update legacy stat elements ด้านบน (stat bar)\r\n+  const elUsers = document.getElementById('stat-users');\r\n+  if (elUsers) elUsers.textContent = totalUsers;\r\n+}\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n+\r\n+  // ดึง history และ users พร้อมกัน\r\n+  const [{ data, error }, { data: users }] = await Promise.all([\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n+    _sb.from('profiles').select('id, username')\r\n+  ]);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  // สร้าง map id → username\r\n+  const userMap = {};\r\n+  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n+\r\n+const TYPE_ICO = {\r\n+  free:             'fi-sr-gift',\r\n+  purchase:         'fi-sr-shopping-cart',\r\n+  purchase_pending: 'fi-sr-clock',       // ✅ เพิ่ม\r\n+  deduct:           'fi-sr-layers',\r\n+  admin:            'fi-sr-shield-check',\r\n+};\r\n+const TYPE_LABEL = {\r\n+  free:             'รับฟรี',\r\n+  purchase:         'ซื้อเครดิต',\r\n+  purchase_pending: 'รอชำระ',            // ✅ เพิ่ม\r\n+  deduct:           'ใช้ไพ่',\r\n+  admin:            'แอดมินเติม',\r\n+};\r\n+\r\n+  const rows = data.map(h => {\r\n+    const isPlus   = h.amount > 0;\r\n+    const typeKey  = h.type || 'deduct';\r\n+    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n+    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n+    const date     = new Date(h.created_at);\r\n+    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n+\r\n+    return `\r\n+    <div class=\"hist-row\">\r\n+      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n+        <i class=\"fi ${ico}\"></i>\r\n+      </div>\r\n+      <div class=\"hist-info\">\r\n+        <div class=\"hist-top\">\r\n+          <span class=\"hist-note\">${h.note || label}</span>\r\n+          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n+        </div>\r\n+        <div class=\"hist-sub\">\r\n+          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n+        ${isPlus ? '+' : ''}${h.amount}\r\n+        <span class=\"hist-unit\">เครดิต</span>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+\r\n+  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n+}\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n+\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const TYPE_ICO = {\r\n+    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n+    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n+  };\r\n+\r\n+  listEl.innerHTML = data.map(a => {\r\n+    const t       = a.type || 'info';\r\n+    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n+    const expires = a.expires_at\r\n+      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n+      : null;\r\n+    return `\r\n+    <div class=\"ann-row\">\r\n+      <div class=\"ann-row-ico ann-ico-${t}\">\r\n+        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n+      </div>\r\n+      <div class=\"ann-row-info\">\r\n+        <div class=\"ann-row-top\">\r\n+          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n+          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n+          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n+            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n+            ${a.active ? 'เผยแพร่' : 'draft'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n+        </div>\r\n+        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n+        <div class=\"ann-row-sub\">\r\n+          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n+          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"ann-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+  const active  = document.getElementById('ann-active')?.value === '1';\r\n+  const expires = document.getElementById('ann-expires')?.value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type,\r\n+    show_popup: popup,\r\n+    active,\r\n+    expires_at: expires ? new Date(expires).toISOString() : null,\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  document.getElementById('ann-expires').value = '';\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  // update stats-bar (ด้านบนสุดของ admin panel)\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+\r\n+  // render deck list ใน tab กองไพ่\r\n+  const l = document.getElementById('adm-deck-list');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm\" style=\"background:rgba(245,197,24,.08);border:1px solid rgba(245,197,24,.2);color:var(--gold);\" onclick=\"duplicateDeck('${d.id}')\"><i class=\"fi fi-sr-copy-alt\"></i> ก็อปปี้</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+\r\n+async function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  if (!d) return;\r\n+  d.hidden = !d.hidden;\r\n+\r\n+  // บันทึกไป Supabase จริง\r\n+  const { error } = await _sb.from('decks').update({ hidden: d.hidden }).eq('id', id);\r\n+  if (error) {\r\n+    d.hidden = !d.hidden; // rollback\r\n+    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n+    return;\r\n+  }\r\n+\r\n+  renderAdmDecks();\r\n+  renderDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n+async function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n+\r\n+  let deck;\r\n+  if (editDeckId) {\r\n+    deck = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(deck, { name, icon, theme, desc, cost, category });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    deck = { id, name, icon, theme, desc, cost, category, hidden: false };\r\n+    DB.decks.push(deck);\r\n+    DB.cards[deck.id] = [];\r\n+  }\r\n+\r\n+  const ok = await saveDeckToDB(deck);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB();\r\n+  closeDeckForm();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+}\r\n+\r\n+async function delDeck(id) {\r\n+  const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n+  if (!ok) return;\r\n+\r\n+  // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n+  const ok1= await deleteDeckFromDB(id);\r\n+  if (!ok1){ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // อัปเดต memory cache\r\n+  DB.decks = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+async function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  const cards = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) cards.push({ cat, text });\r\n+  });\r\n+\r\n+  // บันทึกไป Supabase\r\n+  const ok = await saveCardsToDB(id, cards);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB(); // reload จาก Supabase\r\n+  renderAdmDecks();\r\n+  toast(`บันทึก ${cards.length} ใบแล้ว`, 'success');\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)    || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value              || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value)  || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  DB.settings.deckLayout   = document.getElementById('st-layout').value || 'auto'; // ← เพิ่ม\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n+\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+}\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      const parsed = JSON.parse(ev.target.result);\r\n+\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n+\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n+\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n+\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n+}\r\n+/* ---- SETTINGS TAB ---- */\r\n+async function loadSettingsTab() {\r\n+  // โหลด packages list ในแท็บตั้งค่า\r\n+  if (typeof loadPkgList === 'function') await loadPkgList();\r\n+\r\n+  // sync ค่า settings fields\r\n+  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n+  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n+  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n+  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n+  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n+  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n+}\r\n+\r\n+/* ---- PACKAGE MANAGER ---- */\r\n+let editPkgId = null;\r\n+\r\n+async function loadPkgList() {\r\n+  const el = document.getElementById('pkg-list');\r\n+  if (!el) return;\r\n+  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('packages')\r\n+    .select('*')\r\n+    .order('sort_order', { ascending: true });\r\n+\r\n+  if (error || !data?.length) {\r\n+    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  el.innerHTML = data.map(p => `\r\n+    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n+      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n+        <i class=\"fi fi-sr-coins\"></i>\r\n+      </div>\r\n+      <div class=\"pkg-row-info\">\r\n+        <div class=\"pkg-row-top\">\r\n+          <span class=\"pkg-row-name\">${p.name}</span>\r\n+          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n+          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n+        </div>\r\n+        <div class=\"pkg-row-detail\">\r\n+          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span>ลำดับ ${p.sort_order}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"pkg-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n+          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n+        </button>\r\n+        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n+          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n+}\r\n+\r\n+function openPkgForm(id = null) {\r\n+  editPkgId = id;\r\n+  if (id) {\r\n+    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n+      if (!data) return;\r\n+      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n+      document.getElementById('pf-name').value     = data.name;\r\n+      document.getElementById('pf-coins').value    = data.coins;\r\n+      document.getElementById('pf-bonus').value    = data.bonus;\r\n+      document.getElementById('pf-price').value    = data.price;\r\n+      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n+      document.getElementById('pf-order').value    = data.sort_order;\r\n+      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n+    });\r\n+  } else {\r\n+    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n+    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n+    document.getElementById('pf-bonus').value    = '0';\r\n+    document.getElementById('pf-featured').value = '0';\r\n+    document.getElementById('pf-order').value    = '0';\r\n+    document.getElementById('pf-active').value   = '1';\r\n+  }\r\n+  document.getElementById('pkg-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closePkgForm() {\r\n+  document.getElementById('pkg-form-ov').classList.remove('show');\r\n+  editPkgId = null;\r\n+}\r\n+\r\n+async function savePkg() {\r\n+  const name     = document.getElementById('pf-name').value.trim();\r\n+  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n+  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n+  const price    = parseInt(document.getElementById('pf-price').value);\r\n+  const featured = document.getElementById('pf-featured').value === '1';\r\n+  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n+  const active   = document.getElementById('pf-active').value === '1';\r\n+\r\n+  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n+\r\n+  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n+\r\n+  const { error } = editPkgId\r\n+    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n+    : await _sb.from('packages').insert(payload);\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n+  closePkgForm();\r\n+  loadPkgList();\r\n+  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n+}\r\n+\r\n+async function togglePkg(id, active) {\r\n+  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n+  loadPkgList();\r\n+}\r\n+\r\n+async function deletePkg(id) {\r\n+  const ok = await customConfirm('ลบแพ็กเกจนี้?');\r\n+  if (!ok) return;\r\n+  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบแพ็กเกจแล้ว', 'info');\r\n+  loadPkgList();\r\n+}\r\n+/* =========================================\r\n+   DECK DUPLICATOR\r\n+   ========================================= */\r\n+async function duplicateDeck(srcId) {\r\n+  const src = DB.decks.find(d => d.id === srcId);\r\n+  if (!src) return;\r\n+\r\n+  const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n+  if (!newName?.trim()) return;\r\n+\r\n+  const newId   = 'd' + Date.now();\r\n+  const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n+\r\n+  // บันทึก deck ใหม่\r\n+  const deckOk = await saveDeckToDB(newDeck);\r\n+  if (!deckOk) { toast('เกิดข้อผิดพลาด ก็อปกองไพ่ไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // ก็อปไพ่ทั้งหมด\r\n+  const srcCards = DB.cards[srcId] || [];\r\n+  if (srcCards.length) {\r\n+    const cardOk = await saveCardsToDB(newId, srcCards);\r\n+    if (!cardOk) { toast('ก็อปกองไพ่สำเร็จ แต่ไพ่บางส่วนอาจไม่ครบ', 'warning'); }\r\n+  }\r\n+\r\n+  await initDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast(`ก็อปปี้ \"${newName.trim()}\" สำเร็จ! ${srcCards.length} ใบ`, 'success');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CARD SEARCH & FILTER\r\n+   ========================================= */\r\n+let _csShowDup = false;\r\n+\r\n+function initCardSearch() {\r\n+  // populate deck dropdown\r\n+  const deckSel = document.getElementById('cs-deck');\r\n+  if (!deckSel) return;\r\n+  deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id; o.textContent = d.name;\r\n+    deckSel.appendChild(o);\r\n+  });\r\n+\r\n+  // populate category dropdown (unique cats across all decks)\r\n+  const catSel = document.getElementById('cs-cat');\r\n+  if (catSel) {\r\n+    const cats = new Set();\r\n+    Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n+    catSel.innerHTML = '<option value=\"\">ทุกหมวด</option>';\r\n+    [...cats].sort().forEach(cat => {\r\n+      const o = document.createElement('option');\r\n+      o.value = cat; o.textContent = cat;\r\n+      catSel.appendChild(o);\r\n+    });\r\n+  }\r\n+\r\n+  _csShowDup = false;\r\n+  const dupBtn = document.getElementById('cs-dup-btn');\r\n+  if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n+\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function toggleDupFilter() {\r\n+  _csShowDup = !_csShowDup;\r\n+  const btn = document.getElementById('cs-dup-btn');\r\n+  if (btn) {\r\n+    btn.style.background = _csShowDup\r\n+      ? 'rgba(245,197,24,.25)' : 'rgba(245,197,24,.08)';\r\n+    btn.style.borderColor = _csShowDup ? 'var(--gold)' : 'rgba(245,197,24,.25)';\r\n+  }\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function runCardSearch() {\r\n+  const query    = (document.getElementById('cs-query')?.value || '').trim().toLowerCase();\r\n+  const deckId   = document.getElementById('cs-deck')?.value  || '';\r\n+  const catFilt  = document.getElementById('cs-cat')?.value   || '';\r\n+  const results  = document.getElementById('cs-results');\r\n+  const summary  = document.getElementById('cs-summary');\r\n+  if (!results) return;\r\n+\r\n+  // รวมไพ่ทั้งหมด + แนบ deck info\r\n+  let all = [];\r\n+  DB.decks.forEach(d => {\r\n+    (DB.cards[d.id] || []).forEach((c, i) => {\r\n+      all.push({ deck: d, cat: c.cat || 'ทั่วไป', text: c.text, idx: i });\r\n+    });\r\n+  });\r\n+\r\n+  // filter by deck\r\n+  if (deckId) all = all.filter(c => c.deck.id === deckId);\r\n+  // filter by category\r\n+  if (catFilt) all = all.filter(c => c.cat === catFilt);\r\n+  // filter by search query\r\n+  if (query) all = all.filter(c => c.text.toLowerCase().includes(query));\r\n+\r\n+  // filter duplicates (same text ใน deck เดียวกัน หรือข้ามกองก็ได้)\r\n+  if (_csShowDup) {\r\n+    const textCount = {};\r\n+    all.forEach(c => { textCount[c.text] = (textCount[c.text] || 0) + 1; });\r\n+    all = all.filter(c => textCount[c.text] > 1);\r\n+  }\r\n+\r\n+  // summary\r\n+  const totalShown = all.length;\r\n+  summary.textContent = totalShown\r\n+    ? `พบ ${totalShown} ใบ${_csShowDup ? ' (ข้อความซ้ำ)' : ''}`\r\n+    : 'ไม่พบไพ่ที่ตรงกัน';\r\n+\r\n+  if (!all.length) {\r\n+    results.innerHTML = `\r\n+      <div style=\"text-align:center;padding:40px;color:var(--text3);\">\r\n+        <i class=\"fi fi-sr-search\" style=\"font-size:2rem;opacity:.3;\"></i>\r\n+        <div style=\"margin-top:10px;font-size:.85rem;\">ไม่พบไพ่</div>\r\n+      </div>`;\r\n+    return;\r\n+  }\r\n+\r\n+  // highlight matching text\r\n+  function hl(text) {\r\n+    if (!query) return text;\r\n+    const re = new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')})`, 'gi');\r\n+    return text.replace(re, '<mark style=\"background:rgba(229,9,20,.3);color:#fff;border-radius:2px;padding:0 2px;\">$1</mark>');\r\n+  }\r\n+\r\n+  results.innerHTML = all.map(c => {\r\n+    const th = getTh(c.deck);\r\n+    return `\r\n+    <div style=\"\r\n+      display:flex;align-items:center;gap:12px;\r\n+      background:#161616;border:1px solid #1e1e1e;\r\n+      border-radius:10px;padding:10px 14px;\r\n+      transition:border-color .2s;\r\n+    \" onmouseover=\"this.style.borderColor='#2a2a2a'\" onmouseout=\"this.style.borderColor='#1e1e1e'\">\r\n+      <div class=\"drow-ico ${th}\" style=\"width:32px;height:32px;font-size:.78rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+        <i class=\"fi ${c.deck.icon || 'fi-sr-layers'}\"></i>\r\n+      </div>\r\n+      <div style=\"flex:1;min-width:0;\">\r\n+        <div style=\"font-size:.88rem;line-height:1.4;\">${hl(c.text)}</div>\r\n+        <div style=\"display:flex;align-items:center;gap:8px;margin-top:4px;\">\r\n+          <span style=\"font-size:.72rem;color:var(--text3);\">${c.deck.name}</span>\r\n+          <span style=\"color:#333;font-size:.6rem;\">·</span>\r\n+          <span style=\"font-size:.7rem;color:var(--text2);background:#1a1a1a;border:1px solid #2a2a2a;\r\n+                       border-radius:20px;padding:1px 8px;\">${c.cat}</span>\r\n+        </div>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function confirmReset() {\r\n+  const ok = await customConfirm('รีเซ็ตข้อมูลทั้งหมด?');\r\n+  if (!ok) return;\r\n+  resetDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('รีเซ็ตแล้ว', 'info');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CUSTOM CONFIRM / PROMPT DIALOGS\r\n+   ========================================= */\r\n+function customConfirm(message) {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-confirm-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-confirm-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:320px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:1rem;font-weight:600;\r\n+          margin-bottom:22px;line-height:1.5;color:#fff;\" id=\"cc-msg\"></div>\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cc-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cc-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ยืนยัน</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cc-msg').textContent = message;\r\n+    ov.querySelector('#cc-ok').onclick     = () => { ov.remove(); resolve(true);  };\r\n+    ov.querySelector('#cc-cancel').onclick = () => { ov.remove(); resolve(false); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(false); } });\r\n+    document.body.appendChild(ov);\r\n+  });\r\n+}\r\n+\r\n+function customPrompt(message, defaultValue = '') {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-prompt-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-prompt-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:340px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:.95rem;font-weight:600;\r\n+          margin-bottom:14px;color:#fff;\" id=\"cp-msg\"></div>\r\n+        <input id=\"cp-input\" type=\"text\" style=\"\r\n+          width:100%;background:#151515;border:1px solid #2a2a2a;border-radius:8px;\r\n+          color:#fff;padding:10px 12px;font-family:'Kanit',sans-serif;font-size:.95rem;\r\n+          outline:none;box-sizing:border-box;margin-bottom:18px;\r\n+        \">\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cp-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cp-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ตกลง</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cp-msg').textContent = message;\r\n+    const inp = ov.querySelector('#cp-input');\r\n+    inp.value = defaultValue;\r\n+    const confirm = () => { ov.remove(); resolve(inp.value.trim() || null); };\r\n+    ov.querySelector('#cp-ok').onclick     = confirm;\r\n+    ov.querySelector('#cp-cancel').onclick = () => { ov.remove(); resolve(null); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(null); } });\r\n+    document.body.appendChild(ov);\r\n+    setTimeout(() => { inp.focus(); inp.select(); }, 50);\r\n+    inp.addEventListener('keydown', e => {\r\n+      if (e.key === 'Enter') confirm();\r\n+      if (e.key === 'Escape') { ov.remove(); resolve(null); }\r\n+    });\r\n+  });\r\n+}\r\n+\r\n+/* =========================================\r\n+   BULK PASTE & MULTI ADD\r\n+   ========================================= */\r\n+function addMultiRows(n = 5) {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  for (let i = 0; i < n; i++) addCRow();\r\n+  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n+}\r\n+\r\n+function openBulkPaste() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+  const ov = document.createElement('div');\r\n+  ov.id = 'bulk-paste-ov';\r\n+  ov.style.cssText = `\r\n+    position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n+    z-index:700;display:flex;align-items:center;justify-content:center;\r\n+    backdrop-filter:blur(6px);\r\n+  `;\r\n+  ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n+  ov.innerHTML = `\r\n+    <div style=\"\r\n+      background:linear-gradient(145deg,#181818,#101010);\r\n+      border:1px solid #2a2a2a;border-radius:20px;\r\n+      padding:28px 24px;max-width:520px;width:94%;position:relative;\r\n+    \">\r\n+      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+        border-radius:20px 20px 0 0;\"></div>\r\n+      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n+        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n+      </div>\r\n+      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n+        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ:\r\n+        <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n+        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n+      </div>\r\n+      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n+        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n+        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\" style=\"\r\n+          flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n+          color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;outline:none;\">\r\n+      </div>\r\n+      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n+        width:100%;height:220px;background:#0d0d0d;border:1px solid #2a2a2a;border-radius:10px;\r\n+        color:#fff;padding:12px;font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        line-height:1.7;resize:vertical;outline:none;box-sizing:border-box;\r\n+      \"></textarea>\r\n+      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n+      <div style=\"display:flex;gap:10px;\">\r\n+        <button onclick=\"closeBulkPaste()\" style=\"\r\n+          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        \">ยกเลิก</button>\r\n+        <button onclick=\"applyBulkPaste()\" style=\"\r\n+          flex:2;background:var(--red);border:none;color:#fff;\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n+          display:flex;align-items:center;justify-content:center;gap:8px;\r\n+        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+  document.body.appendChild(ov);\r\n+  const ta = ov.querySelector('#bulk-textarea');\r\n+  const pc = ov.querySelector('#bulk-preview-count');\r\n+  ta.addEventListener('input', () => {\r\n+    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n+    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n+  });\r\n+  ta.focus();\r\n+}\r\n+\r\n+function closeBulkPaste() {\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+}\r\n+\r\n+function applyBulkPaste() {\r\n+  const ta     = document.getElementById('bulk-textarea');\r\n+  const defCat = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n+  const lines  = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n+  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n+  lines.forEach(line => {\r\n+    if (line.includes('|')) {\r\n+      const [cat, ...rest] = line.split('|');\r\n+      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n+    } else {\r\n+      addCRow(line, defCat);\r\n+    }\r\n+  });\r\n+  closeBulkPaste();\r\n+  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775622868510,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -10,1231 +10,11 @@\n   document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n   const target = document.getElementById('tc-' + name);\r\n   if (target) target.classList.add('act');\r\n \r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-  if (name === 'settings') { loadSettingsTab(); }\r\n-  if (name === 'cards')    { renderAdmSel(); }\r\n-  if (name === 'search')   { initCardSearch(); }\r\n-  if (name === 'sheet')    {} // static content\r\n-}\r\n+  // อัปเดต URL ให้ต่างกันแต่ละ tab\r\n+  history.replaceState({}, '', '#admin/' + name);\r\n \r\n-/* ---- TAB LOADERS ---- */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  if (!container) return;\r\n-  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n-  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n-}\r\n-\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n-\r\n-  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n-  const [\r\n-    users,\r\n-    { data: history },\r\n-    { data: packages }\r\n-  ] = await Promise.all([\r\n-    API.getAllUsers(),\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n-    _sb.from('packages').select('*').eq('is_active', true)\r\n-  ]);\r\n-\r\n-  const now        = new Date();\r\n-  const weekAgo    = new Date(now - 7  * 86400000);\r\n-  const monthAgo   = new Date(now - 30 * 86400000);\r\n-\r\n-  // ---- ผู้ใช้ ----\r\n-  const totalUsers    = users.length;\r\n-  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n-  const adminUsers    = users.filter(u => u.is_admin).length;\r\n-  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n-  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n-  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n-  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n-\r\n-  // ---- กองไพ่ & ไพ่ ----\r\n-  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n-  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n-  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n-\r\n-  // top deck by card count\r\n-  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n-\r\n-  // ---- เครดิต / ประวัติ ----\r\n-  const hist         = history || [];\r\n-  const purchases    = hist.filter(h => h.type === 'purchase');\r\n-  const frees        = hist.filter(h => h.type === 'free');\r\n-  const deducts      = hist.filter(h => h.type === 'deduct');\r\n-  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n-  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n-\r\n-  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n-  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n-    const pkg = (packages || []).find(p => {\r\n-      const total = p.coins + p.bonus;\r\n-      return total === h.amount;\r\n-    });\r\n-    return s + (pkg ? pkg.price : 0);\r\n-  }, 0);\r\n-\r\n-  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n-  const newThisWeek  = users.filter(u => {\r\n-    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n-    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n-  }).length;\r\n-\r\n-  // ---- สร้าง HTML ----\r\n-  container.innerHTML = `\r\n-    <!-- ROW 1: ตัวเลขหลัก -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n-            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n-            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n-            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 2: เครดิต -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n-            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 3: กองไพ่ -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 4: Top Decks -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${decksSorted.slice(0, 6).map((d, i) => {\r\n-          const cnt    = (DB.cards[d.id] || []).length;\r\n-          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n-          const pct    = Math.round(cnt / maxCnt * 100);\r\n-          const th     = getTh(d);\r\n-          return `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n-              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;min-width:0;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n-                ${d.name}\r\n-                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n-              </div>\r\n-              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n-                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n-            </div>\r\n-          </div>`;\r\n-        }).join('')}\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 5: Top Users by Credits -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n-              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;\">\r\n-                ${u.username}\r\n-                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n-                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n-              </div>\r\n-              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n-                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n-              ${(u.credits||0).toLocaleString()}\r\n-              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n-            </div>\r\n-          </div>`).join('')}\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n-\r\n-  // update legacy stat elements ด้านบน (stat bar)\r\n-  const elUsers = document.getElementById('stat-users');\r\n-  if (elUsers) elUsers.textContent = totalUsers;\r\n-}\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n-\r\n-  // ดึง history และ users พร้อมกัน\r\n-  const [{ data, error }, { data: users }] = await Promise.all([\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n-    _sb.from('profiles').select('id, username')\r\n-  ]);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  // สร้าง map id → username\r\n-  const userMap = {};\r\n-  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n-\r\n-const TYPE_ICO = {\r\n-  free:             'fi-sr-gift',\r\n-  purchase:         'fi-sr-shopping-cart',\r\n-  purchase_pending: 'fi-sr-clock',       // ✅ เพิ่ม\r\n-  deduct:           'fi-sr-layers',\r\n-  admin:            'fi-sr-shield-check',\r\n-};\r\n-const TYPE_LABEL = {\r\n-  free:             'รับฟรี',\r\n-  purchase:         'ซื้อเครดิต',\r\n-  purchase_pending: 'รอชำระ',            // ✅ เพิ่ม\r\n-  deduct:           'ใช้ไพ่',\r\n-  admin:            'แอดมินเติม',\r\n-};\r\n-\r\n-  const rows = data.map(h => {\r\n-    const isPlus   = h.amount > 0;\r\n-    const typeKey  = h.type || 'deduct';\r\n-    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n-    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n-    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n-    const date     = new Date(h.created_at);\r\n-    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n-    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n-\r\n-    return `\r\n-    <div class=\"hist-row\">\r\n-      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n-        <i class=\"fi ${ico}\"></i>\r\n-      </div>\r\n-      <div class=\"hist-info\">\r\n-        <div class=\"hist-top\">\r\n-          <span class=\"hist-note\">${h.note || label}</span>\r\n-          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n-        </div>\r\n-        <div class=\"hist-sub\">\r\n-          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n-        ${isPlus ? '+' : ''}${h.amount}\r\n-        <span class=\"hist-unit\">เครดิต</span>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-\r\n-  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n-}\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-  if (!container) return;\r\n-  const listEl = document.getElementById('announce-list');\r\n-  if (!listEl) return;\r\n-\r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n-\r\n-  if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const TYPE_ICO = {\r\n-    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n-    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n-  };\r\n-\r\n-  listEl.innerHTML = data.map(a => {\r\n-    const t       = a.type || 'info';\r\n-    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n-    const expires = a.expires_at\r\n-      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n-      : null;\r\n-    return `\r\n-    <div class=\"ann-row\">\r\n-      <div class=\"ann-row-ico ann-ico-${t}\">\r\n-        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n-      </div>\r\n-      <div class=\"ann-row-info\">\r\n-        <div class=\"ann-row-top\">\r\n-          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n-          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n-          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n-            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n-            ${a.active ? 'เผยแพร่' : 'draft'}\r\n-          </span>\r\n-          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n-        </div>\r\n-        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n-        <div class=\"ann-row-sub\">\r\n-          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n-          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"ann-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n-          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-\r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n-  const active  = document.getElementById('ann-active')?.value === '1';\r\n-  const expires = document.getElementById('ann-expires')?.value || null;\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type,\r\n-    show_popup: popup,\r\n-    active,\r\n-    expires_at: expires ? new Date(expires).toISOString() : null,\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  document.getElementById('ann-expires').value = '';\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function toggleAnnounce(id, active) {\r\n-  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-  // update stats-bar (ด้านบนสุดของ admin panel)\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-\r\n-  // render deck list ใน tab กองไพ่\r\n-  const l = document.getElementById('adm-deck-list');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm\" style=\"background:rgba(245,197,24,.08);border:1px solid rgba(245,197,24,.2);color:var(--gold);\" onclick=\"duplicateDeck('${d.id}')\"><i class=\"fi fi-sr-copy-alt\"></i> ก็อปปี้</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n-}\r\n-\r\n-async function toggleDeck(id) {\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  if (!d) return;\r\n-  d.hidden = !d.hidden;\r\n-\r\n-  // บันทึกไป Supabase จริง\r\n-  const { error } = await _sb.from('decks').update({ hidden: d.hidden }).eq('id', id);\r\n-  if (error) {\r\n-    d.hidden = !d.hidden; // rollback\r\n-    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n-    return;\r\n-  }\r\n-\r\n-  renderAdmDecks();\r\n-  renderDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n-}\r\n-\r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value     = d.name;\r\n-    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value    = d.theme || 'default';\r\n-    document.getElementById('df-desc').value     = d.desc  || '';\r\n-    document.getElementById('df-cost').value     = d.cost  || 1;\r\n-    document.getElementById('df-category').value = d.category || '';\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n-\r\n-async function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n-  const theme    = document.getElementById('df-theme').value            || 'default';\r\n-  const desc     = document.getElementById('df-desc').value.trim();\r\n-  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n-  const category = document.getElementById('df-category').value.trim() || '';\r\n-\r\n-  let deck;\r\n-  if (editDeckId) {\r\n-    deck = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(deck, { name, icon, theme, desc, cost, category });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    deck = { id, name, icon, theme, desc, cost, category, hidden: false };\r\n-    DB.decks.push(deck);\r\n-    DB.cards[deck.id] = [];\r\n-  }\r\n-\r\n-  const ok = await saveDeckToDB(deck);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  await initDB();\r\n-  closeDeckForm();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n-}\r\n-\r\n-async function delDeck(id) {\r\n-  const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n-  if (!ok) return;\r\n-\r\n-  // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n-  const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok1){ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  // อัปเดต memory cache\r\n-  DB.decks = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n-}\r\n-\r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n-}\r\n-\r\n-async function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  const cards = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) cards.push({ cat, text });\r\n-  });\r\n-\r\n-  // บันทึกไป Supabase\r\n-  const ok = await saveCardsToDB(id, cards);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  await initDB(); // reload จาก Supabase\r\n-  renderAdmDecks();\r\n-  toast(`บันทึก ${cards.length} ใบแล้ว`, 'success');\r\n-}\r\n-\r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)    || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value              || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value)  || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  DB.settings.deckLayout   = document.getElementById('st-layout').value || 'auto'; // ← เพิ่ม\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n-}\r\n-\r\n-// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url')?.value?.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-\r\n-  API.setSheetUrl(url);\r\n-  toast('กำลังดึงข้อมูล...', 'info');\r\n-\r\n-  API.syncFromSheets(url)\r\n-    .then(({ decks, cards }) => {\r\n-      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      renderDecks();\r\n-    })\r\n-    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n-}\r\n-\r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n-}\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n-\r\n-/* [FIX #6] validate JSON structure ก่อน import */\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      const parsed = JSON.parse(ev.target.result);\r\n-\r\n-      // validate required fields\r\n-      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n-      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n-      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n-\r\n-      // validate deck structure\r\n-      for (const d of parsed.decks) {\r\n-        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n-        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n-      }\r\n-\r\n-      // validate cards structure\r\n-      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n-        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n-        for (const c of cards) {\r\n-          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n-        }\r\n-      }\r\n-\r\n-      DB = parsed;\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch (err) {\r\n-      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n-    }\r\n-  };\r\n-  r.readAsText(f);\r\n-  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n-  e.target.value = '';\r\n-}\r\n-/* ---- SETTINGS TAB ---- */\r\n-async function loadSettingsTab() {\r\n-  // โหลด packages list ในแท็บตั้งค่า\r\n-  if (typeof loadPkgList === 'function') await loadPkgList();\r\n-\r\n-  // sync ค่า settings fields\r\n-  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n-  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n-  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n-  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n-  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n-  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n-  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n-}\r\n-\r\n-/* ---- PACKAGE MANAGER ---- */\r\n-let editPkgId = null;\r\n-\r\n-async function loadPkgList() {\r\n-  const el = document.getElementById('pkg-list');\r\n-  if (!el) return;\r\n-  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('packages')\r\n-    .select('*')\r\n-    .order('sort_order', { ascending: true });\r\n-\r\n-  if (error || !data?.length) {\r\n-    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  el.innerHTML = data.map(p => `\r\n-    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n-      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n-        <i class=\"fi fi-sr-coins\"></i>\r\n-      </div>\r\n-      <div class=\"pkg-row-info\">\r\n-        <div class=\"pkg-row-top\">\r\n-          <span class=\"pkg-row-name\">${p.name}</span>\r\n-          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n-          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n-        </div>\r\n-        <div class=\"pkg-row-detail\">\r\n-          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span>ลำดับ ${p.sort_order}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"pkg-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n-          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n-        </button>\r\n-        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n-          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('');\r\n-}\r\n-\r\n-function openPkgForm(id = null) {\r\n-  editPkgId = id;\r\n-  if (id) {\r\n-    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n-      if (!data) return;\r\n-      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n-      document.getElementById('pf-name').value     = data.name;\r\n-      document.getElementById('pf-coins').value    = data.coins;\r\n-      document.getElementById('pf-bonus').value    = data.bonus;\r\n-      document.getElementById('pf-price').value    = data.price;\r\n-      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n-      document.getElementById('pf-order').value    = data.sort_order;\r\n-      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n-    });\r\n-  } else {\r\n-    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n-    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n-    document.getElementById('pf-bonus').value    = '0';\r\n-    document.getElementById('pf-featured').value = '0';\r\n-    document.getElementById('pf-order').value    = '0';\r\n-    document.getElementById('pf-active').value   = '1';\r\n-  }\r\n-  document.getElementById('pkg-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closePkgForm() {\r\n-  document.getElementById('pkg-form-ov').classList.remove('show');\r\n-  editPkgId = null;\r\n-}\r\n-\r\n-async function savePkg() {\r\n-  const name     = document.getElementById('pf-name').value.trim();\r\n-  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n-  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n-  const price    = parseInt(document.getElementById('pf-price').value);\r\n-  const featured = document.getElementById('pf-featured').value === '1';\r\n-  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n-  const active   = document.getElementById('pf-active').value === '1';\r\n-\r\n-  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n-\r\n-  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n-\r\n-  const { error } = editPkgId\r\n-    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n-    : await _sb.from('packages').insert(payload);\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n-  closePkgForm();\r\n-  loadPkgList();\r\n-  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n-}\r\n-\r\n-async function togglePkg(id, active) {\r\n-  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n-  loadPkgList();\r\n-}\r\n-\r\n-async function deletePkg(id) {\r\n-  const ok = await customConfirm('ลบแพ็กเกจนี้?');\r\n-  if (!ok) return;\r\n-  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบแพ็กเกจแล้ว', 'info');\r\n-  loadPkgList();\r\n-}\r\n-/* =========================================\r\n-   DECK DUPLICATOR\r\n-   ========================================= */\r\n-async function duplicateDeck(srcId) {\r\n-  const src = DB.decks.find(d => d.id === srcId);\r\n-  if (!src) return;\r\n-\r\n-  const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n-  if (!newName?.trim()) return;\r\n-\r\n-  const newId   = 'd' + Date.now();\r\n-  const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n-\r\n-  // บันทึก deck ใหม่\r\n-  const deckOk = await saveDeckToDB(newDeck);\r\n-  if (!deckOk) { toast('เกิดข้อผิดพลาด ก็อปกองไพ่ไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  // ก็อปไพ่ทั้งหมด\r\n-  const srcCards = DB.cards[srcId] || [];\r\n-  if (srcCards.length) {\r\n-    const cardOk = await saveCardsToDB(newId, srcCards);\r\n-    if (!cardOk) { toast('ก็อปกองไพ่สำเร็จ แต่ไพ่บางส่วนอาจไม่ครบ', 'warning'); }\r\n-  }\r\n-\r\n-  await initDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast(`ก็อปปี้ \"${newName.trim()}\" สำเร็จ! ${srcCards.length} ใบ`, 'success');\r\n-}\r\n-\r\n-/* =========================================\r\n-   CARD SEARCH & FILTER\r\n-   ========================================= */\r\n-let _csShowDup = false;\r\n-\r\n-function initCardSearch() {\r\n-  // populate deck dropdown\r\n-  const deckSel = document.getElementById('cs-deck');\r\n-  if (!deckSel) return;\r\n-  deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id; o.textContent = d.name;\r\n-    deckSel.appendChild(o);\r\n-  });\r\n-\r\n-  // populate category dropdown (unique cats across all decks)\r\n-  const catSel = document.getElementById('cs-cat');\r\n-  if (catSel) {\r\n-    const cats = new Set();\r\n-    Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n-    catSel.innerHTML = '<option value=\"\">ทุกหมวด</option>';\r\n-    [...cats].sort().forEach(cat => {\r\n-      const o = document.createElement('option');\r\n-      o.value = cat; o.textContent = cat;\r\n-      catSel.appendChild(o);\r\n-    });\r\n-  }\r\n-\r\n-  _csShowDup = false;\r\n-  const dupBtn = document.getElementById('cs-dup-btn');\r\n-  if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n-\r\n-  runCardSearch();\r\n-}\r\n-\r\n-function toggleDupFilter() {\r\n-  _csShowDup = !_csShowDup;\r\n-  const btn = document.getElementById('cs-dup-btn');\r\n-  if (btn) {\r\n-    btn.style.background = _csShowDup\r\n-      ? 'rgba(245,197,24,.25)' : 'rgba(245,197,24,.08)';\r\n-    btn.style.borderColor = _csShowDup ? 'var(--gold)' : 'rgba(245,197,24,.25)';\r\n-  }\r\n-  runCardSearch();\r\n-}\r\n-\r\n-function runCardSearch() {\r\n-  const query    = (document.getElementById('cs-query')?.value || '').trim().toLowerCase();\r\n-  const deckId   = document.getElementById('cs-deck')?.value  || '';\r\n-  const catFilt  = document.getElementById('cs-cat')?.value   || '';\r\n-  const results  = document.getElementById('cs-results');\r\n-  const summary  = document.getElementById('cs-summary');\r\n-  if (!results) return;\r\n-\r\n-  // รวมไพ่ทั้งหมด + แนบ deck info\r\n-  let all = [];\r\n-  DB.decks.forEach(d => {\r\n-    (DB.cards[d.id] || []).forEach((c, i) => {\r\n-      all.push({ deck: d, cat: c.cat || 'ทั่วไป', text: c.text, idx: i });\r\n-    });\r\n-  });\r\n-\r\n-  // filter by deck\r\n-  if (deckId) all = all.filter(c => c.deck.id === deckId);\r\n-  // filter by category\r\n-  if (catFilt) all = all.filter(c => c.cat === catFilt);\r\n-  // filter by search query\r\n-  if (query) all = all.filter(c => c.text.toLowerCase().includes(query));\r\n-\r\n-  // filter duplicates (same text ใน deck เดียวกัน หรือข้ามกองก็ได้)\r\n-  if (_csShowDup) {\r\n-    const textCount = {};\r\n-    all.forEach(c => { textCount[c.text] = (textCount[c.text] || 0) + 1; });\r\n-    all = all.filter(c => textCount[c.text] > 1);\r\n-  }\r\n-\r\n-  // summary\r\n-  const totalShown = all.length;\r\n-  summary.textContent = totalShown\r\n-    ? `พบ ${totalShown} ใบ${_csShowDup ? ' (ข้อความซ้ำ)' : ''}`\r\n-    : 'ไม่พบไพ่ที่ตรงกัน';\r\n-\r\n-  if (!all.length) {\r\n-    results.innerHTML = `\r\n-      <div style=\"text-align:center;padding:40px;color:var(--text3);\">\r\n-        <i class=\"fi fi-sr-search\" style=\"font-size:2rem;opacity:.3;\"></i>\r\n-        <div style=\"margin-top:10px;font-size:.85rem;\">ไม่พบไพ่</div>\r\n-      </div>`;\r\n-    return;\r\n-  }\r\n-\r\n-  // highlight matching text\r\n-  function hl(text) {\r\n-    if (!query) return text;\r\n-    const re = new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')})`, 'gi');\r\n-    return text.replace(re, '<mark style=\"background:rgba(229,9,20,.3);color:#fff;border-radius:2px;padding:0 2px;\">$1</mark>');\r\n-  }\r\n-\r\n-  results.innerHTML = all.map(c => {\r\n-    const th = getTh(c.deck);\r\n-    return `\r\n-    <div style=\"\r\n-      display:flex;align-items:center;gap:12px;\r\n-      background:#161616;border:1px solid #1e1e1e;\r\n-      border-radius:10px;padding:10px 14px;\r\n-      transition:border-color .2s;\r\n-    \" onmouseover=\"this.style.borderColor='#2a2a2a'\" onmouseout=\"this.style.borderColor='#1e1e1e'\">\r\n-      <div class=\"drow-ico ${th}\" style=\"width:32px;height:32px;font-size:.78rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n-        <i class=\"fi ${c.deck.icon || 'fi-sr-layers'}\"></i>\r\n-      </div>\r\n-      <div style=\"flex:1;min-width:0;\">\r\n-        <div style=\"font-size:.88rem;line-height:1.4;\">${hl(c.text)}</div>\r\n-        <div style=\"display:flex;align-items:center;gap:8px;margin-top:4px;\">\r\n-          <span style=\"font-size:.72rem;color:var(--text3);\">${c.deck.name}</span>\r\n-          <span style=\"color:#333;font-size:.6rem;\">·</span>\r\n-          <span style=\"font-size:.7rem;color:var(--text2);background:#1a1a1a;border:1px solid #2a2a2a;\r\n-                       border-radius:20px;padding:1px 8px;\">${c.cat}</span>\r\n-        </div>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-\r\n-async function confirmReset() {\r\n-  const ok = await customConfirm('รีเซ็ตข้อมูลทั้งหมด?');\r\n-  if (!ok) return;\r\n-  resetDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  toast('รีเซ็ตแล้ว', 'info');\r\n-}\r\n-\r\n-/* =========================================\r\n-   CUSTOM CONFIRM / PROMPT DIALOGS\r\n-   ========================================= */\r\n-function customConfirm(message) {\r\n-  return new Promise(resolve => {\r\n-    document.getElementById('custom-confirm-ov')?.remove();\r\n-    const ov = document.createElement('div');\r\n-    ov.id = 'custom-confirm-ov';\r\n-    ov.style.cssText = `\r\n-      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n-      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n-      backdrop-filter:blur(4px);\r\n-    `;\r\n-    ov.innerHTML = `\r\n-      <div style=\"\r\n-        background:linear-gradient(145deg,#1c1c1c,#111);\r\n-        border:1px solid #2a2a2a;border-radius:16px;\r\n-        padding:28px 28px 22px;max-width:320px;width:90%;\r\n-        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n-      \">\r\n-        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-          border-radius:16px 16px 0 0;\"></div>\r\n-        <div style=\"font-family:'Kanit',sans-serif;font-size:1rem;font-weight:600;\r\n-          margin-bottom:22px;line-height:1.5;color:#fff;\" id=\"cc-msg\"></div>\r\n-        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n-          <button id=\"cc-cancel\" style=\"\r\n-            background:transparent;border:1px solid #333;color:var(--text2);\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-          \">ยกเลิก</button>\r\n-          <button id=\"cc-ok\" style=\"\r\n-            background:var(--red);border:none;color:#fff;\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n-          \">ยืนยัน</button>\r\n-        </div>\r\n-      </div>\r\n-    `;\r\n-    ov.querySelector('#cc-msg').textContent = message;\r\n-    ov.querySelector('#cc-ok').onclick     = () => { ov.remove(); resolve(true);  };\r\n-    ov.querySelector('#cc-cancel').onclick = () => { ov.remove(); resolve(false); };\r\n-    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(false); } });\r\n-    document.body.appendChild(ov);\r\n-  });\r\n-}\r\n-\r\n-function customPrompt(message, defaultValue = '') {\r\n-  return new Promise(resolve => {\r\n-    document.getElementById('custom-prompt-ov')?.remove();\r\n-    const ov = document.createElement('div');\r\n-    ov.id = 'custom-prompt-ov';\r\n-    ov.style.cssText = `\r\n-      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n-      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n-      backdrop-filter:blur(4px);\r\n-    `;\r\n-    ov.innerHTML = `\r\n-      <div style=\"\r\n-        background:linear-gradient(145deg,#1c1c1c,#111);\r\n-        border:1px solid #2a2a2a;border-radius:16px;\r\n-        padding:28px 28px 22px;max-width:340px;width:90%;\r\n-        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n-      \">\r\n-        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-          border-radius:16px 16px 0 0;\"></div>\r\n-        <div style=\"font-family:'Kanit',sans-serif;font-size:.95rem;font-weight:600;\r\n-          margin-bottom:14px;color:#fff;\" id=\"cp-msg\"></div>\r\n-        <input id=\"cp-input\" type=\"text\" style=\"\r\n-          width:100%;background:#151515;border:1px solid #2a2a2a;border-radius:8px;\r\n-          color:#fff;padding:10px 12px;font-family:'Kanit',sans-serif;font-size:.95rem;\r\n-          outline:none;box-sizing:border-box;margin-bottom:18px;\r\n-        \">\r\n-        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n-          <button id=\"cp-cancel\" style=\"\r\n-            background:transparent;border:1px solid #333;color:var(--text2);\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-          \">ยกเลิก</button>\r\n-          <button id=\"cp-ok\" style=\"\r\n-            background:var(--red);border:none;color:#fff;\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n-          \">ตกลง</button>\r\n-        </div>\r\n-      </div>\r\n-    `;\r\n-    ov.querySelector('#cp-msg').textContent = message;\r\n-    const inp = ov.querySelector('#cp-input');\r\n-    inp.value = defaultValue;\r\n-    const confirm = () => { ov.remove(); resolve(inp.value.trim() || null); };\r\n-    ov.querySelector('#cp-ok').onclick     = confirm;\r\n-    ov.querySelector('#cp-cancel').onclick = () => { ov.remove(); resolve(null); };\r\n-    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(null); } });\r\n-    document.body.appendChild(ov);\r\n-    setTimeout(() => { inp.focus(); inp.select(); }, 50);\r\n-    inp.addEventListener('keydown', e => {\r\n-      if (e.key === 'Enter') confirm();\r\n-      if (e.key === 'Escape') { ov.remove(); resolve(null); }\r\n-    });\r\n-  });\r\n-}\r\n-\r\n-/* =========================================\r\n-   BULK PASTE & MULTI ADD\r\n-   ========================================= */\r\n-function addMultiRows(n = 5) {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  for (let i = 0; i < n; i++) addCRow();\r\n-  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n-}\r\n-\r\n-function openBulkPaste() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-\r\n-  document.getElementById('bulk-paste-ov')?.remove();\r\n-  const ov = document.createElement('div');\r\n-  ov.id = 'bulk-paste-ov';\r\n-  ov.style.cssText = `\r\n-    position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n-    z-index:700;display:flex;align-items:center;justify-content:center;\r\n-    backdrop-filter:blur(6px);\r\n-  `;\r\n-  ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n-  ov.innerHTML = `\r\n-    <div style=\"\r\n-      background:linear-gradient(145deg,#181818,#101010);\r\n-      border:1px solid #2a2a2a;border-radius:20px;\r\n-      padding:28px 24px;max-width:520px;width:94%;position:relative;\r\n-    \">\r\n-      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-        border-radius:20px 20px 0 0;\"></div>\r\n-      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n-        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n-      </div>\r\n-      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n-        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ:\r\n-        <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n-        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n-      </div>\r\n-      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n-        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n-        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\" style=\"\r\n-          flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n-          color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;outline:none;\">\r\n-      </div>\r\n-      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n-        width:100%;height:220px;background:#0d0d0d;border:1px solid #2a2a2a;border-radius:10px;\r\n-        color:#fff;padding:12px;font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-        line-height:1.7;resize:vertical;outline:none;box-sizing:border-box;\r\n-      \"></textarea>\r\n-      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n-      <div style=\"display:flex;gap:10px;\">\r\n-        <button onclick=\"closeBulkPaste()\" style=\"\r\n-          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-        \">ยกเลิก</button>\r\n-        <button onclick=\"applyBulkPaste()\" style=\"\r\n-          flex:2;background:var(--red);border:none;color:#fff;\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n-          display:flex;align-items:center;justify-content:center;gap:8px;\r\n-        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n-  document.body.appendChild(ov);\r\n-  const ta = ov.querySelector('#bulk-textarea');\r\n-  const pc = ov.querySelector('#bulk-preview-count');\r\n-  ta.addEventListener('input', () => {\r\n-    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n-    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n-  });\r\n-  ta.focus();\r\n-}\r\n-\r\n-function closeBulkPaste() {\r\n-  document.getElementById('bulk-paste-ov')?.remove();\r\n-}\r\n-\r\n-function applyBulkPaste() {\r\n-  const ta     = document.getElementById('bulk-textarea');\r\n-  const defCat = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n-  const lines  = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n-  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n-  lines.forEach(line => {\r\n-    if (line.includes('|')) {\r\n-      const [cat, ...rest] = line.split('|');\r\n-      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n-    } else {\r\n-      addCRow(line, defCat);\r\n-    }\r\n-  });\r\n-  closeBulkPaste();\r\n-  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n-}\n-/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n-\r\n-let editDeckId = null;\r\n-\r\n-/* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');\r\n-\r\n   if (name === 'users')    loadUserTab();\r\n   if (name === 'stats')    loadStatsTab();\r\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n@@ -1509,14 +289,20 @@\n   const userMap = {};\r\n   (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n \r\n   const TYPE_ICO = {\r\n-    free: 'fi-sr-gift', purchase: 'fi-sr-shopping-cart',\r\n-    deduct: 'fi-sr-layers', admin: 'fi-sr-shield-check',\r\n+    free:             'fi-sr-gift',\r\n+    purchase:         'fi-sr-shopping-cart',\r\n+    purchase_pending: 'fi-sr-clock',       // ✅ เพิ่ม\r\n+    deduct:           'fi-sr-layers',\r\n+    admin:            'fi-sr-shield-check',\r\n   };\r\n   const TYPE_LABEL = {\r\n-    free: 'รับฟรี', purchase: 'ซื้อเครดิต',\r\n-    deduct: 'ใช้ไพ่', admin: 'แอดมินเติม',\r\n+    free:             'รับฟรี',\r\n+    purchase:         'ซื้อเครดิต',\r\n+    purchase_pending: 'รอชำระ',            // ✅ เพิ่ม\r\n+    deduct:           'ใช้ไพ่',\r\n+    admin:            'แอดมินเติม',\r\n   };\r\n \r\n   const rows = data.map(h => {\r\n     const isPlus   = h.amount > 0;\r\n"
                },
                {
                    "date": 1775696553054,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -19,10 +19,11 @@\n   if (name === 'history')  loadHistoryTab();\r\n   if (name === 'announce') loadAnnounceTab();\r\n   if (name === 'settings') { loadSettingsTab(); }\r\n   if (name === 'cards')    { renderAdmSel(); }\r\n-  if (name === 'search')   { initCardSearch(); }\r\n-  if (name === 'sheet')    {} // static content\r\n+if (name === 'decks')    { renderAdmDecks(); renderAdmSel(); }  // ← เพิ่ม\r\n+if (name === 'search')   { initCardSearch(); }\r\n+if (name === 'sheet')    {} // static content\r\n }\r\n \r\n /* ---- TAB LOADERS ---- */\r\n async function loadUserTab() {\r\n"
                },
                {
                    "date": 1775701021344,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -478,8 +478,12 @@\n         </div>\r\n       </div>\r\n     </div>\r\n     <div class=\"drow-acts\">\r\n+        <div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n+    <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n+    <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n+  </div>\r\n       <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n       </button>\r\n"
                },
                {
                    "date": 1775701039856,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -479,11 +479,11 @@\n       </div>\r\n     </div>\r\n     <div class=\"drow-acts\">\r\n         <div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n-    <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n-    <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n-  </div>\r\n+          <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n+          <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n+        </div>\r\n       <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n       </button>\r\n@@ -493,9 +493,35 @@\n     </div>`;\r\n     l.appendChild(r);\r\n   });\r\n }\r\n+async function moveDeck(id, dir) {\r\n+  const idx = DB.decks.findIndex(d => d.id === id);\r\n+  const swapIdx = idx + dir;\r\n+  if (idx < 0 || swapIdx < 0 || swapIdx >= DB.decks.length) return;\r\n \r\n+  const a = DB.decks[idx];\r\n+  const b = DB.decks[swapIdx];\r\n+  const tmpOrder = a.sort_order ?? idx;\r\n+  a.sort_order = b.sort_order ?? swapIdx;\r\n+  b.sort_order = tmpOrder;\r\n+\r\n+  DB.decks[idx]     = b;\r\n+  DB.decks[swapIdx] = a;\r\n+\r\n+  const [r1, r2] = await Promise.all([\r\n+    _sb.from('decks').update({ sort_order: a.sort_order }).eq('id', a.id),\r\n+    _sb.from('decks').update({ sort_order: b.sort_order }).eq('id', b.id),\r\n+  ]);\r\n+\r\n+  if (r1.error || r2.error) {\r\n+    toast('บันทึกลำดับไม่สำเร็จ', 'error');\r\n+    await initDB();\r\n+  }\r\n+\r\n+  renderAdmDecks();\r\n+  renderDecks();\r\n+}\r\n async function toggleDeck(id) {\r\n   const d = DB.decks.find(x => x.id === id);\r\n   if (!d) return;\r\n   d.hidden = !d.hidden;\r\n"
                },
                {
                    "date": 1775701103462,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -478,12 +478,12 @@\n         </div>\r\n       </div>\r\n     </div>\r\n     <div class=\"drow-acts\">\r\n-        <div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n-          <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n-          <button class=\"btn-sm\" style=\"padding:4px 8px;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n-        </div>\r\n+<div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n+  <button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(229,9,20,.15);border:1px solid rgba(229,9,20,.3);color:var(--red);\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n+  <button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(229,9,20,.15);border:1px solid rgba(229,9,20,.3);color:var(--red);\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n+</div>\r\n       <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n       </button>\r\n"
                },
                {
                    "date": 1775701170573,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -479,10 +479,10 @@\n       </div>\r\n     </div>\r\n     <div class=\"drow-acts\">\r\n <div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n-  <button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(229,9,20,.15);border:1px solid rgba(229,9,20,.3);color:var(--red);\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n-  <button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(229,9,20,.15);border:1px solid rgba(229,9,20,.3);color:var(--red);\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n+<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n+<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n </div>\r\n       <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n         <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n         ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n"
                },
                {
                    "date": 1775703315499,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -549,8 +549,86 @@\n     s.appendChild(o);\r\n   });\r\n }\r\n \r\n+const THEME_PRESETS = [\r\n+  { name:'fire',            bg:'linear-gradient(135deg,rgba(229,57,53,.35),rgba(183,28,28,.2))',   color:'#ff5252' },\r\n+  { name:'magic',           bg:'linear-gradient(135deg,rgba(156,39,176,.35),rgba(106,27,154,.2))', color:'#e040fb' },\r\n+  { name:'skull',           bg:'linear-gradient(135deg,rgba(229,9,20,.35),rgba(150,0,10,.2))',     color:'#ff1744' },\r\n+  { name:'heart',           bg:'linear-gradient(135deg,rgba(233,30,99,.35),rgba(173,20,87,.2))',   color:'#ff4081' },\r\n+  { name:'star',            bg:'linear-gradient(135deg,rgba(245,197,24,.35),rgba(198,145,0,.2))',  color:'#ffd740' },\r\n+  { name:'bolt',            bg:'linear-gradient(135deg,rgba(255,152,0,.35),rgba(230,81,0,.2))',    color:'#ffab40' },\r\n+  { name:'ocean',           bg:'linear-gradient(135deg,rgba(33,150,243,.35),rgba(13,71,161,.2))',  color:'#40c4ff' },\r\n+  { name:'emerald',         bg:'linear-gradient(135deg,rgba(0,188,140,.35),rgba(0,121,107,.2))',   color:'#64ffda' },\r\n+  { name:'lavender',        bg:'linear-gradient(135deg,rgba(149,117,205,.35),rgba(94,53,177,.2))', color:'#ce93d8' },\r\n+  { name:'silver',          bg:'linear-gradient(135deg,rgba(176,190,197,.2),rgba(96,125,139,.15))',color:'#eceff1' },\r\n+  { name:'rose',            bg:'linear-gradient(135deg,rgba(236,64,122,.35),rgba(173,20,87,.2))',  color:'#ff80ab' },\r\n+  { name:'amber',           bg:'linear-gradient(135deg,rgba(255,193,7,.35),rgba(255,111,0,.2))',   color:'#ffe57f' },\r\n+  { name:'default',         bg:'linear-gradient(135deg,rgba(255,255,255,.08),rgba(255,255,255,.04))', color:'#cfd8dc' },\r\n+  { name:'gold_amber',      bg:'linear-gradient(135deg,rgba(245,197,24,.55),rgba(255,120,0,.55))', color:'#ffd740' },\r\n+  { name:'emerald_teal',    bg:'linear-gradient(135deg,rgba(0,200,100,.5),rgba(0,230,200,.5))',    color:'#69ffda' },\r\n+  { name:'rose_heart',      bg:'linear-gradient(135deg,rgba(255,60,100,.5),rgba(220,20,80,.5))',   color:'#ff80ab' },\r\n+  { name:'ocean_lavender',  bg:'linear-gradient(135deg,rgba(30,140,255,.5),rgba(140,90,220,.5))',  color:'#90caff' },\r\n+  { name:'fire_amber',      bg:'linear-gradient(135deg,rgba(230,50,40,.55),rgba(255,140,0,.55))',  color:'#ff7043' },\r\n+  { name:'magic_lavender',  bg:'linear-gradient(135deg,rgba(160,30,190,.55),rgba(140,100,220,.55))',color:'#e040fb'},\r\n+  { name:'bolt_gold',       bg:'linear-gradient(135deg,rgba(255,140,0,.55),rgba(240,200,0,.55))',  color:'#ffcc02' },\r\n+  { name:'ocean_emerald',   bg:'linear-gradient(135deg,rgba(20,140,255,.5),rgba(0,200,160,.5))',   color:'#40c4ff' },\r\n+];\r\n+\r\n+function _initThemePicker(currentTheme) {\r\n+  const swatches = document.getElementById('df-theme-swatches');\r\n+  if (!swatches) return;\r\n+  swatches.innerHTML = '';\r\n+\r\n+  THEME_PRESETS.forEach(t => {\r\n+    const sw = document.createElement('div');\r\n+    sw.title = t.name;\r\n+    sw.style.cssText = `\r\n+      width:32px;height:32px;border-radius:8px;cursor:pointer;\r\n+      background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n+      transition:.2s;flex-shrink:0;\r\n+    `;\r\n+    sw.onclick = () => _selectTheme(t.name, t.bg, t.color);\r\n+    swatches.appendChild(sw);\r\n+  });\r\n+\r\n+  // set preview เริ่มต้น\r\n+  const cur = THEME_PRESETS.find(t => t.name === currentTheme) || THEME_PRESETS[0];\r\n+  _applyThemePreview(cur.bg, cur.color);\r\n+  document.getElementById('df-theme').value = currentTheme;\r\n+\r\n+  // custom color pickers\r\n+  document.getElementById('df-color1').oninput =\r\n+  document.getElementById('df-color2').oninput = () => {\r\n+    const c1 = document.getElementById('df-color1').value;\r\n+    const c2 = document.getElementById('df-color2').value;\r\n+    const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n+    _applyThemePreview(bg, c1);\r\n+    document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n+    // deselect swatches\r\n+    swatches.querySelectorAll('div').forEach(s => s.style.borderColor = 'transparent');\r\n+  };\r\n+}\r\n+\r\n+function _selectTheme(name, bg, color) {\r\n+  document.getElementById('df-theme').value = name;\r\n+  _applyThemePreview(bg, color);\r\n+  // highlight swatch ที่เลือก\r\n+  document.getElementById('df-theme-swatches').querySelectorAll('div').forEach((s,i) => {\r\n+    s.style.borderColor = THEME_PRESETS[i]?.name === name ? '#fff' : 'transparent';\r\n+  });\r\n+}\r\n+\r\n+function _applyThemePreview(bg, color) {\r\n+  const prev = document.getElementById('df-theme-preview');\r\n+  const ico  = document.getElementById('df-theme-preview-ico');\r\n+  if (!prev || !ico) return;\r\n+  prev.style.background = bg;\r\n+  ico.style.color = color;\r\n+  const iconVal = document.getElementById('df-icon')?.value || 'fi-sr-flame';\r\n+  ico.className = 'fi ' + iconVal;\r\n+}\r\n+\r\n /* ---- DECK FORM ---- */\r\n function openDeckForm(id) {\r\n   editDeckId = id || null;\r\n   if (id) {\r\n"
                },
                {
                    "date": 1775703336491,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -647,8 +647,18 @@\n     document.getElementById('df-theme').value = 'fire';\r\n     document.getElementById('df-cost').value  = '1';\r\n   }\r\n   document.getElementById('deck-form-ov').classList.add('show');\r\n+  // ใส่ต่อจาก document.getElementById('deck-form-ov').classList.add('show');\r\n+_initThemePicker(document.getElementById('df-theme').value || 'fire');\r\n+\r\n+// sync preview เมื่อเปลี่ยน icon\r\n+document.getElementById('df-icon').oninput = () => {\r\n+  _applyThemePreview(\r\n+    document.getElementById('df-theme-preview').style.background,\r\n+    document.getElementById('df-theme-preview-ico').style.color\r\n+  );\r\n+};\r\n }\r\n \r\n function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n function editDeck(id)    { openDeckForm(id); }\r\n"
                },
                {
                    "date": 1775703830519,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -617,18 +617,19 @@\n     s.style.borderColor = THEME_PRESETS[i]?.name === name ? '#fff' : 'transparent';\r\n   });\r\n }\r\n \r\n-function _applyThemePreview(bg, color) {\r\n+function _applyThemePreview(bg, color, name = '') {\r\n   const prev = document.getElementById('df-theme-preview');\r\n   const ico  = document.getElementById('df-theme-preview-ico');\r\n+  const nm   = document.getElementById('df-theme-preview-name');\r\n   if (!prev || !ico) return;\r\n   prev.style.background = bg;\r\n   ico.style.color = color;\r\n+  if (nm) nm.textContent = name.toUpperCase();\r\n   const iconVal = document.getElementById('df-icon')?.value || 'fi-sr-flame';\r\n   ico.className = 'fi ' + iconVal;\r\n }\r\n-\r\n /* ---- DECK FORM ---- */\r\n function openDeckForm(id) {\r\n   editDeckId = id || null;\r\n   if (id) {\r\n"
                },
                {
                    "date": 1775703842416,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -610,15 +610,14 @@\n }\r\n \r\n function _selectTheme(name, bg, color) {\r\n   document.getElementById('df-theme').value = name;\r\n-  _applyThemePreview(bg, color);\r\n-  // highlight swatch ที่เลือก\r\n+  _applyThemePreview(bg, color, name.replace(/_/g,' '));\r\n   document.getElementById('df-theme-swatches').querySelectorAll('div').forEach((s,i) => {\r\n     s.style.borderColor = THEME_PRESETS[i]?.name === name ? '#fff' : 'transparent';\r\n+    s.style.transform   = THEME_PRESETS[i]?.name === name ? 'scale(1.2)' : 'scale(1)';\r\n   });\r\n }\r\n-\r\n function _applyThemePreview(bg, color, name = '') {\r\n   const prev = document.getElementById('df-theme-preview');\r\n   const ico  = document.getElementById('df-theme-preview-ico');\r\n   const nm   = document.getElementById('df-theme-preview-name');\r\n"
                },
                {
                    "date": 1775703873039,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -596,19 +596,20 @@\n   _applyThemePreview(cur.bg, cur.color);\r\n   document.getElementById('df-theme').value = currentTheme;\r\n \r\n   // custom color pickers\r\n-  document.getElementById('df-color1').oninput =\r\n-  document.getElementById('df-color2').oninput = () => {\r\n-    const c1 = document.getElementById('df-color1').value;\r\n-    const c2 = document.getElementById('df-color2').value;\r\n-    const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n-    _applyThemePreview(bg, c1);\r\n-    document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n-    // deselect swatches\r\n-    swatches.querySelectorAll('div').forEach(s => s.style.borderColor = 'transparent');\r\n-  };\r\n-}\r\n+document.getElementById('df-color1').oninput =\r\n+document.getElementById('df-color2').oninput = () => {\r\n+  const c1 = document.getElementById('df-color1').value;\r\n+  const c2 = document.getElementById('df-color2').value;\r\n+  const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n+  _applyThemePreview(bg, c1, 'custom');\r\n+  document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n+  const cp = document.getElementById('df-custom-preview');\r\n+  if (cp) cp.style.background = bg;\r\n+  document.getElementById('df-theme-swatches').querySelectorAll('div')\r\n+    .forEach(s => { s.style.borderColor='transparent'; s.style.transform='scale(1)'; });\r\n+};\r\n \r\n function _selectTheme(name, bg, color) {\r\n   document.getElementById('df-theme').value = name;\r\n   _applyThemePreview(bg, color, name.replace(/_/g,' '));\r\n"
                },
                {
                    "date": 1775703894762,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -581,13 +581,15 @@\n \r\n   THEME_PRESETS.forEach(t => {\r\n     const sw = document.createElement('div');\r\n     sw.title = t.name;\r\n-    sw.style.cssText = `\r\n-      width:32px;height:32px;border-radius:8px;cursor:pointer;\r\n-      background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n-      transition:.2s;flex-shrink:0;\r\n-    `;\r\n+sw.style.cssText = `\r\n+  width:36px;height:36px;border-radius:10px;cursor:pointer;\r\n+  background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n+  transition:all .2s;flex-shrink:0;\r\n+  transform:${currentTheme===t.name?'scale(1.2)':'scale(1)'};\r\n+  box-shadow:0 2px 8px rgba(0,0,0,.4);\r\n+`;\r\n     sw.onclick = () => _selectTheme(t.name, t.bg, t.color);\r\n     swatches.appendChild(sw);\r\n   });\r\n \r\n@@ -608,8 +610,9 @@\n   if (cp) cp.style.background = bg;\r\n   document.getElementById('df-theme-swatches').querySelectorAll('div')\r\n     .forEach(s => { s.style.borderColor='transparent'; s.style.transform='scale(1)'; });\r\n };\r\n+}\r\n \r\n function _selectTheme(name, bg, color) {\r\n   document.getElementById('df-theme').value = name;\r\n   _applyThemePreview(bg, color, name.replace(/_/g,' '));\r\n"
                },
                {
                    "date": 1775746381309,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,1349 @@\n+/* =========================================\r\n+   admin.js — Admin Panel Logic\r\n+   ========================================= */\r\n+\r\n+let editDeckId = null;\r\n+\r\n+/* ---- TABS ---- */\r\n+function switchTab(name, idx) {\r\n+  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n+  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n+  const target = document.getElementById('tc-' + name);\r\n+  if (target) target.classList.add('act');\r\n+\r\n+  // อัปเดต URL ให้ต่างกันแต่ละ tab\r\n+  history.replaceState({}, '', '#admin/' + name);\r\n+\r\n+  if (name === 'users')    loadUserTab();\r\n+  if (name === 'stats')    loadStatsTab();\r\n+  if (name === 'history')  loadHistoryTab();\r\n+  if (name === 'announce') loadAnnounceTab();\r\n+  if (name === 'settings') { loadSettingsTab(); }\r\n+  if (name === 'cards')    { renderAdmSel(); }\r\n+if (name === 'decks')    { renderAdmDecks(); renderAdmSel(); }  // ← เพิ่ม\r\n+if (name === 'search')   { initCardSearch(); }\r\n+if (name === 'sheet')    {} // static content\r\n+}\r\n+\r\n+/* ---- TAB LOADERS ---- */\r\n+async function loadUserTab() {\r\n+  const container = document.getElementById('tc-users');\r\n+  if (!container) return;\r\n+  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n+  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n+}\r\n+\r\n+async function loadStatsTab() {\r\n+  const container = document.getElementById('tc-stats');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n+\r\n+  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n+  const [\r\n+    users,\r\n+    { data: history },\r\n+    { data: packages }\r\n+  ] = await Promise.all([\r\n+    API.getAllUsers(),\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n+    _sb.from('packages').select('*').eq('is_active', true)\r\n+  ]);\r\n+\r\n+  const now        = new Date();\r\n+  const weekAgo    = new Date(now - 7  * 86400000);\r\n+  const monthAgo   = new Date(now - 30 * 86400000);\r\n+\r\n+  // ---- ผู้ใช้ ----\r\n+  const totalUsers    = users.length;\r\n+  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n+  const adminUsers    = users.filter(u => u.is_admin).length;\r\n+  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n+  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n+  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n+  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n+\r\n+  // ---- กองไพ่ & ไพ่ ----\r\n+  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n+  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n+  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n+\r\n+  // top deck by card count\r\n+  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n+\r\n+  // ---- เครดิต / ประวัติ ----\r\n+  const hist         = history || [];\r\n+  const purchases    = hist.filter(h => h.type === 'purchase');\r\n+  const frees        = hist.filter(h => h.type === 'free');\r\n+  const deducts      = hist.filter(h => h.type === 'deduct');\r\n+  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n+  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n+\r\n+  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n+  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n+    const pkg = (packages || []).find(p => {\r\n+      const total = p.coins + p.bonus;\r\n+      return total === h.amount;\r\n+    });\r\n+    return s + (pkg ? pkg.price : 0);\r\n+  }, 0);\r\n+\r\n+  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n+  const newThisWeek  = users.filter(u => {\r\n+    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n+    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n+  }).length;\r\n+\r\n+  // ---- สร้าง HTML ----\r\n+  container.innerHTML = `\r\n+    <!-- ROW 1: ตัวเลขหลัก -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n+            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n+            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n+            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 2: เครดิต -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n+            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 3: กองไพ่ -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n+      <div class=\"stats-grid-4\">\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n+            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n+          </div>\r\n+        </div>\r\n+        <div class=\"stat-card2\">\r\n+          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n+          <div class=\"sc2-body\">\r\n+            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n+            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n+          </div>\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 4: Top Decks -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${decksSorted.slice(0, 6).map((d, i) => {\r\n+          const cnt    = (DB.cards[d.id] || []).length;\r\n+          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n+          const pct    = Math.round(cnt / maxCnt * 100);\r\n+          const th     = getTh(d);\r\n+          return `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;min-width:0;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n+                ${d.name}\r\n+                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n+              </div>\r\n+              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n+                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n+            </div>\r\n+            <div style=\"text-align:right;flex-shrink:0;\">\r\n+              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n+              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n+            </div>\r\n+          </div>`;\r\n+        }).join('')}\r\n+      </div>\r\n+    </div>\r\n+\r\n+    <!-- ROW 5: Top Users by Credits -->\r\n+    <div class=\"sheet-sec\">\r\n+      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n+      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n+        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n+          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n+            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n+            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n+              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n+            </div>\r\n+            <div style=\"flex:1;\">\r\n+              <div style=\"font-weight:700;font-size:.88rem;\">\r\n+                ${u.username}\r\n+                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n+                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n+              </div>\r\n+              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n+                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n+              </div>\r\n+            </div>\r\n+            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n+              ${(u.credits||0).toLocaleString()}\r\n+              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n+            </div>\r\n+          </div>`).join('')}\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+\r\n+  // update legacy stat elements ด้านบน (stat bar)\r\n+  const elUsers = document.getElementById('stat-users');\r\n+  if (elUsers) elUsers.textContent = totalUsers;\r\n+}\r\n+async function loadHistoryTab() {\r\n+  const container = document.getElementById('tc-history');\r\n+  if (!container) return;\r\n+  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n+\r\n+  // ดึง history และ users พร้อมกัน\r\n+  const [{ data, error }, { data: users }] = await Promise.all([\r\n+    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n+    _sb.from('profiles').select('id, username')\r\n+  ]);\r\n+\r\n+  if (error || !data?.length) {\r\n+    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  // สร้าง map id → username\r\n+  const userMap = {};\r\n+  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n+\r\n+  const TYPE_ICO = {\r\n+    free:             'fi-sr-gift',\r\n+    purchase:         'fi-sr-shopping-cart',\r\n+    purchase_pending: 'fi-sr-clock',       // ✅ เพิ่ม\r\n+    deduct:           'fi-sr-layers',\r\n+    admin:            'fi-sr-shield-check',\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    free:             'รับฟรี',\r\n+    purchase:         'ซื้อเครดิต',\r\n+    purchase_pending: 'รอชำระ',            // ✅ เพิ่ม\r\n+    deduct:           'ใช้ไพ่',\r\n+    admin:            'แอดมินเติม',\r\n+  };\r\n+\r\n+  const rows = data.map(h => {\r\n+    const isPlus   = h.amount > 0;\r\n+    const typeKey  = h.type || 'deduct';\r\n+    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n+    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n+    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n+    const date     = new Date(h.created_at);\r\n+    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n+    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n+\r\n+    return `\r\n+    <div class=\"hist-row\">\r\n+      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n+        <i class=\"fi ${ico}\"></i>\r\n+      </div>\r\n+      <div class=\"hist-info\">\r\n+        <div class=\"hist-top\">\r\n+          <span class=\"hist-note\">${h.note || label}</span>\r\n+          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n+        </div>\r\n+        <div class=\"hist-sub\">\r\n+          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n+          <span class=\"hist-sep\">·</span>\r\n+          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n+        ${isPlus ? '+' : ''}${h.amount}\r\n+        <span class=\"hist-unit\">เครดิต</span>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+\r\n+  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n+}\r\n+async function loadAnnounceTab() {\r\n+  const container = document.getElementById('tc-announce');\r\n+  if (!container) return;\r\n+  const listEl = document.getElementById('announce-list');\r\n+  if (!listEl) return;\r\n+\r\n+  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('announcements')\r\n+    .select('*')\r\n+    .order('created_at', { ascending: false });\r\n+\r\n+  if (error || !data?.length) {\r\n+    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  const TYPE_ICO = {\r\n+    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n+    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n+  };\r\n+  const TYPE_LABEL = {\r\n+    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n+  };\r\n+\r\n+  listEl.innerHTML = data.map(a => {\r\n+    const t       = a.type || 'info';\r\n+    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n+    const expires = a.expires_at\r\n+      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n+      : null;\r\n+    return `\r\n+    <div class=\"ann-row\">\r\n+      <div class=\"ann-row-ico ann-ico-${t}\">\r\n+        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n+      </div>\r\n+      <div class=\"ann-row-info\">\r\n+        <div class=\"ann-row-top\">\r\n+          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n+          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n+          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n+            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n+            ${a.active ? 'เผยแพร่' : 'draft'}\r\n+          </span>\r\n+          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n+        </div>\r\n+        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n+        <div class=\"ann-row-sub\">\r\n+          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n+          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"ann-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n+          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n+          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function createAnnounce() {\r\n+  const title   = document.getElementById('ann-title')?.value.trim();\r\n+  const message = document.getElementById('ann-message')?.value.trim();\r\n+  const type    = document.getElementById('ann-type')?.value || 'info';\r\n+  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n+  const active  = document.getElementById('ann-active')?.value === '1';\r\n+  const expires = document.getElementById('ann-expires')?.value || null;\r\n+\r\n+  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n+\r\n+  const { error } = await _sb.from('announcements').insert({\r\n+    title, message, type,\r\n+    show_popup: popup,\r\n+    active,\r\n+    expires_at: expires ? new Date(expires).toISOString() : null,\r\n+  });\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast('เพิ่มประกาศแล้ว', 'success');\r\n+  document.getElementById('ann-title').value   = '';\r\n+  document.getElementById('ann-message').value = '';\r\n+  document.getElementById('ann-expires').value = '';\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function deleteAnnounce(id) {\r\n+  if (!confirm('ลบประกาศนี้?')) return;\r\n+  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบประกาศแล้ว', 'info');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+async function toggleAnnounce(id, active) {\r\n+  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n+  loadAnnounceTab();\r\n+}\r\n+\r\n+/* ---- DECK LIST ---- */\r\n+function renderAdmDecks() {\r\n+  // update stats-bar (ด้านบนสุดของ admin panel)\r\n+  const deckEl = document.getElementById('stat-decks');\r\n+  const cardEl = document.getElementById('stat-cards');\r\n+  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n+  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n+\r\n+  // render deck list ใน tab กองไพ่\r\n+  const l = document.getElementById('adm-deck-list');\r\n+  if (!l) return;\r\n+  l.innerHTML = '';\r\n+  DB.decks.forEach(d => {\r\n+    const cnt = (DB.cards[d.id] || []).length;\r\n+    const th  = getTh(d);\r\n+    const r   = document.createElement('div');\r\n+    r.className = 'drow';\r\n+    r.innerHTML = `\r\n+    <div class=\"drow-info\">\r\n+      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n+      <div>\r\n+        <div class=\"drow-nm\">${d.name}</div>\r\n+        <div class=\"drow-meta\">\r\n+          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n+          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n+          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n+        </div>\r\n+      </div>\r\n+    </div>\r\n+    <div class=\"drow-acts\">\r\n+<div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n+<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n+<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n+</div>\r\n+      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n+        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n+        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n+      </button>\r\n+      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n+      <button class=\"btn-sm\" style=\"background:rgba(245,197,24,.08);border:1px solid rgba(245,197,24,.2);color:var(--gold);\" onclick=\"duplicateDeck('${d.id}')\"><i class=\"fi fi-sr-copy-alt\"></i> ก็อปปี้</button>\r\n+      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n+    </div>`;\r\n+    l.appendChild(r);\r\n+  });\r\n+}\r\n+async function moveDeck(id, dir) {\r\n+  const idx = DB.decks.findIndex(d => d.id === id);\r\n+  const swapIdx = idx + dir;\r\n+  if (idx < 0 || swapIdx < 0 || swapIdx >= DB.decks.length) return;\r\n+\r\n+  const a = DB.decks[idx];\r\n+  const b = DB.decks[swapIdx];\r\n+  const tmpOrder = a.sort_order ?? idx;\r\n+  a.sort_order = b.sort_order ?? swapIdx;\r\n+  b.sort_order = tmpOrder;\r\n+\r\n+  DB.decks[idx]     = b;\r\n+  DB.decks[swapIdx] = a;\r\n+\r\n+  const [r1, r2] = await Promise.all([\r\n+    _sb.from('decks').update({ sort_order: a.sort_order }).eq('id', a.id),\r\n+    _sb.from('decks').update({ sort_order: b.sort_order }).eq('id', b.id),\r\n+  ]);\r\n+\r\n+  if (r1.error || r2.error) {\r\n+    toast('บันทึกลำดับไม่สำเร็จ', 'error');\r\n+    await initDB();\r\n+  }\r\n+\r\n+  renderAdmDecks();\r\n+  renderDecks();\r\n+}\r\n+async function toggleDeck(id) {\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  if (!d) return;\r\n+  d.hidden = !d.hidden;\r\n+\r\n+  // บันทึกไป Supabase จริง\r\n+  const { error } = await _sb.from('decks').update({ hidden: d.hidden }).eq('id', id);\r\n+  if (error) {\r\n+    d.hidden = !d.hidden; // rollback\r\n+    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n+    return;\r\n+  }\r\n+\r\n+  renderAdmDecks();\r\n+  renderDecks();\r\n+  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+function renderAdmSel() {\r\n+  const s = document.getElementById('adm-deck-sel');\r\n+  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id;\r\n+    o.textContent = d.name;\r\n+    s.appendChild(o);\r\n+  });\r\n+}\r\n+\r\n+const THEME_PRESETS = [\r\n+  { name:'fire',            bg:'linear-gradient(135deg,rgba(229,57,53,.35),rgba(183,28,28,.2))',   color:'#ff5252' },\r\n+  { name:'magic',           bg:'linear-gradient(135deg,rgba(156,39,176,.35),rgba(106,27,154,.2))', color:'#e040fb' },\r\n+  { name:'skull',           bg:'linear-gradient(135deg,rgba(229,9,20,.35),rgba(150,0,10,.2))',     color:'#ff1744' },\r\n+  { name:'heart',           bg:'linear-gradient(135deg,rgba(233,30,99,.35),rgba(173,20,87,.2))',   color:'#ff4081' },\r\n+  { name:'star',            bg:'linear-gradient(135deg,rgba(245,197,24,.35),rgba(198,145,0,.2))',  color:'#ffd740' },\r\n+  { name:'bolt',            bg:'linear-gradient(135deg,rgba(255,152,0,.35),rgba(230,81,0,.2))',    color:'#ffab40' },\r\n+  { name:'ocean',           bg:'linear-gradient(135deg,rgba(33,150,243,.35),rgba(13,71,161,.2))',  color:'#40c4ff' },\r\n+  { name:'emerald',         bg:'linear-gradient(135deg,rgba(0,188,140,.35),rgba(0,121,107,.2))',   color:'#64ffda' },\r\n+  { name:'lavender',        bg:'linear-gradient(135deg,rgba(149,117,205,.35),rgba(94,53,177,.2))', color:'#ce93d8' },\r\n+  { name:'silver',          bg:'linear-gradient(135deg,rgba(176,190,197,.2),rgba(96,125,139,.15))',color:'#eceff1' },\r\n+  { name:'rose',            bg:'linear-gradient(135deg,rgba(236,64,122,.35),rgba(173,20,87,.2))',  color:'#ff80ab' },\r\n+  { name:'amber',           bg:'linear-gradient(135deg,rgba(255,193,7,.35),rgba(255,111,0,.2))',   color:'#ffe57f' },\r\n+  { name:'default',         bg:'linear-gradient(135deg,rgba(255,255,255,.08),rgba(255,255,255,.04))', color:'#cfd8dc' },\r\n+  { name:'gold_amber',      bg:'linear-gradient(135deg,rgba(245,197,24,.55),rgba(255,120,0,.55))', color:'#ffd740' },\r\n+  { name:'emerald_teal',    bg:'linear-gradient(135deg,rgba(0,200,100,.5),rgba(0,230,200,.5))',    color:'#69ffda' },\r\n+  { name:'rose_heart',      bg:'linear-gradient(135deg,rgba(255,60,100,.5),rgba(220,20,80,.5))',   color:'#ff80ab' },\r\n+  { name:'ocean_lavender',  bg:'linear-gradient(135deg,rgba(30,140,255,.5),rgba(140,90,220,.5))',  color:'#90caff' },\r\n+  { name:'fire_amber',      bg:'linear-gradient(135deg,rgba(230,50,40,.55),rgba(255,140,0,.55))',  color:'#ff7043' },\r\n+  { name:'magic_lavender',  bg:'linear-gradient(135deg,rgba(160,30,190,.55),rgba(140,100,220,.55))',color:'#e040fb'},\r\n+  { name:'bolt_gold',       bg:'linear-gradient(135deg,rgba(255,140,0,.55),rgba(240,200,0,.55))',  color:'#ffcc02' },\r\n+  { name:'ocean_emerald',   bg:'linear-gradient(135deg,rgba(20,140,255,.5),rgba(0,200,160,.5))',   color:'#40c4ff' },\r\n+];\r\n+\r\n+function _initThemePicker(currentTheme) {\r\n+  const swatches = document.getElementById('df-theme-swatches');\r\n+  if (!swatches) return;\r\n+  swatches.innerHTML = '';\r\n+\r\n+  THEME_PRESETS.forEach(t => {\r\n+    const sw = document.createElement('div');\r\n+    sw.title = t.name;\r\n+sw.style.cssText = `\r\n+  width:36px;height:36px;border-radius:10px;cursor:pointer;\r\n+  background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n+  transition:all .2s;flex-shrink:0;\r\n+  transform:${currentTheme===t.name?'scale(1.2)':'scale(1)'};\r\n+  box-shadow:0 2px 8px rgba(0,0,0,.4);\r\n+`;\r\n+    sw.onclick = () => _selectTheme(t.name, t.bg, t.color);\r\n+    swatches.appendChild(sw);\r\n+  });\r\n+\r\n+  // set preview เริ่มต้น\r\n+  const cur = THEME_PRESETS.find(t => t.name === currentTheme) || THEME_PRESETS[0];\r\n+  _applyThemePreview(cur.bg, cur.color);\r\n+  document.getElementById('df-theme').value = currentTheme;\r\n+\r\n+  // custom color pickers\r\n+document.getElementById('df-color1').oninput =\r\n+document.getElementById('df-color2').oninput = () => {\r\n+  const c1 = document.getElementById('df-color1').value;\r\n+  const c2 = document.getElementById('df-color2').value;\r\n+  const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n+  _applyThemePreview(bg, c1, 'custom');\r\n+  document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n+  const cp = document.getElementById('df-custom-preview');\r\n+  if (cp) cp.style.background = bg;\r\n+  document.getElementById('df-theme-swatches').querySelectorAll('div')\r\n+    .forEach(s => { s.style.borderColor='transparent'; s.style.transform='scale(1)'; });\r\n+};\r\n+}\r\n+\r\n+function _selectTheme(name, bg, color) {\r\n+  document.getElementById('df-theme').value = name;\r\n+  _applyThemePreview(bg, color, name.replace(/_/g,' '));\r\n+  document.getElementById('df-theme-swatches').querySelectorAll('div').forEach((s,i) => {\r\n+    s.style.borderColor = THEME_PRESETS[i]?.name === name ? '#fff' : 'transparent';\r\n+    s.style.transform   = THEME_PRESETS[i]?.name === name ? 'scale(1.2)' : 'scale(1)';\r\n+  });\r\n+}\r\n+function _applyThemePreview(bg, color, name = '') {\r\n+  const prev = document.getElementById('df-theme-preview');\r\n+  const ico  = document.getElementById('df-theme-preview-ico');\r\n+  const nm   = document.getElementById('df-theme-preview-name');\r\n+  if (!prev || !ico) return;\r\n+  prev.style.background = bg;\r\n+  ico.style.color = color;\r\n+  if (nm) nm.textContent = name.toUpperCase();\r\n+  const iconVal = document.getElementById('df-icon')?.value || 'fi-sr-flame';\r\n+  ico.className = 'fi ' + iconVal;\r\n+}\r\n+/* ---- DECK FORM ---- */\r\n+function openDeckForm(id) {\r\n+  editDeckId = id || null;\r\n+  if (id) {\r\n+    const d = DB.decks.find(x => x.id === id);\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n+    document.getElementById('df-name').value     = d.name;\r\n+    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value    = d.theme || 'default';\r\n+    document.getElementById('df-desc').value     = d.desc  || '';\r\n+    document.getElementById('df-cost').value     = d.cost  || 1;\r\n+    document.getElementById('df-category').value = d.category || '';\r\n+  } else {\r\n+    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n+    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n+    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n+    document.getElementById('df-theme').value = 'fire';\r\n+    document.getElementById('df-cost').value  = '1';\r\n+  }\r\n+  document.getElementById('deck-form-ov').classList.add('show');\r\n+  // ใส่ต่อจาก document.getElementById('deck-form-ov').classList.add('show');\r\n+_initThemePicker(document.getElementById('df-theme').value || 'fire');\r\n+\r\n+// sync preview เมื่อเปลี่ยน icon\r\n+document.getElementById('df-icon').oninput = () => {\r\n+  _applyThemePreview(\r\n+    document.getElementById('df-theme-preview').style.background,\r\n+    document.getElementById('df-theme-preview-ico').style.color\r\n+  );\r\n+};\r\n+}\r\n+\r\n+function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n+function editDeck(id)    { openDeckForm(id); }\r\n+\r\n+async function saveDeck() {\r\n+  const name = document.getElementById('df-name').value.trim();\r\n+  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n+  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n+  const theme    = document.getElementById('df-theme').value            || 'default';\r\n+  const desc     = document.getElementById('df-desc').value.trim();\r\n+  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n+  const category = document.getElementById('df-category').value.trim() || '';\r\n+\r\n+  let deck;\r\n+  if (editDeckId) {\r\n+    deck = DB.decks.find(x => x.id === editDeckId);\r\n+    Object.assign(deck, { name, icon, theme, desc, cost, category });\r\n+  } else {\r\n+    const id = 'd' + Date.now();\r\n+    deck = { id, name, icon, theme, desc, cost, category, hidden: false };\r\n+    DB.decks.push(deck);\r\n+    DB.cards[deck.id] = [];\r\n+  }\r\n+const ok = await saveDeckToDB(deck);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB();\r\n+  injectCustomThemes(); // ← เพิ่มบรรทัดนี้\r\n+  closeDeckForm();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast('บันทึกกองไพ่แล้ว', 'success');\r\n+}\r\n+\r\n+async function delDeck(id) {\r\n+  const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n+  if (!ok) return;\r\n+\r\n+  // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n+  const ok1= await deleteDeckFromDB(id);\r\n+  if (!ok1){ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // อัปเดต memory cache\r\n+  DB.decks = DB.decks.filter(d => d.id !== id);\r\n+  delete DB.cards[id];\r\n+\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast('ลบกองไพ่แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- CARD EDITOR ---- */\r\n+function loadCE() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  if (!id) {\r\n+    ed.innerHTML = '';\r\n+    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n+    return;\r\n+  }\r\n+  const d = DB.decks.find(x => x.id === id);\r\n+  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n+  ed.innerHTML = '';\r\n+  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n+}\r\n+\r\n+function addCRow(text = '', cat = '') {\r\n+  const ed = document.getElementById('card-list-ed');\r\n+  const r  = document.createElement('div');\r\n+  r.className = 'ced-row';\r\n+  r.innerHTML = `\r\n+    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n+    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n+    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n+    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n+  ed.appendChild(r);\r\n+  r.querySelector('input:last-of-type').focus();\r\n+}\r\n+\r\n+async function saveCards() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n+  const cards = [];\r\n+  rows.forEach(r => {\r\n+    const ins  = r.querySelectorAll('input');\r\n+    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n+    const text = ins[1].value.trim();\r\n+    if (text) cards.push({ cat, text });\r\n+  });\r\n+\r\n+  // บันทึกไป Supabase\r\n+  const ok = await saveCardsToDB(id, cards);\r\n+  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  await initDB(); // reload จาก Supabase\r\n+  renderAdmDecks();\r\n+  toast(`บันทึก ${cards.length} ใบแล้ว`, 'success');\r\n+}\r\n+\r\n+/* ---- SETTINGS ---- */\r\n+function saveSettings() {\r\n+  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)    || 10;\r\n+  DB.settings.siteName     = document.getElementById('st-nm').value              || 'DRINKORDOOM';\r\n+  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n+  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n+  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value)  || 1;\r\n+  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n+  DB.settings.deckLayout   = document.getElementById('st-layout').value || 'auto'; // ← เพิ่ม\r\n+  saveDB(); applyTestMode();\r\n+  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n+}\r\n+\r\n+// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n+function connectSheet() {\r\n+  const url = document.getElementById('sheet-url')?.value?.trim();\r\n+  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n+\r\n+  API.setSheetUrl(url);\r\n+  toast('กำลังดึงข้อมูล...', 'info');\r\n+\r\n+  API.syncFromSheets(url)\r\n+    .then(({ decks, cards }) => {\r\n+      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      renderDecks();\r\n+    })\r\n+    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n+}\r\n+\r\n+/* ---- EXPORT / IMPORT ---- */\r\n+function exportData() {\r\n+  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n+  const a = document.createElement('a');\r\n+  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n+}\r\n+function importData() { document.getElementById('imp-file').click(); }\r\n+\r\n+/* [FIX #6] validate JSON structure ก่อน import */\r\n+function handleImport(e) {\r\n+  const f = e.target.files[0]; if (!f) return;\r\n+  const r = new FileReader();\r\n+  r.onload = ev => {\r\n+    try {\r\n+      const parsed = JSON.parse(ev.target.result);\r\n+\r\n+      // validate required fields\r\n+      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n+      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n+      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n+\r\n+      // validate deck structure\r\n+      for (const d of parsed.decks) {\r\n+        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n+        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n+      }\r\n+\r\n+      // validate cards structure\r\n+      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n+        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n+        for (const c of cards) {\r\n+          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n+        }\r\n+      }\r\n+\r\n+      DB = parsed;\r\n+      saveDB();\r\n+      renderAdmDecks();\r\n+      renderAdmSel();\r\n+      toast('Import สำเร็จ', 'success');\r\n+    } catch (err) {\r\n+      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n+    }\r\n+  };\r\n+  r.readAsText(f);\r\n+  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n+  e.target.value = '';\r\n+}\r\n+/* ---- SETTINGS TAB ---- */\r\n+async function loadSettingsTab() {\r\n+  // โหลด packages list ในแท็บตั้งค่า\r\n+  if (typeof loadPkgList === 'function') await loadPkgList();\r\n+\r\n+  // sync ค่า settings fields\r\n+  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n+  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n+  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n+  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n+  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n+  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n+  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n+}\r\n+\r\n+/* ---- PACKAGE MANAGER ---- */\r\n+let editPkgId = null;\r\n+\r\n+async function loadPkgList() {\r\n+  const el = document.getElementById('pkg-list');\r\n+  if (!el) return;\r\n+  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n+\r\n+  const { data, error } = await _sb\r\n+    .from('packages')\r\n+    .select('*')\r\n+    .order('sort_order', { ascending: true });\r\n+\r\n+  if (error || !data?.length) {\r\n+    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n+    return;\r\n+  }\r\n+\r\n+  el.innerHTML = data.map(p => `\r\n+    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n+      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n+        <i class=\"fi fi-sr-coins\"></i>\r\n+      </div>\r\n+      <div class=\"pkg-row-info\">\r\n+        <div class=\"pkg-row-top\">\r\n+          <span class=\"pkg-row-name\">${p.name}</span>\r\n+          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n+          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n+        </div>\r\n+        <div class=\"pkg-row-detail\">\r\n+          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n+          <span class=\"pkg-sep\">·</span>\r\n+          <span>ลำดับ ${p.sort_order}</span>\r\n+        </div>\r\n+      </div>\r\n+      <div class=\"pkg-row-acts\">\r\n+        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n+          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n+        </button>\r\n+        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n+          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n+        </button>\r\n+        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n+          <i class=\"fi fi-sr-trash\"></i>\r\n+        </button>\r\n+      </div>\r\n+    </div>\r\n+  `).join('');\r\n+}\r\n+\r\n+function openPkgForm(id = null) {\r\n+  editPkgId = id;\r\n+  if (id) {\r\n+    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n+      if (!data) return;\r\n+      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n+      document.getElementById('pf-name').value     = data.name;\r\n+      document.getElementById('pf-coins').value    = data.coins;\r\n+      document.getElementById('pf-bonus').value    = data.bonus;\r\n+      document.getElementById('pf-price').value    = data.price;\r\n+      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n+      document.getElementById('pf-order').value    = data.sort_order;\r\n+      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n+    });\r\n+  } else {\r\n+    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n+    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n+    document.getElementById('pf-bonus').value    = '0';\r\n+    document.getElementById('pf-featured').value = '0';\r\n+    document.getElementById('pf-order').value    = '0';\r\n+    document.getElementById('pf-active').value   = '1';\r\n+  }\r\n+  document.getElementById('pkg-form-ov').classList.add('show');\r\n+}\r\n+\r\n+function closePkgForm() {\r\n+  document.getElementById('pkg-form-ov').classList.remove('show');\r\n+  editPkgId = null;\r\n+}\r\n+\r\n+async function savePkg() {\r\n+  const name     = document.getElementById('pf-name').value.trim();\r\n+  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n+  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n+  const price    = parseInt(document.getElementById('pf-price').value);\r\n+  const featured = document.getElementById('pf-featured').value === '1';\r\n+  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n+  const active   = document.getElementById('pf-active').value === '1';\r\n+\r\n+  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n+\r\n+  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n+\r\n+  const { error } = editPkgId\r\n+    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n+    : await _sb.from('packages').insert(payload);\r\n+\r\n+  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n+\r\n+  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n+  closePkgForm();\r\n+  loadPkgList();\r\n+  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n+}\r\n+\r\n+async function togglePkg(id, active) {\r\n+  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n+  loadPkgList();\r\n+}\r\n+\r\n+async function deletePkg(id) {\r\n+  const ok = await customConfirm('ลบแพ็กเกจนี้?');\r\n+  if (!ok) return;\r\n+  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n+  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n+  toast('ลบแพ็กเกจแล้ว', 'info');\r\n+  loadPkgList();\r\n+}\r\n+/* =========================================\r\n+   DECK DUPLICATOR\r\n+   ========================================= */\r\n+async function duplicateDeck(srcId) {\r\n+  const src = DB.decks.find(d => d.id === srcId);\r\n+  if (!src) return;\r\n+\r\n+  const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n+  if (!newName?.trim()) return;\r\n+\r\n+  const newId   = 'd' + Date.now();\r\n+  const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n+\r\n+  // บันทึก deck ใหม่\r\n+  const deckOk = await saveDeckToDB(newDeck);\r\n+  if (!deckOk) { toast('เกิดข้อผิดพลาด ก็อปกองไพ่ไม่สำเร็จ', 'error'); return; }\r\n+\r\n+  // ก็อปไพ่ทั้งหมด\r\n+  const srcCards = DB.cards[srcId] || [];\r\n+  if (srcCards.length) {\r\n+    const cardOk = await saveCardsToDB(newId, srcCards);\r\n+    if (!cardOk) { toast('ก็อปกองไพ่สำเร็จ แต่ไพ่บางส่วนอาจไม่ครบ', 'warning'); }\r\n+  }\r\n+\r\n+  await initDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  renderDecks();\r\n+  toast(`ก็อปปี้ \"${newName.trim()}\" สำเร็จ! ${srcCards.length} ใบ`, 'success');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CARD SEARCH & FILTER\r\n+   ========================================= */\r\n+let _csShowDup = false;\r\n+\r\n+function initCardSearch() {\r\n+  // populate deck dropdown\r\n+  const deckSel = document.getElementById('cs-deck');\r\n+  if (!deckSel) return;\r\n+  deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n+  DB.decks.forEach(d => {\r\n+    const o = document.createElement('option');\r\n+    o.value = d.id; o.textContent = d.name;\r\n+    deckSel.appendChild(o);\r\n+  });\r\n+\r\n+  // populate category dropdown (unique cats across all decks)\r\n+  const catSel = document.getElementById('cs-cat');\r\n+  if (catSel) {\r\n+    const cats = new Set();\r\n+    Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n+    catSel.innerHTML = '<option value=\"\">ทุกหมวด</option>';\r\n+    [...cats].sort().forEach(cat => {\r\n+      const o = document.createElement('option');\r\n+      o.value = cat; o.textContent = cat;\r\n+      catSel.appendChild(o);\r\n+    });\r\n+  }\r\n+\r\n+  _csShowDup = false;\r\n+  const dupBtn = document.getElementById('cs-dup-btn');\r\n+  if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n+\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function toggleDupFilter() {\r\n+  _csShowDup = !_csShowDup;\r\n+  const btn = document.getElementById('cs-dup-btn');\r\n+  if (btn) {\r\n+    btn.style.background = _csShowDup\r\n+      ? 'rgba(245,197,24,.25)' : 'rgba(245,197,24,.08)';\r\n+    btn.style.borderColor = _csShowDup ? 'var(--gold)' : 'rgba(245,197,24,.25)';\r\n+  }\r\n+  runCardSearch();\r\n+}\r\n+\r\n+function runCardSearch() {\r\n+  const query    = (document.getElementById('cs-query')?.value || '').trim().toLowerCase();\r\n+  const deckId   = document.getElementById('cs-deck')?.value  || '';\r\n+  const catFilt  = document.getElementById('cs-cat')?.value   || '';\r\n+  const results  = document.getElementById('cs-results');\r\n+  const summary  = document.getElementById('cs-summary');\r\n+  if (!results) return;\r\n+\r\n+  // รวมไพ่ทั้งหมด + แนบ deck info\r\n+  let all = [];\r\n+  DB.decks.forEach(d => {\r\n+    (DB.cards[d.id] || []).forEach((c, i) => {\r\n+      all.push({ deck: d, cat: c.cat || 'ทั่วไป', text: c.text, idx: i });\r\n+    });\r\n+  });\r\n+\r\n+  // filter by deck\r\n+  if (deckId) all = all.filter(c => c.deck.id === deckId);\r\n+  // filter by category\r\n+  if (catFilt) all = all.filter(c => c.cat === catFilt);\r\n+  // filter by search query\r\n+  if (query) all = all.filter(c => c.text.toLowerCase().includes(query));\r\n+\r\n+  // filter duplicates (same text ใน deck เดียวกัน หรือข้ามกองก็ได้)\r\n+  if (_csShowDup) {\r\n+    const textCount = {};\r\n+    all.forEach(c => { textCount[c.text] = (textCount[c.text] || 0) + 1; });\r\n+    all = all.filter(c => textCount[c.text] > 1);\r\n+  }\r\n+\r\n+  // summary\r\n+  const totalShown = all.length;\r\n+  summary.textContent = totalShown\r\n+    ? `พบ ${totalShown} ใบ${_csShowDup ? ' (ข้อความซ้ำ)' : ''}`\r\n+    : 'ไม่พบไพ่ที่ตรงกัน';\r\n+\r\n+  if (!all.length) {\r\n+    results.innerHTML = `\r\n+      <div style=\"text-align:center;padding:40px;color:var(--text3);\">\r\n+        <i class=\"fi fi-sr-search\" style=\"font-size:2rem;opacity:.3;\"></i>\r\n+        <div style=\"margin-top:10px;font-size:.85rem;\">ไม่พบไพ่</div>\r\n+      </div>`;\r\n+    return;\r\n+  }\r\n+\r\n+  // highlight matching text\r\n+  function hl(text) {\r\n+    if (!query) return text;\r\n+    const re = new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')})`, 'gi');\r\n+    return text.replace(re, '<mark style=\"background:rgba(229,9,20,.3);color:#fff;border-radius:2px;padding:0 2px;\">$1</mark>');\r\n+  }\r\n+\r\n+  results.innerHTML = all.map(c => {\r\n+    const th = getTh(c.deck);\r\n+    return `\r\n+    <div style=\"\r\n+      display:flex;align-items:center;gap:12px;\r\n+      background:#161616;border:1px solid #1e1e1e;\r\n+      border-radius:10px;padding:10px 14px;\r\n+      transition:border-color .2s;\r\n+    \" onmouseover=\"this.style.borderColor='#2a2a2a'\" onmouseout=\"this.style.borderColor='#1e1e1e'\">\r\n+      <div class=\"drow-ico ${th}\" style=\"width:32px;height:32px;font-size:.78rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n+        <i class=\"fi ${c.deck.icon || 'fi-sr-layers'}\"></i>\r\n+      </div>\r\n+      <div style=\"flex:1;min-width:0;\">\r\n+        <div style=\"font-size:.88rem;line-height:1.4;\">${hl(c.text)}</div>\r\n+        <div style=\"display:flex;align-items:center;gap:8px;margin-top:4px;\">\r\n+          <span style=\"font-size:.72rem;color:var(--text3);\">${c.deck.name}</span>\r\n+          <span style=\"color:#333;font-size:.6rem;\">·</span>\r\n+          <span style=\"font-size:.7rem;color:var(--text2);background:#1a1a1a;border:1px solid #2a2a2a;\r\n+                       border-radius:20px;padding:1px 8px;\">${c.cat}</span>\r\n+        </div>\r\n+      </div>\r\n+    </div>`;\r\n+  }).join('');\r\n+}\r\n+\r\n+async function confirmReset() {\r\n+  const ok = await customConfirm('รีเซ็ตข้อมูลทั้งหมด?');\r\n+  if (!ok) return;\r\n+  resetDB();\r\n+  renderAdmDecks();\r\n+  renderAdmSel();\r\n+  toast('รีเซ็ตแล้ว', 'info');\r\n+}\r\n+\r\n+/* =========================================\r\n+   CUSTOM CONFIRM / PROMPT DIALOGS\r\n+   ========================================= */\r\n+function customConfirm(message) {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-confirm-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-confirm-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:320px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:1rem;font-weight:600;\r\n+          margin-bottom:22px;line-height:1.5;color:#fff;\" id=\"cc-msg\"></div>\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cc-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cc-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ยืนยัน</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cc-msg').textContent = message;\r\n+    ov.querySelector('#cc-ok').onclick     = () => { ov.remove(); resolve(true);  };\r\n+    ov.querySelector('#cc-cancel').onclick = () => { ov.remove(); resolve(false); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(false); } });\r\n+    document.body.appendChild(ov);\r\n+  });\r\n+}\r\n+\r\n+function customPrompt(message, defaultValue = '') {\r\n+  return new Promise(resolve => {\r\n+    document.getElementById('custom-prompt-ov')?.remove();\r\n+    const ov = document.createElement('div');\r\n+    ov.id = 'custom-prompt-ov';\r\n+    ov.style.cssText = `\r\n+      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n+      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n+      backdrop-filter:blur(4px);\r\n+    `;\r\n+    ov.innerHTML = `\r\n+      <div style=\"\r\n+        background:linear-gradient(145deg,#1c1c1c,#111);\r\n+        border:1px solid #2a2a2a;border-radius:16px;\r\n+        padding:28px 28px 22px;max-width:340px;width:90%;\r\n+        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n+      \">\r\n+        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+          border-radius:16px 16px 0 0;\"></div>\r\n+        <div style=\"font-family:'Kanit',sans-serif;font-size:.95rem;font-weight:600;\r\n+          margin-bottom:14px;color:#fff;\" id=\"cp-msg\"></div>\r\n+        <input id=\"cp-input\" type=\"text\" style=\"\r\n+          width:100%;background:#151515;border:1px solid #2a2a2a;border-radius:8px;\r\n+          color:#fff;padding:10px 12px;font-family:'Kanit',sans-serif;font-size:.95rem;\r\n+          outline:none;box-sizing:border-box;margin-bottom:18px;\r\n+        \">\r\n+        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n+          <button id=\"cp-cancel\" style=\"\r\n+            background:transparent;border:1px solid #333;color:var(--text2);\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+          \">ยกเลิก</button>\r\n+          <button id=\"cp-ok\" style=\"\r\n+            background:var(--red);border:none;color:#fff;\r\n+            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n+            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n+          \">ตกลง</button>\r\n+        </div>\r\n+      </div>\r\n+    `;\r\n+    ov.querySelector('#cp-msg').textContent = message;\r\n+    const inp = ov.querySelector('#cp-input');\r\n+    inp.value = defaultValue;\r\n+    const confirm = () => { ov.remove(); resolve(inp.value.trim() || null); };\r\n+    ov.querySelector('#cp-ok').onclick     = confirm;\r\n+    ov.querySelector('#cp-cancel').onclick = () => { ov.remove(); resolve(null); };\r\n+    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(null); } });\r\n+    document.body.appendChild(ov);\r\n+    setTimeout(() => { inp.focus(); inp.select(); }, 50);\r\n+    inp.addEventListener('keydown', e => {\r\n+      if (e.key === 'Enter') confirm();\r\n+      if (e.key === 'Escape') { ov.remove(); resolve(null); }\r\n+    });\r\n+  });\r\n+}\r\n+\r\n+/* =========================================\r\n+   BULK PASTE & MULTI ADD\r\n+   ========================================= */\r\n+function addMultiRows(n = 5) {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+  for (let i = 0; i < n; i++) addCRow();\r\n+  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n+}\r\n+\r\n+function openBulkPaste() {\r\n+  const id = document.getElementById('adm-deck-sel').value;\r\n+  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n+\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+  const ov = document.createElement('div');\r\n+  ov.id = 'bulk-paste-ov';\r\n+  ov.style.cssText = `\r\n+    position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n+    z-index:700;display:flex;align-items:center;justify-content:center;\r\n+    backdrop-filter:blur(6px);\r\n+  `;\r\n+  ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n+  ov.innerHTML = `\r\n+    <div style=\"\r\n+      background:linear-gradient(145deg,#181818,#101010);\r\n+      border:1px solid #2a2a2a;border-radius:20px;\r\n+      padding:28px 24px;max-width:520px;width:94%;position:relative;\r\n+    \">\r\n+      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n+        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n+        border-radius:20px 20px 0 0;\"></div>\r\n+      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n+        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n+      </div>\r\n+      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n+        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ:\r\n+        <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n+        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n+      </div>\r\n+      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n+        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n+        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\" style=\"\r\n+          flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n+          color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;outline:none;\">\r\n+      </div>\r\n+      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n+        width:100%;height:220px;background:#0d0d0d;border:1px solid #2a2a2a;border-radius:10px;\r\n+        color:#fff;padding:12px;font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        line-height:1.7;resize:vertical;outline:none;box-sizing:border-box;\r\n+      \"></textarea>\r\n+      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n+      <div style=\"display:flex;gap:10px;\">\r\n+        <button onclick=\"closeBulkPaste()\" style=\"\r\n+          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n+        \">ยกเลิก</button>\r\n+        <button onclick=\"applyBulkPaste()\" style=\"\r\n+          flex:2;background:var(--red);border:none;color:#fff;\r\n+          padding:11px;border-radius:8px;cursor:pointer;\r\n+          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n+          display:flex;align-items:center;justify-content:center;gap:8px;\r\n+        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n+      </div>\r\n+    </div>\r\n+  `;\r\n+  document.body.appendChild(ov);\r\n+  const ta = ov.querySelector('#bulk-textarea');\r\n+  const pc = ov.querySelector('#bulk-preview-count');\r\n+  ta.addEventListener('input', () => {\r\n+    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n+    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n+  });\r\n+  ta.focus();\r\n+}\r\n+\r\n+function closeBulkPaste() {\r\n+  document.getElementById('bulk-paste-ov')?.remove();\r\n+}\r\n+\r\n+function applyBulkPaste() {\r\n+  const ta     = document.getElementById('bulk-textarea');\r\n+  const defCat = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n+  const lines  = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n+  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n+  lines.forEach(line => {\r\n+    if (line.includes('|')) {\r\n+      const [cat, ...rest] = line.split('|');\r\n+      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n+    } else {\r\n+      addCRow(line, defCat);\r\n+    }\r\n+  });\r\n+  closeBulkPaste();\r\n+  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1775746542328,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -573,1357 +573,9 @@\n   { name:'bolt_gold',       bg:'linear-gradient(135deg,rgba(255,140,0,.55),rgba(240,200,0,.55))',  color:'#ffcc02' },\r\n   { name:'ocean_emerald',   bg:'linear-gradient(135deg,rgba(20,140,255,.5),rgba(0,200,160,.5))',   color:'#40c4ff' },\r\n ];\r\n \r\n-function _initThemePicker(currentTheme) {\r\n-  const swatches = document.getElementById('df-theme-swatches');\r\n-  if (!swatches) return;\r\n-  swatches.innerHTML = '';\r\n \r\n-  THEME_PRESETS.forEach(t => {\r\n-    const sw = document.createElement('div');\r\n-    sw.title = t.name;\r\n-sw.style.cssText = `\r\n-  width:36px;height:36px;border-radius:10px;cursor:pointer;\r\n-  background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n-  transition:all .2s;flex-shrink:0;\r\n-  transform:${currentTheme===t.name?'scale(1.2)':'scale(1)'};\r\n-  box-shadow:0 2px 8px rgba(0,0,0,.4);\r\n-`;\r\n-    sw.onclick = () => _selectTheme(t.name, t.bg, t.color);\r\n-    swatches.appendChild(sw);\r\n-  });\r\n-\r\n-  // set preview เริ่มต้น\r\n-  const cur = THEME_PRESETS.find(t => t.name === currentTheme) || THEME_PRESETS[0];\r\n-  _applyThemePreview(cur.bg, cur.color);\r\n-  document.getElementById('df-theme').value = currentTheme;\r\n-\r\n-  // custom color pickers\r\n-document.getElementById('df-color1').oninput =\r\n-document.getElementById('df-color2').oninput = () => {\r\n-  const c1 = document.getElementById('df-color1').value;\r\n-  const c2 = document.getElementById('df-color2').value;\r\n-  const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n-  _applyThemePreview(bg, c1, 'custom');\r\n-  document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n-  const cp = document.getElementById('df-custom-preview');\r\n-  if (cp) cp.style.background = bg;\r\n-  document.getElementById('df-theme-swatches').querySelectorAll('div')\r\n-    .forEach(s => { s.style.borderColor='transparent'; s.style.transform='scale(1)'; });\r\n-};\r\n-}\r\n-\r\n-function _selectTheme(name, bg, color) {\r\n-  document.getElementById('df-theme').value = name;\r\n-  _applyThemePreview(bg, color, name.replace(/_/g,' '));\r\n-  document.getElementById('df-theme-swatches').querySelectorAll('div').forEach((s,i) => {\r\n-    s.style.borderColor = THEME_PRESETS[i]?.name === name ? '#fff' : 'transparent';\r\n-    s.style.transform   = THEME_PRESETS[i]?.name === name ? 'scale(1.2)' : 'scale(1)';\r\n-  });\r\n-}\r\n-function _applyThemePreview(bg, color, name = '') {\r\n-  const prev = document.getElementById('df-theme-preview');\r\n-  const ico  = document.getElementById('df-theme-preview-ico');\r\n-  const nm   = document.getElementById('df-theme-preview-name');\r\n-  if (!prev || !ico) return;\r\n-  prev.style.background = bg;\r\n-  ico.style.color = color;\r\n-  if (nm) nm.textContent = name.toUpperCase();\r\n-  const iconVal = document.getElementById('df-icon')?.value || 'fi-sr-flame';\r\n-  ico.className = 'fi ' + iconVal;\r\n-}\r\n-/* ---- DECK FORM ---- */\r\n-function openDeckForm(id) {\r\n-  editDeckId = id || null;\r\n-  if (id) {\r\n-    const d = DB.decks.find(x => x.id === id);\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขกองไพ่`;\r\n-    document.getElementById('df-name').value     = d.name;\r\n-    document.getElementById('df-icon').value     = d.icon  || 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value    = d.theme || 'default';\r\n-    document.getElementById('df-desc').value     = d.desc  || '';\r\n-    document.getElementById('df-cost').value     = d.cost  || 1;\r\n-    document.getElementById('df-category').value = d.category || '';\r\n-  } else {\r\n-    document.getElementById('df-title').innerHTML = `<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เพิ่มกองไพ่ใหม่`;\r\n-    ['df-name', 'df-desc', 'df-category'].forEach(i => document.getElementById(i).value = '');\r\n-    document.getElementById('df-icon').value  = 'fi-sr-flame';\r\n-    document.getElementById('df-theme').value = 'fire';\r\n-    document.getElementById('df-cost').value  = '1';\r\n-  }\r\n-  document.getElementById('deck-form-ov').classList.add('show');\r\n-  // ใส่ต่อจาก document.getElementById('deck-form-ov').classList.add('show');\r\n-_initThemePicker(document.getElementById('df-theme').value || 'fire');\r\n-\r\n-// sync preview เมื่อเปลี่ยน icon\r\n-document.getElementById('df-icon').oninput = () => {\r\n-  _applyThemePreview(\r\n-    document.getElementById('df-theme-preview').style.background,\r\n-    document.getElementById('df-theme-preview-ico').style.color\r\n-  );\r\n-};\r\n-}\r\n-\r\n-function closeDeckForm() { document.getElementById('deck-form-ov').classList.remove('show'); }\r\n-function editDeck(id)    { openDeckForm(id); }\r\n-\r\n-async function saveDeck() {\r\n-  const name = document.getElementById('df-name').value.trim();\r\n-  if (!name) { toast('กรุณาใส่ชื่อกองไพ่', 'warning'); return; }\r\n-  const icon     = document.getElementById('df-icon').value.trim()     || 'fi-sr-flame';\r\n-  const theme    = document.getElementById('df-theme').value            || 'default';\r\n-  const desc     = document.getElementById('df-desc').value.trim();\r\n-  const cost     = parseInt(document.getElementById('df-cost').value)   || 1;\r\n-  const category = document.getElementById('df-category').value.trim() || '';\r\n-\r\n-  let deck;\r\n-  if (editDeckId) {\r\n-    deck = DB.decks.find(x => x.id === editDeckId);\r\n-    Object.assign(deck, { name, icon, theme, desc, cost, category });\r\n-  } else {\r\n-    const id = 'd' + Date.now();\r\n-    deck = { id, name, icon, theme, desc, cost, category, hidden: false };\r\n-    DB.decks.push(deck);\r\n-    DB.cards[deck.id] = [];\r\n-  }\r\n-const ok = await saveDeckToDB(deck);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  await initDB();\r\n-  injectCustomThemes(); // ← เพิ่มบรรทัดนี้\r\n-  closeDeckForm();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast('บันทึกกองไพ่แล้ว', 'success');\r\n-}\r\n-\r\n-async function delDeck(id) {\r\n-  const ok = await customConfirm('ลบกองไพ่นี้? ไพ่ทั้งหมดจะหายด้วย');\r\n-  if (!ok) return;\r\n-\r\n-  // ลบจาก Supabase จริง (cards จะถูกลบ cascade หรือเรียก deleteCardsToDB ก่อน)\r\n-  const ok1= await deleteDeckFromDB(id);\r\n-  if (!ok1){ toast('เกิดข้อผิดพลาด ลบไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  // อัปเดต memory cache\r\n-  DB.decks = DB.decks.filter(d => d.id !== id);\r\n-  delete DB.cards[id];\r\n-\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast('ลบกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- CARD EDITOR ---- */\r\n-function loadCE() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  if (!id) {\r\n-    ed.innerHTML = '';\r\n-    document.getElementById('ce-title').innerHTML = '<i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> เลือกกองไพ่ก่อน';\r\n-    return;\r\n-  }\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  document.getElementById('ce-title').innerHTML = `<i class=\"fi ${d.icon || 'fi-sr-layers'}\" style=\"color:var(--red);\"></i> ${d.name} — แก้ไขไพ่`;\r\n-  ed.innerHTML = '';\r\n-  (DB.cards[id] || []).forEach(c => addCRow(c.text, c.cat));\r\n-}\r\n-\r\n-function addCRow(text = '', cat = '') {\r\n-  const ed = document.getElementById('card-list-ed');\r\n-  const r  = document.createElement('div');\r\n-  r.className = 'ced-row';\r\n-  r.innerHTML = `\r\n-    <i class=\"fi fi-sr-tag\" style=\"color:var(--text3);font-size:.78rem;flex-shrink:0;\"></i>\r\n-    <input class=\"ci\" type=\"text\" value=\"${cat.replace(/\"/g, '&quot;')}\" placeholder=\"หมวด\">\r\n-    <input type=\"text\" value=\"${text.replace(/\"/g, '&quot;')}\" placeholder=\"ข้อความในไพ่...\">\r\n-    <button class=\"del-btn\" onclick=\"this.parentElement.remove()\"><i class=\"fi fi-sr-cross\"></i></button>`;\r\n-  ed.appendChild(r);\r\n-  r.querySelector('input:last-of-type').focus();\r\n-}\r\n-\r\n-async function saveCards() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-\r\n-  const rows = document.querySelectorAll('#card-list-ed .ced-row');\r\n-  const cards = [];\r\n-  rows.forEach(r => {\r\n-    const ins  = r.querySelectorAll('input');\r\n-    const cat  = ins[0].value.trim() || 'ทั่วไป';\r\n-    const text = ins[1].value.trim();\r\n-    if (text) cards.push({ cat, text });\r\n-  });\r\n-\r\n-  // บันทึกไป Supabase\r\n-  const ok = await saveCardsToDB(id, cards);\r\n-  if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  await initDB(); // reload จาก Supabase\r\n-  renderAdmDecks();\r\n-  toast(`บันทึก ${cards.length} ใบแล้ว`, 'success');\r\n-}\r\n-\r\n-/* ---- SETTINGS ---- */\r\n-function saveSettings() {\r\n-  DB.settings.startCredit  = parseInt(document.getElementById('st-cr').value)    || 10;\r\n-  DB.settings.siteName     = document.getElementById('st-nm').value              || 'DRINKORDOOM';\r\n-  DB.settings.testMode     = parseInt(document.getElementById('st-test').value);\r\n-  DB.settings.showModal    = parseInt(document.getElementById('st-modal').value);\r\n-  DB.settings.defaultCost  = parseInt(document.getElementById('st-cost').value)  || 1;\r\n-  DB.settings.topupEnabled = parseInt(document.getElementById('st-topup').value);\r\n-  DB.settings.deckLayout   = document.getElementById('st-layout').value || 'auto'; // ← เพิ่ม\r\n-  saveDB(); applyTestMode();\r\n-  toast('บันทึกการตั้งค่าแล้ว', 'success');\r\n-}\r\n-\r\n-// [FIX] connectSheet นิยามครั้งเดียวที่นี่ — ลบออกจาก api.js แล้ว\r\n-function connectSheet() {\r\n-  const url = document.getElementById('sheet-url')?.value?.trim();\r\n-  if (!url) { toast('กรุณาใส่ URL', 'warning'); return; }\r\n-\r\n-  API.setSheetUrl(url);\r\n-  toast('กำลังดึงข้อมูล...', 'info');\r\n-\r\n-  API.syncFromSheets(url)\r\n-    .then(({ decks, cards }) => {\r\n-      toast(`โหลดสำเร็จ! ${decks} กอง, ${cards} ใบ`, 'success');\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      renderDecks();\r\n-    })\r\n-    .catch(() => toast('เชื่อมต่อไม่ได้ — ตรวจสอบ URL และ Share settings', 'error'));\r\n-}\r\n-\r\n-/* ---- EXPORT / IMPORT ---- */\r\n-function exportData() {\r\n-  const b = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });\r\n-  const a = document.createElement('a');\r\n-  a.href = URL.createObjectURL(b); a.download = 'dealshot_data.json'; a.click();\r\n-}\r\n-function importData() { document.getElementById('imp-file').click(); }\r\n-\r\n-/* [FIX #6] validate JSON structure ก่อน import */\r\n-function handleImport(e) {\r\n-  const f = e.target.files[0]; if (!f) return;\r\n-  const r = new FileReader();\r\n-  r.onload = ev => {\r\n-    try {\r\n-      const parsed = JSON.parse(ev.target.result);\r\n-\r\n-      // validate required fields\r\n-      if (!parsed || typeof parsed !== 'object') throw new Error('ไม่ใช่ JSON object');\r\n-      if (!Array.isArray(parsed.decks))          throw new Error('decks ต้องเป็น array');\r\n-      if (typeof parsed.cards !== 'object')      throw new Error('cards ต้องเป็น object');\r\n-\r\n-      // validate deck structure\r\n-      for (const d of parsed.decks) {\r\n-        if (!d.id || typeof d.id !== 'string')   throw new Error(`deck ไม่มี id`);\r\n-        if (!d.name || typeof d.name !== 'string') throw new Error(`deck ${d.id} ไม่มี name`);\r\n-      }\r\n-\r\n-      // validate cards structure\r\n-      for (const [deckId, cards] of Object.entries(parsed.cards)) {\r\n-        if (!Array.isArray(cards)) throw new Error(`cards[${deckId}] ต้องเป็น array`);\r\n-        for (const c of cards) {\r\n-          if (typeof c.text !== 'string') throw new Error(`card ใน ${deckId} ไม่มี text`);\r\n-        }\r\n-      }\r\n-\r\n-      DB = parsed;\r\n-      saveDB();\r\n-      renderAdmDecks();\r\n-      renderAdmSel();\r\n-      toast('Import สำเร็จ', 'success');\r\n-    } catch (err) {\r\n-      toast(`ไฟล์ไม่ถูกต้อง: ${err.message}`, 'error');\r\n-    }\r\n-  };\r\n-  r.readAsText(f);\r\n-  // reset file input เพื่อให้ import ไฟล์เดิมซ้ำได้\r\n-  e.target.value = '';\r\n-}\r\n-/* ---- SETTINGS TAB ---- */\r\n-async function loadSettingsTab() {\r\n-  // โหลด packages list ในแท็บตั้งค่า\r\n-  if (typeof loadPkgList === 'function') await loadPkgList();\r\n-\r\n-  // sync ค่า settings fields\r\n-  const setVal = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };\r\n-  setVal('st-cr',    DB.settings?.startCredit  ?? 10);\r\n-  setVal('st-nm',    DB.settings?.siteName     ?? 'DRINKORDOOM');\r\n-  setVal('st-test',  DB.settings?.testMode     ?? 1);\r\n-  setVal('st-modal', DB.settings?.showModal    ?? 1);\r\n-  setVal('st-cost',  DB.settings?.defaultCost  ?? 1);\r\n-  setVal('st-topup', DB.settings?.topupEnabled ?? 1);\r\n-}\r\n-\r\n-/* ---- PACKAGE MANAGER ---- */\r\n-let editPkgId = null;\r\n-\r\n-async function loadPkgList() {\r\n-  const el = document.getElementById('pkg-list');\r\n-  if (!el) return;\r\n-  el.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('packages')\r\n-    .select('*')\r\n-    .order('sort_order', { ascending: true });\r\n-\r\n-  if (error || !data?.length) {\r\n-    el.innerHTML = '<p style=\"color:var(--text3);\">ไม่พบแพ็กเกจ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  el.innerHTML = data.map(p => `\r\n-    <div class=\"pkg-row ${!p.is_active ? 'pkg-inactive' : ''}\">\r\n-      <div class=\"pkg-row-ico ${p.is_featured ? 'pkg-ico-featured' : ''}\">\r\n-        <i class=\"fi fi-sr-coins\"></i>\r\n-      </div>\r\n-      <div class=\"pkg-row-info\">\r\n-        <div class=\"pkg-row-top\">\r\n-          <span class=\"pkg-row-name\">${p.name}</span>\r\n-          ${p.is_featured ? '<span class=\"pkg-badge pkg-badge-featured\"><i class=\"fi fi-sr-star\"></i> แนะนำ</span>' : ''}\r\n-          ${!p.is_active ? '<span class=\"pkg-badge pkg-badge-off\">ซ่อน</span>' : ''}\r\n-        </div>\r\n-        <div class=\"pkg-row-detail\">\r\n-          <span><i class=\"fi fi-sr-coins\"></i> ${p.coins} เครดิต${p.bonus > 0 ? ` +${p.bonus} โบนัส` : ''}</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span><i class=\"fi fi-sr-baht-sign\"></i> ฿${p.price} บาท</span>\r\n-          <span class=\"pkg-sep\">·</span>\r\n-          <span>ลำดับ ${p.sort_order}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"pkg-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"openPkgForm('${p.id}')\">\r\n-          <i class=\"fi fi-sr-edit\"></i> แก้ไข\r\n-        </button>\r\n-        <button class=\"btn-sm ${p.is_active ? 'btn-hidden' : 'btn-visible'}\" onclick=\"togglePkg('${p.id}', ${!p.is_active})\">\r\n-          <i class=\"fi fi-sr-eye${p.is_active ? '-crossed' : ''}\"></i>\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deletePkg('${p.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>\r\n-  `).join('');\r\n-}\r\n-\r\n-function openPkgForm(id = null) {\r\n-  editPkgId = id;\r\n-  if (id) {\r\n-    _sb.from('packages').select('*').eq('id', id).single().then(({ data }) => {\r\n-      if (!data) return;\r\n-      document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-edit\" style=\"color:var(--red);\"></i> แก้ไขแพ็กเกจ';\r\n-      document.getElementById('pf-name').value     = data.name;\r\n-      document.getElementById('pf-coins').value    = data.coins;\r\n-      document.getElementById('pf-bonus').value    = data.bonus;\r\n-      document.getElementById('pf-price').value    = data.price;\r\n-      document.getElementById('pf-featured').value = data.is_featured ? '1' : '0';\r\n-      document.getElementById('pf-order').value    = data.sort_order;\r\n-      document.getElementById('pf-active').value   = data.is_active ? '1' : '0';\r\n-    });\r\n-  } else {\r\n-    document.getElementById('pf-title').innerHTML = '<i class=\"fi fi-sr-coins\" style=\"color:var(--red);\"></i> เพิ่มแพ็กเกจ';\r\n-    ['pf-name','pf-coins','pf-bonus','pf-price'].forEach(id => document.getElementById(id).value = '');\r\n-    document.getElementById('pf-bonus').value    = '0';\r\n-    document.getElementById('pf-featured').value = '0';\r\n-    document.getElementById('pf-order').value    = '0';\r\n-    document.getElementById('pf-active').value   = '1';\r\n-  }\r\n-  document.getElementById('pkg-form-ov').classList.add('show');\r\n-}\r\n-\r\n-function closePkgForm() {\r\n-  document.getElementById('pkg-form-ov').classList.remove('show');\r\n-  editPkgId = null;\r\n-}\r\n-\r\n-async function savePkg() {\r\n-  const name     = document.getElementById('pf-name').value.trim();\r\n-  const coins    = parseInt(document.getElementById('pf-coins').value);\r\n-  const bonus    = parseInt(document.getElementById('pf-bonus').value) || 0;\r\n-  const price    = parseInt(document.getElementById('pf-price').value);\r\n-  const featured = document.getElementById('pf-featured').value === '1';\r\n-  const order    = parseInt(document.getElementById('pf-order').value) || 0;\r\n-  const active   = document.getElementById('pf-active').value === '1';\r\n-\r\n-  if (!name || !coins || !price) { toast('กรุณากรอกข้อมูลให้ครบ', 'warning'); return; }\r\n-\r\n-  const payload = { name, coins, bonus, price, is_featured: featured, sort_order: order, is_active: active };\r\n-\r\n-  const { error } = editPkgId\r\n-    ? await _sb.from('packages').update(payload).eq('id', editPkgId)\r\n-    : await _sb.from('packages').insert(payload);\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast(editPkgId ? 'แก้ไขแพ็กเกจแล้ว' : 'เพิ่มแพ็กเกจแล้ว', 'success');\r\n-  closePkgForm();\r\n-  loadPkgList();\r\n-  loadPackages(); // รีโหลดหน้าเติมเครดิตด้วย\r\n-}\r\n-\r\n-async function togglePkg(id, active) {\r\n-  const { error } = await _sb.from('packages').update({ is_active: active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เปิดแพ็กเกจแล้ว' : 'ซ่อนแพ็กเกจแล้ว', active ? 'success' : 'info');\r\n-  loadPkgList();\r\n-}\r\n-\r\n-async function deletePkg(id) {\r\n-  const ok = await customConfirm('ลบแพ็กเกจนี้?');\r\n-  if (!ok) return;\r\n-  const { error } = await _sb.from('packages').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบแพ็กเกจแล้ว', 'info');\r\n-  loadPkgList();\r\n-}\r\n-/* =========================================\r\n-   DECK DUPLICATOR\r\n-   ========================================= */\r\n-async function duplicateDeck(srcId) {\r\n-  const src = DB.decks.find(d => d.id === srcId);\r\n-  if (!src) return;\r\n-\r\n-  const newName = await customPrompt(`ชื่อกองไพ่ใหม่:`, `${src.name} (สำเนา)`);\r\n-  if (!newName?.trim()) return;\r\n-\r\n-  const newId   = 'd' + Date.now();\r\n-  const newDeck = { ...src, id: newId, name: newName.trim(), hidden: false };\r\n-\r\n-  // บันทึก deck ใหม่\r\n-  const deckOk = await saveDeckToDB(newDeck);\r\n-  if (!deckOk) { toast('เกิดข้อผิดพลาด ก็อปกองไพ่ไม่สำเร็จ', 'error'); return; }\r\n-\r\n-  // ก็อปไพ่ทั้งหมด\r\n-  const srcCards = DB.cards[srcId] || [];\r\n-  if (srcCards.length) {\r\n-    const cardOk = await saveCardsToDB(newId, srcCards);\r\n-    if (!cardOk) { toast('ก็อปกองไพ่สำเร็จ แต่ไพ่บางส่วนอาจไม่ครบ', 'warning'); }\r\n-  }\r\n-\r\n-  await initDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  renderDecks();\r\n-  toast(`ก็อปปี้ \"${newName.trim()}\" สำเร็จ! ${srcCards.length} ใบ`, 'success');\r\n-}\r\n-\r\n-/* =========================================\r\n-   CARD SEARCH & FILTER\r\n-   ========================================= */\r\n-let _csShowDup = false;\r\n-\r\n-function initCardSearch() {\r\n-  // populate deck dropdown\r\n-  const deckSel = document.getElementById('cs-deck');\r\n-  if (!deckSel) return;\r\n-  deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id; o.textContent = d.name;\r\n-    deckSel.appendChild(o);\r\n-  });\r\n-\r\n-  // populate category dropdown (unique cats across all decks)\r\n-  const catSel = document.getElementById('cs-cat');\r\n-  if (catSel) {\r\n-    const cats = new Set();\r\n-    Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n-    catSel.innerHTML = '<option value=\"\">ทุกหมวด</option>';\r\n-    [...cats].sort().forEach(cat => {\r\n-      const o = document.createElement('option');\r\n-      o.value = cat; o.textContent = cat;\r\n-      catSel.appendChild(o);\r\n-    });\r\n-  }\r\n-\r\n-  _csShowDup = false;\r\n-  const dupBtn = document.getElementById('cs-dup-btn');\r\n-  if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n-\r\n-  runCardSearch();\r\n-}\r\n-\r\n-function toggleDupFilter() {\r\n-  _csShowDup = !_csShowDup;\r\n-  const btn = document.getElementById('cs-dup-btn');\r\n-  if (btn) {\r\n-    btn.style.background = _csShowDup\r\n-      ? 'rgba(245,197,24,.25)' : 'rgba(245,197,24,.08)';\r\n-    btn.style.borderColor = _csShowDup ? 'var(--gold)' : 'rgba(245,197,24,.25)';\r\n-  }\r\n-  runCardSearch();\r\n-}\r\n-\r\n-function runCardSearch() {\r\n-  const query    = (document.getElementById('cs-query')?.value || '').trim().toLowerCase();\r\n-  const deckId   = document.getElementById('cs-deck')?.value  || '';\r\n-  const catFilt  = document.getElementById('cs-cat')?.value   || '';\r\n-  const results  = document.getElementById('cs-results');\r\n-  const summary  = document.getElementById('cs-summary');\r\n-  if (!results) return;\r\n-\r\n-  // รวมไพ่ทั้งหมด + แนบ deck info\r\n-  let all = [];\r\n-  DB.decks.forEach(d => {\r\n-    (DB.cards[d.id] || []).forEach((c, i) => {\r\n-      all.push({ deck: d, cat: c.cat || 'ทั่วไป', text: c.text, idx: i });\r\n-    });\r\n-  });\r\n-\r\n-  // filter by deck\r\n-  if (deckId) all = all.filter(c => c.deck.id === deckId);\r\n-  // filter by category\r\n-  if (catFilt) all = all.filter(c => c.cat === catFilt);\r\n-  // filter by search query\r\n-  if (query) all = all.filter(c => c.text.toLowerCase().includes(query));\r\n-\r\n-  // filter duplicates (same text ใน deck เดียวกัน หรือข้ามกองก็ได้)\r\n-  if (_csShowDup) {\r\n-    const textCount = {};\r\n-    all.forEach(c => { textCount[c.text] = (textCount[c.text] || 0) + 1; });\r\n-    all = all.filter(c => textCount[c.text] > 1);\r\n-  }\r\n-\r\n-  // summary\r\n-  const totalShown = all.length;\r\n-  summary.textContent = totalShown\r\n-    ? `พบ ${totalShown} ใบ${_csShowDup ? ' (ข้อความซ้ำ)' : ''}`\r\n-    : 'ไม่พบไพ่ที่ตรงกัน';\r\n-\r\n-  if (!all.length) {\r\n-    results.innerHTML = `\r\n-      <div style=\"text-align:center;padding:40px;color:var(--text3);\">\r\n-        <i class=\"fi fi-sr-search\" style=\"font-size:2rem;opacity:.3;\"></i>\r\n-        <div style=\"margin-top:10px;font-size:.85rem;\">ไม่พบไพ่</div>\r\n-      </div>`;\r\n-    return;\r\n-  }\r\n-\r\n-  // highlight matching text\r\n-  function hl(text) {\r\n-    if (!query) return text;\r\n-    const re = new RegExp(`(${query.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')})`, 'gi');\r\n-    return text.replace(re, '<mark style=\"background:rgba(229,9,20,.3);color:#fff;border-radius:2px;padding:0 2px;\">$1</mark>');\r\n-  }\r\n-\r\n-  results.innerHTML = all.map(c => {\r\n-    const th = getTh(c.deck);\r\n-    return `\r\n-    <div style=\"\r\n-      display:flex;align-items:center;gap:12px;\r\n-      background:#161616;border:1px solid #1e1e1e;\r\n-      border-radius:10px;padding:10px 14px;\r\n-      transition:border-color .2s;\r\n-    \" onmouseover=\"this.style.borderColor='#2a2a2a'\" onmouseout=\"this.style.borderColor='#1e1e1e'\">\r\n-      <div class=\"drow-ico ${th}\" style=\"width:32px;height:32px;font-size:.78rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n-        <i class=\"fi ${c.deck.icon || 'fi-sr-layers'}\"></i>\r\n-      </div>\r\n-      <div style=\"flex:1;min-width:0;\">\r\n-        <div style=\"font-size:.88rem;line-height:1.4;\">${hl(c.text)}</div>\r\n-        <div style=\"display:flex;align-items:center;gap:8px;margin-top:4px;\">\r\n-          <span style=\"font-size:.72rem;color:var(--text3);\">${c.deck.name}</span>\r\n-          <span style=\"color:#333;font-size:.6rem;\">·</span>\r\n-          <span style=\"font-size:.7rem;color:var(--text2);background:#1a1a1a;border:1px solid #2a2a2a;\r\n-                       border-radius:20px;padding:1px 8px;\">${c.cat}</span>\r\n-        </div>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-\r\n-async function confirmReset() {\r\n-  const ok = await customConfirm('รีเซ็ตข้อมูลทั้งหมด?');\r\n-  if (!ok) return;\r\n-  resetDB();\r\n-  renderAdmDecks();\r\n-  renderAdmSel();\r\n-  toast('รีเซ็ตแล้ว', 'info');\r\n-}\r\n-\r\n-/* =========================================\r\n-   CUSTOM CONFIRM / PROMPT DIALOGS\r\n-   ========================================= */\r\n-function customConfirm(message) {\r\n-  return new Promise(resolve => {\r\n-    document.getElementById('custom-confirm-ov')?.remove();\r\n-    const ov = document.createElement('div');\r\n-    ov.id = 'custom-confirm-ov';\r\n-    ov.style.cssText = `\r\n-      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n-      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n-      backdrop-filter:blur(4px);\r\n-    `;\r\n-    ov.innerHTML = `\r\n-      <div style=\"\r\n-        background:linear-gradient(145deg,#1c1c1c,#111);\r\n-        border:1px solid #2a2a2a;border-radius:16px;\r\n-        padding:28px 28px 22px;max-width:320px;width:90%;\r\n-        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n-      \">\r\n-        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-          border-radius:16px 16px 0 0;\"></div>\r\n-        <div style=\"font-family:'Kanit',sans-serif;font-size:1rem;font-weight:600;\r\n-          margin-bottom:22px;line-height:1.5;color:#fff;\" id=\"cc-msg\"></div>\r\n-        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n-          <button id=\"cc-cancel\" style=\"\r\n-            background:transparent;border:1px solid #333;color:var(--text2);\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-          \">ยกเลิก</button>\r\n-          <button id=\"cc-ok\" style=\"\r\n-            background:var(--red);border:none;color:#fff;\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n-          \">ยืนยัน</button>\r\n-        </div>\r\n-      </div>\r\n-    `;\r\n-    ov.querySelector('#cc-msg').textContent = message;\r\n-    ov.querySelector('#cc-ok').onclick     = () => { ov.remove(); resolve(true);  };\r\n-    ov.querySelector('#cc-cancel').onclick = () => { ov.remove(); resolve(false); };\r\n-    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(false); } });\r\n-    document.body.appendChild(ov);\r\n-  });\r\n-}\r\n-\r\n-function customPrompt(message, defaultValue = '') {\r\n-  return new Promise(resolve => {\r\n-    document.getElementById('custom-prompt-ov')?.remove();\r\n-    const ov = document.createElement('div');\r\n-    ov.id = 'custom-prompt-ov';\r\n-    ov.style.cssText = `\r\n-      position:fixed;inset:0;background:rgba(0,0,0,.75);\r\n-      z-index:9999;display:flex;align-items:center;justify-content:center;\r\n-      backdrop-filter:blur(4px);\r\n-    `;\r\n-    ov.innerHTML = `\r\n-      <div style=\"\r\n-        background:linear-gradient(145deg,#1c1c1c,#111);\r\n-        border:1px solid #2a2a2a;border-radius:16px;\r\n-        padding:28px 28px 22px;max-width:340px;width:90%;\r\n-        position:relative;box-shadow:0 20px 60px rgba(0,0,0,.8);\r\n-      \">\r\n-        <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-          background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-          border-radius:16px 16px 0 0;\"></div>\r\n-        <div style=\"font-family:'Kanit',sans-serif;font-size:.95rem;font-weight:600;\r\n-          margin-bottom:14px;color:#fff;\" id=\"cp-msg\"></div>\r\n-        <input id=\"cp-input\" type=\"text\" style=\"\r\n-          width:100%;background:#151515;border:1px solid #2a2a2a;border-radius:8px;\r\n-          color:#fff;padding:10px 12px;font-family:'Kanit',sans-serif;font-size:.95rem;\r\n-          outline:none;box-sizing:border-box;margin-bottom:18px;\r\n-        \">\r\n-        <div style=\"display:flex;gap:10px;justify-content:flex-end;\">\r\n-          <button id=\"cp-cancel\" style=\"\r\n-            background:transparent;border:1px solid #333;color:var(--text2);\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-          \">ยกเลิก</button>\r\n-          <button id=\"cp-ok\" style=\"\r\n-            background:var(--red);border:none;color:#fff;\r\n-            padding:9px 20px;border-radius:8px;cursor:pointer;\r\n-            font-family:'Kanit',sans-serif;font-size:.88rem;font-weight:700;\r\n-          \">ตกลง</button>\r\n-        </div>\r\n-      </div>\r\n-    `;\r\n-    ov.querySelector('#cp-msg').textContent = message;\r\n-    const inp = ov.querySelector('#cp-input');\r\n-    inp.value = defaultValue;\r\n-    const confirm = () => { ov.remove(); resolve(inp.value.trim() || null); };\r\n-    ov.querySelector('#cp-ok').onclick     = confirm;\r\n-    ov.querySelector('#cp-cancel').onclick = () => { ov.remove(); resolve(null); };\r\n-    ov.addEventListener('click', e => { if (e.target === ov) { ov.remove(); resolve(null); } });\r\n-    document.body.appendChild(ov);\r\n-    setTimeout(() => { inp.focus(); inp.select(); }, 50);\r\n-    inp.addEventListener('keydown', e => {\r\n-      if (e.key === 'Enter') confirm();\r\n-      if (e.key === 'Escape') { ov.remove(); resolve(null); }\r\n-    });\r\n-  });\r\n-}\r\n-\r\n-/* =========================================\r\n-   BULK PASTE & MULTI ADD\r\n-   ========================================= */\r\n-function addMultiRows(n = 5) {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-  for (let i = 0; i < n; i++) addCRow();\r\n-  toast(`เพิ่ม ${n} ช่องแล้ว`, 'info');\r\n-}\r\n-\r\n-function openBulkPaste() {\r\n-  const id = document.getElementById('adm-deck-sel').value;\r\n-  if (!id) { toast('เลือกกองไพ่ก่อน', 'warning'); return; }\r\n-\r\n-  document.getElementById('bulk-paste-ov')?.remove();\r\n-  const ov = document.createElement('div');\r\n-  ov.id = 'bulk-paste-ov';\r\n-  ov.style.cssText = `\r\n-    position:fixed;inset:0;background:rgba(0,0,0,.88);\r\n-    z-index:700;display:flex;align-items:center;justify-content:center;\r\n-    backdrop-filter:blur(6px);\r\n-  `;\r\n-  ov.addEventListener('click', e => { if (e.target === ov) closeBulkPaste(); });\r\n-  ov.innerHTML = `\r\n-    <div style=\"\r\n-      background:linear-gradient(145deg,#181818,#101010);\r\n-      border:1px solid #2a2a2a;border-radius:20px;\r\n-      padding:28px 24px;max-width:520px;width:94%;position:relative;\r\n-    \">\r\n-      <div style=\"position:absolute;top:0;left:0;right:0;height:2px;\r\n-        background:linear-gradient(90deg,transparent,var(--red),transparent);\r\n-        border-radius:20px 20px 0 0;\"></div>\r\n-      <div style=\"font-family:'Kanit',sans-serif;font-weight:800;font-size:1.1rem;letter-spacing:2px;margin-bottom:6px;\">\r\n-        <i class=\"fi fi-sr-copy-alt\" style=\"color:var(--red);\"></i> วางไพ่หลายใบพร้อมกัน\r\n-      </div>\r\n-      <div style=\"color:var(--text3);font-size:.78rem;margin-bottom:14px;\">\r\n-        1 บรรทัด = 1 ใบ &nbsp;·&nbsp; รูปแบบ:\r\n-        <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">ข้อความ</code>\r\n-        หรือ <code style=\"background:#222;padding:1px 5px;border-radius:4px;\">หมวด | ข้อความ</code>\r\n-      </div>\r\n-      <div style=\"display:flex;gap:8px;margin-bottom:10px;align-items:center;\">\r\n-        <label style=\"font-size:.8rem;color:var(--text2);white-space:nowrap;\">หมวดเริ่มต้น:</label>\r\n-        <input id=\"bulk-default-cat\" type=\"text\" placeholder=\"ทั่วไป\" style=\"\r\n-          flex:1;background:#151515;border:1px solid #2a2a2a;border-radius:6px;\r\n-          color:#fff;padding:6px 10px;font-family:'Kanit',sans-serif;font-size:.85rem;outline:none;\">\r\n-      </div>\r\n-      <textarea id=\"bulk-textarea\" placeholder=\"วางข้อความที่นี่...\\nเช่น:\\nดื่มเบียร์ 1 แก้ว\\nทำท่าตลก\\nพรีเมียม | ดื่ม 3 แก้วรัวๆ\" style=\"\r\n-        width:100%;height:220px;background:#0d0d0d;border:1px solid #2a2a2a;border-radius:10px;\r\n-        color:#fff;padding:12px;font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-        line-height:1.7;resize:vertical;outline:none;box-sizing:border-box;\r\n-      \"></textarea>\r\n-      <div id=\"bulk-preview-count\" style=\"font-size:.75rem;color:var(--text3);margin:6px 0 14px;min-height:18px;\"></div>\r\n-      <div style=\"display:flex;gap:10px;\">\r\n-        <button onclick=\"closeBulkPaste()\" style=\"\r\n-          flex:1;background:transparent;border:1px solid #333;color:var(--text2);\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-size:.88rem;\r\n-        \">ยกเลิก</button>\r\n-        <button onclick=\"applyBulkPaste()\" style=\"\r\n-          flex:2;background:var(--red);border:none;color:#fff;\r\n-          padding:11px;border-radius:8px;cursor:pointer;\r\n-          font-family:'Kanit',sans-serif;font-weight:700;font-size:.95rem;\r\n-          display:flex;align-items:center;justify-content:center;gap:8px;\r\n-        \"><i class=\"fi fi-sr-add\"></i> เพิ่มไพ่</button>\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n-  document.body.appendChild(ov);\r\n-  const ta = ov.querySelector('#bulk-textarea');\r\n-  const pc = ov.querySelector('#bulk-preview-count');\r\n-  ta.addEventListener('input', () => {\r\n-    const lines = ta.value.split('\\n').filter(l => l.trim());\r\n-    pc.textContent = lines.length ? `จะเพิ่ม ${lines.length} ใบ` : '';\r\n-  });\r\n-  ta.focus();\r\n-}\r\n-\r\n-function closeBulkPaste() {\r\n-  document.getElementById('bulk-paste-ov')?.remove();\r\n-}\r\n-\r\n-function applyBulkPaste() {\r\n-  const ta     = document.getElementById('bulk-textarea');\r\n-  const defCat = (document.getElementById('bulk-default-cat')?.value.trim()) || 'ทั่วไป';\r\n-  const lines  = ta.value.split('\\n').map(l => l.trim()).filter(Boolean);\r\n-  if (!lines.length) { toast('ไม่มีข้อความ', 'warning'); return; }\r\n-  lines.forEach(line => {\r\n-    if (line.includes('|')) {\r\n-      const [cat, ...rest] = line.split('|');\r\n-      addCRow(rest.join('|').trim(), cat.trim() || defCat);\r\n-    } else {\r\n-      addCRow(line, defCat);\r\n-    }\r\n-  });\r\n-  closeBulkPaste();\r\n-  toast(`เพิ่ม ${lines.length} ใบแล้ว — กด \"บันทึก\" เพื่อบันทึก`, 'success');\r\n-}\n-/* =========================================\r\n-   admin.js — Admin Panel Logic\r\n-   ========================================= */\r\n-\r\n-let editDeckId = null;\r\n-\r\n-/* ---- TABS ---- */\r\n-function switchTab(name, idx) {\r\n-  document.querySelectorAll('.tab').forEach((t, i) => t.classList.toggle('act', i === idx));\r\n-  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('act'));\r\n-  const target = document.getElementById('tc-' + name);\r\n-  if (target) target.classList.add('act');\r\n-\r\n-  // อัปเดต URL ให้ต่างกันแต่ละ tab\r\n-  history.replaceState({}, '', '#admin/' + name);\r\n-\r\n-  if (name === 'users')    loadUserTab();\r\n-  if (name === 'stats')    loadStatsTab();\r\n-  if (name === 'history')  loadHistoryTab();\r\n-  if (name === 'announce') loadAnnounceTab();\r\n-  if (name === 'settings') { loadSettingsTab(); }\r\n-  if (name === 'cards')    { renderAdmSel(); }\r\n-if (name === 'decks')    { renderAdmDecks(); renderAdmSel(); }  // ← เพิ่ม\r\n-if (name === 'search')   { initCardSearch(); }\r\n-if (name === 'sheet')    {} // static content\r\n-}\r\n-\r\n-/* ---- TAB LOADERS ---- */\r\n-async function loadUserTab() {\r\n-  const container = document.getElementById('tc-users');\r\n-  if (!container) return;\r\n-  // loadAdminUserList จาก app.js inject เข้า #admin-user-list ซึ่งอยู่ใน tc-users แล้ว\r\n-  if (typeof loadAdminUserList === 'function') await loadAdminUserList();\r\n-}\r\n-\r\n-async function loadStatsTab() {\r\n-  const container = document.getElementById('tc-stats');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\"><i class=\"fi fi-sr-spinner fi-spin\"></i> กำลังโหลดสถิติ...</p>';\r\n-\r\n-  // ดึงข้อมูลพร้อมกันทั้งหมด\r\n-  const [\r\n-    users,\r\n-    { data: history },\r\n-    { data: packages }\r\n-  ] = await Promise.all([\r\n-    API.getAllUsers(),\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(500),\r\n-    _sb.from('packages').select('*').eq('is_active', true)\r\n-  ]);\r\n-\r\n-  const now        = new Date();\r\n-  const weekAgo    = new Date(now - 7  * 86400000);\r\n-  const monthAgo   = new Date(now - 30 * 86400000);\r\n-\r\n-  // ---- ผู้ใช้ ----\r\n-  const totalUsers    = users.length;\r\n-  const bannedUsers   = users.filter(u => u.is_banned).length;\r\n-  const adminUsers    = users.filter(u => u.is_admin).length;\r\n-  const activeWeek    = users.filter(u => u.last_seen && new Date(u.last_seen) >= weekAgo).length;\r\n-  const activeMonth   = users.filter(u => u.last_seen && new Date(u.last_seen) >= monthAgo).length;\r\n-  const totalCredits  = users.reduce((s, u) => s + (u.credits || 0), 0);\r\n-  const topCredUser   = [...users].sort((a, b) => (b.credits||0) - (a.credits||0))[0];\r\n-\r\n-  // ---- กองไพ่ & ไพ่ ----\r\n-  const activeDecks  = DB.decks.filter(d => !d.hidden).length;\r\n-  const hiddenDecks  = DB.decks.filter(d =>  d.hidden).length;\r\n-  const totalCards   = Object.values(DB.cards).reduce((s, a) => s + a.length, 0);\r\n-\r\n-  // top deck by card count\r\n-  const decksSorted  = [...DB.decks].sort((a, b) => (DB.cards[b.id]||[]).length - (DB.cards[a.id]||[]).length);\r\n-\r\n-  // ---- เครดิต / ประวัติ ----\r\n-  const hist         = history || [];\r\n-  const purchases    = hist.filter(h => h.type === 'purchase');\r\n-  const frees        = hist.filter(h => h.type === 'free');\r\n-  const deducts      = hist.filter(h => h.type === 'deduct');\r\n-  const totalEarned  = purchases.reduce((s, h) => s + (h.amount || 0), 0);\r\n-  const totalUsed    = Math.abs(deducts.reduce((s, h) => s + (h.amount || 0), 0));\r\n-\r\n-  const purchaseThisMonth = purchases.filter(h => new Date(h.created_at) >= monthAgo);\r\n-  const revenueEst        = purchaseThisMonth.reduce((s, h) => {\r\n-    const pkg = (packages || []).find(p => {\r\n-      const total = p.coins + p.bonus;\r\n-      return total === h.amount;\r\n-    });\r\n-    return s + (pkg ? pkg.price : 0);\r\n-  }, 0);\r\n-\r\n-  // ---- ผู้ใช้ลงทะเบียนใหม่ ----\r\n-  const newThisWeek  = users.filter(u => {\r\n-    // ใช้ last_seen เป็น proxy ถ้าไม่มี created_at\r\n-    return u.last_seen && new Date(u.last_seen) >= weekAgo;\r\n-  }).length;\r\n-\r\n-  // ---- สร้าง HTML ----\r\n-  container.innerHTML = `\r\n-    <!-- ROW 1: ตัวเลขหลัก -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-users\" style=\"color:var(--gold);\"></i> ผู้ใช้งาน</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-users\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalUsers.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ผู้ใช้ทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-user-check\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeWeek}</div>\r\n-            <div class=\"sc2-lbl\">Active 7 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(33,150,243,.1);color:#42a5f5;\"><i class=\"fi fi-sr-calendar\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#42a5f5;\">${activeMonth}</div>\r\n-            <div class=\"sc2-lbl\">Active 30 วัน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-ban\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">${bannedUsers}</div>\r\n-            <div class=\"sc2-lbl\">ถูกแบน</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 2: เครดิต -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-coins\" style=\"color:var(--gold);\"></i> เครดิตในระบบ</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-piggy-bank\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCredits.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตคงเหลือ (ทุกคน)</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-add\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">+${totalEarned.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ซื้อทั้งหมด</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-layers\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--red);\">-${totalUsed.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">เครดิตที่ใช้ไปรวม</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(156,39,176,.1);color:#ce93d8;\"><i class=\"fi fi-sr-gift\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#ce93d8;\">${frees.length}</div>\r\n-            <div class=\"sc2-lbl\">รับฟรีทั้งหมด (ครั้ง)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 3: กองไพ่ -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-layers\" style=\"color:var(--red);\"></i> กองไพ่ & ไพ่</h3>\r\n-      <div class=\"stats-grid-4\">\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(76,175,80,.1);color:#4caf50;\"><i class=\"fi fi-sr-eye\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#4caf50;\">${activeDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่แสดง</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(85,85,85,.15);color:#777;\"><i class=\"fi fi-sr-eye-crossed\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:#777;\">${hiddenDecks}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่ซ่อน</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(229,9,20,.1);color:var(--red);\"><i class=\"fi fi-sr-copy-alt\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\">${totalCards.toLocaleString()}</div>\r\n-            <div class=\"sc2-lbl\">ไพ่ทั้งหมดในระบบ</div>\r\n-          </div>\r\n-        </div>\r\n-        <div class=\"stat-card2\">\r\n-          <div class=\"sc2-ico\" style=\"background:rgba(245,197,24,.1);color:var(--gold);\"><i class=\"fi fi-sr-trophy\"></i></div>\r\n-          <div class=\"sc2-body\">\r\n-            <div class=\"sc2-val\" style=\"color:var(--gold);font-size:1rem;\">${decksSorted[0]?.name || '—'}</div>\r\n-            <div class=\"sc2-lbl\">กองไพ่ที่มีไพ่มากสุด (${(DB.cards[decksSorted[0]?.id]||[]).length} ใบ)</div>\r\n-          </div>\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 4: Top Decks -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-ranking-star\" style=\"color:var(--gold);\"></i> กองไพ่ — จัดอันดับตามจำนวนไพ่</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${decksSorted.slice(0, 6).map((d, i) => {\r\n-          const cnt    = (DB.cards[d.id] || []).length;\r\n-          const maxCnt = (DB.cards[decksSorted[0]?.id] || []).length || 1;\r\n-          const pct    = Math.round(cnt / maxCnt * 100);\r\n-          const th     = getTh(d);\r\n-          return `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div class=\"drow-ico ${th}\" style=\"width:34px;height:34px;font-size:.85rem;flex-shrink:0;border-radius:8px;display:flex;align-items:center;justify-content:center;\">\r\n-              <i class=\"fi ${d.icon||'fi-sr-layers'}\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;min-width:0;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;\">\r\n-                ${d.name}\r\n-                ${d.hidden ? '<span style=\"color:var(--text3);font-size:.7rem;margin-left:6px;\">(ซ่อน)</span>' : ''}\r\n-              </div>\r\n-              <div style=\"margin-top:5px;height:4px;background:#222;border-radius:4px;overflow:hidden;\">\r\n-                <div style=\"height:100%;width:${pct}%;background:linear-gradient(90deg,var(--red),#ff4d4d);border-radius:4px;transition:.5s;\"></div>\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--gold);\">${cnt}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">ใบ</div>\r\n-            </div>\r\n-            <div style=\"text-align:right;flex-shrink:0;\">\r\n-              <div style=\"font-weight:700;color:var(--text2);\">${d.cost||1}</div>\r\n-              <div style=\"font-size:.68rem;color:var(--text3);\">เครดิต/ใบ</div>\r\n-            </div>\r\n-          </div>`;\r\n-        }).join('')}\r\n-      </div>\r\n-    </div>\r\n-\r\n-    <!-- ROW 5: Top Users by Credits -->\r\n-    <div class=\"sheet-sec\">\r\n-      <h3><i class=\"fi fi-sr-crown\" style=\"color:var(--gold);\"></i> ผู้ใช้ที่มีเครดิตสูงสุด</h3>\r\n-      <div style=\"display:flex;flex-direction:column;gap:8px;margin-top:12px;\">\r\n-        ${[...users].sort((a,b)=>(b.credits||0)-(a.credits||0)).slice(0,5).map((u, i) => `\r\n-          <div style=\"display:flex;align-items:center;gap:12px;background:#161616;border:1px solid #222;border-radius:10px;padding:10px 14px;\">\r\n-            <div style=\"width:22px;color:${i<3?'var(--gold)':'var(--text3)'};font-weight:900;font-size:.9rem;text-align:center;\">${i+1}</div>\r\n-            <div style=\"width:34px;height:34px;border-radius:50%;background:rgba(229,9,20,.12);border:1px solid rgba(229,9,20,.2);display:flex;align-items:center;justify-content:center;color:var(--red);flex-shrink:0;\">\r\n-              <i class=\"fi ${u.is_admin?'fi-sr-shield-check':'fi-sr-user'}\" style=\"font-size:.8rem;\"></i>\r\n-            </div>\r\n-            <div style=\"flex:1;\">\r\n-              <div style=\"font-weight:700;font-size:.88rem;\">\r\n-                ${u.username}\r\n-                ${u.is_admin?'<span style=\"color:var(--red);font-size:.7rem;margin-left:6px;\">admin</span>':''}\r\n-                ${u.is_banned?'<span style=\"color:#777;font-size:.7rem;margin-left:6px;\">banned</span>':''}\r\n-              </div>\r\n-              <div style=\"font-size:.72rem;color:var(--text3);\">\r\n-                ${u.last_seen ? 'Active ' + new Date(u.last_seen).toLocaleDateString('th-TH',{day:'2-digit',month:'short'}) : 'ไม่มีข้อมูล'}\r\n-              </div>\r\n-            </div>\r\n-            <div style=\"font-weight:900;font-size:1.1rem;color:var(--gold);\">\r\n-              ${(u.credits||0).toLocaleString()}\r\n-              <span style=\"font-size:.65rem;color:var(--text3);font-weight:400;\">เครดิต</span>\r\n-            </div>\r\n-          </div>`).join('')}\r\n-      </div>\r\n-    </div>\r\n-  `;\r\n-\r\n-  // update legacy stat elements ด้านบน (stat bar)\r\n-  const elUsers = document.getElementById('stat-users');\r\n-  if (elUsers) elUsers.textContent = totalUsers;\r\n-}\r\n-async function loadHistoryTab() {\r\n-  const container = document.getElementById('tc-history');\r\n-  if (!container) return;\r\n-  container.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:16px;\">กำลังโหลด...</p>';\r\n-\r\n-  // ดึง history และ users พร้อมกัน\r\n-  const [{ data, error }, { data: users }] = await Promise.all([\r\n-    _sb.from('credit_history').select('*').order('created_at', { ascending: false }).limit(100),\r\n-    _sb.from('profiles').select('id, username')\r\n-  ]);\r\n-\r\n-  if (error || !data?.length) {\r\n-    container.innerHTML = '<p style=\"color:var(--text3);padding:16px;\">ไม่พบประวัติ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  // สร้าง map id → username\r\n-  const userMap = {};\r\n-  (users || []).forEach(u => { userMap[u.id] = u.username; });\r\n-\r\n-  const TYPE_ICO = {\r\n-    free:             'fi-sr-gift',\r\n-    purchase:         'fi-sr-shopping-cart',\r\n-    purchase_pending: 'fi-sr-clock',       // ✅ เพิ่ม\r\n-    deduct:           'fi-sr-layers',\r\n-    admin:            'fi-sr-shield-check',\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    free:             'รับฟรี',\r\n-    purchase:         'ซื้อเครดิต',\r\n-    purchase_pending: 'รอชำระ',            // ✅ เพิ่ม\r\n-    deduct:           'ใช้ไพ่',\r\n-    admin:            'แอดมินเติม',\r\n-  };\r\n-\r\n-  const rows = data.map(h => {\r\n-    const isPlus   = h.amount > 0;\r\n-    const typeKey  = h.type || 'deduct';\r\n-    const ico      = TYPE_ICO[typeKey]   || 'fi-sr-coins';\r\n-    const label    = TYPE_LABEL[typeKey] || typeKey;\r\n-    const username = userMap[h.user_id]  || h.user_id?.slice(0,8) || '?';\r\n-    const date     = new Date(h.created_at);\r\n-    const dateStr  = date.toLocaleDateString('th-TH', { day:'2-digit', month:'short', year:'numeric' });\r\n-    const timeStr  = date.toLocaleTimeString('th-TH', { hour:'2-digit', minute:'2-digit' });\r\n-\r\n-    return `\r\n-    <div class=\"hist-row\">\r\n-      <div class=\"hist-ico ${isPlus ? 'hist-ico-plus' : 'hist-ico-minus'}\">\r\n-        <i class=\"fi ${ico}\"></i>\r\n-      </div>\r\n-      <div class=\"hist-info\">\r\n-        <div class=\"hist-top\">\r\n-          <span class=\"hist-note\">${h.note || label}</span>\r\n-          <span class=\"hist-type-badge hist-type-${typeKey}\">${label}</span>\r\n-        </div>\r\n-        <div class=\"hist-sub\">\r\n-          <span class=\"hist-user\"><i class=\"fi fi-sr-user\"></i> ${username}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-date\"><i class=\"fi fi-sr-calendar\"></i> ${dateStr}</span>\r\n-          <span class=\"hist-sep\">·</span>\r\n-          <span class=\"hist-time\"><i class=\"fi fi-sr-clock\"></i> ${timeStr}</span>\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"hist-amount ${isPlus ? 'hist-plus' : 'hist-minus'}\">\r\n-        ${isPlus ? '+' : ''}${h.amount}\r\n-        <span class=\"hist-unit\">เครดิต</span>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-\r\n-  container.innerHTML = `<div style=\"display:flex;flex-direction:column;gap:10px;\">${rows}</div>`;\r\n-}\r\n-async function loadAnnounceTab() {\r\n-  const container = document.getElementById('tc-announce');\r\n-  if (!container) return;\r\n-  const listEl = document.getElementById('announce-list');\r\n-  if (!listEl) return;\r\n-\r\n-  listEl.innerHTML = '<p style=\"color:var(--text3);font-size:.85rem;padding:12px;\">กำลังโหลด...</p>';\r\n-\r\n-  const { data, error } = await _sb\r\n-    .from('announcements')\r\n-    .select('*')\r\n-    .order('created_at', { ascending: false });\r\n-\r\n-  if (error || !data?.length) {\r\n-    listEl.innerHTML = '<p style=\"color:var(--text3);padding:12px;\">ไม่พบประกาศ</p>';\r\n-    return;\r\n-  }\r\n-\r\n-  const TYPE_ICO = {\r\n-    info: 'fi-sr-info', promo: 'fi-sr-star',\r\n-    warning: 'fi-sr-triangle-warning', success: 'fi-sr-check-circle'\r\n-  };\r\n-  const TYPE_LABEL = {\r\n-    info: 'ข้อมูล', promo: 'โปรโมชั่น', warning: 'แจ้งเตือน', success: 'ข่าวดี'\r\n-  };\r\n-\r\n-  listEl.innerHTML = data.map(a => {\r\n-    const t       = a.type || 'info';\r\n-    const created = new Date(a.created_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' });\r\n-    const expires = a.expires_at\r\n-      ? new Date(a.expires_at).toLocaleString('th-TH', { dateStyle:'medium', timeStyle:'short' })\r\n-      : null;\r\n-    return `\r\n-    <div class=\"ann-row\">\r\n-      <div class=\"ann-row-ico ann-ico-${t}\">\r\n-        <i class=\"fi ${TYPE_ICO[t] || 'fi-sr-bell'}\"></i>\r\n-      </div>\r\n-      <div class=\"ann-row-info\">\r\n-        <div class=\"ann-row-top\">\r\n-          <span class=\"ann-row-title\">${a.title || '(ไม่มีชื่อ)'}</span>\r\n-          <span class=\"ann-badge ann-badge-${t}\">${TYPE_LABEL[t] || t}</span>\r\n-          <span class=\"ann-badge ${a.active ? 'ann-badge-active' : 'ann-badge-draft'}\">\r\n-            <i class=\"fi ${a.active ? 'fi-sr-eye' : 'fi-sr-eye-crossed'}\"></i>\r\n-            ${a.active ? 'เผยแพร่' : 'draft'}\r\n-          </span>\r\n-          ${a.show_popup ? '<span class=\"ann-badge ann-badge-info\"><i class=\"fi fi-sr-window-popup\"></i> popup</span>' : ''}\r\n-        </div>\r\n-        <div class=\"ann-row-msg\">${a.message || ''}</div>\r\n-        <div class=\"ann-row-sub\">\r\n-          <span><i class=\"fi fi-sr-calendar\"></i> ${created}</span>\r\n-          ${expires ? `<span class=\"sep\">·</span><span><i class=\"fi fi-sr-hourglass-end\"></i> หมดอายุ ${expires}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-      <div class=\"ann-row-acts\">\r\n-        <button class=\"btn-sm btn-edit\" onclick=\"toggleAnnounce('${a.id}', ${!a.active})\">\r\n-          <i class=\"fi fi-sr-${a.active ? 'eye-crossed' : 'eye'}\"></i>\r\n-          ${a.active ? 'ซ่อน' : 'เผยแพร่'}\r\n-        </button>\r\n-        <button class=\"btn-sm btn-del\" onclick=\"deleteAnnounce('${a.id}')\">\r\n-          <i class=\"fi fi-sr-trash\"></i>\r\n-        </button>\r\n-      </div>\r\n-    </div>`;\r\n-  }).join('');\r\n-}\r\n-\r\n-async function createAnnounce() {\r\n-  const title   = document.getElementById('ann-title')?.value.trim();\r\n-  const message = document.getElementById('ann-message')?.value.trim();\r\n-  const type    = document.getElementById('ann-type')?.value || 'info';\r\n-  const popup   = document.getElementById('ann-popup')?.value === '1';\r\n-  const active  = document.getElementById('ann-active')?.value === '1';\r\n-  const expires = document.getElementById('ann-expires')?.value || null;\r\n-\r\n-  if (!title || !message) { toast('กรุณากรอกหัวข้อและข้อความ', 'warning'); return; }\r\n-\r\n-  const { error } = await _sb.from('announcements').insert({\r\n-    title, message, type,\r\n-    show_popup: popup,\r\n-    active,\r\n-    expires_at: expires ? new Date(expires).toISOString() : null,\r\n-  });\r\n-\r\n-  if (error) { toast('เกิดข้อผิดพลาด: ' + error.message, 'error'); return; }\r\n-\r\n-  toast('เพิ่มประกาศแล้ว', 'success');\r\n-  document.getElementById('ann-title').value   = '';\r\n-  document.getElementById('ann-message').value = '';\r\n-  document.getElementById('ann-expires').value = '';\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function deleteAnnounce(id) {\r\n-  if (!confirm('ลบประกาศนี้?')) return;\r\n-  const { error } = await _sb.from('announcements').delete().eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast('ลบประกาศแล้ว', 'info');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-async function toggleAnnounce(id, active) {\r\n-  const { error } = await _sb.from('announcements').update({ active }).eq('id', id);\r\n-  if (error) { toast('เกิดข้อผิดพลาด', 'error'); return; }\r\n-  toast(active ? 'เผยแพร่ประกาศแล้ว' : 'ซ่อนประกาศแล้ว', 'success');\r\n-  loadAnnounceTab();\r\n-}\r\n-\r\n-/* ---- DECK LIST ---- */\r\n-function renderAdmDecks() {\r\n-  // update stats-bar (ด้านบนสุดของ admin panel)\r\n-  const deckEl = document.getElementById('stat-decks');\r\n-  const cardEl = document.getElementById('stat-cards');\r\n-  if (deckEl) deckEl.textContent = DB.decks.filter(d => !d.hidden).length;\r\n-  if (cardEl) cardEl.textContent = Object.values(DB.cards).reduce((s, arr) => s + arr.length, 0).toLocaleString();\r\n-\r\n-  // render deck list ใน tab กองไพ่\r\n-  const l = document.getElementById('adm-deck-list');\r\n-  if (!l) return;\r\n-  l.innerHTML = '';\r\n-  DB.decks.forEach(d => {\r\n-    const cnt = (DB.cards[d.id] || []).length;\r\n-    const th  = getTh(d);\r\n-    const r   = document.createElement('div');\r\n-    r.className = 'drow';\r\n-    r.innerHTML = `\r\n-    <div class=\"drow-info\">\r\n-      <div class=\"drow-ico ${th}\"><i class=\"fi ${d.icon || 'fi-sr-layers'}\"></i></div>\r\n-      <div>\r\n-        <div class=\"drow-nm\">${d.name}</div>\r\n-        <div class=\"drow-meta\">\r\n-          <span class=\"tag\"><i class=\"fi fi-rr-copy\" style=\"font-size:.7rem;\"></i> ${cnt} ใบ</span>\r\n-          <span class=\"tag\" style=\"color:var(--gold);\"><i class=\"fi fi-sr-coins\" style=\"font-size:.7rem;\"></i> ${d.cost || 1}/ใบ</span>\r\n-          ${d.desc ? `<span style=\"color:var(--text3);\">${d.desc}</span>` : ''}\r\n-        </div>\r\n-      </div>\r\n-    </div>\r\n-    <div class=\"drow-acts\">\r\n-<div style=\"display:flex;flex-direction:column;gap:2px;margin-right:2px;\">\r\n-<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',-1)\" title=\"เลื่อนขึ้น\"><i class=\"fi fi-sr-angle-up\"></i></button>\r\n-<button class=\"btn-sm\" style=\"padding:4px 10px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#888;\" onclick=\"moveDeck('${d.id}',1)\" title=\"เลื่อนลง\"><i class=\"fi fi-sr-angle-down\"></i></button>\r\n-</div>\r\n-      <button class=\"btn-sm ${d.hidden ? 'btn-hidden' : 'btn-visible'}\" onclick=\"toggleDeck('${d.id}')\">\r\n-        <i class=\"fi ${d.hidden ? 'fi-sr-eye-crossed' : 'fi-sr-eye'}\"></i>\r\n-        ${d.hidden ? 'ซ่อนอยู่' : 'แสดงอยู่'}\r\n-      </button>\r\n-      <button class=\"btn-sm btn-edit\" onclick=\"editDeck('${d.id}')\"><i class=\"fi fi-sr-edit\"></i> แก้ไข</button>\r\n-      <button class=\"btn-sm\" style=\"background:rgba(245,197,24,.08);border:1px solid rgba(245,197,24,.2);color:var(--gold);\" onclick=\"duplicateDeck('${d.id}')\"><i class=\"fi fi-sr-copy-alt\"></i> ก็อปปี้</button>\r\n-      <button class=\"btn-sm btn-del\"  onclick=\"delDeck('${d.id}')\"><i class=\"fi fi-sr-trash\"></i> ลบ</button>\r\n-    </div>`;\r\n-    l.appendChild(r);\r\n-  });\r\n-}\r\n-async function moveDeck(id, dir) {\r\n-  const idx = DB.decks.findIndex(d => d.id === id);\r\n-  const swapIdx = idx + dir;\r\n-  if (idx < 0 || swapIdx < 0 || swapIdx >= DB.decks.length) return;\r\n-\r\n-  const a = DB.decks[idx];\r\n-  const b = DB.decks[swapIdx];\r\n-  const tmpOrder = a.sort_order ?? idx;\r\n-  a.sort_order = b.sort_order ?? swapIdx;\r\n-  b.sort_order = tmpOrder;\r\n-\r\n-  DB.decks[idx]     = b;\r\n-  DB.decks[swapIdx] = a;\r\n-\r\n-  const [r1, r2] = await Promise.all([\r\n-    _sb.from('decks').update({ sort_order: a.sort_order }).eq('id', a.id),\r\n-    _sb.from('decks').update({ sort_order: b.sort_order }).eq('id', b.id),\r\n-  ]);\r\n-\r\n-  if (r1.error || r2.error) {\r\n-    toast('บันทึกลำดับไม่สำเร็จ', 'error');\r\n-    await initDB();\r\n-  }\r\n-\r\n-  renderAdmDecks();\r\n-  renderDecks();\r\n-}\r\n-async function toggleDeck(id) {\r\n-  const d = DB.decks.find(x => x.id === id);\r\n-  if (!d) return;\r\n-  d.hidden = !d.hidden;\r\n-\r\n-  // บันทึกไป Supabase จริง\r\n-  const { error } = await _sb.from('decks').update({ hidden: d.hidden }).eq('id', id);\r\n-  if (error) {\r\n-    d.hidden = !d.hidden; // rollback\r\n-    toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error');\r\n-    return;\r\n-  }\r\n-\r\n-  renderAdmDecks();\r\n-  renderDecks();\r\n-  toast(d.hidden ? 'ซ่อนกองไพ่แล้ว' : 'แสดงกองไพ่แล้ว', 'info');\r\n-}\r\n-\r\n-function renderAdmSel() {\r\n-  const s = document.getElementById('adm-deck-sel');\r\n-  s.innerHTML = '<option value=\"\">-- เลือกกองไพ่ --</option>';\r\n-  DB.decks.forEach(d => {\r\n-    const o = document.createElement('option');\r\n-    o.value = d.id;\r\n-    o.textContent = d.name;\r\n-    s.appendChild(o);\r\n-  });\r\n-}\r\n-\r\n-const THEME_PRESETS = [\r\n-  { name:'fire',            bg:'linear-gradient(135deg,rgba(229,57,53,.35),rgba(183,28,28,.2))',   color:'#ff5252' },\r\n-  { name:'magic',           bg:'linear-gradient(135deg,rgba(156,39,176,.35),rgba(106,27,154,.2))', color:'#e040fb' },\r\n-  { name:'skull',           bg:'linear-gradient(135deg,rgba(229,9,20,.35),rgba(150,0,10,.2))',     color:'#ff1744' },\r\n-  { name:'heart',           bg:'linear-gradient(135deg,rgba(233,30,99,.35),rgba(173,20,87,.2))',   color:'#ff4081' },\r\n-  { name:'star',            bg:'linear-gradient(135deg,rgba(245,197,24,.35),rgba(198,145,0,.2))',  color:'#ffd740' },\r\n-  { name:'bolt',            bg:'linear-gradient(135deg,rgba(255,152,0,.35),rgba(230,81,0,.2))',    color:'#ffab40' },\r\n-  { name:'ocean',           bg:'linear-gradient(135deg,rgba(33,150,243,.35),rgba(13,71,161,.2))',  color:'#40c4ff' },\r\n-  { name:'emerald',         bg:'linear-gradient(135deg,rgba(0,188,140,.35),rgba(0,121,107,.2))',   color:'#64ffda' },\r\n-  { name:'lavender',        bg:'linear-gradient(135deg,rgba(149,117,205,.35),rgba(94,53,177,.2))', color:'#ce93d8' },\r\n-  { name:'silver',          bg:'linear-gradient(135deg,rgba(176,190,197,.2),rgba(96,125,139,.15))',color:'#eceff1' },\r\n-  { name:'rose',            bg:'linear-gradient(135deg,rgba(236,64,122,.35),rgba(173,20,87,.2))',  color:'#ff80ab' },\r\n-  { name:'amber',           bg:'linear-gradient(135deg,rgba(255,193,7,.35),rgba(255,111,0,.2))',   color:'#ffe57f' },\r\n-  { name:'default',         bg:'linear-gradient(135deg,rgba(255,255,255,.08),rgba(255,255,255,.04))', color:'#cfd8dc' },\r\n-  { name:'gold_amber',      bg:'linear-gradient(135deg,rgba(245,197,24,.55),rgba(255,120,0,.55))', color:'#ffd740' },\r\n-  { name:'emerald_teal',    bg:'linear-gradient(135deg,rgba(0,200,100,.5),rgba(0,230,200,.5))',    color:'#69ffda' },\r\n-  { name:'rose_heart',      bg:'linear-gradient(135deg,rgba(255,60,100,.5),rgba(220,20,80,.5))',   color:'#ff80ab' },\r\n-  { name:'ocean_lavender',  bg:'linear-gradient(135deg,rgba(30,140,255,.5),rgba(140,90,220,.5))',  color:'#90caff' },\r\n-  { name:'fire_amber',      bg:'linear-gradient(135deg,rgba(230,50,40,.55),rgba(255,140,0,.55))',  color:'#ff7043' },\r\n-  { name:'magic_lavender',  bg:'linear-gradient(135deg,rgba(160,30,190,.55),rgba(140,100,220,.55))',color:'#e040fb'},\r\n-  { name:'bolt_gold',       bg:'linear-gradient(135deg,rgba(255,140,0,.55),rgba(240,200,0,.55))',  color:'#ffcc02' },\r\n-  { name:'ocean_emerald',   bg:'linear-gradient(135deg,rgba(20,140,255,.5),rgba(0,200,160,.5))',   color:'#40c4ff' },\r\n-];\r\n-\r\n function _initThemePicker(currentTheme) {\r\n   const swatches = document.getElementById('df-theme-swatches');\r\n   if (!swatches) return;\r\n   swatches.innerHTML = '';\r\n@@ -2039,8 +691,9 @@\n   const ok = await saveDeckToDB(deck);\r\n   if (!ok) { toast('เกิดข้อผิดพลาด บันทึกไม่สำเร็จ', 'error'); return; }\r\n \r\n   await initDB();\r\n+  injectCustomThemes(); // ← เพิ่มบรรทัดนี้\r\n   closeDeckForm();\r\n   renderAdmDecks();\r\n   renderAdmSel();\r\n   renderDecks();\r\n"
                },
                {
                    "date": 1775746569235,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -573,51 +573,46 @@\n   { name:'bolt_gold',       bg:'linear-gradient(135deg,rgba(255,140,0,.55),rgba(240,200,0,.55))',  color:'#ffcc02' },\r\n   { name:'ocean_emerald',   bg:'linear-gradient(135deg,rgba(20,140,255,.5),rgba(0,200,160,.5))',   color:'#40c4ff' },\r\n ];\r\n \r\n+\r\n function _initThemePicker(currentTheme) {\r\n   const swatches = document.getElementById('df-theme-swatches');\r\n   if (!swatches) return;\r\n   swatches.innerHTML = '';\r\n \r\n   THEME_PRESETS.forEach(t => {\r\n     const sw = document.createElement('div');\r\n-    sw.title = t.name.replace(/_/g, ' ');\r\n-    const isSel = currentTheme === t.name;\r\n-    sw.style.cssText = `\r\n-      aspect-ratio:1;border-radius:8px;cursor:pointer;\r\n-      background:${t.bg};\r\n-      border:2px solid ${isSel ? '#fff' : 'transparent'};\r\n-      transition:all .15s;\r\n-      transform:${isSel ? 'scale(1.15)' : 'scale(1)'};\r\n-      outline:${isSel ? '1px solid rgba(255,255,255,.2)' : 'none'};\r\n-      outline-offset:2px;\r\n-    `;\r\n-    sw.onmouseenter = () => { if (currentTheme !== t.name) sw.style.transform = 'scale(1.08)'; };\r\n-    sw.onmouseleave = () => { if (currentTheme !== t.name) sw.style.transform = 'scale(1)'; };\r\n+    sw.title = t.name;\r\n+sw.style.cssText = `\r\n+  width:36px;height:36px;border-radius:10px;cursor:pointer;\r\n+  background:${t.bg};border:2px solid ${currentTheme===t.name?'#fff':'transparent'};\r\n+  transition:all .2s;flex-shrink:0;\r\n+  transform:${currentTheme===t.name?'scale(1.2)':'scale(1)'};\r\n+  box-shadow:0 2px 8px rgba(0,0,0,.4);\r\n+`;\r\n     sw.onclick = () => _selectTheme(t.name, t.bg, t.color);\r\n     swatches.appendChild(sw);\r\n   });\r\n \r\n+  // set preview เริ่มต้น\r\n   const cur = THEME_PRESETS.find(t => t.name === currentTheme) || THEME_PRESETS[0];\r\n-  _applyThemePreview(cur.bg, cur.color, cur.name.replace(/_/g, ' '));\r\n+  _applyThemePreview(cur.bg, cur.color);\r\n   document.getElementById('df-theme').value = currentTheme;\r\n \r\n-  document.getElementById('df-color1').oninput =\r\n-  document.getElementById('df-color2').oninput = () => {\r\n-    const c1 = document.getElementById('df-color1').value;\r\n-    const c2 = document.getElementById('df-color2').value;\r\n-    const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n-    _applyThemePreview(bg, c1, 'custom');\r\n-    document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n-    const cp = document.getElementById('df-custom-preview');\r\n-    if (cp) cp.style.background = bg;\r\n-    swatches.querySelectorAll('div').forEach(s => {\r\n-      s.style.borderColor = 'transparent';\r\n-      s.style.transform = 'scale(1)';\r\n-      s.style.outline = 'none';\r\n-    });\r\n-  };\r\n+  // custom color pickers\r\n+document.getElementById('df-color1').oninput =\r\n+document.getElementById('df-color2').oninput = () => {\r\n+  const c1 = document.getElementById('df-color1').value;\r\n+  const c2 = document.getElementById('df-color2').value;\r\n+  const bg = `linear-gradient(135deg,${c1}88,${c2}55)`;\r\n+  _applyThemePreview(bg, c1, 'custom');\r\n+  document.getElementById('df-theme').value = `custom:${c1}:${c2}`;\r\n+  const cp = document.getElementById('df-custom-preview');\r\n+  if (cp) cp.style.background = bg;\r\n+  document.getElementById('df-theme-swatches').querySelectorAll('div')\r\n+    .forEach(s => { s.style.borderColor='transparent'; s.style.transform='scale(1)'; });\r\n+};\r\n }\r\n \r\n function _selectTheme(name, bg, color) {\r\n   document.getElementById('df-theme').value = name;\r\n"
                },
                {
                    "date": 1775777114343,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1023,9 +1023,8 @@\n    ========================================= */\r\n let _csShowDup = false;\r\n \r\n function initCardSearch() {\r\n-  // populate deck dropdown\r\n   const deckSel = document.getElementById('cs-deck');\r\n   if (!deckSel) return;\r\n   deckSel.innerHTML = '<option value=\"\">ทุกกอง</option>';\r\n   DB.decks.forEach(d => {\r\n@@ -1033,9 +1032,8 @@\n     o.value = d.id; o.textContent = d.name;\r\n     deckSel.appendChild(o);\r\n   });\r\n \r\n-  // populate category dropdown (unique cats across all decks)\r\n   const catSel = document.getElementById('cs-cat');\r\n   if (catSel) {\r\n     const cats = new Set();\r\n     Object.values(DB.cards).forEach(arr => arr.forEach(c => cats.add(c.cat || 'ทั่วไป')));\r\n@@ -1050,8 +1048,27 @@\n   _csShowDup = false;\r\n   const dupBtn = document.getElementById('cs-dup-btn');\r\n   if (dupBtn) dupBtn.style.background = 'rgba(245,197,24,.08)';\r\n \r\n+  // ✅ เพิ่มปุ่มลบซ้ำถ้ายังไม่มี\r\n+  const toolbar = dupBtn?.parentElement;\r\n+  if (toolbar && !document.getElementById('cs-del-dup-btn')) {\r\n+    const delBtn = document.createElement('button');\r\n+    delBtn.id = 'cs-del-dup-btn';\r\n+    delBtn.innerHTML = '<i class=\"fi fi-sr-trash\"></i> ลบคำซ้ำ';\r\n+    delBtn.style.cssText = `\r\n+      display:flex;align-items:center;gap:6px;\r\n+      background:rgba(229,9,20,.08);border:1px solid rgba(229,9,20,.25);\r\n+      color:var(--red);padding:8px 14px;border-radius:8px;cursor:pointer;\r\n+      font-family:'Kanit',sans-serif;font-size:.85rem;font-weight:600;\r\n+      white-space:nowrap;transition:all .2s;\r\n+    `;\r\n+    delBtn.onmouseover = () => delBtn.style.background = 'rgba(229,9,20,.18)';\r\n+    delBtn.onmouseout  = () => delBtn.style.background = 'rgba(229,9,20,.08)';\r\n+    delBtn.onclick = removeDuplicateCards;\r\n+    toolbar.appendChild(delBtn);\r\n+  }\r\n+\r\n   runCardSearch();\r\n }\r\n \r\n function toggleDupFilter() {\r\n"
                }
            ],
            "date": 1774366244454,
            "name": "Commit-0",
            "content": ""
        }
    ]
}