{
    "sourceFile": "js/components.js",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 38,
            "patches": [
                {
                    "date": 1774366249004,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1774366375878,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,332 @@\n+let deckCards   = [];\r\n+let revCount    = 0;\r\n+let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n+let stackIndex  = 0;\r\n+let showModal = true;\r\n+\r\n+/* ---- ตรวจจับมือถือ ---- */\r\n+function isMobile() {\r\n+  return window.innerWidth <= 640;\r\n+}\r\n+\r\n+/* ---- LAYOUT TOGGLE ---- */\r\n+function setLayout(mode) {\r\n+  layoutMode = mode;\r\n+  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+  renderCards();\r\n+}\r\n+\r\n+function toggleModal() {\r\n+  showModal = !showModal;\r\n+  const btn = document.getElementById('lt-modal');\r\n+  btn.classList.toggle('act', showModal);\r\n+  btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n+  toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- START GAME ---- */\r\n+function startGame() {\r\n+  if (!selDeck) return;\r\n+  const deck  = DB.decks.find(d => d.id === selDeck);\r\n+  const cards = DB.cards[selDeck] || [];\r\n+\r\n+  if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n+  if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n+\r\n+  deckCards  = [...cards].sort(() => Math.random() - .5);\r\n+  revCount   = 0;\r\n+  stackIndex = 0;\r\n+\r\n+  document.getElementById('home-screen').style.display = 'none';\r\n+  document.getElementById('game-screen').style.display = 'block';\r\n+\r\n+  const th = getTh(deck);\r\n+  const iw = document.getElementById('g-ico-wrap');\r\n+  iw.className = 'g-deck-ico ' + th;\r\n+  document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n+  document.getElementById('g-nm').textContent      = deck.name;\r\n+  document.getElementById('cost-show').textContent = deck.cost || 1;\r\n+\r\n+  const rw = document.getElementById('res-ico-wrap');\r\n+  rw.className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+\r\n+  updRem();\r\n+  updateCr();\r\n+  renderCards();\r\n+}\r\n+\r\n+/* ---- REMAINING COUNT ---- */\r\n+function updRem() {\r\n+  const t = deckCards.length;\r\n+  document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 1 — SCATTER (กระจายเละ)\r\n+═══════════════════════════════════════════ */\r\n+function renderScatter() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+\r\n+  const mobile = isMobile();\r\n+  const deck     = DB.decks.find(d => d.id === selDeck);\r\n+  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n+  const deckTheme = getTh(deck);\r\n+\r\n+  // วัดพื้นที่จริงของ wrap\r\n+  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n+  const CW = mobile ? 72 : 96;\r\n+  const CH = mobile ? 104 : 138;\r\n+\r\n+  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n+  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n+  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n+  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n+\r\n+  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n+\r\n+  const total = deckCards.length;\r\n+\r\n+  const ZX = mobile ? 6 : 7;\r\n+  const ZY = mobile ? 5 : 5;\r\n+  const zW = W / ZX;\r\n+  const zH = H / ZY;\r\n+\r\n+  const zones = [];\r\n+  for (let zy = 0; zy < ZY; zy++)\r\n+    for (let zx = 0; zx < ZX; zx++)\r\n+      zones.push([zx, zy]);\r\n+\r\n+  for (let i = zones.length - 1; i > 0; i--) {\r\n+    const j = Math.floor(Math.random() * (i + 1));\r\n+    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n+  }\r\n+\r\n+  const frag = document.createDocumentFragment();\r\n+  const els  = [];\r\n+\r\n+  for (let i = 0; i < total; i++) {\r\n+    const [zx, zy] = zones[i % zones.length];\r\n+    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n+    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n+    const ang = (Math.random() - .5) * 72;\r\n+    const z   = Math.floor(Math.random() * total) + 1;\r\n+\r\n+    const el = document.createElement('div');\r\n+    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n+    el.dataset.i     = i;\r\n+    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n+    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    el.onclick       = () => flipCard(el, i, ang);\r\n+\r\n+    frag.appendChild(el);\r\n+    els.push(el);\r\n+  }\r\n+\r\n+  wrap.appendChild(frag);\r\n+\r\n+  const delay = mobile ? 5 : 10;\r\n+  els.forEach((el, i) => {\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n+      el.style.opacity    = '1';\r\n+    }, i * delay);\r\n+  });\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n+═══════════════════════════════════════════ */\r\n+function renderStack() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+  wrap.style.cssText = 'min-height:420px;position:relative;';\r\n+\r\n+  wrap.innerHTML = [\r\n+    '<div class=\"stack-layout\">',\r\n+      '<div class=\"stack-section stack-center\">',\r\n+        '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n+          '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n+          'กองไพ่',\r\n+        '</div>',\r\n+        '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n+        '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n+      '</div>',\r\n+    '</div>'\r\n+  ].join('');\r\n+\r\n+  buildStackPile();\r\n+}\r\n+\r\n+function buildStackPile() {\r\n+  const pileWrap = document.getElementById('stack-pile-wrap');\r\n+  const countEl  = document.getElementById('stack-count');\r\n+  if (!pileWrap) return;\r\n+\r\n+  const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n+  const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n+  const deckTheme = getTh(deck);                            // เพิ่ม\r\n+  pileWrap.innerHTML = '';\r\n+  const remaining = deckCards.length - stackIndex;\r\n+  if (countEl) countEl.textContent = remaining + ' ใบ';\r\n+\r\n+  if (remaining <= 0) {\r\n+    pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n+      + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n+      + 'หยิบครบแล้ว!</div>';\r\n+    return;\r\n+  }\r\n+\r\n+  const showCount = Math.min(remaining, 6);\r\n+  for (let i = showCount - 1; i >= 0; i--) {\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'crd stack-crd';\r\n+    const off = i * 2.5;\r\n+    const ang = (i - showCount / 2) * 1.8;\r\n+    el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n+      + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n+      + (i === 0\r\n+        ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n+        : 'cursor:default;');\r\n+  el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    if (i === 0) el.onclick = () => flipTopCard(el);\r\n+    pileWrap.appendChild(el);\r\n+  }\r\n+}\r\n+\r\n+function flipTopCard(el) {\r\n+  if (stackIndex >= deckCards.length) return;\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+\r\n+  el.style.animation     = '';\r\n+  el.style.pointerEvents = 'none';\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  const cardData = deckCards[stackIndex];\r\n+  stackIndex++;\r\n+  revCount++;\r\n+  updRem();\r\n+\r\n+  setTimeout(() => {\r\n+    el.classList.remove('flipping');\r\n+\r\n+    // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n+    el.classList.add('revealed');\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n+      + '</div>';\r\n+\r\n+    // fade out และ remove\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 300);\r\n+\r\n+    buildStackPile();\r\n+    setTimeout(() => showRes(cardData, deck), 200);\r\n+  }, 560);\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   RENDER CARDS — เลือก mode\r\n+═══════════════════════════════════════════ */\r\n+function renderCards() {\r\n+  if (layoutMode === 'stack') renderStack();\r\n+  else renderScatter();\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   FLIP CARD — scatter mode\r\n+═══════════════════════════════════════════ */\r\n+function flipCard(el, idx, ang) {\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  setTimeout(() => {\r\n+    const card = deckCards[idx % deckCards.length];\r\n+    el.classList.remove('flipping');\r\n+    el.classList.add('revealed');\r\n+    el.style.transform = 'rotate(' + ang + 'deg)';\r\n+    el.style.zIndex    = '200';\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n+      + '</div>';\r\n+    revCount++;\r\n+    updRem();\r\n+    setTimeout(() => showRes(card, deck), 200);\r\n+\r\n+    // fade out และ remove หลัง modal โผล่\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 800);\r\n+\r\n+  }, 560);\r\n+}\r\n+\r\n+/* ---- RESULT MODAL ---- */\r\n+function showRes(card, deck) {\r\n+  const th = getTh(deck);\r\n+  document.getElementById('res-dlbl').innerHTML =\r\n+    '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n+  document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+  document.getElementById('res-cat').textContent = card.cat;\r\n+  document.getElementById('res-txt').textContent  = card.text;\r\n+  document.getElementById('res-overlay').classList.add('show');\r\n+}\r\n+\r\n+function closeRes() {\r\n+  document.getElementById('res-overlay').classList.remove('show');\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774396041733,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -303,10 +303,9 @@\n     revCount++;\r\n     updRem();\r\n     setTimeout(() => showRes(card, deck), 200);\r\n \r\n-    // fade out และ remove หลัง modal โผล่\r\n-    setTimeout(() => {\r\n+    // fade out และ remove หลัง modal โผล่    setTimeout(() => {\r\n       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n"
                },
                {
                    "date": 1774396047140,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -303,9 +303,9 @@\n     revCount++;\r\n     updRem();\r\n     setTimeout(() => showRes(card, deck), 200);\r\n \r\n-    // fade out และ remove หลัง modal โผล่    setTimeout(() => {\r\n+    setTimeout(() => {\r\n       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n@@ -313,9 +313,8 @@\n \r\n   }, 560);\r\n }\r\n \r\n-/* ---- RESULT MODAL ---- */\r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n   document.getElementById('res-dlbl').innerHTML =\r\n     '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n"
                },
                {
                    "date": 1774397560566,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,332 @@\n+let deckCards   = [];\r\n+let revCount    = 0;\r\n+let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n+let stackIndex  = 0;\r\n+let showModal = true;\r\n+\r\n+/* ---- ตรวจจับมือถือ ---- */\r\n+function isMobile() {\r\n+  return window.innerWidth <= 640;\r\n+}\r\n+\r\n+/* ---- LAYOUT TOGGLE ---- */\r\n+function setLayout(mode) {\r\n+  layoutMode = mode;\r\n+  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+  renderCards();\r\n+}\r\n+\r\n+function toggleModal() {\r\n+  showModal = !showModal;\r\n+  const btn = document.getElementById('lt-modal');\r\n+  btn.classList.toggle('act', showModal);\r\n+  btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n+  toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- START GAME ---- */\r\n+function startGame() {\r\n+  if (!selDeck) return;\r\n+  const deck  = DB.decks.find(d => d.id === selDeck);\r\n+  const cards = DB.cards[selDeck] || [];\r\n+\r\n+  if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n+  if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n+\r\n+  deckCards  = [...cards].sort(() => Math.random() - .5);\r\n+  revCount   = 0;\r\n+  stackIndex = 0;\r\n+\r\n+  document.getElementById('home-screen').style.display = 'none';\r\n+  document.getElementById('game-screen').style.display = 'block';\r\n+\r\n+  const th = getTh(deck);\r\n+  const iw = document.getElementById('g-ico-wrap');\r\n+  iw.className = 'g-deck-ico ' + th;\r\n+  document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n+  document.getElementById('g-nm').textContent      = deck.name;\r\n+  document.getElementById('cost-show').textContent = deck.cost || 1;\r\n+\r\n+  const rw = document.getElementById('res-ico-wrap');\r\n+  rw.className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+\r\n+\r\n+  document.getElementById('btn-admin').style.display = 'none';\r\n+  updRem();\r\n+  updateCr();\r\n+  renderCards();\r\n+}\r\n+\r\n+/* ---- REMAINING COUNT ---- */\r\n+function updRem() {\r\n+  const t = deckCards.length;\r\n+  document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 1 — SCATTER (กระจายเละ)\r\n+═══════════════════════════════════════════ */\r\n+function renderScatter() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+\r\n+  const mobile = isMobile();\r\n+  const deck     = DB.decks.find(d => d.id === selDeck);\r\n+  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n+  const deckTheme = getTh(deck);\r\n+\r\n+  // วัดพื้นที่จริงของ wrap\r\n+  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n+  const CW = mobile ? 72 : 96;\r\n+  const CH = mobile ? 104 : 138;\r\n+\r\n+  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n+  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n+  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n+  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n+\r\n+  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n+\r\n+  const total = deckCards.length;\r\n+\r\n+  const ZX = mobile ? 6 : 7;\r\n+  const ZY = mobile ? 5 : 5;\r\n+  const zW = W / ZX;\r\n+  const zH = H / ZY;\r\n+\r\n+  const zones = [];\r\n+  for (let zy = 0; zy < ZY; zy++)\r\n+    for (let zx = 0; zx < ZX; zx++)\r\n+      zones.push([zx, zy]);\r\n+\r\n+  for (let i = zones.length - 1; i > 0; i--) {\r\n+    const j = Math.floor(Math.random() * (i + 1));\r\n+    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n+  }\r\n+\r\n+  const frag = document.createDocumentFragment();\r\n+  const els  = [];\r\n+\r\n+  for (let i = 0; i < total; i++) {\r\n+    const [zx, zy] = zones[i % zones.length];\r\n+    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n+    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n+    const ang = (Math.random() - .5) * 72;\r\n+    const z   = Math.floor(Math.random() * total) + 1;\r\n+\r\n+    const el = document.createElement('div');\r\n+    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n+    el.dataset.i     = i;\r\n+    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n+    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    el.onclick       = () => flipCard(el, i, ang);\r\n+\r\n+    frag.appendChild(el);\r\n+    els.push(el);\r\n+  }\r\n+\r\n+  wrap.appendChild(frag);\r\n+\r\n+  const delay = mobile ? 5 : 10;\r\n+  els.forEach((el, i) => {\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n+      el.style.opacity    = '1';\r\n+    }, i * delay);\r\n+  });\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n+═══════════════════════════════════════════ */\r\n+function renderStack() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+  wrap.style.cssText = 'min-height:420px;position:relative;';\r\n+\r\n+  wrap.innerHTML = [\r\n+    '<div class=\"stack-layout\">',\r\n+      '<div class=\"stack-section stack-center\">',\r\n+        '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n+          '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n+          'กองไพ่',\r\n+        '</div>',\r\n+        '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n+        '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n+      '</div>',\r\n+    '</div>'\r\n+  ].join('');\r\n+\r\n+  buildStackPile();\r\n+}\r\n+\r\n+function buildStackPile() {\r\n+  const pileWrap = document.getElementById('stack-pile-wrap');\r\n+  const countEl  = document.getElementById('stack-count');\r\n+  if (!pileWrap) return;\r\n+\r\n+  const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n+  const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n+  const deckTheme = getTh(deck);                            // เพิ่ม\r\n+  pileWrap.innerHTML = '';\r\n+  const remaining = deckCards.length - stackIndex;\r\n+  if (countEl) countEl.textContent = remaining + ' ใบ';\r\n+\r\n+  if (remaining <= 0) {\r\n+    pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n+      + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n+      + 'หยิบครบแล้ว!</div>';\r\n+    return;\r\n+  }\r\n+\r\n+  const showCount = Math.min(remaining, 6);\r\n+  for (let i = showCount - 1; i >= 0; i--) {\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'crd stack-crd';\r\n+    const off = i * 2.5;\r\n+    const ang = (i - showCount / 2) * 1.8;\r\n+    el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n+      + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n+      + (i === 0\r\n+        ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n+        : 'cursor:default;');\r\n+  el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    if (i === 0) el.onclick = () => flipTopCard(el);\r\n+    pileWrap.appendChild(el);\r\n+  }\r\n+}\r\n+\r\n+function flipTopCard(el) {\r\n+  if (stackIndex >= deckCards.length) return;\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+\r\n+  el.style.animation     = '';\r\n+  el.style.pointerEvents = 'none';\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  const cardData = deckCards[stackIndex];\r\n+  stackIndex++;\r\n+  revCount++;\r\n+  updRem();\r\n+\r\n+  setTimeout(() => {\r\n+    el.classList.remove('flipping');\r\n+\r\n+    // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n+    el.classList.add('revealed');\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n+      + '</div>';\r\n+\r\n+    // fade out และ remove\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 300);\r\n+\r\n+    buildStackPile();\r\n+    setTimeout(() => showRes(cardData, deck), 200);\r\n+  }, 560);\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   RENDER CARDS — เลือก mode\r\n+═══════════════════════════════════════════ */\r\n+function renderCards() {\r\n+  if (layoutMode === 'stack') renderStack();\r\n+  else renderScatter();\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   FLIP CARD — scatter mode\r\n+═══════════════════════════════════════════ */\r\n+function flipCard(el, idx, ang) {\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  setTimeout(() => {\r\n+    const card = deckCards[idx % deckCards.length];\r\n+    el.classList.remove('flipping');\r\n+    el.classList.add('revealed');\r\n+    el.style.transform = 'rotate(' + ang + 'deg)';\r\n+    el.style.zIndex    = '200';\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n+      + '</div>';\r\n+    revCount++;\r\n+    updRem();\r\n+    setTimeout(() => showRes(card, deck), 200);\r\n+\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 800);\r\n+\r\n+  }, 560);\r\n+}\r\n+\r\n+function showRes(card, deck) {\r\n+  const th = getTh(deck);\r\n+  document.getElementById('res-dlbl').innerHTML =\r\n+    '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n+  document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+  document.getElementById('res-cat').textContent = card.cat;\r\n+  document.getElementById('res-txt').textContent  = card.text;\r\n+  document.getElementById('res-overlay').classList.add('show');\r\n+}\r\n+\r\n+function closeRes() {\r\n+  document.getElementById('res-overlay').classList.remove('show');\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774397795935,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -51,345 +51,16 @@\n   const rw = document.getElementById('res-ico-wrap');\r\n   rw.className = 'res-ico-wrap ' + th;\r\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n-\r\n+    \r\n   document.getElementById('btn-admin').style.display = 'none';\r\n-  updRem();\r\n-  updateCr();\r\n-  renderCards();\r\n-}\r\n \r\n-/* ---- REMAINING COUNT ---- */\r\n-function updRem() {\r\n-  const t = deckCards.length;\r\n-  document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   โหมด 1 — SCATTER (กระจายเละ)\r\n-═══════════════════════════════════════════ */\r\n-function renderScatter() {\r\n-  const wrap = document.getElementById('cards-wrap');\r\n-  wrap.innerHTML = '';\r\n-\r\n-  const mobile = isMobile();\r\n-  const deck     = DB.decks.find(d => d.id === selDeck);\r\n-  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n-  const deckTheme = getTh(deck);\r\n-\r\n-  // วัดพื้นที่จริงของ wrap\r\n-  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n-  const CW = mobile ? 72 : 96;\r\n-  const CH = mobile ? 104 : 138;\r\n-\r\n-  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n-  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n-  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n-  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n-\r\n-  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n-\r\n-  const total = deckCards.length;\r\n-\r\n-  const ZX = mobile ? 6 : 7;\r\n-  const ZY = mobile ? 5 : 5;\r\n-  const zW = W / ZX;\r\n-  const zH = H / ZY;\r\n-\r\n-  const zones = [];\r\n-  for (let zy = 0; zy < ZY; zy++)\r\n-    for (let zx = 0; zx < ZX; zx++)\r\n-      zones.push([zx, zy]);\r\n-\r\n-  for (let i = zones.length - 1; i > 0; i--) {\r\n-    const j = Math.floor(Math.random() * (i + 1));\r\n-    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n-  }\r\n-\r\n-  const frag = document.createDocumentFragment();\r\n-  const els  = [];\r\n-\r\n-  for (let i = 0; i < total; i++) {\r\n-    const [zx, zy] = zones[i % zones.length];\r\n-    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n-    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n-    const ang = (Math.random() - .5) * 72;\r\n-    const z   = Math.floor(Math.random() * total) + 1;\r\n-\r\n-    const el = document.createElement('div');\r\n-    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n-    el.dataset.i     = i;\r\n-    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n-    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-    el.onclick       = () => flipCard(el, i, ang);\r\n-\r\n-    frag.appendChild(el);\r\n-    els.push(el);\r\n-  }\r\n-\r\n-  wrap.appendChild(frag);\r\n-\r\n-  const delay = mobile ? 5 : 10;\r\n-  els.forEach((el, i) => {\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n-      el.style.opacity    = '1';\r\n-    }, i * delay);\r\n-  });\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n-═══════════════════════════════════════════ */\r\n-function renderStack() {\r\n-  const wrap = document.getElementById('cards-wrap');\r\n-  wrap.innerHTML = '';\r\n-  wrap.style.cssText = 'min-height:420px;position:relative;';\r\n-\r\n-  wrap.innerHTML = [\r\n-    '<div class=\"stack-layout\">',\r\n-      '<div class=\"stack-section stack-center\">',\r\n-        '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n-          '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n-          'กองไพ่',\r\n-        '</div>',\r\n-        '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n-        '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n-      '</div>',\r\n-    '</div>'\r\n-  ].join('');\r\n-\r\n-  buildStackPile();\r\n-}\r\n-\r\n-function buildStackPile() {\r\n-  const pileWrap = document.getElementById('stack-pile-wrap');\r\n-  const countEl  = document.getElementById('stack-count');\r\n-  if (!pileWrap) return;\r\n-\r\n-  const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n-  const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n-  const deckTheme = getTh(deck);                            // เพิ่ม\r\n-  pileWrap.innerHTML = '';\r\n-  const remaining = deckCards.length - stackIndex;\r\n-  if (countEl) countEl.textContent = remaining + ' ใบ';\r\n-\r\n-  if (remaining <= 0) {\r\n-    pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n-      + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n-      + 'หยิบครบแล้ว!</div>';\r\n-    return;\r\n-  }\r\n-\r\n-  const showCount = Math.min(remaining, 6);\r\n-  for (let i = showCount - 1; i >= 0; i--) {\r\n-    const el  = document.createElement('div');\r\n-    el.className = 'crd stack-crd';\r\n-    const off = i * 2.5;\r\n-    const ang = (i - showCount / 2) * 1.8;\r\n-    el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n-      + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n-      + (i === 0\r\n-        ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n-        : 'cursor:default;');\r\n-  el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-    if (i === 0) el.onclick = () => flipTopCard(el);\r\n-    pileWrap.appendChild(el);\r\n-  }\r\n-}\r\n-\r\n-function flipTopCard(el) {\r\n-  if (stackIndex >= deckCards.length) return;\r\n-  const deck = DB.decks.find(d => d.id === selDeck);\r\n-  const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-  credits -= cost;\r\n-  saveCr();\r\n-  updateCr();\r\n-\r\n-  el.style.animation     = '';\r\n-  el.style.pointerEvents = 'none';\r\n-  el.classList.add('flipping');\r\n-\r\n-  const rect = el.getBoundingClientRect();\r\n-  for (let i = 0; i < 8; i++) {\r\n-    const s = document.createElement('div');\r\n-    s.className = 'spk';\r\n-    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-    document.body.appendChild(s);\r\n-    setTimeout(() => s.remove(), 900);\r\n-  }\r\n-\r\n-  const cardData = deckCards[stackIndex];\r\n-  stackIndex++;\r\n-  revCount++;\r\n   updRem();\r\n-\r\n-  setTimeout(() => {\r\n-    el.classList.remove('flipping');\r\n-\r\n-    // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n-    el.classList.add('revealed');\r\n-    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n-      + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n-      + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n-      + '</div>';\r\n-\r\n-    // fade out และ remove\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-      el.style.opacity    = '0';\r\n-      el.style.transform  = 'scale(0.6)';\r\n-      setTimeout(() => el.remove(), 420);\r\n-    }, 300);\r\n-\r\n-    buildStackPile();\r\n-    setTimeout(() => showRes(cardData, deck), 200);\r\n-  }, 560);\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   RENDER CARDS — เลือก mode\r\n-═══════════════════════════════════════════ */\r\n-function renderCards() {\r\n-  if (layoutMode === 'stack') renderStack();\r\n-  else renderScatter();\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   FLIP CARD — scatter mode\r\n-═══════════════════════════════════════════ */\r\n-function flipCard(el, idx, ang) {\r\n-  const deck = DB.decks.find(d => d.id === selDeck);\r\n-  const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-  if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n-  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-  credits -= cost;\r\n-  saveCr();\r\n   updateCr();\r\n-  el.classList.add('flipping');\r\n-\r\n-  const rect = el.getBoundingClientRect();\r\n-  for (let i = 0; i < 8; i++) {\r\n-    const s = document.createElement('div');\r\n-    s.className = 'spk';\r\n-    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-    document.body.appendChild(s);\r\n-    setTimeout(() => s.remove(), 900);\r\n-  }\r\n-\r\n-  setTimeout(() => {\r\n-    const card = deckCards[idx % deckCards.length];\r\n-    el.classList.remove('flipping');\r\n-    el.classList.add('revealed');\r\n-    el.style.transform = 'rotate(' + ang + 'deg)';\r\n-    el.style.zIndex    = '200';\r\n-    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n-      + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n-      + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n-      + '</div>';\r\n-    revCount++;\r\n-    updRem();\r\n-    setTimeout(() => showRes(card, deck), 200);\r\n-\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-      el.style.opacity    = '0';\r\n-      el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n-      setTimeout(() => el.remove(), 420);\r\n-    }, 800);\r\n-\r\n-  }, 560);\r\n-}\r\n-\r\n-function showRes(card, deck) {\r\n-  const th = getTh(deck);\r\n-  document.getElementById('res-dlbl').innerHTML =\r\n-    '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n-  document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n-  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-  document.getElementById('res-cat').textContent = card.cat;\r\n-  document.getElementById('res-txt').textContent  = card.text;\r\n-  document.getElementById('res-overlay').classList.add('show');\r\n-}\r\n-\r\n-function closeRes() {\r\n-  document.getElementById('res-overlay').classList.remove('show');\r\n-}\n-let deckCards   = [];\r\n-let revCount    = 0;\r\n-let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n-let stackIndex  = 0;\r\n-let showModal = true;\r\n-\r\n-/* ---- ตรวจจับมือถือ ---- */\r\n-function isMobile() {\r\n-  return window.innerWidth <= 640;\r\n-}\r\n-\r\n-/* ---- LAYOUT TOGGLE ---- */\r\n-function setLayout(mode) {\r\n-  layoutMode = mode;\r\n-  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n   renderCards();\r\n }\r\n \r\n-function toggleModal() {\r\n-  showModal = !showModal;\r\n-  const btn = document.getElementById('lt-modal');\r\n-  btn.classList.toggle('act', showModal);\r\n-  btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n-  toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- START GAME ---- */\r\n-function startGame() {\r\n-  if (!selDeck) return;\r\n-  const deck  = DB.decks.find(d => d.id === selDeck);\r\n-  const cards = DB.cards[selDeck] || [];\r\n-\r\n-  if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n-  if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n-\r\n-  deckCards  = [...cards].sort(() => Math.random() - .5);\r\n-  revCount   = 0;\r\n-  stackIndex = 0;\r\n-\r\n-  document.getElementById('home-screen').style.display = 'none';\r\n-  document.getElementById('game-screen').style.display = 'block';\r\n-\r\n-  const th = getTh(deck);\r\n-  const iw = document.getElementById('g-ico-wrap');\r\n-  iw.className = 'g-deck-ico ' + th;\r\n-  document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n-  document.getElementById('g-nm').textContent      = deck.name;\r\n-  document.getElementById('cost-show').textContent = deck.cost || 1;\r\n-\r\n-  const rw = document.getElementById('res-ico-wrap');\r\n-  rw.className = 'res-ico-wrap ' + th;\r\n-  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-\r\n-  updRem();\r\n-  updateCr();\r\n-  renderCards();\r\n-}\r\n-\r\n /* ---- REMAINING COUNT ---- */\r\n function updRem() {\r\n   const t = deckCards.length;\r\n   document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n"
                },
                {
                    "date": 1774397838666,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -51,9 +51,9 @@\n   const rw = document.getElementById('res-ico-wrap');\r\n   rw.className = 'res-ico-wrap ' + th;\r\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n-    \r\n+  document.body.classList.remove('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n \r\n   updRem();\r\n   updateCr();\r\n"
                },
                {
                    "date": 1774397847631,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -51,9 +51,9 @@\n   const rw = document.getElementById('res-ico-wrap');\r\n   rw.className = 'res-ico-wrap ' + th;\r\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n-  document.body.classList.remove('in-game'); // เพิ่ม\r\n+  document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n \r\n   updRem();\r\n   updateCr();\r\n"
                },
                {
                    "date": 1774401725988,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,9 +53,9 @@\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n-\r\n+    \r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774401750482,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,9 +53,10 @@\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n-    \r\n+  history.pushState({ deck: selDeck }, '', '/play/' + selDeck);\r\n+\r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774401784167,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -330,5 +330,19 @@\n }\r\n \r\n function closeRes() {\r\n   document.getElementById('res-overlay').classList.remove('show');\r\n-}\n\\ No newline at end of file\n+}\r\n+\r\n+\r\n+window.addEventListener('popstate', function(e) {\r\n+  if (e.state && e.state.deck) {\r\n+    // อยู่หน้าเกม\r\n+  } else {\r\n+    // กลับหน้าหลัก\r\n+    document.getElementById('game-screen').style.display = 'none';\r\n+    document.getElementById('home-screen').style.display = '';\r\n+    document.getElementById('admin-screen').style.display = 'none';\r\n+    document.body.classList.remove('in-game');\r\n+    document.getElementById('btn-admin').style.display = '';\r\n+  }\r\n+});\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774401892773,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,9 +53,9 @@\n   document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n \r\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n-  history.pushState({ deck: selDeck }, '', '/play/' + selDeck);\r\n+  history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n \r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n"
                },
                {
                    "date": 1774444836337,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,348 @@\n+let deckCards   = [];\r\n+let revCount    = 0;\r\n+let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n+let stackIndex  = 0;\r\n+let showModal = true;\r\n+\r\n+/* ---- ตรวจจับมือถือ ---- */\r\n+function isMobile() {\r\n+  return window.innerWidth <= 640;\r\n+}\r\n+\r\n+/* ---- LAYOUT TOGGLE ---- */\r\n+function setLayout(mode) {\r\n+  layoutMode = mode;\r\n+  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n+  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n+  renderCards();\r\n+}\r\n+\r\n+function toggleModal() {\r\n+  showModal = !showModal;\r\n+  const btn = document.getElementById('lt-modal');\r\n+  btn.classList.toggle('act', showModal);\r\n+  btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n+  toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n+}\r\n+\r\n+/* ---- START GAME ---- */\r\n+function startGame() {\r\n+  if (!selDeck) return;\r\n+  const deck  = DB.decks.find(d => d.id === selDeck);\r\n+  const cards = DB.cards[selDeck] || [];\r\n+\r\n+  if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n+  if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n+\r\n+  deckCards  = [...cards].sort(() => Math.random() - .5);\r\n+  revCount   = 0;\r\n+  stackIndex = 0;\r\n+\r\n+  document.getElementById('home-screen').style.display = 'none';\r\n+  document.getElementById('game-screen').style.display = 'block';\r\n+\r\n+  const th = getTh(deck);\r\n+  const iw = document.getElementById('g-ico-wrap');\r\n+  iw.className = 'g-deck-ico ' + th;\r\n+  document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n+  document.getElementById('g-nm').textContent      = deck.name;\r\n+  document.getElementById('cost-show').textContent = deck.cost || 1;\r\n+\r\n+  const rw = document.getElementById('res-ico-wrap');\r\n+  rw.className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+\r\n+  document.body.classList.add('in-game'); // เพิ่ม\r\n+  document.getElementById('btn-admin').style.display = 'none';\r\n+  history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n+\r\n+  updRem();\r\n+  updateCr();\r\n+  renderCards();\r\n+}\r\n+\r\n+/* ---- REMAINING COUNT ---- */\r\n+function updRem() {\r\n+  const t = deckCards.length;\r\n+  document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 1 — SCATTER (กระจายเละ)\r\n+═══════════════════════════════════════════ */\r\n+function renderScatter() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+\r\n+  const mobile = isMobile();\r\n+  const deck     = DB.decks.find(d => d.id === selDeck);\r\n+  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n+  const deckTheme = getTh(deck);\r\n+\r\n+  // วัดพื้นที่จริงของ wrap\r\n+  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n+  const CW = mobile ? 72 : 96;\r\n+  const CH = mobile ? 104 : 138;\r\n+\r\n+  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n+  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n+  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n+  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n+\r\n+  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n+\r\n+  const total = Math.min(deckCards.length, isMobile() ? 20 : 30);\r\n+\r\n+  const ZX = mobile ? 6 : 7;\r\n+  const ZY = mobile ? 5 : 5;\r\n+  const zW = W / ZX;\r\n+  const zH = H / ZY;\r\n+\r\n+  const zones = [];\r\n+  for (let zy = 0; zy < ZY; zy++)\r\n+    for (let zx = 0; zx < ZX; zx++)\r\n+      zones.push([zx, zy]);\r\n+\r\n+  for (let i = zones.length - 1; i > 0; i--) {\r\n+    const j = Math.floor(Math.random() * (i + 1));\r\n+    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n+  }\r\n+\r\n+  const frag = document.createDocumentFragment();\r\n+  const els  = [];\r\n+\r\n+  for (let i = 0; i < total; i++) {\r\n+    const [zx, zy] = zones[i % zones.length];\r\n+    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n+    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n+    const ang = (Math.random() - .5) * 72;\r\n+    const z   = Math.floor(Math.random() * total) + 1;\r\n+\r\n+    const el = document.createElement('div');\r\n+    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n+    el.dataset.i     = i;\r\n+    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n+    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    el.onclick       = () => flipCard(el, i, ang);\r\n+\r\n+    frag.appendChild(el);\r\n+    els.push(el);\r\n+  }\r\n+\r\n+  wrap.appendChild(frag);\r\n+\r\n+  const delay = mobile ? 5 : 10;\r\n+  els.forEach((el, i) => {\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n+      el.style.opacity    = '1';\r\n+    }, i * delay);\r\n+  });\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n+═══════════════════════════════════════════ */\r\n+function renderStack() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n+  wrap.style.cssText = 'min-height:420px;position:relative;';\r\n+\r\n+  wrap.innerHTML = [\r\n+    '<div class=\"stack-layout\">',\r\n+      '<div class=\"stack-section stack-center\">',\r\n+        '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n+          '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n+          'กองไพ่',\r\n+        '</div>',\r\n+        '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n+        '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n+      '</div>',\r\n+    '</div>'\r\n+  ].join('');\r\n+\r\n+  buildStackPile();\r\n+}\r\n+\r\n+function buildStackPile() {\r\n+  const pileWrap = document.getElementById('stack-pile-wrap');\r\n+  const countEl  = document.getElementById('stack-count');\r\n+  if (!pileWrap) return;\r\n+\r\n+  const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n+  const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n+  const deckTheme = getTh(deck);                            // เพิ่ม\r\n+  pileWrap.innerHTML = '';\r\n+  const remaining = deckCards.length - stackIndex;\r\n+  if (countEl) countEl.textContent = remaining + ' ใบ';\r\n+\r\n+  if (remaining <= 0) {\r\n+    pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n+      + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n+      + 'หยิบครบแล้ว!</div>';\r\n+    return;\r\n+  }\r\n+\r\n+  const showCount = Math.min(remaining, 6);\r\n+  for (let i = showCount - 1; i >= 0; i--) {\r\n+    const el  = document.createElement('div');\r\n+    el.className = 'crd stack-crd';\r\n+    const off = i * 2.5;\r\n+    const ang = (i - showCount / 2) * 1.8;\r\n+    el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n+      + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n+      + (i === 0\r\n+        ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n+        : 'cursor:default;');\r\n+  el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    if (i === 0) el.onclick = () => flipTopCard(el);\r\n+    pileWrap.appendChild(el);\r\n+  }\r\n+}\r\n+\r\n+function flipTopCard(el) {\r\n+  if (stackIndex >= deckCards.length) return;\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+\r\n+  el.style.animation     = '';\r\n+  el.style.pointerEvents = 'none';\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  const cardData = deckCards[stackIndex];\r\n+  stackIndex++;\r\n+  revCount++;\r\n+  updRem();\r\n+\r\n+  setTimeout(() => {\r\n+    el.classList.remove('flipping');\r\n+\r\n+    // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n+    el.classList.add('revealed');\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n+      + '</div>';\r\n+\r\n+    // fade out และ remove\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 300);\r\n+\r\n+    buildStackPile();\r\n+    setTimeout(() => showRes(cardData, deck), 200);\r\n+  }, 560);\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   RENDER CARDS — เลือก mode\r\n+═══════════════════════════════════════════ */\r\n+function renderCards() {\r\n+  if (layoutMode === 'stack') renderStack();\r\n+  else renderScatter();\r\n+}\r\n+\r\n+/* ═══════════════════════════════════════════\r\n+   FLIP CARD — scatter mode\r\n+═══════════════════════════════════════════ */\r\n+function flipCard(el, idx, ang) {\r\n+  const deck = DB.decks.find(d => d.id === selDeck);\r\n+  const cost = deck ? (deck.cost || 1) : 1;\r\n+\r\n+  if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n+  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n+\r\n+  credits -= cost;\r\n+  saveCr();\r\n+  updateCr();\r\n+  el.classList.add('flipping');\r\n+\r\n+  const rect = el.getBoundingClientRect();\r\n+  for (let i = 0; i < 8; i++) {\r\n+    const s = document.createElement('div');\r\n+    s.className = 'spk';\r\n+    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n+      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n+      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n+      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n+      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n+    document.body.appendChild(s);\r\n+    setTimeout(() => s.remove(), 900);\r\n+  }\r\n+\r\n+  setTimeout(() => {\r\n+    const card = deckCards[idx % deckCards.length];\r\n+    el.classList.remove('flipping');\r\n+    el.classList.add('revealed');\r\n+    el.style.transform = 'rotate(' + ang + 'deg)';\r\n+    el.style.zIndex    = '200';\r\n+    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n+      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n+      + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n+      + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n+      + '</div>';\r\n+    revCount++;\r\n+    updRem();\r\n+    setTimeout(() => showRes(card, deck), 200);\r\n+\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n+      el.style.opacity    = '0';\r\n+      el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n+      setTimeout(() => el.remove(), 420);\r\n+    }, 800);\r\n+\r\n+  }, 560);\r\n+}\r\n+\r\n+function showRes(card, deck) {\r\n+  const th = getTh(deck);\r\n+  document.getElementById('res-dlbl').innerHTML =\r\n+    '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n+  document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n+  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n+  document.getElementById('res-cat').textContent = card.cat;\r\n+  document.getElementById('res-txt').textContent  = card.text;\r\n+  document.getElementById('res-overlay').classList.add('show');\r\n+}\r\n+\r\n+function closeRes() {\r\n+  document.getElementById('res-overlay').classList.remove('show');\r\n+}\r\n+\r\n+\r\n+window.addEventListener('popstate', function(e) {\r\n+  if (e.state && e.state.deck) {\r\n+    // อยู่หน้าเกม\r\n+  } else {\r\n+    // กลับหน้าหลัก\r\n+    document.getElementById('game-screen').style.display = 'none';\r\n+    document.getElementById('home-screen').style.display = '';\r\n+    document.getElementById('admin-screen').style.display = 'none';\r\n+    document.body.classList.remove('in-game');\r\n+    document.getElementById('btn-admin').style.display = '';\r\n+  }\r\n+});\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774444895725,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -90,9 +90,9 @@\n   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n \r\n   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n \r\n-  const total = Math.min(deckCards.length, isMobile() ? 20 : 30);\r\n+  const total = Math.min(deckCards.length, isMobile() ? 10 : 30);\r\n \r\n   const ZX = mobile ? 6 : 7;\r\n   const ZY = mobile ? 5 : 5;\r\n   const zW = W / ZX;\r\n@@ -344,353 +344,5 @@\n     document.getElementById('admin-screen').style.display = 'none';\r\n     document.body.classList.remove('in-game');\r\n     document.getElementById('btn-admin').style.display = '';\r\n   }\r\n-});\n-let deckCards   = [];\r\n-let revCount    = 0;\r\n-let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n-let stackIndex  = 0;\r\n-let showModal = true;\r\n-\r\n-/* ---- ตรวจจับมือถือ ---- */\r\n-function isMobile() {\r\n-  return window.innerWidth <= 640;\r\n-}\r\n-\r\n-/* ---- LAYOUT TOGGLE ---- */\r\n-function setLayout(mode) {\r\n-  layoutMode = mode;\r\n-  document.getElementById('lt-scatter').classList.toggle('act', mode === 'scatter');\r\n-  document.getElementById('lt-stack').classList.toggle('act', mode === 'stack');\r\n-  renderCards();\r\n-}\r\n-\r\n-function toggleModal() {\r\n-  showModal = !showModal;\r\n-  const btn = document.getElementById('lt-modal');\r\n-  btn.classList.toggle('act', showModal);\r\n-  btn.querySelector('i').className = showModal ? 'fi fi-sr-eye' : 'fi fi-sr-eye-crossed';\r\n-  toast(showModal ? 'เปิด modal แล้ว' : 'ปิด modal แล้ว', 'info');\r\n-}\r\n-\r\n-/* ---- START GAME ---- */\r\n-function startGame() {\r\n-  if (!selDeck) return;\r\n-  const deck  = DB.decks.find(d => d.id === selDeck);\r\n-  const cards = DB.cards[selDeck] || [];\r\n-\r\n-  if (!cards.length) { toast('กองไพ่นี้ยังไม่มีไพ่!', 'warning'); return; }\r\n-  if (credits < (deck.cost || 1)) { toast('เครดิตไม่พอ! เติมก่อนนะ', 'error'); openTopup(); return; }\r\n-\r\n-  deckCards  = [...cards].sort(() => Math.random() - .5);\r\n-  revCount   = 0;\r\n-  stackIndex = 0;\r\n-\r\n-  document.getElementById('home-screen').style.display = 'none';\r\n-  document.getElementById('game-screen').style.display = 'block';\r\n-\r\n-  const th = getTh(deck);\r\n-  const iw = document.getElementById('g-ico-wrap');\r\n-  iw.className = 'g-deck-ico ' + th;\r\n-  document.getElementById('g-ico').className       = 'fi ' + (deck.icon || 'fi-sr-layers');\r\n-  document.getElementById('g-nm').textContent      = deck.name;\r\n-  document.getElementById('cost-show').textContent = deck.cost || 1;\r\n-\r\n-  const rw = document.getElementById('res-ico-wrap');\r\n-  rw.className = 'res-ico-wrap ' + th;\r\n-  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-\r\n-  document.body.classList.add('in-game'); // เพิ่ม\r\n-  document.getElementById('btn-admin').style.display = 'none';\r\n-  history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n-\r\n-  updRem();\r\n-  updateCr();\r\n-  renderCards();\r\n-}\r\n-\r\n-/* ---- REMAINING COUNT ---- */\r\n-function updRem() {\r\n-  const t = deckCards.length;\r\n-  document.getElementById('g-rm').textContent = 'เหลือ ' + (t - revCount) + '/' + t + ' ใบ';\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   โหมด 1 — SCATTER (กระจายเละ)\r\n-═══════════════════════════════════════════ */\r\n-function renderScatter() {\r\n-  const wrap = document.getElementById('cards-wrap');\r\n-  wrap.innerHTML = '';\r\n-\r\n-  const mobile = isMobile();\r\n-  const deck     = DB.decks.find(d => d.id === selDeck);\r\n-  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n-  const deckTheme = getTh(deck);\r\n-\r\n-  // วัดพื้นที่จริงของ wrap\r\n-  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n-  const CW = mobile ? 72 : 96;\r\n-  const CH = mobile ? 104 : 138;\r\n-\r\n-  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n-  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n-  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n-  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n-\r\n-  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n-\r\n-  const total = deckCards.length;\r\n-\r\n-  const ZX = mobile ? 6 : 7;\r\n-  const ZY = mobile ? 5 : 5;\r\n-  const zW = W / ZX;\r\n-  const zH = H / ZY;\r\n-\r\n-  const zones = [];\r\n-  for (let zy = 0; zy < ZY; zy++)\r\n-    for (let zx = 0; zx < ZX; zx++)\r\n-      zones.push([zx, zy]);\r\n-\r\n-  for (let i = zones.length - 1; i > 0; i--) {\r\n-    const j = Math.floor(Math.random() * (i + 1));\r\n-    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n-  }\r\n-\r\n-  const frag = document.createDocumentFragment();\r\n-  const els  = [];\r\n-\r\n-  for (let i = 0; i < total; i++) {\r\n-    const [zx, zy] = zones[i % zones.length];\r\n-    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n-    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n-    const ang = (Math.random() - .5) * 72;\r\n-    const z   = Math.floor(Math.random() * total) + 1;\r\n-\r\n-    const el = document.createElement('div');\r\n-    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n-    el.dataset.i     = i;\r\n-    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n-    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-    el.onclick       = () => flipCard(el, i, ang);\r\n-\r\n-    frag.appendChild(el);\r\n-    els.push(el);\r\n-  }\r\n-\r\n-  wrap.appendChild(frag);\r\n-\r\n-  const delay = mobile ? 5 : 10;\r\n-  els.forEach((el, i) => {\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n-      el.style.opacity    = '1';\r\n-    }, i * delay);\r\n-  });\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n-═══════════════════════════════════════════ */\r\n-function renderStack() {\r\n-  const wrap = document.getElementById('cards-wrap');\r\n-  wrap.innerHTML = '';\r\n-  wrap.style.cssText = 'min-height:420px;position:relative;';\r\n-\r\n-  wrap.innerHTML = [\r\n-    '<div class=\"stack-layout\">',\r\n-      '<div class=\"stack-section stack-center\">',\r\n-        '<div class=\"discard-label\" style=\"justify-content:center;\">',\r\n-          '<i class=\"fi fi-sr-layers\" style=\"color:var(--text3);font-size:.8rem;\"></i>',\r\n-          'กองไพ่',\r\n-        '</div>',\r\n-        '<div class=\"stack-pile-wrap\" id=\"stack-pile-wrap\"></div>',\r\n-        '<div class=\"stack-count\" id=\"stack-count\"></div>',\r\n-      '</div>',\r\n-    '</div>'\r\n-  ].join('');\r\n-\r\n-  buildStackPile();\r\n-}\r\n-\r\n-function buildStackPile() {\r\n-  const pileWrap = document.getElementById('stack-pile-wrap');\r\n-  const countEl  = document.getElementById('stack-count');\r\n-  if (!pileWrap) return;\r\n-\r\n-  const deck      = DB.decks.find(d => d.id === selDeck);  // เพิ่ม\r\n-  const deckIcon  = deck?.icon || 'fi-sr-layers';           // เพิ่ม\r\n-  const deckTheme = getTh(deck);                            // เพิ่ม\r\n-  pileWrap.innerHTML = '';\r\n-  const remaining = deckCards.length - stackIndex;\r\n-  if (countEl) countEl.textContent = remaining + ' ใบ';\r\n-\r\n-  if (remaining <= 0) {\r\n-    pileWrap.innerHTML = '<div style=\"color:var(--text3);font-size:.85rem;text-align:center;padding:40px 16px;line-height:2;\">'\r\n-      + '<i class=\"fi fi-sr-check-circle\" style=\"color:var(--red);display:block;font-size:2rem;margin-bottom:8px;\"></i>'\r\n-      + 'หยิบครบแล้ว!</div>';\r\n-    return;\r\n-  }\r\n-\r\n-  const showCount = Math.min(remaining, 6);\r\n-  for (let i = showCount - 1; i >= 0; i--) {\r\n-    const el  = document.createElement('div');\r\n-    el.className = 'crd stack-crd';\r\n-    const off = i * 2.5;\r\n-    const ang = (i - showCount / 2) * 1.8;\r\n-    el.style.cssText = 'position:absolute;left:' + off + 'px;top:' + off + 'px;'\r\n-      + 'transform:rotate(' + ang + 'deg);z-index:' + (showCount - i) + ';'\r\n-      + (i === 0\r\n-        ? 'cursor:pointer;box-shadow:0 0 22px var(--glow),3px 6px 18px rgba(0,0,0,.8);animation:pulse-card 2s ease-in-out infinite;'\r\n-        : 'cursor:default;');\r\n-  el.innerHTML = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-    if (i === 0) el.onclick = () => flipTopCard(el);\r\n-    pileWrap.appendChild(el);\r\n-  }\r\n-}\r\n-\r\n-function flipTopCard(el) {\r\n-  if (stackIndex >= deckCards.length) return;\r\n-  const deck = DB.decks.find(d => d.id === selDeck);\r\n-  const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-  credits -= cost;\r\n-  saveCr();\r\n-  updateCr();\r\n-\r\n-  el.style.animation     = '';\r\n-  el.style.pointerEvents = 'none';\r\n-  el.classList.add('flipping');\r\n-\r\n-  const rect = el.getBoundingClientRect();\r\n-  for (let i = 0; i < 8; i++) {\r\n-    const s = document.createElement('div');\r\n-    s.className = 'spk';\r\n-    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-    document.body.appendChild(s);\r\n-    setTimeout(() => s.remove(), 900);\r\n-  }\r\n-\r\n-  const cardData = deckCards[stackIndex];\r\n-  stackIndex++;\r\n-  revCount++;\r\n-  updRem();\r\n-\r\n-  setTimeout(() => {\r\n-    el.classList.remove('flipping');\r\n-\r\n-    // แสดงหน้าไพ่ชั่วคราวก่อน fade out\r\n-    el.classList.add('revealed');\r\n-    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.7rem;\"></i>'\r\n-      + '<div class=\"crd-cat\">' + cardData.cat + '</div>'\r\n-      + '<div class=\"crd-txt\">' + (cardData.text.length > 30 ? cardData.text.slice(0, 28) + '…' : cardData.text) + '</div>'\r\n-      + '</div>';\r\n-\r\n-    // fade out และ remove\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-      el.style.opacity    = '0';\r\n-      el.style.transform  = 'scale(0.6)';\r\n-      setTimeout(() => el.remove(), 420);\r\n-    }, 300);\r\n-\r\n-    buildStackPile();\r\n-    setTimeout(() => showRes(cardData, deck), 200);\r\n-  }, 560);\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   RENDER CARDS — เลือก mode\r\n-═══════════════════════════════════════════ */\r\n-function renderCards() {\r\n-  if (layoutMode === 'stack') renderStack();\r\n-  else renderScatter();\r\n-}\r\n-\r\n-/* ═══════════════════════════════════════════\r\n-   FLIP CARD — scatter mode\r\n-═══════════════════════════════════════════ */\r\n-function flipCard(el, idx, ang) {\r\n-  const deck = DB.decks.find(d => d.id === selDeck);\r\n-  const cost = deck ? (deck.cost || 1) : 1;\r\n-\r\n-  if (el.classList.contains('revealed') || el.classList.contains('flipping')) return;\r\n-  if (credits < cost) { toast('เครดิตไม่พอ! ต้องใช้ ' + cost + ' เครดิต', 'error'); openTopup(); return; }\r\n-\r\n-  credits -= cost;\r\n-  saveCr();\r\n-  updateCr();\r\n-  el.classList.add('flipping');\r\n-\r\n-  const rect = el.getBoundingClientRect();\r\n-  for (let i = 0; i < 8; i++) {\r\n-    const s = document.createElement('div');\r\n-    s.className = 'spk';\r\n-    s.style.cssText = 'left:' + (rect.left + Math.random() * rect.width) + 'px;'\r\n-      + 'top:' + (rect.top + Math.random() * rect.height) + 'px;'\r\n-      + '--tx:' + ((Math.random() - .5) * 80) + 'px;'\r\n-      + '--ty:-' + (Math.random() * 60 + 20) + 'px;'\r\n-      + 'background:' + (Math.random() > .5 ? 'var(--red)' : 'var(--gold)') + ';';\r\n-    document.body.appendChild(s);\r\n-    setTimeout(() => s.remove(), 900);\r\n-  }\r\n-\r\n-  setTimeout(() => {\r\n-    const card = deckCards[idx % deckCards.length];\r\n-    el.classList.remove('flipping');\r\n-    el.classList.add('revealed');\r\n-    el.style.transform = 'rotate(' + ang + 'deg)';\r\n-    el.style.zIndex    = '200';\r\n-    el.innerHTML = '<div class=\"crd-revealed-inner\">'\r\n-      + '<i class=\"fi fi-sr-check\" style=\"color:var(--red);font-size:.8rem;\"></i>'\r\n-      + '<div class=\"crd-cat\">' + card.cat + '</div>'\r\n-      + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n-      + '</div>';\r\n-    revCount++;\r\n-    updRem();\r\n-    setTimeout(() => showRes(card, deck), 200);\r\n-\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n-      el.style.opacity    = '0';\r\n-      el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n-      setTimeout(() => el.remove(), 420);\r\n-    }, 800);\r\n-\r\n-  }, 560);\r\n-}\r\n-\r\n-function showRes(card, deck) {\r\n-  const th = getTh(deck);\r\n-  document.getElementById('res-dlbl').innerHTML =\r\n-    '<i class=\"fi ' + (deck.icon || 'fi-sr-layers') + '\" style=\"color:var(--red);\"></i> ' + deck.name;\r\n-  document.getElementById('res-ico-wrap').className = 'res-ico-wrap ' + th;\r\n-  document.getElementById('res-ico').className = 'fi ' + (deck.icon || 'fi-sr-flame');\r\n-  document.getElementById('res-cat').textContent = card.cat;\r\n-  document.getElementById('res-txt').textContent  = card.text;\r\n-  document.getElementById('res-overlay').classList.add('show');\r\n-}\r\n-\r\n-function closeRes() {\r\n-  document.getElementById('res-overlay').classList.remove('show');\r\n-}\r\n-\r\n-\r\n-window.addEventListener('popstate', function(e) {\r\n-  if (e.state && e.state.deck) {\r\n-    // อยู่หน้าเกม\r\n-  } else {\r\n-    // กลับหน้าหลัก\r\n-    document.getElementById('game-screen').style.display = 'none';\r\n-    document.getElementById('home-screen').style.display = '';\r\n-    document.getElementById('admin-screen').style.display = 'none';\r\n-    document.body.classList.remove('in-game');\r\n-    document.getElementById('btn-admin').style.display = '';\r\n-  }\r\n });\n\\ No newline at end of file\n"
                },
                {
                    "date": 1774444915395,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,9 +2,9 @@\n let revCount    = 0;\r\n let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n let stackIndex  = 0;\r\n let showModal = true;\r\n-\r\n+let renderOffset = 0;\r\n /* ---- ตรวจจับมือถือ ---- */\r\n function isMobile() {\r\n   return window.innerWidth <= 640;\r\n }\r\n@@ -90,9 +90,9 @@\n   const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n \r\n   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n \r\n-  const total = Math.min(deckCards.length, isMobile() ? 10 : 30);\r\n+  const total = Math.min(deckCards.length, isMobile() ? 10 : 20);\r\n \r\n   const ZX = mobile ? 6 : 7;\r\n   const ZY = mobile ? 5 : 5;\r\n   const zW = W / ZX;\r\n"
                },
                {
                    "date": 1774444936841,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,8 +3,9 @@\n let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n let stackIndex  = 0;\r\n let showModal = true;\r\n let renderOffset = 0;\r\n+\r\n /* ---- ตรวจจับมือถือ ---- */\r\n function isMobile() {\r\n   return window.innerWidth <= 640;\r\n }\r\n@@ -26,8 +27,9 @@\n }\r\n \r\n /* ---- START GAME ---- */\r\n function startGame() {\r\n+    renderOffset = 0;\r\n   if (!selDeck) return;\r\n   const deck  = DB.decks.find(d => d.id === selDeck);\r\n   const cards = DB.cards[selDeck] || [];\r\n \r\n"
                },
                {
                    "date": 1774444943913,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,9 +27,8 @@\n }\r\n \r\n /* ---- START GAME ---- */\r\n function startGame() {\r\n-    renderOffset = 0;\r\n   if (!selDeck) return;\r\n   const deck  = DB.decks.find(d => d.id === selDeck);\r\n   const cards = DB.cards[selDeck] || [];\r\n \r\n@@ -56,9 +55,9 @@\n \r\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n   history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n-\r\n+  renderOffset = 0;\r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774444977478,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -56,8 +56,9 @@\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n   history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n   renderOffset = 0;\r\n+\r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n@@ -129,8 +130,10 @@\n     frag.appendChild(el);\r\n     els.push(el);\r\n   }\r\n \r\n+\r\n+  \r\n   wrap.appendChild(frag);\r\n \r\n   const delay = mobile ? 5 : 10;\r\n   els.forEach((el, i) => {\r\n"
                },
                {
                    "date": 1774444984204,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -130,9 +130,13 @@\n     frag.appendChild(el);\r\n     els.push(el);\r\n   }\r\n \r\n+  const BATCH = isMobile() ? 20 : 30;\r\n+const slice = deckCards.slice(renderOffset, renderOffset + BATCH);\r\n+const total = slice.length;\r\n \r\n+\r\n   \r\n   wrap.appendChild(frag);\r\n \r\n   const delay = mobile ? 5 : 10;\r\n"
                },
                {
                    "date": 1774445043912,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -130,14 +130,8 @@\n     frag.appendChild(el);\r\n     els.push(el);\r\n   }\r\n \r\n-  const BATCH = isMobile() ? 20 : 30;\r\n-const slice = deckCards.slice(renderOffset, renderOffset + BATCH);\r\n-const total = slice.length;\r\n-\r\n-\r\n-  \r\n   wrap.appendChild(frag);\r\n \r\n   const delay = mobile ? 5 : 10;\r\n   els.forEach((el, i) => {\r\n"
                },
                {
                    "date": 1774445066217,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -315,9 +315,9 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-\r\n+    \r\n   }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n"
                },
                {
                    "date": 1774445072698,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -315,9 +315,15 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-    \r\n+    const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n+if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n+  renderOffset += BATCH;\r\n+  setTimeout(() => renderScatter(), 500);\r\n+  toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+}\r\n+\r\n   }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n"
                },
                {
                    "date": 1774445119375,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -71,78 +71,9 @@\n \r\n /* ═══════════════════════════════════════════\r\n    โหมด 1 — SCATTER (กระจายเละ)\r\n ═══════════════════════════════════════════ */\r\n-function renderScatter() {\r\n-  const wrap = document.getElementById('cards-wrap');\r\n-  wrap.innerHTML = '';\r\n \r\n-  const mobile = isMobile();\r\n-  const deck     = DB.decks.find(d => d.id === selDeck);\r\n-  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n-  const deckTheme = getTh(deck);\r\n-\r\n-  // วัดพื้นที่จริงของ wrap\r\n-  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n-  const CW = mobile ? 72 : 96;\r\n-  const CH = mobile ? 104 : 138;\r\n-\r\n-  // วัดพื้นที่ความสูงจริง = จอ - header - cost-hint - padding\r\n-  const hdr      = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n-  const hint     = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n-  const H        = window.innerHeight - hdr - hint - 48; // 48 = padding เผื่อไว้\r\n-\r\n-  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n-\r\n-  const total = Math.min(deckCards.length, isMobile() ? 10 : 20);\r\n-\r\n-  const ZX = mobile ? 6 : 7;\r\n-  const ZY = mobile ? 5 : 5;\r\n-  const zW = W / ZX;\r\n-  const zH = H / ZY;\r\n-\r\n-  const zones = [];\r\n-  for (let zy = 0; zy < ZY; zy++)\r\n-    for (let zx = 0; zx < ZX; zx++)\r\n-      zones.push([zx, zy]);\r\n-\r\n-  for (let i = zones.length - 1; i > 0; i--) {\r\n-    const j = Math.floor(Math.random() * (i + 1));\r\n-    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n-  }\r\n-\r\n-  const frag = document.createDocumentFragment();\r\n-  const els  = [];\r\n-\r\n-  for (let i = 0; i < total; i++) {\r\n-    const [zx, zy] = zones[i % zones.length];\r\n-    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n-    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n-    const ang = (Math.random() - .5) * 72;\r\n-    const z   = Math.floor(Math.random() * total) + 1;\r\n-\r\n-    const el = document.createElement('div');\r\n-    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n-    el.dataset.i     = i;\r\n-    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n-    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n-    el.onclick       = () => flipCard(el, i, ang);\r\n-\r\n-    frag.appendChild(el);\r\n-    els.push(el);\r\n-  }\r\n-\r\n-  wrap.appendChild(frag);\r\n-\r\n-  const delay = mobile ? 5 : 10;\r\n-  els.forEach((el, i) => {\r\n-    setTimeout(() => {\r\n-      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n-      el.style.opacity    = '1';\r\n-    }, i * delay);\r\n-  });\r\n-}\r\n-\r\n /* ═══════════════════════════════════════════\r\n    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n ═══════════════════════════════════════════ */\r\n function renderStack() {\r\n@@ -316,13 +247,13 @@\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n     const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n-if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n-  renderOffset += BATCH;\r\n-  setTimeout(() => renderScatter(), 500);\r\n-  toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-}\r\n+        if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n+        renderOffset += BATCH;\r\n+        setTimeout(() => renderScatter(), 500);\r\n+        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+        }\r\n \r\n   }, 560);\r\n }\r\n \r\n"
                },
                {
                    "date": 1774445232971,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -71,9 +71,78 @@\n \r\n /* ═══════════════════════════════════════════\r\n    โหมด 1 — SCATTER (กระจายเละ)\r\n ═══════════════════════════════════════════ */\r\n+function renderScatter() {\r\n+  const wrap = document.getElementById('cards-wrap');\r\n+  wrap.innerHTML = '';\r\n \r\n+  const mobile = isMobile();\r\n+  const deck     = DB.decks.find(d => d.id === selDeck);\r\n+  const deckIcon = deck?.icon || 'fi-sr-layers';\r\n+  const deckTheme = getTh(deck);\r\n+\r\n+  const W  = wrap.getBoundingClientRect().width || window.innerWidth - 32;\r\n+  const CW = mobile ? 72 : 96;\r\n+  const CH = mobile ? 104 : 138;\r\n+\r\n+  const hdr  = document.querySelector('.game-hdr')?.getBoundingClientRect().height || 80;\r\n+  const hint = document.querySelector('.cost-hint')?.getBoundingClientRect().height || 48;\r\n+  const H    = window.innerHeight - hdr - hint - 48;\r\n+\r\n+  wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n+\r\n+  const BATCH = mobile ? 10 : 20;\r\n+  const slice = deckCards.slice(renderOffset, renderOffset + BATCH);\r\n+  const total = slice.length;\r\n+\r\n+  const ZX = mobile ? 6 : 7;\r\n+  const ZY = mobile ? 5 : 5;\r\n+  const zW = W / ZX;\r\n+  const zH = H / ZY;\r\n+\r\n+  const zones = [];\r\n+  for (let zy = 0; zy < ZY; zy++)\r\n+    for (let zx = 0; zx < ZX; zx++)\r\n+      zones.push([zx, zy]);\r\n+\r\n+  for (let i = zones.length - 1; i > 0; i--) {\r\n+    const j = Math.floor(Math.random() * (i + 1));\r\n+    [zones[i], zones[j]] = [zones[j], zones[i]];\r\n+  }\r\n+\r\n+  const frag = document.createDocumentFragment();\r\n+  const els  = [];\r\n+\r\n+  for (let i = 0; i < total; i++) {\r\n+    const [zx, zy] = zones[i % zones.length];\r\n+    const x   = zx * zW + Math.random() * Math.max(4, zW - CW);\r\n+    const y   = zy * zH + Math.random() * Math.max(4, zH - CH);\r\n+    const ang = (Math.random() - .5) * 72;\r\n+    const z   = Math.floor(Math.random() * total) + 1;\r\n+    const globalIdx = renderOffset + i; // index จริงใน deckCards\r\n+\r\n+    const el = document.createElement('div');\r\n+    el.className     = 'crd' + (mobile ? ' crd-sm' : '');\r\n+    el.dataset.i     = globalIdx;\r\n+    el.style.cssText = 'left:' + x + 'px;top:' + y + 'px;transform:rotate(' + ang + 'deg);z-index:' + z + ';opacity:0;';\r\n+    el.innerHTML     = '<div class=\"crd-back ' + deckTheme + '\"><i class=\"fi ' + deckIcon + '\"></i></div>';\r\n+    el.onclick       = () => flipCard(el, globalIdx, ang);\r\n+\r\n+    frag.appendChild(el);\r\n+    els.push(el);\r\n+  }\r\n+\r\n+  wrap.appendChild(frag);\r\n+\r\n+  const delay = mobile ? 5 : 10;\r\n+  els.forEach((el, i) => {\r\n+    setTimeout(() => {\r\n+      el.style.transition = 'all .25s cubic-bezier(.23,1,.32,1)';\r\n+      el.style.opacity    = '1';\r\n+    }, i * delay);\r\n+  });\r\n+}\r\n /* ═══════════════════════════════════════════\r\n    โหมด 2 — STACK (เรียงตับ หยิบทีละใบ)\r\n ═══════════════════════════════════════════ */\r\n function renderStack() {\r\n"
                },
                {
                    "date": 1774445290524,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -307,8 +307,13 @@\n       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n       + '</div>';\r\n     revCount++;\r\n     updRem();\r\n+    const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n+if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n+  renderOffset += BATCH;\r\n+  setTimeout(() => { renderScatter(); toast('โหลดไพ่ชุดถัดไป...', 'info'); }, 600);\r\n+}\r\n     setTimeout(() => showRes(card, deck), 200);\r\n \r\n     setTimeout(() => {\r\n       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n"
                },
                {
                    "date": 1774445309054,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -307,13 +307,8 @@\n       + '<div class=\"crd-txt\">' + (card.text.length > 42 ? card.text.slice(0, 39) + '…' : card.text) + '</div>'\r\n       + '</div>';\r\n     revCount++;\r\n     updRem();\r\n-    const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n-if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n-  renderOffset += BATCH;\r\n-  setTimeout(() => { renderScatter(); toast('โหลดไพ่ชุดถัดไป...', 'info'); }, 600);\r\n-}\r\n     setTimeout(() => showRes(card, deck), 200);\r\n \r\n     setTimeout(() => {\r\n       el.style.transition = 'opacity .4s ease, transform .4s ease';\r\n"
                },
                {
                    "date": 1774445360184,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -56,9 +56,9 @@\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n   history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n   renderOffset = 0;\r\n-\r\n+renderOffset = 0;\r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774445466921,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,8 +3,9 @@\n let layoutMode  = 'scatter';   // 'scatter' | 'stack'\r\n let stackIndex  = 0;\r\n let showModal = true;\r\n let renderOffset = 0;\r\n+const BATCH = 20;\r\n \r\n /* ---- ตรวจจับมือถือ ---- */\r\n function isMobile() {\r\n   return window.innerWidth <= 640;\r\n@@ -56,9 +57,9 @@\n   document.body.classList.add('in-game'); // เพิ่ม\r\n   document.getElementById('btn-admin').style.display = 'none';\r\n   history.pushState({ deck: selDeck }, '', '#play/' + selDeck);\r\n   renderOffset = 0;\r\n-renderOffset = 0;\r\n+\r\n   updRem();\r\n   updateCr();\r\n   renderCards();\r\n }\r\n"
                },
                {
                    "date": 1774445505894,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -91,9 +91,10 @@\n   const H    = window.innerHeight - hdr - hint - 48;\r\n \r\n   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n \r\n-  const BATCH = mobile ? 10 : 20;\r\n+  const batchSize = isMobile() ? 10 : 20;\r\n+const slice = deckCards.slice(renderOffset, renderOffset + batchSize);\r\n   const slice = deckCards.slice(renderOffset, renderOffset + BATCH);\r\n   const total = slice.length;\r\n \r\n   const ZX = mobile ? 6 : 7;\r\n"
                },
                {
                    "date": 1774445542263,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -93,9 +93,8 @@\n   wrap.style.cssText = 'width:100%;height:' + H + 'px;position:relative;overflow:hidden;';\r\n \r\n   const batchSize = isMobile() ? 10 : 20;\r\n const slice = deckCards.slice(renderOffset, renderOffset + batchSize);\r\n-  const slice = deckCards.slice(renderOffset, renderOffset + BATCH);\r\n   const total = slice.length;\r\n \r\n   const ZX = mobile ? 6 : 7;\r\n   const ZY = mobile ? 5 : 5;\r\n@@ -317,17 +316,18 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-    const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n-        if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n-        renderOffset += BATCH;\r\n-        setTimeout(() => renderScatter(), 500);\r\n-        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-        }\r\n-\r\n-  }, 560);\r\n+const batchSize = isMobile() ? 10 : 20;\r\n+const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n+if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n+  renderOffset += batchSize;\r\n+  setTimeout(() => {\r\n+    renderScatter();\r\n+    toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+  }, 900);\r\n }\r\n+}\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n   document.getElementById('res-dlbl').innerHTML =\r\n"
                },
                {
                    "date": 1774445552038,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -316,18 +316,18 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-const batchSize = isMobile() ? 10 : 20;\r\n-const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n-if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n-  renderOffset += batchSize;\r\n-  setTimeout(() => {\r\n-    renderScatter();\r\n-    toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-  }, 900);\r\n+        const batchSize = isMobile() ? 10 : 20;\r\n+        const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n+        if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n+        renderOffset += batchSize;\r\n+        setTimeout(() => {\r\n+            renderScatter();\r\n+            toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+        }, 900);\r\n+\r\n }\r\n-}\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n   document.getElementById('res-dlbl').innerHTML =\r\n"
                },
                {
                    "date": 1774445560285,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -324,9 +324,9 @@\n         setTimeout(() => {\r\n             renderScatter();\r\n             toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n         }, 900);\r\n-\r\n+        \r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445566889,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -316,17 +316,17 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-        const batchSize = isMobile() ? 10 : 20;\r\n-        const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n-        if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n-        renderOffset += batchSize;\r\n-        setTimeout(() => {\r\n-            renderScatter();\r\n-            toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-        }, 900);\r\n-        \r\n+    const batchSize = isMobile() ? 10 : 20;\r\n+    const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n+    if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n+    renderOffset += batchSize;\r\n+    setTimeout(() => {\r\n+        renderScatter();\r\n+        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+    }, 900);\r\n+\r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445594695,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -316,17 +316,19 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-    const batchSize = isMobile() ? 10 : 20;\r\n-    const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n-    if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n-    renderOffset += batchSize;\r\n-    setTimeout(() => {\r\n-        renderScatter();\r\n-        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-    }, 900);\r\n+const batchSize = isMobile() ? 10 : 20;\r\n+const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n+if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n+  renderOffset += batchSize;\r\n+  setTimeout(() => {\r\n+    renderScatter();\r\n+    toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+  }, 900);\r\n+}\r\n \r\n+  }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445600645,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -316,19 +316,18 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-const batchSize = isMobile() ? 10 : 20;\r\n-const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n-if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n-  renderOffset += batchSize;\r\n-  setTimeout(() => {\r\n-    renderScatter();\r\n-    toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-  }, 900);\r\n-}\r\n+    const batchSize = isMobile() ? 10 : 20;\r\n+    const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n+    if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n+    renderOffset += batchSize;\r\n+    setTimeout(() => {\r\n+        renderScatter();\r\n+        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+    }, 900);\r\n+    }\r\n \r\n-  }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445607333,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -326,8 +326,9 @@\n         toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n     }, 900);\r\n     }\r\n \r\n+  }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445613179,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -324,11 +324,10 @@\n     setTimeout(() => {\r\n         renderScatter();\r\n         toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n     }, 900);\r\n-    }\r\n+)\r\n \r\n-  }, 560);\r\n }\r\n \r\n function showRes(card, deck) {\r\n   const th = getTh(deck);\r\n"
                },
                {
                    "date": 1774445619679,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -324,9 +324,9 @@\n     setTimeout(() => {\r\n         renderScatter();\r\n         toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n     }, 900);\r\n-    })})\r\n+    })\r\n \r\n }\r\n \r\n function showRes(card, deck) {\r\n"
                },
                {
                    "date": 1774445640429,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -316,17 +316,14 @@\n       el.style.opacity    = '0';\r\n       el.style.transform  = 'scale(0.6) rotate(' + ang + 'deg)';\r\n       setTimeout(() => el.remove(), 420);\r\n     }, 800);\r\n-const batchSize = isMobile() ? 10 : 20;\r\n-const remaining = document.querySelectorAll('.crd:not(.revealed)').length - 1;\r\n-if (remaining === 0 && renderOffset + batchSize < deckCards.length) {\r\n-  renderOffset += batchSize;\r\n-  setTimeout(() => {\r\n-    renderScatter();\r\n-    toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n-  }, 900);\r\n-}\r\n+    const remaining = document.querySelectorAll('.crd:not(.revealed)').length;\r\n+        if (remaining === 0 && renderOffset + BATCH < deckCards.length) {\r\n+        renderOffset += BATCH;\r\n+        setTimeout(() => renderScatter(), 500);\r\n+        toast('โหลดไพ่ชุดถัดไป...', 'info');\r\n+        }\r\n \r\n   }, 560);\r\n }\r\n \r\n"
                }
            ],
            "date": 1774366249004,
            "name": "Commit-0",
            "content": ""
        }
    ]
}